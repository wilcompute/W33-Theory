# Passes 1074--1078: Schur extension, GSp character fingerprint, spread--frame algebra, invariant restriction, and hardware backend

## Release status

All five requested directions were executed above the live Pass-1073 frontier.

- Exact certificate checks: **50/50 PASS**.
- Focused regression: **7 pytest tests passed in 0.05 s**.
- Pass 1074 Lean source is written and wired for the umbrella build. No local Lean executable was available, so no local `lake build` is claimed.
- No physical optical experiment was run; Pass 1078 is deterministic control and analysis software with synthetic validation fixtures.

## Pass 1074 -- normalized Schur cocycle extension

The canonical projective section of `Sp(4,3) -> PSp(4,3)` is packaged as a normalized `ZMod 2` cocycle. The executable certificate enumerates all `51840` matrices of `Sp(4,3)`, all `25920` projective classes, and checks `648000` cocycle identities on all base elements and ordered generator pairs.

The pair multiplication

\[
([g],a)([h],b)=([gh],a+b+c(g,h))
\]

reconstructs the full `Sp(4,3)` matrix group. A shortest detected positive-generator word of length ten projects to the identity and lifts to `-I`. The Pass-1055/1063 four-row sign equations still XOR to `0=1`, blocking a splitting section.

`formal/W33/Pass1074SchurCocycleExtension.lean` defines normalized cocycles, extension multiplication, associativity, identity laws, the central kernel, projection compatibility, the section/coboundary criterion, and the concrete Pass-1063 non-splitting lock.

## Pass 1075 -- the order-103680 group is `GSp(4,3)`

The group is identified intrinsically, not by an ATLAS suffix. It is the exact matrix group generated by `Sp(4,3)` transvections and a multiplier-two similitude:

\[
M^TJM=\mu(M)J,\qquad \mu(M)\in\{1,2\}.
\]

Enumeration gives order `103680` and **38 conjugacy classes**. The executable witness generates the complete class records: size, centralizer, order, multiplier, trace, fixed-space dimension, and fixed points on the 40-point, 36-spread, and 540-frame carriers. The committed compact certificate is SHA-256 locked to that complete output.

Permutation-character inner products give

\[
\langle\pi_{40},\pi_{40}\rangle=3,
\qquad
\langle\pi_{36},\pi_{36}\rangle=3.
\]

The frame action exposes a new fusion statement:

\[
\operatorname{rank}_{Sp(4,3)}(540)=32,
\qquad
\operatorname{rank}_{GSp(4,3)}(540)=22.
\]

Thus the outer similitude fuses ten inner orbitals. A companion GAP/CTblLib script requests the irreducible character table when GAP is available; no unexecuted GAP output is presented as a result.

## Pass 1076 -- the exact `36 x 540` incidence algebra

Let `B` be spread--frame incidence. Then

\[
B\in\{0,1\}^{36\times540},\qquad
\deg_{\rm spread}=45,\qquad
\deg_{\rm frame}=3,
\]

and `36*45 = 540*3 = 1620`.

Using the checked valency-15 spread orbital `A`,

\[
BB^T=45I+6A.
\]

Since

\[
\operatorname{spec}(A)=15^1\oplus3^{15}\oplus(-3)^{20},
\]

we obtain

\[
\operatorname{spec}(BB^T)=135^1\oplus63^{15}\oplus27^{20}.
\]

The frame carrier therefore has a `504`-dimensional incidence kernel. Distinct frame pairs share zero, one, or two spreads, but those coarse relation matrices are **not closed under multiplication**. The frame side cannot be compressed to common-spread count; the rank-32 inner and rank-22 full orbital structures are essential.

This consumes Pass 1071 tactical regularity and Pass 1072 orbital identification rather than reclaiming them.

## Pass 1077 -- `G32 -> G25` invariant restriction

The CHEVIE reflection models were reduced at the good prime `43`, with `omega = 6`, and enumerated exactly:

\[
|G_{25}|=648,\qquad |G_{32}|=155520.
\]

Reynolds-orbit invariants define a `G25` basic system `u6,v9,w12`. On `x4=0`, the validated modular restrictions begin

\[
F_{12}=4w_{12}+24u_6^2,
\]

\[
F_{18}=37v_9^2+6u_6w_{12}+u_6^3\pmod{43},
\]

with exact degree-24 and degree-30 formulas in the release JSON.

Every nonzero term contains an even power of `v9`. Therefore the image is contained in

\[
\mathbb F_{43}[u_6,w_{12},v_9^2]
\]

and is not surjective onto `F43[u6,v9,w12]`. The degree-six and sign-sensitive degree-nine invariants are genuinely new parabolic data.

The arrangement restriction is also exact: `G25` has 12 reflecting hyperplanes, `G32` has 40, and `x4=0` is itself one `G32` reflecting hyperplane. After dividing that factor, the remaining 39 restrict as the 12 `G25` hyperplanes once plus nine additional hyperplanes with multiplicity three.

## Pass 1078 -- deterministic photonic hardware backend

The preregistered protocol now compiles to a 40-mode adjacent-MZI rectangular mesh. Every W33 context is routed exactly to detector ports `0,1,2,3`.

Measured resources are:

- 40 contexts and 40 movable point-star gauge cycles;
- 6480 elementary calibration/projector operations;
- maximum mesh depth 8;
- maximum 102 MZIs for one context route;
- 2880 context-routing MZIs across the 40 contexts;
- four detector channels and 40 delay slots.

The backend includes SHA-256 schedule locking, HMAC-signed manifests, a synthetic-only blinded index, strict real-data CSV validation, preregistered sequential stopping, fail-closed calibration gates, and separate signature-checked offline unblinding.

Synthetic fixtures verify positive, negative, and bad-calibration branches. Bad calibration terminates as `inconclusive_no_claim`. The connector rejected the expanded schedule payload, so `hardware/w33_pass1078_macro_schedule.json` is a compact generation contract locked to the expanded schedule hash; the committed compiler regenerates the full schedule deterministically.

## Authoritative artifacts

Executable witnesses:

- `analysis/w33_pass1074_schur_extension_formal_certificate.py`
- `analysis/w33_pass1075_gsp43_character_fingerprint.py`
- `analysis/w33_pass1075_gsp43_character_table.g`
- `analysis/w33_pass1076_spread_frame_incidence_algebra.py`
- `analysis/w33_pass1077_g32_g25_invariant_restriction.py`
- `analysis/w33_pass1078_photonic_hardware_backend.py`

Formal source:

- `formal/W33/Pass1074SchurCocycleExtension.lean`

Machine certificates:

- `data/w33_pass1074_schur_extension_formal_certificate.json`
- `data/w33_pass1075_gsp43_character_fingerprint.json`
- `data/w33_pass1076_spread_frame_incidence_algebra.json`
- `data/w33_pass1077_g32_g25_invariant_restriction.json`
- `data/w33_pass1078_photonic_hardware_backend.json`
- `data/w33_pass1074_1078_release.json`

Hardware surfaces:

- `hardware/w33_pass1078_mesh_manifest.json`
- `hardware/w33_pass1078_macro_schedule.json`
- `hardware/w33_pass1078_blinded_index.json`
- `hardware/w33_pass1078_real_data_fixture.csv`

Regression and CI:

- `tests/test_w33_pass1074_1078.py`
- `.github/workflows/pass1074_1078_exact.yml`
