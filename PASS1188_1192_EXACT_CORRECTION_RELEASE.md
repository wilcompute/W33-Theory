# Passes 1188–1192 — Exact Parallel-Correction Release

## 1188 — Exact residual Wedderburn recovery

The 2240 carrier, 2195 kernel, and 1952 residual have commutant dimensions `1193`, `1118`, and `1109`. The exact residual is

`13*1 + 16*6 + 5*15 + 4*15a + 21*20 + 2*24 + 9*30 + 4*60a + 10*64 + 1*90`.

## 1189 — Group-extension and character correction

`PSp(4,3)` has order 25920. `Sp(4,3)` and `W(E6)` each have order 51840 but are different extensions: the former is a central cover and the latter is the outer split extension `PSp(4,3):2`. The exact 25 W(E6) character degrees square-sum to 51840. A 432-set bridge requires an explicit `S5 ∩ PSp = A5` computation.

## 1190 — Exact Ihara degree 40

The Hashimoto quadratic coefficient is `k-1=11`. The determinant, all `Tr(B^n)` through 40, and all primitive reduced-cycle counts through 40 are exact. The first nonzero primitive counts are `pi_3=320`, `pi_4=3480`, and `pi_5=36288`; every count through 40 is integral and nonnegative.

## 1191 — Exact rank-three point module

Five symplectic transvections generate a projective permutation group of order 25920 on 40 points. A point stabilizer has subdegrees `[1,12,27]`; the exact multiplicity-free module is `1+24+15`.

## 1192 — Corpus-wide synthesis guard

Every active synthesis surface from Passes 1158–1187 is scanned fail-closed for the rejected character table, incorrect Ihara coefficient, conflated group extensions, impossible `S5` quotient, D5-adjoint promotion, tensor-product-by-dimension claim, simulated MeatAxe claim, and wrong symplectic point count.
