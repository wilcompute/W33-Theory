name: q4-diamond-paper

on:
  push:
    branches: [ master ]
    paths:
      - 'paper/w33_q4_diamond_machine_audited_synthesis.tex'
      - '.github/workflows/q4-diamond-paper.yml'
  pull_request:
    paths:
      - 'paper/w33_q4_diamond_machine_audited_synthesis.tex'
  workflow_dispatch:

jobs:
  build-q4-diamond-paper:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install TeX Live
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y texlive-latex-extra texlive-fonts-recommended latexmk
      - name: Compile Q4 diamond paper
        run: |
          pdflatex -interaction=nonstopmode -halt-on-error w33_q4_diamond_machine_audited_synthesis.tex
          pdflatex -interaction=nonstopmode -halt-on-error w33_q4_diamond_machine_audited_synthesis.tex
        working-directory: ${{ github.workspace }}/paper
      - name: Verify PDF produced
        run: |
          ls -lh w33_q4_diamond_machine_audited_synthesis.pdf
        working-directory: ${{ github.workspace }}/paper
      - name: Upload PDF artifact
        uses: actions/upload-artifact@v4
        with:
          name: w33-q4-diamond-paper
          path: paper/w33_q4_diamond_machine_audited_synthesis.pdf
          retention-days: 90
