name: Disabled obsolete Pages source switch

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: "echo 'Disabled: GitHub Pages must remain on master:/docs.'"
