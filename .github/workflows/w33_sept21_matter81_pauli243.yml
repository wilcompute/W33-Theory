name: W33 Sept21 matter81 Pauli243 restriction replay
on:
  push:
    branches: [master]
    paths:
      - 'analysis/w33_e8_matter81_pauli243_restriction.py'
      - 'tests/test_w33_e8_matter81_pauli243_restriction.py'
      - '.github/workflows/w33_sept21_matter81_pauli243.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  exact-replay:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python -m pip install --upgrade pip pytest
      - run: PYTHONPATH=. python analysis/w33_e8_matter81_pauli243_restriction.py
      - run: PYTHONPATH=. python -m pytest -q tests/test_w33_e8_matter81_pauli243_restriction.py
      - name: Freeze certificate
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -f -- data/w33_e8_matter81_pauli243_restriction.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'Freeze matter81 Pauli243 restriction certificate [skip ci]'
          git push origin HEAD:master
