# Part DCLXXI — Holonomy Markov Recurrence Bridge

## Why this part exists

`Part DCLXX` proved that the first two non-stationary slices of the averaged witness dynamics already reconstruct the full projector split.

The next honest question is whether the whole future evolution still needs explicit power formulas, or whether the witness-average dynamics have already collapsed to a finite linear recurrence.

This part proves the strongest dynamical compression so far:

> after subtracting the stationary mode, the entire future is generated by the first two slices through one exact order-two recurrence.

## Stationary-subtracted evolution

Let

$$
X_t = K^t - P_0.
$$

Then `DCLXIX` gives

$$
X_t = 4^{-t}P_+ + \left(\frac25\right)^t P_-.
$$

So there are only two nontrivial modes left once the stationary rank-`1` sector is removed.

## Exact recurrence

The verifier proves that for every `t \ge 1`,

$$
X_{t+2} = \frac{13}{20} X_{t+1} - \frac{1}{10} X_t.
$$

So the witness-average dynamics after stationary subtraction are an exact second-order linear system.

This is not approximate asymptotics. It is an exact operator identity.

## Characteristic roots

The characteristic polynomial is

$$
\lambda^2 - \frac{13}{20}\lambda + \frac{1}{10}.
$$

Its discriminant is

$$
\frac{9}{400},
$$

so it factors exactly as

$$
\left(\lambda - \frac14\right)\left(\lambda - \frac25\right).
$$

These are exactly the fast and slow nontrivial decay rates from `DCLXIX`.

So the recurrence is simply the compressed form of the two-mode spectral decomposition.

## Minimal order

The verifier checks that `X_1` and `X_2` are linearly independent, so this order-two recurrence is **minimal**.

That means:

- order zero is impossible,
- order one is impossible,
- order two is exactly enough.

So the stationary-subtracted witness-average evolution has reached its irreducible dynamical form.

## Channel-level recurrence

Because every `X_t` remains three-valued, each channel obeys the same exact recurrence.

The first two rows are:

For `t=1`:

$$
(X_1)_{\mathrm{diag}}=\frac{3}{10},
\qquad
(X_1)_{\mathrm{edge}}=-\frac{1}{40},
\qquad
(X_1)_{\mathrm{nonedge}}=0.
$$

For `t=2`:

$$
(X_2)_{\mathrm{diag}}=\frac{39}{400},
\qquad
(X_2)_{\mathrm{edge}}=-\frac{11}{800},
\qquad
(X_2)_{\mathrm{nonedge}}=\frac{1}{400}.
$$

These two slices generate every later channel value through the same recurrence.

## Why this is a breakthrough

This is the sharpest dynamical compression yet.

After `DCLXVIII`, the witness family averaged to a clean Markov kernel.

After `DCLXIX`, every power had an exact two-mode decomposition.

After `DCLXX`, the first two non-stationary slices recovered the full projector split.

After `DCLXXI`, we now know something stronger still:

> once the stationary mode is removed, the entire future is generated by a minimal order-two recurrence.

So the finite witness-average dynamics are now not only algebraically closed, dynamically closed, spectrally filtered, and self-identifying.

They are also **recurrence-complete**.

## Executable artifact

Verifier:

```text
verify_dclxxi_holonomy_markov_recurrence_bridge.py
```

Tests:

```text
tests/test_dclxxi_holonomy_markov_recurrence_bridge.py
```

Generated summary:

```text
data/dclxxi_holonomy_markov_recurrence_bridge.json
```

---
*W33-Theory | Part DCLXXI | after subtracting the stationary mode, the witness-average dynamics collapse to a minimal order-two exact recurrence with roots 1/4 and 2/5.*
