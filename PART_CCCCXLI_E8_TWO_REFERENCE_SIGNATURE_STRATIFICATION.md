# PART CCCCXLI — E8 Two-Reference Signature Stratification

This part upgrades CCCCXL from a degeneracy statement to an exact
stratification theorem.

## Setup

For each unordered pair of E8 reference roots $(a,b)$ (including $a=b$),
classify all roots $r$ by tuple class $(r\cdot a, r\cdot b)$, and encode the
class-count multiset as a signature.

Total pairs searched:

$$
\binom{240}{2}+240=28920.
$$

## Exact classification

There are exactly **three** signature families, with pair multiplicities:

$$
360,\quad 13440,\quad 15120.
$$

Only one family is feasible for the $24/108/108$ packet partition, and its size
is exactly

$$
15120.
$$

## Consequence

Two-reference degeneracy is highly structured: not arbitrary, but partitioned
into three exact strata with one feasible stratum.

## Honesty boundary

This is still a classification theorem for the two-reference ansatz.
The unique constructive operator dictionary remains open.
