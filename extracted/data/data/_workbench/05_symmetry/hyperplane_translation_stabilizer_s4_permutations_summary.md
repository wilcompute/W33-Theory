# Stabilizer elements as S4 permutations on b-coordinates

b-coordinates are (t0, -t1, -t2, t3) with sum zero; the four pairs correspond to which b-coordinate is zero.
See `hyperplane_translation_stabilizer_s4_permutations.csv` for elem_index -> permutation + sign.
