# Passes 2937–2945 evidence request

This branch exists only to trigger and retain observable evidence for the source packet already on `master`.

The dedicated workflow recomputes the exact certificate, runs eight focused regressions, simulates all 81 clean codewords and 1,296 directed one-bit faults, synthesizes and places the isodual syndrome decoder on HX8K, integrates the Photonic Holonet, machine blueprint, and website, and compiles all three canonical PDFs.

Do not merge until the job is green and generated evidence has been inspected.
