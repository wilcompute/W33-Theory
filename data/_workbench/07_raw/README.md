# Raw bundles and backups

Bundles in `data/`:
- `_archives/bundles/*/*_bundle.zip` (mirrors of key folders)
- `_archives/bundles/toe/toe_bundle.zip` (zip-of-zips)
- `_archives/extracted/toe_bundle` (extracted zip-of-zips)

Backups:
- Removed (IS Backup folders and extracted mnt_data backups cleaned up).
- Archive: `data/_archives/IS_Backup.zip`

Variants:
- `data/_variants` (non-identical suffix duplicates preserved for audit)
