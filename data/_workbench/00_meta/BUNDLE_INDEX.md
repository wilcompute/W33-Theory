# Bundle index

Summary:
- total bundles: 37
- unique stems: 31
- extracted targets present: 30
- extracted targets missing: 7

Notes:
- stems are `*_bundle.zip` with `_bundle` and trailing ` (n)` removed.
- extracted targets are resolved to `_toe`, `_is`, `_pg32`, or `_archives/extracted`.

Outputs:
- `data/_workbench/00_meta/BUNDLE_INDEX.csv`
