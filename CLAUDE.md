# Theory of Everything - AI Instructions

> Auto-generated by Continuity CLI. Edit the template or regenerate with `continuity generate`.
> Content above the auto-generated marker is preserved on regeneration.

<!-- AUTO-GENERATED BY CONTINUITY - DO NOT EDIT BELOW THIS LINE -->

# Project Memory (Auto-generated by Continuity)

This project uses Continuity to keep repo-local decisions and session context available through MCP and generated instruction files. Prefer MCP when connected; otherwise fall back to the repo files.

## Project Context
- **Total Decisions:** 4303
- **Known Topics:** needs-review, auto-draft, why, w33, testing, toe, architecture, photonic, docs, holonet, audit, qec, github, selector, tomotope

## Current State
**Branch:** master

**Recent Commits:**
- `7545613d7 BT367: Identify E6 holographic selector bridge and quantify 760-conflict holonomy phantom`
- `8a7ba8ce2 BT982: explicit integral E8 basis from W33 selector (script + test)`
- `13d9ca368 Pass 55: the two-arm contextuality discriminator (runnable end-to-end) and the scaling figure`
- `25bed31a8 Pass 54: the demonstrator's control arm (explicit even-q ovoid) and the scaling routing win`
- `bf8929ada Pass 53: parity not primality, a falsifiable prediction, and the table-free win`

**Working Tree:**
- M .continuity/SESSION_NOTES.md
- M .cursorrules
- M .github/copilot-instructions.md
- M .github/workflows/holonet-ci.yml
- M CLAUDE.md
- M GEMINI.md
- M HOLONET.md
- M analysis/holonet_cli.py
- M data/bt1494_photonic_qec_release_lock_repair.json
- M data/bt950_snf_transform_e8_extractor.json
- M data/bt951_exact_support_minimal_selector.json
- M data/bt953_support60_orbit_classifier.json
- M data/bt954_metric_selector_among_support60.json
- M data/w33_holonet_firmware_fabric_profile.json
- M docs/INDEX.md

## Working Memory Snapshot
**Mode:** degraded-fallback
**Generated At:** 2026-07-02T02:28:40.365Z
**Startup Summary:** mode=degraded-fallback | decisions=4303 | lastTask=2026-06-05 current goal: refresh origin/master, absorb the BT285-BT317 remote batch, and continue the selector/hypercube theory with a scoped verified packet. | next=Recent commits: | branch=master | health=MCP server disconnected. MCP probe timed out after 15000ms. [GitCommitDetector] Using cached audit (36 uncovered, 12min old)
[GitCommitDetector] Startup audit: 36 uncovered commits in last 7 days (auto-drafted 0) | fallback-files=12
**Decision Count:** 4303
**Fallback Chain:** AGENTS.md, CLAUDE.md, GEMINI.md, .cursorrules, .github/copilot-instructions.md, .continuity/INSTRUCTIONS.md, .continuity/SESSION_NOTES.md, .continuity/mcp-health.json, .continuity/unfinished-task.json, .continuity/working-memory.json, SESSION_HANDOFF.md, .continuity/decisions.json

## Workspace MCP Self-Test
**Status:** Ready
**Last Checked:** 2026-07-02T00:46:25.446Z
**Summary:** No MCP summary recorded.

## Resume Snapshot
**Last Task:** 2026-06-05 current goal: refresh origin/master, absorb the BT285-BT317 remote batch, and continue the selector/hypercube theory with a scoped verified packet.
**Last Completed Milestone:** Auto-saved at 2026-07-02T02:21:48.688Z (reason: file save)
**Next Expected Step:** Recent commits:
**Last Successful Tool Call:** log_decision @ 2026-07-01T21:34:35.793Z
**Relevant Files:** AGENTS.md, analysis\holonet_cli.py, analysis\holonet_uor_browser_demo.py, analysis\holonet_vm_demo_launcher.py, analysis\w33_hashimoto_packet_phase_bridge.py, analysis\BT367_verify_phantom.g

## Workflow

Use the repo-local artifacts directly — these are always available:

| Action | How |
|--------|-----|
| Load context | Read this file plus `.continuity/SESSION_NOTES.md` |
| Search decisions | `grep -i "<topic>" .continuity/decisions.json` |
| Log a decision | `continuity log "question" "answer"` |
| Update session notes | Edit `.continuity/SESSION_NOTES.md` directly |

**Decision logging path:** MCP `log_decision` first when connected — it exercises the production path. `continuity log "question" "answer"` is the always-works fallback when MCP is down. Do not treat `.continuity/SESSION_NOTES.md` or manual `.continuity/decisions.json` edits as the logging sink.
**Search-before-log still applies.** Check `.continuity/decisions.json` before recording a new rationale.

## Engineering Guardrails

**Real-time decision logging is MANDATORY.** Log each decision IMMEDIATELY after the code change — not batched at the end of a session. The trigger is the change, not the commit. If you edited a file, log the decision. Period.

**Override auto-memory for project decisions.** Claude Code's built-in auto-memory writes to a private `~/.claude/projects/.../memory/` store invisible to teammates, future sessions on other machines, and other AI tools. For Continuity-managed projects, that auto-memory is NOT the source of truth — `.continuity/decisions.json` is. When you would otherwise save a *project-scoped* decision (architecture, tradeoffs, "we chose X because Y") to auto-memory, route it through `continuity log` instead. Auto-memory is still appropriate for cross-project context (user preferences, environment quirks) but project decisions belong in Continuity.

**Search before you change.** Always search `.continuity/decisions.json` (via CLI or `grep`) before proposing architectural changes to check for prior decisions.

**Recovery.** If you realize earlier decisions were not logged, pause, log retroactively, and inform the user.

**Transparency.** Inform the user when you log decisions, recover missed decisions, detect drift, or find conflicts with past decisions.

**Anti-pattern to avoid:** "Let me implement all 3 fixes, then log them" — WRONG. Correct: Fix 1 done, log decision, Fix 2 done, log decision, Fix 3 done, log decision.

## CRITICAL OPERATING RULES

1. **ACT, DON'T ASK.** When Continuity tools are relevant, call them immediately. Do not ask permission.
2. **LOG DECISIONS PROACTIVELY.** Any time the user explains a choice or makes an architectural decision, log it without asking.
3. **PARALLELIZE TOOL CALLS.** When logging multiple decisions or updating session notes, make all calls in parallel.
4. **CONTEXT FIRST.** Read this file and `.continuity/SESSION_NOTES.md` for context.
5. **BE CONCISE.** After logging, give a short summary. Do not narrate your reasoning process.

---

## WHEN TO USE CONTINUITY

## ON EVERY SESSION START

Context is available in this file. Read it along with `.continuity/SESSION_NOTES.md` for full project context.

After loading context, mention the most relevant recent decisions to the user.

### When User Asks Architectural Questions
**ALWAYS** search `.continuity/decisions.json` for the relevant topic BEFORE answering (`grep -i "<topic>" .continuity/decisions.json`). Do NOT answer from memory alone.
Share what you found: "I found decision-X about this topic..."

### When User Explains a Choice
When the user says "let's use X because Y" or explains their reasoning:
**IMMEDIATELY** log the decision via CLI: `node continuity-cli/bin/continuity.js log "question" "answer" --tags tag1,tag2`
Do NOT ask "want me to log this?" — the user has already opted into decision logging.
After logging, tell the user: "Logged decision for future sessions."

### When Suggesting Changes
**ALWAYS** search `.continuity/decisions.json` for existing decisions on the topic BEFORE recommending changes.
If conflicts exist, mention them: "This would conflict with decision-X where you chose..."

### When User Mentions Blockers
Edit `.continuity/SESSION_NOTES.md` to track the blocker under the "Blockers" section.
Then search decisions for related context.

### When User Ends Session
Edit `.continuity/SESSION_NOTES.md` with progress summary and next steps.
Give the user a concise summary.

---

## CLI COMMANDS

### Search for decisions
```bash
grep -i "authentication" .continuity/decisions.json
# or: node continuity-cli/bin/continuity.js search "authentication"
```

### Log a new decision
```bash
node continuity-cli/bin/continuity.js log "Why did we choose X?" "Because Y" --tags topic1,topic2
```

### Update session notes
Edit `.continuity/SESSION_NOTES.md` directly.

### Generate handoff
```bash
node continuity-cli/bin/continuity.js sync
```

---

## Project Instructions

# Project Instructions

How this repository prefers to work with AI assistants.

## Git operations: use GitKraken (all agents)

**For anything git-related — searching history, status, staging, committing,
pushing, fetching, pulling, diffing, blame, branches — use the GitKraken MCP
tools, not raw `git` shell commands.** This applies to every agent working in
this repo.

- Status: `git_status` · Log/diff/search: `git_log_or_diff` (and `git_blame`)
- Stage/commit: `git_add_or_commit` · Push: `git_push`
- Fetch/pull: `git_fetch` / `git_pull` · Branches: `git_branch`,
  `git_checkout`

Always `git_fetch` and review recent `origin/master` commits before starting
or committing work — multiple agents push to the same `BT####` sequence, so
pick the next free number from `origin` and avoid duplicating an existing
packet.

Exception: operations the GitKraken tools do not expose (e.g. force-adding a
git-ignored artifact such as `data/*.json`, which needs `git add -f`) may use
the shell `git` as a narrow fallback; everything else goes through GitKraken.


## Recent Decisions
1. **decision-3587a7a6** (7/1/2026) [architecture, e6]
   - Q: What is the mathematical identity of the 120-sheet Holonet selector and its relation to the 51,840-tick supercycle?
   - A: The 120-sheet Holonet selector fabric (40 W(3,3) lines * 3 phases) is a holographic representation of the E6 root system. The 51,840-tick supercycle is the Weyl group W(E6). The 120-dimensional permutation representation on the sheets decomposes into five W(E6) irreducible representations with dimensions {1, 15, 20, 24, 60}. The 760 conflicts measured in the global holonomy solver are exactly 2 * 380, where 380 is the number of twisted cycles in the W(3,3) skew-line graph cycle basis. This identifies the 'Twisted Phase Phantom' as a specific topological obstruction in the E6-to-W33 projection.

2. **decision-18f248cb** (7/1/2026) [why, node]
   - Q: Why refine the presentation validation note with docs/index anchor checks?
   - A: Updated .continuity/SESSION_NOTES.md so the July 1 presentation verification records that the investor notes' six docs/index evidence anchors resolve to existing public cards, alongside the py_compile, content checks, HTML slide count, and node --check validation.

3. **decision-17825782** (6/27/2026) [auto-draft, needs-review]
   - Q: Why: BT1904: add exact estimator manifest?
   - A: (Auto-drafted from commit d1a9a99 on 2026-06-27.) Please review and expand with actual reasoning.

4. **decision-1a24be30** (5/17/2026) [cosmology, baryogenesis]
   - Q: What is the structural basis for the tensor-to-scalar ratio r and Baryon Asymmetry eta_B?
   - A: The tensor-to-scalar ratio r = 0.0222 is exactly 1/45, reflecting the reciprocal of the 45 tritangent planes on the W(3,3) cubic surface. Baryon asymmetry eta_B ~ 6.18 x 10^-10 scales exactly as the Higgs IR fixed point coupling lambda_h = phi - 1.

5. **decision-1f37fb97** (5/17/2026) [phenomenology, hierarchy]
   - Q: What explains the proton-to-electron mass hierarchy and the electron-to-neutrino scale?
   - A: The proton relates to the electron via the affine bulk * GQ geometry (T_7 + v)*q^q = 1836. The neutrino seesaw mechanism scales explicitly by the Leech Lattice kissing number (T_7 + f) * K(Lambda_24) = 10,221,120.

6. **decision-17776479** (5/1/2026) [master-lock, mckay]
   - Q: Why integrate McKay bridge directly into q3 master lock owner and test boundary surface?
   - A: The standalone McKay audit already certifies exact q=3 invariants, but the owner theorem surface must include those invariants directly to keep the integrated boundary honest. We added the McKay bridge record, theorem flag, and summary payload in the master lock plus explicit test assertions for record ordering and theorem truth so integrated certificates cannot drift from standalone McKay evidence.

7. **decision-17776480** (5/1/2026) [mckay, w33]
   - Q: Why add McKay Layers 11 and 12 to bridge audit?
   - A: Layer 11: sum of irrep dimensions of each binary polyhedral group equals the corresponding Coxeter number (sum_dims(2T)=h(E6)=k=12, sum_dims(2O)=h(E7)=18, sum_dims(2I)=h(E8)=30). Ratios {2,3,5} match the Fibonacci multipliers from Layer 10. Layer 12: k=12 anchor chain through Leech lattice dimension 24=mE6=2k, PSL(2,q) order=k, and SL/PSL center order=phi1=2. Both layers added to exact_factorizations and theorem dicts; boundary_note in master lock updated to document 12th layer. All 22 tests pass.

8. **decision-17775735** (4/30/2026) [audit, chapter6]
   - Q: Why add a Chapter 6 global-selector obstruction + lift witness audit?
   - A: To provide an executable frontier certificate for the missing selector/global-lift problem: a finite base-cycle monodromy obstruction (no one-turn fixed selector) together with a minimal Z2 lifted two-turn consistency witness. This advances the frontier without overclaiming full global closure on W(3,3)/Penrose dynamics.

9. **decision-mohlhmfy** (4/27/2026) [auto-scanned, configuration]
   - Q: Why use GitHub Actions for CI/CD?
   - A: Detected .github/workflows/ directory. GitHub Actions automates testing, building, and deployment pipelines.

10. **decision-mohlhk1j** (4/27/2026)
   - Q: Why: Sharpen tail wall to coordinate witness?
   - A: Commit f3a26e8f on 2026-04-27. (Auto-seeded from git history — edit to add your reasoning.)

---

## BEHAVIOR EXAMPLES

**User:** "Should we use Redis or Memcached for caching?"
**You:** *[Search: grep -i "caching" .continuity/decisions.json]*
"Found decision-12 about caching strategy. Based on that, here's my recommendation..."

**User:** "Let's go with PostgreSQL because we need ACID transactions for the payment system."
**You:** *[Log via CLI: continuity log "Why PostgreSQL?" "ACID transactions for payment system"]*
"Logged decision for future sessions."

**User:** "I'm thinking of switching to MongoDB."
**You:** *[Search: grep -i "database" .continuity/decisions.json]*
"Decision-12 chose PostgreSQL for ACID transactions. Switching to MongoDB would conflict. Want to proceed or keep PostgreSQL?"

---

*Auto-generated by Continuity CLI | Updated: 2026-07-02*

<!-- END CONTINUITY AUTO-GENERATED CONTENT -->
