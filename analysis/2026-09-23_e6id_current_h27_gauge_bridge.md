# 2026-09-23 — Canonical E6-id to current H27 compiler gauge bridge

A hidden gauge mismatch in the September compiler frontier is now explicit and
closed.

The canonical signed E6 cubic lives on labels e6id = 0,...,26. Pass1103 gives
an exact Heisenberg coordinate table on those labels, but the later
physical-Clifford compiler independently chose a different H27 address gauge.
Those two raw labelings must not be identified: their 45-line sets overlap in
only 10 triads, although all nine firewall center fibers are common.

The current compiler gauge is fixed by the physical-Clifford H27 normal form
\[
(a,b,c)=Z^aX^b\omega^c
\]
and five selected order-three directions. To connect the canonical cubic to
that gauge, impose two anchors:

1. canonical e6id 0 maps to H27 identity (0,0,0);
2. the canonical bad-nine/firewall spread maps to the current pure-center
   spread generated by (0,0,1).

A minimum-remaining-values backtracking solve on the colored
\(\operatorname{GQ}(2,4)\) incidence graph then closes in exactly 27 search
nodes. The resulting 27-point permutation is a bijection and satisfies:

- all 45 canonical cubic triads map exactly to the current five-direction
  right-coset line set;
- all nine canonical firewall fibers map exactly to the nine current center
  cosets;
- e6id 0 -> (0,0,0) -> complete frame 0.

This bridge is now the required row-index map whenever the canonical E6 cubic
is combined with the current \(K=H_{27}\times C_3\) Fourier/compiler chart.
Pass1103 remains a valid historical Heisenberg gauge, but it is used only as
such.

The bridge is a finite anchored coordinate conjugacy. It is not a dynamical
vacuum choice, nor does it make the two older coordinate tables literally
identical.

Evidence:
- analysis/w33_e6id_current_h27_gauge_bridge.py
- data/w33_e6id_current_h27_gauge_bridge.json
- tests/test_w33_e6id_current_h27_gauge_bridge.py
