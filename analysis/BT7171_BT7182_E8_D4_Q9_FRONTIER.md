# Passes 7171–7182 — E8/D4 association closure, q=9 local basin, E6 charge quotient, and D4 spread duality

## Boundary first

This packet is finite and exact where stated. It does **not** prove `alpha(W(3,9))=51`; Pass7180 only excludes target 48 inside a certified deletion-radius-five basin around the known residual clique in the canonical `(1,3,5)` anchor graph. It also makes no continuum particle, gauge-field, or hardware claim.

A correction from the unfrozen handoff is preserved explicitly: the 27 W33 nonneighbor `C6` fibers are **not** one-to-one with the 27 minuscule E6 weights. The weights arise after quotienting an 81-root sector by the selected A2 charge plane and are distributed across the raw fibers.

## 7171 — the complete colored E8 root scheme is generated by the +1 relation

On all 240 E8 roots, the inner-product relations have valencies

`1,56,126,56,1`

for equal, `+1`, orthogonal, `-1`, antipodal. The positive-inner-product graph has spectrum

`56^1 + 28^8 + 8^35 + (-2)^112 + (-4)^84`.

It therefore has five distinct eigenspaces and generates the whole five-dimensional Bose–Mesner algebra. Relative to the W33 `C6` lift, every relation is reconstructed by the W33 base relation, the cyclic shift `S`, and the exact cross-fiber voltage offsets. Thus the earlier positive-edge lift extends to the complete colored E8 association scheme.

## 7172/7179 — the selected 90 D4s form a rank-five association scheme

The 90 center-quad lifts are 90 selected D4 subsystems. The four nontrivial relations have degrees

`1,32,32,24`

and pair counts

`45,1440,1440,1080`.

The simultaneous eigenmatrix is

```
1   1  32  32  24
1  -1   8  -8   0
1   1  -4  -4   6
1   1   2   2  -6
1  -1  -4   4   0
```

with multiplicities `1,15,20,24,30`. The complete Krein table is nonnegative, but exhaustive primitive-idempotent ordering finds no Q-polynomial ordering.

The degree-32 share-one-point relation has exactly 80 maximal 9-cliques: 40 point-stars and 40 neighborhood-stars. The unique valency-one D4-complement involution swaps the two families. Quotienting them reconstructs W33, giving

`Aut(D90) = Aut(W33) x C2`, order `103680`.

## 7173/7180 — target 48 is excluded through deletion radius five

The known residual 47-clique in anchor type `(1,3,5)` splits as 42 invertible states plus five rank-one states. For exactly `d` deletions from the 42-state invertible core, arbitrary compatible additions from every other normalized state give exact maxima

`d=0..5 : 47,46,47,46,46,46`.

Hence no target-48 residual clique lies within deletion radius five of that core. This is a local basin theorem, not global q=9 optimality.

## 7174 — the E8 Z12 phase and Pauli commutator phase are different classes

The E8 complement-triangle holonomy is

`1^1440, 11^1440, 3^180, 9^180 (mod 12)`.

The canonical two-qutrit Pauli commutator cochain on the same W33 complement triangles has holonomy

`0^810, 4^1200, 8^1230 (mod 12)`.

Triangle holonomy is invariant under a coboundary, so the two cochains cannot be cohomologous: the E8 class is odd-valued while the Pauli class is divisible by four. The recurring coefficient group `mu_12` is real, but the two finite phase geometries are distinct.

## 7175/7181 — W33 shell to E6 x A2, with the corrected fiber statement

Choose one E8 six-root fiber as A2. Its W33 shell `1+12+27` becomes

`6_A2 + 72_E6 + 81 + 81`.

Each 81-root sector projects modulo the selected A2 plane to 27 E6 weights, each with multiplicity three, and the `1/3` inner-product relation is the Schlaefli graph `srg(27,16,10,8)`.

The raw 27 W33 nonneighbor fibers instead fall into nine classes of three and form a regular `C3`-voltage cover of `K9`. Base-triangle voltage holonomy is

`0^12, 1^36, 2^36`.

This is the corrected coordinate-free bridge.

## 7176 — each selected D4 is a miniature four-fiber C6 lift

Restrict the E8 hexagonal construction to the four W33 fibers of one center-quad. The 24-root D4 positive-root graph has spectrum

`8^1 + 4^4 + 0^9 + (-2)^8 + (-4)^2`.

Its 12 antipodal root lines split into exactly three orthogonal four-frames, each frame taking one antipodal pair from each of the four six-root fibers. This gives a concrete finite triality frame inside every selected D4.

## 7177/7182 — the D4 support codes close into the Steiner code duality

The 90 center-quad/D4 support vectors span the full even-weight code `[40,39,2]_2`.

The 45 orthogonal `D4 perp D4` supports span `[40,15,8]_2`.

The 27 ten-D4 spreads, viewed on the 45 orthogonal-pair coordinates, generate

`[45,21,5]_2`.

Its dual is

`[45,24,6]_2`

with exactly 120 minimum words. Exhaustive support comparison identifies those 120 words objectwise with the induced `K3,3` supports already known as the Pass5019 Steiner circuit code. Thus the ten-D4 spread code is exactly its binary dual.

## 7178/7182 — D4 lattice glue and the ten-D4 atlas

All E8 roots contain exactly 3150 D4 subsystems. The selected W33 family contains 90.

For two selected D4s, the W33 relation determines the integral lattice behavior:

- share one W33 fiber: span rank 6;
- disjoint cross-count 4: rank 8 and index 1 in E8;
- disjoint cross-count 7: rank 8 and index 1;
- orthogonal complements: rank 8 and index 4.

The orthogonal pair has diagonal discriminant glue

`{(0,0),(v,v),(s,s),(c,c)} ~= (Z/2)^2`.

Each of the 27 ten-D4 spreads consists of five orthogonal index-4 pairs and forty nonorthogonal index-1 pairs. Any two D4s chosen from different orthogonal pairs already generate the full E8 lattice. Therefore the ten-D4 object is not a direct `D4^10` sum; it is a redundant atlas of E8 presentations.

## Synthesis

The strongest exact chain is now

`W33 colored relations + C6 phase -> complete colored E8 root scheme`,

with a selected subsystem hierarchy

`90 D4 -> 45 (D4 perp D4) -> 27 ten-D4 spreads`,

and a coding shadow

`[45,21,5]^⊥ = [45,24,6]`,

where the 120 dual minimum words are the known Steiner `K3,3` circuits.

Meanwhile q=9 is locally much more rigid but remains globally open.
