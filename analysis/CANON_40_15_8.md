# Canonical owner: `[40,15,8]`, the sentinel dark-mode code

*De-duplication anchor, not a new result. Third-largest cluster in the sweep:
**29 files** assert `[40,15,8]` while citing none of the others. Companion to
[`CANON_240_81_3.md`](CANON_240_81_3.md) and [`CANON_40_10_4.md`](CANON_40_10_4.md).*

---

## Ownership

| what | file | first commit | when |
|---|---|---|---|
| **`[40,15,8]`** | **`analysis/w33_pass159_sentinel_dark_modes.py`** | **`9b8231481`** | **2026-07-10 14:48:33** |

Its predecessor `analysis/w33_pass158_chiral_trade_lattice_two_480s.py`
(`6af94d35d`, 2026-07-10 14:03:42) sets up the sentinel eigenspace but does not
assert this parameter triple; Pass 159 is the first to do so.

## What it is

A **linear** code `[40,15,8]` — note the single brackets. It is *not* a stabilizer
code and must not be conflated with `[[40,10,4]]`, which lives on the same 40
coordinates and is quantum. Same substrate, different object, different bracket
convention.

Pass 159's framing: the `g = 15` sentinel eigenspace is the trade lattice, its dark
modes are quantized, and the minimum-weight words are exactly the dark-mode support
quantum. The owner also records the lattice determinant `det(im Nᵀ) = det(ker N) =
2¹⁷·3¹⁰` and an external-balance property — every minimal dark mode is invisible
off its own support.

## The bracket trap

This cluster and the `[[40,10,4]]` cluster overlap heavily in the sweep because
both are "40-something-something" on the same point set. They are distinct:

| | brackets | kind | owner |
|---|---|---|---|
| `[40,15,8]` | single | classical linear code | Pass 159 |
| `[[40,10,4]]` | double | quantum CSS code | Pass 201 |
| `[[40,1,9,4]]` | double, 4 params | **RETRACTED** subsystem claim | Pass 206 withdrawal |

A file quoting "40" parameters should say which of these three it means.

## Scope

Ownership and disambiguation only. The distance and the determinant belong to
Pass 159 and are cited, not re-derived here.
