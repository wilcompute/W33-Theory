# Canonical owner: the `[[40,10,4]]` sentinel CSS code

*De-duplication anchor, not a new result. Second-largest cluster in the sweep:
**29 files** assert `[[40,10,4]]` while citing none of the others. Companion to
[`CANON_240_81_3.md`](CANON_240_81_3.md).*

---

## Ownership

By first commit adding the file (`git log --diff-filter=A`), per the standing rule
that the earlier commit owns it and the later one cites it:

| what | file | first commit | when |
|---|---|---|---|
| **the code `[[40,10,4]]`** | **`analysis/w33_pass201_sentinel_css_logical_shadow.py`** | **`f74c35459`** | **2026-07-13 14:17:55** |
| the orthogonal action on it | `analysis/w33_pass204_transversal_clifford.py` | `592163ba2` | 2026-07-13 14:43:39 |

Pass 204 is the file the sweep ranks first only because it names the parameters
most prominently; it credits Pass 201 in its own docstring. Pass 201 owns it.

## The result, and the scope its owner attached

`PSp(4,3)` permutes the 40 physical coordinates of the CSS code `[[40,10,4]]`,
acting on `H = C^⊥/C` through `O⁺(10,2)`.

The qualification is in Pass 204's own words and is routinely dropped when the
parameters are quoted:

> *"`H` is one 10-dimensional X/Z label copy, **not** the full logical Pauli space"*
> — and therefore not the full logical Clifford quotient.

Quoting `[[40,10,4]]` without that sentence overstates what the action controls.

## A withdrawn neighbour — do not resurrect it

`analysis/w33_pass206_subsystem_claim_withdrawal.g` is a **correction ledger**, and
it sits inside this cluster. It withdraws a subsystem claim `[[40,1,9,4]]`:

> *"The original Python experiment selected one representative in each of three
> filtration layers and then treated nine binary quotient directions as nine
> subsystem gauge qubits. Neither step establishes the advertised theorem: layer
> minima require exhaustive orbit/coset minimisation, and a gauge qubit requires a
> hyperbolic X/Z Pauli pair."*

GAP owns that withdrawal artifact specifically so that re-running the historical
Pass 206 entry point cannot restore the invalid claim. `[[40,1,9,4]]` is **retracted**
and is a different object from `[[40,10,4]]`, which stands.

## Scope

Records ownership and restores two things the cluster drops: the "one label copy,
not the full logical Pauli space" qualification, and the adjacent withdrawal. No
mathematics is introduced and the distance is cited, not re-verified.
