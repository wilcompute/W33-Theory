\ifdefined\WPassFiveTwoFiveFourLoaded\else
\gdef\WPassFiveTwoFiveFourLoaded{1}
\subsection*{Passes 5254--5261: radius-eight echo, modular $L$-Jordan form, and the zero-footprint residual}

The authoritative Passes~5238--5245 close the $q=5$ footprint distance and the complete apartment-code equality shell:
\[
 C_F=[325,65,25]_2,
 \qquad
 \{c:\operatorname{wt}(c)=625\}=\{\text{936 chamber stars}\}.
\]
The results below were developed concurrently under colliding draft filenames and are authoritatively renumbered 5254--5261; the original filenames are retained only as provenance.

\paragraph{The $q=3$ decoder at weight eight.}
The provenance-refined hard-decision decoder has a complete structured first-correction obstruction family at weight eight.  Every apartment center belongs to four local charts, and in each incident chart there are exactly two two-error masks on the other coordinates whose unique local leader is the center.  Independent choice in the four charts gives $2^4=16$ configurations per center and therefore
\[
 1620\cdot16=25920
\]
distinct weight-eight errors.  For every one, maximum vote followed by maximum singleton provenance selects the false center alone.  Nevertheless the full deterministic decoder self-heals in exactly three sweeps,
\[
 8\longrightarrow9\longrightarrow1\longrightarrow0.
\]
Thus the true-only/monotone first-correction guarantee is sharp at radius seven.  These configurations do not disprove eventual radius eight; an orbit-complete census of all weight-eight errors remains open.

\paragraph{Exact modular Jordan structure of the connected $L$ chart graph.}
The large binary radicals found previously are not semisimple reductions of the characteristic-zero spectrum.  At $q=3$,
\[
 \chi_A(x)\bmod2=x^{100}(x+1)^{440},
\]
with zero-primary blocks $J_1^{100}$ and one-primary blocks
\[
 J_1^{70}\oplus J_2^{22}\oplus J_3^{26}\oplus J_4^{62}.
\]
At $q=5$,
\[
 \chi_A(x)\bmod2=x^{3716}(x+1)^{6034},
\]
with
\[
 \ker(x\text{-primary})\sim J_1^{2156}\oplus J_3^{520},
\]
and
\[
 (x+1)\text{-primary}\sim
 J_1^{804}\oplus J_2^{1471}\oplus J_3^{48}\oplus J_4^{536}.
\]
These are exact finite GF$(2)$ Jordan decompositions at $q=3,5$.

\paragraph{Two-root quotient and the outer root shell.}
In canonical unipotent coordinates $(a,b,c,d)$ the commuting subgroup generated by the second and fourth positive-root directions has order $q^2$ and its right cosets are exactly the fixed-$(a,c)$ fibers.  Hence the quotient is coordinatized by $\mathbb F_q^2$.  In every characteristic greater than three, the distance-four root-Cayley shell projects exactly onto
\[
 (\mathbb F_q^*)^2,
\]
and every nonzero $(a,c)$ fiber contains precisely $(q-4)^2$ distance-four states.  Therefore
\[
 |S_4|=(q-1)^2(q-4)^2.
\]
At $q=5$ these fibers are objectwise the 25 $P$ atoms of a chamber star, so the shell occupies the $4\times4$ nonzero atom torus and misses the nine-atom coordinate cross.

\paragraph{The complete minimum dual shell as a real frame.}
Let $H_8$ be the $24375\times325$ incidence matrix of all weight-eight dual footprint words.  The rank-four pair scheme gives
\[
 H_8^TH_8=600I+25R_1+5R_2,
\]
with real spectrum
\[
 4800^1\oplus900^{90}\oplus425^{104}\oplus500^{130}.
\]
Thus $H_8$ has full real column rank.  Over $\mathbb F_2$ its rank is $259$, and the exact binary kernel is
\[
 \boxed{\ker_{\mathbb F_2}H_8=C_F\oplus\langle\mathbf1\rangle.}
\]
The complete minimum dual shell therefore misses exactly one binary bit, global parity; one independent odd weight-nine dual check removes precisely that mode.

\paragraph{Hoffman-13 shortened residual.}
The Hoffman 13-cover gives 13 systematic footprint coordinates.  Its shortening kernel has dimension $52$, and because the cover carriers partition all 156 W-points, vanishing on those coordinates forces even point-input parity.  Hence the shortening lies in the doubly-even hull and has
\[
 [312,52,d_{\rm sh}]_2,
 \qquad 28\le d_{\rm sh}\le40,
\]
with $d_{\rm sh}\in\{28,32,36,40\}$.  Sums of two collinear point footprints in one cover carrier furnish weight-$40$ shortened words.  The exact shortened minimum remains open.

\paragraph{Strict $q=5$ sector: a 560-dimensional zero-footprint block code.}
A local $P$ component is
\[
 C_0=\operatorname{Cut}(K_6)\otimes\operatorname{Cut}(K_6)=[225,25,25]_2.
\]
Exhaustion of all $2^{25}$ local states gives minimum nonzero even weight $40$, with exactly $180$ such words.  Every weight-$40$ word is a simple tensor of factor weights $5\times8$ or $8\times5$:
\[
 180=6\cdot15\cdot2.
\]
Since every strict q5 apartment word has zero $P$ parity, each active $P$ block is even and costs at least $40$ apartments.  Hence any word below $625$ has at most
\[
 \left\lfloor\frac{624}{40}\right\rfloor=15
\]
active $P$ components.

This reduction fits into an exact parity/syndrome sequence.  The full $P$-side space has dimension $8125$; its even-block subspace has dimension $7800$.  The connected-$L$ map has ranks $7500$ and $7240$ on these two spaces respectively.  The apartment code has dimension $625$ and its footprint image has dimension $65$, so the zero-footprint residual has dimension
\[
 \boxed{560}.
\]
Moreover
\[
 0\longrightarrow C_F\longrightarrow\mathbb F_2^{325}
 \longrightarrow
 \operatorname{Im}(L|V_P)/\operatorname{Im}(L|E)
 \longrightarrow0,
\]
where the final quotient has dimension $260$ and dual $C_F^\perp$.  Thus the complete strict q5 problem can now be replaced by one finite block-code target:
\[
 \boxed{d_{\rm block}(K_0)\ge16.}
\]
Such a theorem would eliminate every strict sub-$625$ word.  It is not proved here, so the full q5 distance theorem remains open on the strict side.

\paragraph{Minimum-shell two-distance frame.}
The 156 exact weight-$25$ footprint minimum words are the W$(3,5)$ point footprints.  Their incidence Gram is
\[
 FF^T=24I+4A_W+J,
\]
with spectrum $300^1\oplus40^{90}\oplus0^{65}$.  Every coordinate lies in 12 minimum words, hence the centroid is $(1/13)\mathbf1_{325}$.  After centering the Gram spectrum is $40^{90}\oplus0^{66}$.  Consequently the 156 centered vectors form an equal-norm tight frame for $\mathbb R^{90}$ with frame bound $40$.  After unit normalization the two off-diagonal inner products are
\[
 \boxed{\frac{2}{15}\quad\text{(collinear W-points)},\qquad
       -\frac1{25}\quad\text{(noncollinear W-points)}.}
\]
Thus the exact binary minimum shell recovers W$(3,5)$ collinearity as a real two-distance tight frame.

\paragraph{Evidence firewall.}
The q5 weight-$625$ equality shell is closed, but $d=625$ is not yet a theorem because the 560-dimensional zero-footprint strict residual may in principle contain a word below $625$.  The block-distance-$16$ statement is a sufficient target, not a proved fact.  The q3 decoder has guaranteed radius seven; the 25920 weight-eight echo configurations self-heal, but global eventual radius eight remains open.  The modular $L$-Jordan statements are exact only for q3 and q5, and the geometric P-atom interpretation of the two-root quotient is objectwise certified at q5.
\fi
