# Passes 3364–3375 — Clebsch–Petersen resilient closure

## Status

This packet executes the five requested continuation fronts, two independent high-risk constructions, and a dedicated eighth Clebsch-neighborhood program. The source verifier reconstructs every finite graph and code claim from first principles. The chromatic frontier remains

\[
\boxed{10\leq\chi(H)\leq11}.
\]

No source-only theorem is promoted to an observed FPGA, laboratory, fault-rate, geometric-hardware, or spacetime claim.

## 3364 — full block-distance-invariant nonseparable \(M_{22}\) compiler

The earlier separable family \(A_{45}\otimes K\) cannot improve the Hoffman value nine. The next exact symmetry class is the full block-distance algebra

\[
W=I_{45}\otimes K_0+A_{45}\otimes K_1+(J-I-A_{45})\otimes K_2,
\]

where \(K_0,K_1,K_2\in\mathrm{Sym}_{22}(\mathbb Q)\) are independent. This is already nonseparable unless the three matrices are proportional.

Because the 45-block graph has eigenvalues \(32,2,-4\), while its nonedge matrix has eigenvalues \(12,-3,3\) on the same three eigenspaces, positivity is exactly equivalent to three 22-by-22 conditions:

\[
\boxed{K_0+32K_1+12K_2\succeq0},
\]
\[
\boxed{K_0+2K_1-3K_2\succeq0},
\]
\[
\boxed{K_0-4K_1+3K_2\succeq0}.
\]

Thus the complete block-distance-invariant cone reduces from a raw \(990\times990\) matrix to three rational \(22\times22\) cones, or 759 symmetric coordinates. This compiler is exact. It is not yet a dual certificate excluding ten colours. Any improvement beyond this class must break block-distance invariance and retain oriented bridge/profile data.

## 3365 — self-protecting systematic envelope code

The previous four-bit Clebsch tag was assumed trusted. Here the raw five-bit envelope word is preserved systematically and parity bits are appended so the complete transmitted word corrects any two bit flips.

For valid raw words \(x_i,x_j\), parity labels \(p_i,p_j\in\mathbb F_2^r\) must satisfy

\[
d_H(p_i,p_j)\geq5-d_H(x_i,x_j).
\]

An exhaustive symmetry-reduced backtracking search fixes the first parity word to zero without loss of generality. The complete node counts are:

| parity bits | search nodes | result |
|---:|---:|---|
| 4 | 2 | impossible |
| 5 | 7 | impossible |
| 6 | 443 | impossible |
| 7 | 1,388 | witness found |

Therefore the minimum systematic construction is

\[
\boxed{5+7=12\text{ bits},\qquad d_{\min}=5}.
\]

The frozen 22-word code has pair-distance histogram

\[
5^{79}6^{90}7^{24}8^{15}9^{17}10^5 11^1.
\]

It corrects any two errors across the raw state and parity together. The price of removing the trusted-side-channel assumption is exactly three additional bits beyond the four-bit Clebsch axis tag.

## 3366 — dynamic/static replication boundary

For static one-cycle failover, every one of the 16 logical controller states must exist in two simultaneous physical slots. Hence 32 slots are necessary and the mirrored \(Q_5\) construction is optimal.

Allowing migration after fault detection does not recover a state whose only live copy was destroyed. A sub-32 architecture therefore requires an external checkpoint, parity reconstruction, or recomputation contract. “Dynamic migration” alone does not evade the 32-slot lower bound.

For the mirrored \(Q_5\) state placement:

- catastrophic two-vertex sets: 16 replica pairs;
- survivable two-vertex sets: \(496-16=480\);
- catastrophic three-vertex sets: \(16\cdot30=480\);
- survivable three-vertex sets: \(4960-480=4480\).

A three-vertex set is catastrophic exactly when it contains one complete replica pair. This is a state-availability theorem, not a latency guarantee for every surviving route.

## 3367 — fusion algebra of minimum gauge defects

A minimum defect lives on two edges of one filled triangle and is indexed by an omitted edge \(i\in\{0,1,2\}\) and a nonzero coefficient vector \(v\in\mathbb F_3^5\).

Write this defect as \(T_i(v)\). Then:

1. Same omitted edge:
   \[
   T_i(v)+T_i(w)=T_i(v+w).
   \]
   It annihilates exactly when \(w=-v\); otherwise it remains minimum.

2. Distinct omitted edges in the same face: the sum is another minimum defect exactly when the coefficients cancel on their shared nonzero edge. Otherwise all three face edges become nonzero and the fusion has weight three.

3. Distinct filled faces: the 240 filled triangles are edge-disjoint, so two minimum defects have disjoint supports and generic fusion weight four.

Projectivizing \(v\sim-v\), every direction in \(PG(4,3)\) carries a three-state omitted-edge fusion triangle. The 87,120 projective defects therefore decompose locally into 29,040 exact fusion triples before cross-face composition.

## 3368 — triple-fault mirrored-cube closure

The graph \(Q_5\) has vertex and edge connectivity five. Consequently every set of at most four edge failures leaves the physical network connected. Logical-state availability is stricter because each state has exactly two antipodal-layer copies.

The exact low-order availability census is:

\[
\begin{array}{c|c|c}
\text{failed vertices}&\text{catastrophic}&\text{survivable}\\\hline
2&16&480\\
3&480&4480
\end{array}
\]

Every catastrophic triple contains exactly one replica pair plus one additional vertex. No triple can contain two complete pairs. Full triple-fault schedule optimality remains an RTL/search evidence target rather than a promoted theorem.

## 3369 — the Clebsch–Petersen–\(K_5\) nested geometry

The folded-cube checksum graph admits a canonical rooted decomposition

\[
\boxed{1+5+10}.
\]

Represent each antipodal class of five-bit words by the unique subset of \(\{1,2,3,4,5\}\) of size at most two. Relative to the empty subset:

- distance zero: the empty set;
- distance one: the five singleton directions;
- distance two: the ten 2-subsets.

The five singletons form an independent set. A 2-subset is connected to its two incident singleton directions. Inside the ten-state second shell, two 2-subsets are adjacent exactly when they are disjoint. Therefore

\[
\boxed{\Gamma_2(x)\cong KG(5,2)\cong\text{Petersen graph}}
\]

for every Clebsch vertex \(x\).

The checksum network is consequently a nested incidence machine:

\[
\text{one reference axis}
\longrightarrow
5\text{ fault directions}
\longrightarrow
10\text{ direction pairs/Petersen channels}.
\]

This explains why two-error recovery naturally exposes ten pair channels rather than an arbitrary collection of syndromes.

## 3370 — Cayley, code-coset, and full symmetry descriptions

Choose four independent folded-cube coordinates. The Clebsch graph is the Cayley graph

\[
\operatorname{Cay}\left(\mathbb F_2^4,
\{e_1,e_2,e_3,e_4,e_1+e_2+e_3+e_4\}\right).
\]

The five connection directions form one binary circuit: their sum is zero and every four are a basis. Permuting these five directions gives \(S_5\), while translation gives \(2^4\). Hence

\[
\boxed{\operatorname{Aut}(\mathrm{Clebsch})\cong2^4{:}S_5,\qquad |\operatorname{Aut}|=1920}.
\]

Equivalently, the graph is the coset graph obtained from \(Q_5\) by quotienting the binary repetition code

\[
\langle11111\rangle=[5,1,5].
\]

The five-dimensional raw word, four-dimensional antipodal checksum, and five symmetric fault directions are therefore three views of one exact code quotient.

## 3371 — 16-line geometric incidence boundary

A smooth degree-four del Pezzo, equivalently a Segre quartic over an algebraically closed field of characteristic not two, has 16 lines. The line-incidence graph is another standard realization of the same 16-vertex folded-cube/Clebsch geometry; deleting one line and all five lines meeting it leaves the ten-line Petersen configuration.

This is a mathematically meaningful structural parallel: the same \(1+5+10\) decomposition appears as one line, the five lines meeting it, and the Petersen graph on the remaining ten. The repository does **not** identify controller states with physical algebraic-surface lines, nor does this establish an optical implementation. The connection is retained as an incidence model and falsifiable dictionary only.

## 3372 BONKERS — Petersen ten-channel syndrome router

The ten two-bit error directions are the ten 2-subsets of five raw coordinates. Their disjointness graph is Petersen. A syndrome engine can therefore route pair-fault hypotheses on a fixed cubic network:

- each channel has three disjoint-pair neighbors;
- no triangles occur, suppressing three-way local ambiguity;
- every nonadjacent pair has one shared coordinate and is separated by a two-step Petersen route.

This turns the second shell of the checksum into a hardware scheduling graph for pairwise fault hypotheses. It is a graph-theoretic router proposal, not yet an observed resource or timing improvement.

## 3373 BONKERS — five-direction Fourier checksum

For a character \(a\in\mathbb F_2^4\), the Clebsch adjacency eigenvalue is

\[
\lambda(a)=\sum_{s\in S}(-1)^{a\cdot s}.
\]

Because \(S\) is the five-element binary circuit, the exact spectrum is

\[
\boxed{5^1,1^{10},(-3)^5}.
\]

The multiplicities reproduce the rooted combinatorics in a dual form:

\[
1+10+5=16.
\]

The five-dimensional \(-3\) channel is naturally indexed by the five fault directions; the ten-dimensional \(+1\) channel matches the ten direction pairs. Thus the spatial shell decomposition \(1+5+10\) and Fourier decomposition \(1+10+5\) exchange the singleton and pair sectors. This supplies a compact transform-domain detector architecture for future RTL comparison.

## Evidence boundary

The exact source packet proves the graph, code, fusion, symmetry, and low-order availability statements. The block-distance PSD compiler is exact but does not decide \(\chi(H)\). Remote RTL simulation, synthesis, placement, manuscript builds, and any geometric or physical realization remain separately gated.
