# Pass5444–5451 executed outcomes

This packet is the direct continuation of the all-q Levi Hodge/apartment frame stack and the q=5 K0/q=3 provenance decoder frontiers.

## Pass5444 — all-q apartment Cauchy–Binet volume budget

For signed apartment incidence `C`, rank `r=q^4`, Pass5396 gives `CC^T=N E_cyc` with `N=(q+1)^2(q^2+1)`. After restriction to an orthonormal cycle basis, Cauchy–Binet yields

`sum_{|S|=r} det(C_S^T C_S)=N^r`.

Dependent subsets contribute zero.

## Pass5445 — q=3 redundancy/conditioning tradeoff

The full 1620-apartment frame has rank81, redundancy20, condition1 on the cycle space and pseudoinverse norm `1/sqrt(160)`. The deterministic 81-apartment BFS basis of Pass5443 has condition `sqrt(160)` and inverse norm1. This is an exact comparison of those two systems, not an optimality statement over all 81-apartment bases.

## Pass5446 — all-odd footprint double-quotient ladder

Pass5376 closes the point-side kernel: `ker(F^T)=C_W`, while Pass5350 identifies `M0/C_W ~= Rad(im F^T)`. This is distinct from the apartment-side exact sequence `0 -> D_ap=K0 -> H1^* -> C_F -> 0`, whose kernel has dimension `q^4-g` and remains nonzero.

## Pass5447 — q=5 weight1040 support reduction

Any weight1040 `K0` word must have exactly26 active P-blocks, every one a local weight40 minimum. The weight8 shell moment inequality and the exact 3-class block association scheme leave only21 integer pair profiles; every survivor has310..316 `R1` pairs. Exhaustive testing of all46,800 known 25-block point-footprint-plus-one extensions leaves zero survivors. Weight1040 is not excluded, but any witness must have an exotic26 support.

## Pass5448 — q=3 radius11 frontier

Exact MILPs close `V=1` and `V=2`. `V=3` reduces to two local branches: weight6+5 outsiders and weight7+4 outsiders. `V=4` shrinks from10,963,673,616 raw rooted cases to exactly35,218,336 necessary cases. Representative necessary survivors in each remaining branch self-heal under the deterministic decoder, but the full radius11 census is not closed.

## Pass5449 — apartment lattice-index sum of squares

For any independent rank-`q^4` apartment set `S`, the Gram determinant is `tau(Levi)` times the square of the index of the generated apartment sublattice in the integral Levi cycle lattice. Hence

`sum m_S^2 = N^(q^4)/tau(Levi)`.

At q=3 this is `2^322 * 5^58`.

## Pass5450 — negative Pell square clock

For odd q, the square class of the unsigned Gram determinant is `(q^2+1)/2`. Thus the determinant is a perfect integer square exactly when `q^2-2m^2=-1`. The first nontrivial Pell q-coordinates are `7,41,239,1393,8119,...`; only prime-power q are admissible finite-field parameters. No even q gives a square determinant.

## Pass5451 — Pell/Weil compatibility

Negative Pell implies `q=+/-1 mod8`. Pass5361/5406 independently identify this as the branch where the binary logical Weil factors are individually defined over F2. Hence every odd-q square unsigned Gram determinant lies in the split F2 Weil branch. The converse is false (`q=9` is a simple counterexample).

## Evidence boundary

The packet does not claim q5 weight1040 nonexistence, q3 radius11 closure, globally optimal conditioning of the deterministic BFS basis, any physical meaning for Pell solutions, or remote CI success until the workflow is explicitly observed green.
