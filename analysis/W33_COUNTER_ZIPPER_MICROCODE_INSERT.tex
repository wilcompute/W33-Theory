\subsection{Preemptible authenticated counter microcode}
\label{ssec:counter-zipper-microcode}
The existing authenticated counter ISA admits a persistent path-context
implementation, specializing Huet's zipper rather than asserting a new
data-structure theorem. A trusted continuation carries the committed guest
state, both fibre tags, a phase, and cursor, stack and result roots.
\texttt{SCAN} consumes one carry/borrow digit; \texttt{UNWIND} rebuilds one;
\texttt{COMMIT} emits the existing portal route. Each tick opens at most one
canonical bit node and writes at most one. Only a verified final continuation
changes the committed guest registers and program counter.

A propagated prefix of length $k$ takes $2k+3$ ticks. The finite-prefix
scan and unwind terminate and refine the existing macro instruction.
For a $4096$-bit carry or borrow, the witness gives $8195$ ticks, with the
same result as the independent macro verifier; all $512$ small cases agree.
A short increment commits after three one-tick scheduling rounds while a
long carry remains uncommitted. This bounds bit-node work, not wall-clock
latency, program validation, wire bytes, RAM or physical gate cost.

Retention must cover the continuation, not just the visible guest registers:
a paused borrow has $17$ nodes under the committed roots but $24$ under the
complete live roots. A full-closure checkpoint retains both fibre tags,
recovers under a trusted continuation identity, and survives collection while
its archive reference is strong. A hash-only audit reference does not retain
its bytes. The scalable counter semantics supply the universality argument;
a finite controller or fixed finite hash space alone does not.
The executable, frozen certificate and adversarial recovery tests are
\texttt{w33\_counter\_zipper\_microcode.py},
\texttt{w33\_counter\_zipper\_microcode\_certificate.json} and
\texttt{test\_w33\_counter\_zipper\_microcode.py}.
Prior art, the phase argument and implementation limits are recorded in
\texttt{analysis/W33\_COUNTER\_ZIPPER\_MICROCODE.md}.

Compiler admission is a separate obligation from content identity. In the
dynamic sparse witness, replacing the valid Wasm constant $77$ by $78$
preserved every opcode but formerly left the counter target returning $77$.
The exact-witness compiler now checks decoded operands and module bindings,
rejecting unsupported inputs before target generation. Three regression tests
cover differential memory execution, five same-opcode mutations and an
accepted custom-section variation. This enforces the documented restricted
source language; it is not a general compiler-equivalence theorem.

The process microstep owner now separates reusable arithmetic evidence from
consumption of a particular process cursor. Eight identical-value forks use
$19$ inner proofs for $152$ accepted process-bound submissions; all $152$
immediate duplicate submissions reject. Final values agree with an independent
macro verifier while all eight histories and continuation identities differ.
The trusted serialized owner publishes guest state only at macro commit.
Seven owner tests plus the seven process-kernel and nine zipper tests pass.
This is an in-memory execution protocol, not durable exactly-once external I/O;
see \texttt{analysis/W33\_PROCESS\_MICROSTEP\_OWNER.md} for the executable,
prior ownership, retention roundtrip and scope.

A subsequent SQLite adapter publishes the consumed cursor and its complete
live bit closure in one transaction. Killing a worker before commit recovers
tick $8$ and permits one retry; killing it after commit recovers tick $9$ and
rejects the retry. Both borrow executions finish in $35$ ticks at $65535$.
The final macro commit also survives a lost reply without committing twice.
Five durability tests and seven existing owner tests pass, including a
two-connection submission race and restart after every tick. These are process
exit experiments, not power-loss tests or exactly-once external effects.
The database is trusted authority and each tick writes a full snapshot;
see \texttt{analysis/W33\_DURABLE\_MICROSTEP\_OWNER.md}.

Exact accounting further separates work, content and observable information.
Writing $s(x)$ for binary popcount, $L$ increments from $a$ take
$5L+2s(a)-2s(a+L)$ microticks. From zero, preemption adds $M-s(M)$ bit-write
operations relative to the prior macro backend, but adds no distinct bit nodes
when all earlier counter values remain retained. For a uniform $n$-bit initial
value and $L=2^m$, $0\le m<n$, individual completion times reveal
$m+2-2^{1-(n-m)}$ bits, while total batch time reveals only
$2-2^{1-(n-m)}$ bits. The proof identifies the transcript with the low $m$
bits and the trailing-one count of the remaining high part.
An explicit reversible XOR permutation erases the correlated timing record
using the retained final counter: its conditional entropy is zero even when
its marginal entropy is positive. This supplies no measured heat or zero-energy
device claim. Exact backend counts, $45$ timing partitions and a $1024$-state
permutation witness are in \texttt{analysis/W33\_CARRY\_TIMING\_INFORMATION.md}.

\paragraph{Radix, noisy observations, and synthesized erasure.}
The executable extension
\texttt{w33\_computation\_resource\_frontiers.py} checks an abstract radix-$b$
zipper law
\[
 T_b(a,L)=3L+\frac{2}{b-1}\bigl(L+s_b(a)-s_b(a+L)\bigr).
\]
For $X$ uniform on $0,\ldots,b^n-1$ and $L=b^m$, the individual timing entropy
is $m\log_2 b+h_b(n-m)$, whereas the total duration has entropy $h_b(n-m)$,
where
\[
 h_b(d)=(1-b^{-d})\left(\frac{b}{b-1}\log_2 b-\log_2(b-1)\right).
\]
Forty finite ensembles check this identity; classical radix symbols do not
establish coherent qutrit hardware. For mixed binary arithmetic, each crossing
of the edge $u-1\leftrightarrow u$ costs $3+2v_2(u)$ ticks. A closed cycle at
$2^k$ costs $4k+6$, preventing a uniform constant amortized bound for both
operations using a counter-state potential.

For exact timing $Z$, final counter $Y=X+L$, and independent additive observation
noise $E$, the noisy record $O=Z+E$ satisfies $H(O\mid Y)=H(E)$.
Thus noise can reduce input leakage while leaving randomness that $Y$ cannot
uncompute. Four timings with independent jitter $(-2,0,2)$ of probabilities
$(1/4,1/2,1/4)$ leak about $2.005$ bits but retain six conditional record bits.
For the exact record, a prefix circuit reconstructs the statistic $(r,k)$ from
$Y$ with $d=n-m$ clean ancillas, $2d$ NOT gates, $2(d-1)$ Toffoli gates, and
$m+2+2d-\operatorname{popcount}(d)$ CNOT gates, then restores its ancillas.
Twenty-eight sizes pass exhaustive clean-ancilla checks. The five-bit example
uses eighteen gates. This is a basis-permutation construction, not measured
physical dissipation. The accompanying report separately attributes a published
colloidal-trap work measurement and keeps Holonet calibration open.

\paragraph{Redundant counters and eight observation experiments.}
The follow-up \texttt{W33\_EIGHT\_COMPUTATION\_EXPERIMENTS.md} reports seven
executed computational investigations and one blocked physical collection.
An authenticated redundant counter stores $P-N$ with $P\geq N$ and increments
only $P$ or $N$; canonical root equality supplies its zero test under the trusted
invariant. For $U$ increments, $D$ successful decrements and $Z$ zero decrements,
\[
 T=Z+5(U+D)+2s(P_0)+2s(N_0)-2s(P_f)-2s(N_f).
\]
All 320 tested six-operation paths pass. At the $2^{64}$ boundary, 128 alternating
cycles take 1390 arithmetic microticks rather than 33536, at the cost of growing
representation state; wrapper work and integration into the guest ABI remain
outside this claim.

Retaining reversible noise seeds permits noisy-record erasure, but reusing a
common additive seed exposes timing differences: four timings again reveal all
three input bits in the tested ensemble. A ternary eraser uses eight controlled
modular-shift gates on nine qutrits; its 19683-state permutation and a complex
superposition preserve amplitudes while clearing the intended record and
ancillas in simulation. Sixteen padding policies supply an exact finite
latency--information frontier. Holotrade separately exports signed pencil
preimages and rational primal--dual certificates with real $\ell_1$ optima 5
and 8, so the selected profiles' negativity survives fractional relaxation.
These statements neither construct blockers nor measure physical heat. The
reset-work protocol and analyzer reject synthetic provenance, and physical
collection remains blocked without instrument data.

\paragraph{Redundant guest and controlled-qutrit follow-up (September 9).}
The executable adapter \texttt{w33\_redundant\_guest\_abi.py} implements
both logical counters as monotone differences, with image-bound expected-parent
receipts and derived INC/DECJZ/HALT control.  It passes 550 differential guest
steps; stateful consumption remains a separate owner obligation.
The existing ternary eraser now has a declared one-/two-qutrit target circuit:
44 gates, including 24 two-qutrit gates, using Fourier transforms,
equality-controlled shifts and ninth-root phases.  Basis-gadget and arbitrary
coherent-state checks preserve phases; physical calibration is unprovided.
For four observations of a uniform three-bit input, enumerating all 16 fixed
fresh-seed placement subsets gives minimum leakage $2.512799852$ bits at a
four-bit retained-seed budget, attained by the two alternating subsets.
This finite-family optimum excludes adaptive channels.  The companion
Holotrade dual-potential audit forces 16 zero coefficients for the mass-12
representative and sign constraints for both certified representatives.
See \texttt{W33\_FIVE\_COMPUTATION\_FOLLOWUPS.md}; physical reset collection
remains blocked by missing measurements.

\paragraph{Durable consumption, Clifford--T lowering, and adaptive privacy.}
The September 11 follow-up implements atomic macro-receipt consumption of
redundant two-register guests, persisting the exact four-root live closure with
the current head.  Restart, competing-writer, and process-death controls pass;
physical power loss and external I/O remain outside scope.
The controlled ninth-root target gates now lower to qutrit Fourier, Pauli,
Clifford phase, SUM, and $T=\operatorname{diag}(1,\zeta,\zeta^{-1})$ gates,
using two clean workspaces returned to zero.  The full eraser uses 794 gates
and passes a coherent-state check; this is logical gate-set synthesis, not
measured fault tolerance.
A public causal privacy policy with at most two independent timing masks
(four retained seed bits) improves classical guessing probability from
$53/64$ for the best fixed subset to $49/64$.  Applied to the certified
85-state trace observer, adaptive suppression of at most one output improves
$77/85$ to $71/85$.  These finite-family optima exclude quantum side
information.  Companion Holotrade rational dual receipts certify the existing
mass-28 circuit escapes at negative mass one without a runtime solver.
Physical reset collection remains blocked by absent data.  Full statements
and prior ownership appear in \texttt{W33\_SEPT11\_EXECUTION\_FRONTIERS.md}.

\subsection{Reye sector control and its symplectic obstruction}
The certified carrier
$\{X,Y,Z\}_1\times\{I,Z\}_2\times\{I,X\}_3$ generates
$\mathfrak{su}(2)^{\oplus4}$ under independently switchable signed
continuous Hamiltonian controls. Its rank-two sector projectors are
$Q_{s,t}=(I+sZ_2)(I+tX_3)/4$.
A sector-addressed rotation $\exp(-i\theta A_1Q_{s,t})$ lowers to four
commuting selected-Pauli pulses with angles
$\theta(1,s,t,st)/4$.
Adding $X_2,Z_3,Z_2$ successively gives Lie dimensions
$12,20,36,63$. After the first two additions the commutant is scalar,
but the skew form $J=Y_1Z_2X_3$ is preserved:
$P^TJ+JP=0$. The resulting algebra is compact $\mathfrak{sp}(4)$
on $\mathbb C^8$; adding $Z_2$ gives $\mathfrak{su}(8)$.
Exhaustion of all $\binom{51}{2}=1275$ pairs of extra individual Pauli
controls proves that three such additions are minimal for this fixed carrier.
The executable certificate \texttt{analysis/w33\_reye\_sector\_control.py}
also compiles all 63 Pauli rotations through exact conjugation identities,
checks their coherent matrices, and verifies a second realization.
These are ideal finite-register control results, not a physical Holonet
implementation, a noise threshold, or a scalable universal-machine proof.

\subsection{Connected Reye registers and exact cross-fibre frames}
The register family now has an explicit inter-tile port:
$Z_{1,r}Z_{1,r+1}$, independently switchable on a tile chain.
Local full control and these ports lower routed CNOTs through
$\mathrm{CZ}=e^{-i\pi/4}R_{Z_a}(-\pi/4)R_{Z_b}(-\pi/4)R_{Z_aZ_b}(\pi/4)$.
All 108 ordered routing pairs on one through three tiles pass, and a
non-port two-tile CNOT lowers to 247 declared Pauli pulses with its
full coherent matrix checked. This supplies a logically scalable circuit
architecture under the stated ideal coupling assumptions.
Shortest conjugation templates reduce the 63-rotation total from 369 to
337 pulses and the worst case from 21 to 15; all-pair cost inequalities
certify the bound within the recursive conjugation grammar.
Synthetic channel sweeps separately track coherent gate fidelity and
sector leakage: dephasing can damage coherence without leaving a sector.
These model data are not device calibration.

The Schur cross-fibre kernel also has explicit matrices. With
$\omega=(-1+i\sqrt3)/2$, set
\[
 A=\frac1{\sqrt3}\begin{pmatrix}-1&1\\2&1\end{pmatrix},\qquad
 B=\frac1{\sqrt3}\begin{pmatrix}-1&\omega\\2\bar\omega&1\end{pmatrix}.
\]
They preserve the binary quartic and Hessian, obey $A^2=B^2=I$ and
$AB=-BA$, and generate the sixteen matrices $i^pA^aB^b$.
An explicit conjugator identifies them with the one-qubit Pauli group;
the original-coordinate positive Hermitian form is $\operatorname{diag}(2,1)$.
The phase-aware frame update is
$(p,a,b)(q,c,d)=(p+q+2bc\bmod4,a\mathbin\oplus c,b\mathbin\oplus d)$.
Companion Holotrade now offers an opt-in one-step exact-dual transaction
mode checking actual integer vectors and rational optimality before signed
delivery. Full code, synthetic data, and scope appear in
\texttt{W33\_REYE\_FIVE\_FOLLOWUPS.md}; no physical scalability or
fault-tolerance claim follows from these finite certificates.

\subsection{Routing costs, composite-control limits and frame normalizers}
The next execution packet supplies weighted routing on arbitrary declared
CNOT graphs.  Its move-control-and-restore grammar minimizes twice the
SWAP cost plus the terminal CNOT cost; 210 finite cases match an independent
simple-path oracle.  Commutation across pulse-template boundaries reduces
the 63-rotation batch from 337 to 301 pulses while preserving its full matrix.
A BB1 sequence on selected-sector quadratures improves common amplitude-error
sensitivity but requires nine times the duration.  The tested mixed-error
case becomes worse, so no unconditional robustness claim follows.
Simultaneous phased sector quadratures are an explicit added control assumption.

For the previously supplied Schur-to-Pauli conjugator $S$, the two matrices
$SHS^{-1}$ and $S\operatorname{diag}(1,i)S^{-1}$ normalize the sixteen kernel
matrices and preserve the supplied Hermitian form.  Their induced frame maps
have order 24 and pass all 6144 kernel multiplication checks.  The full
unitary normalizer retains a central $U(1)$; the finite order refers to its
projective action.  No preservation of the whole Schur configuration is
inferred.  Companion Holotrade now verifies bounded exact-dual move histories
with every intermediate state derived in integer arithmetic.
See \texttt{W33\_REYE\_ROUTING\_ROBUST\_FRAMES.md} for interfaces, citations,
reproduction and the negative control-error findings.

\subsection{Shared ports, echoed sectors and quartic-preserving frames}
The dependency-preserving circuit scheduler now reserves both endpoint buses
for cross-register operations: the checked example takes 19 rather than 26
serial ticks. Clifford-frame propagation removes a four-pulse conjugation
identity that commuting-only reduction misses; its 63-rotation benchmark still
lowers to the prior 301 native pulses, so abstract frame counts are not treated
as physical savings. Combining selected-sector BB1 with $Z_2$ echoes suppresses
common amplitude error and modeled $X_2$ crosstalk in the tested cases. The
16-cycle construction assumes 160 instantaneous error-free echo pulses and
simultaneous phased controls; finite-bandwidth performance remains open.

Exactly twelve of the twenty-four explicit projective Clifford frames preserve
$\phi$ up to a phase multiplier $a$. Four corrections satisfying
$\lambda^4a=1$ per accepted frame recover the prior forty-eight quartic lifts.
The Hessian character has three values, four projective frames each, recovering
the prior sixteen-element joint kernel. This connects explicit frame maps to
the existing $A_4$ result without claiming a new classification.
Companion Holotrade verifies arriving move chunks atomically and resumes from
trusted-digest checkpoints by replay; final optimality remains gated on the
exact dual proof. See \texttt{W33\_REYE\_PORTS\_FRAMES\_ECHO\_STREAMS.md}.
