# BT1869-BT1871 summary

Executed the three requested moves after BT1868 and dug deeper through the code and dynamics obstructions.

## Search trail

Searched for existing artifacts around:

```text
BT1869/BT1870/BT1871
[[66,23,?]] distance search
weight-2 logical defects
gauge contrast quotient
global clock stabilizer
BC/Fibonacci Rule-110 ring lifts
gap ratio phi and BT1342 quasicrystal clock
```

No prior distance search, gauge quotient, or larger-ring Rule-110 lift existed.  BT1342 remains the clock/Fibonacci anchor.

## BT1869 — distance search for the [[66,23,?]] skeleton

Input from BT1868:

```text
payload symbols = 66
X rank = 42
Z rank = 1
candidate k = 23
Z stabilizer = global all-distance clock-sum row
```

Result:

```text
weight-2 X logical defect found
quantum CSS distance upper bound = 2
```

Example:

```text
edge(0,1) + 2*edge(0,2)
```

This commutes with the global Z clock row because `1+2=0 mod 3`, and it is not generated by the 44 face-row X stabilizer span.

Verdict: the BT1868 skeleton is a clean commuting object but not a protected memory.

## BT1870 — gauge-contrast quotient construction

The six distance rows split as:

```text
one commuting global clock row
five noncommuting distance contrasts
```

Quotient bookkeeping:

```text
distance row space = span(H1,...,H6)
stabilizer subspace = span(H1+H2+H3+H4+H5+H6)
gauge contrast subspace dimension = 5
quotient dimension = 1
```

Payload accounting:

```text
BT1868 naive k = 23
five gauge contrasts = 5
effective payload sector = 18
```

This matches the BT1862 payload kernel dimension.  The quotient organizes the defect but does not remove the BT1869 weight-2 X logical.

## BT1871 — larger-ring Rule-110 glider lift

Seeded larger rings by BC/Sturmian prefixes with

```text
alpha = arccos(-2/3)/(2*pi)
```

and lengths:

```text
6*F for F in {5,8,13,21,34}
30, 48, 78, 126, 204
```

Results:

```text
N=30,  steps=120: repeat found from 44 to 104, period 60
N=48,  steps=192: no repeat
N=78,  steps=312: no repeat
N=126, steps=504: no repeat
N=204, steps=816: no repeat
```

All tested rings see all eight Rule-110 neighborhoods.  Larger rings keep entropy high and preserve active domain walls.

Conclusion: the length-30 box is too small and can trap the dynamics.  The BC/Fibonacci larger-ring lift is the correct dynamical test bed.

## Boundary

BT1869 falsifies protection for the current CSS skeleton, BT1870 organizes the gauge quotient without solving distance, and BT1871 is a finite larger-ring dynamical diagnostic.  No quantum distance theorem, completed subsystem code, Rule-110 universality proof, or physical implementation is claimed.
