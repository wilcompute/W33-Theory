# Pass 467 — measured blind-run software handoff

Pass 467 freezes the Pass-451 protocol into a fail-closed hardware ingestion runner.

The accepted acquisition contract is:

1. a 16 by 16 exact integer transfer matrix, with one positive denominator per row;
2. a label-free sealed CSV containing sample IDs, SHA256 commitments, and sixteen counts per sample;
3. predictions generated by the unchanged minimum exact affine-fit residual classifier;
4. abstention below the unchanged margin \(1/100\);
5. an optional reveal accepted only after prediction-hash binding and commitment verification;
6. balanced accuracy as the unchanged primary endpoint.

The package includes calibration, sealed-observation, manifest, and reveal templates plus a lab runbook. The parser rejects truth-like columns, duplicate IDs, malformed commitments, out-of-range counts, incomplete acquisition metadata, and unbound reveals.

A deterministic schema rehearsal reproduces the Pass-451 transferred templates and scores 96/96 synthetic samples with balanced accuracy one.

**Physical boundary:** no measured optical transfer matrix or sealed hardware holdout is present. The software gate is closed; the hardware gate remains `OPEN_NO_MEASURED_INPUT`. No laboratory claim is made.
