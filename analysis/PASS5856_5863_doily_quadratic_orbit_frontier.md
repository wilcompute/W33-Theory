# Passes 5856–5863 — doily quadratic orbit, S6 grid model, and binary Radon radical

This packet adds new mathematics after the collision-recovered matrix frontier.

## 5856 — saturated Radon maps collapse into the mod-2 radicals

For the saturated common-W9 coordinate maps,

- rank2(R^T)=5,
- rank2(D)=5,
- rank2(H^T)=2.

The exact new statement is

\[
\operatorname{im}(R^T\bmod2)=\operatorname{im}(D\bmod2)=\operatorname{rad}(A_3\otimes A_3\bmod2),
\]

a 5-dimensional space. The point/heavy radical has dimension 3; all three natural source radical generators map under R^T and D to the single all-ones 3x3 grid vector, so the restriction has rank 1 and kernel dimension 2. H^T instead has a 2-dimensional image inside the heavy radical and kills the point radical.

Thus modulo two the saturated Radon information lands in the degenerate lattice shadow rather than descending to the nondegenerate quotient.

## 5857 — the 16 quadratic refinements are one bent 10+6 packet

On V=M2(F2), let q0(X)=det(X) and B be its polar form. Every refinement is

\[
q_v(X)=q_0(X)+B(v,X).
\]

Exactly 10 refinements have nine nonzero zeros (hyperbolic grids) and 6 have five nonzero zeros (elliptic ovoids). For f_v=(-1)^{q_v}, the symplectic Walsh transform obeys

\[
\widehat f_v=+4f_v
\]

on all ten grid forms and

\[
\widehat f_v=-4f_v
\]

on all six ovoid forms. Therefore the doily 10-grid/6-ovoid hyperplane split is exactly the +/- bent-Walsh sign split of the sixteen quadratic refinements.

## 5858 — the six ovoids reconstruct all ten 3+3 partitions

Any two ovoids meet in exactly one doily point, and their 15 pairwise intersections recover all 15 points. For a grid G, join two ovoid labels when their unique intersection point lies in G. Exactly nine pairs are selected, every label has degree 3, and the graph is K3,3. Its bipartition is an unordered 3+3 partition of the six ovoid labels.

Running over all grids gives exactly all

\[
\frac12\binom63=10
\]

unordered 3+3 partitions.

## 5859 — explicit Sp(4,2) = S6 from the ovoid action

The exact symplectic group has order 720. Its action on the six ovoids gives 720 distinct permutations, hence is faithful and exhausts S6. The ten-grid action is also faithful and, under the previous grid-to-partition map, is exactly the induced S6 action on unordered 3+3 partitions.

## 5860 — K10 edges are pointed doily angles

Every pair of distinct grids meets in five points. Their induced collinearity graph is K1 joined to 2K2: two doily lines through one center. The 45 grid pairs give 45 distinct intersections. The doily has

\[
15\binom32=45
\]

pointed unordered pairs of lines through a point, and these are exactly the same objects. Thus edges of K10 on the ten grids are canonically the pointed two-line angles of the doily.

## 5861 — nested rook graphs L2(4) -> L2(3)

The unit-difference Cayley graph on all 16 matrices is L2(4)=SRG(16,6,2,2). At zero, the six neighbours are the six units and the nine distance-two vertices are the nine rank-one matrices. Their induced graph is

\[
L_2(3)=\operatorname{SRG}(9,4,1,2),
\]

the 3x3 determinant grid. The determinant stabilizer restricts faithfully to 72 permutations of this grid, exactly its full row/column/ruling symmetry.

## 5862 — the simplex puncture deletes a unit-only nonisotropic line

Under the explicit Pass-5825 product-coordinate relabeling, the three deleted coordinates in the simplex [15,4,8] -> Reye [12,4,6] puncture map to a projective line whose three points are all units. There are exactly two projective lines wholly inside the six units. Both are nonisotropic for the doily polar form, and transpose fixes each setwise.

Thus the puncture does not delete a doily context; it deletes one of the two nonisotropic unit triples in the six-point complement of the determinant grid.

## 5863 — boundary

All statements are finite geometry, coding, lattice and Fourier results. The six ovoid labels are not six particles; the bent signs are not q=5 physical phases; the mod-2 radicals are not gauge fields; and the rook inclusion is not a spacetime hierarchy.
