# Passes 3769–3786 — GQ(4,2), Veldkamp PG(5,2), W33 ovoids, axial algebra, and controlled lattice symmetry breaking

## Status

`PASS_EXACT_EIGHT_FRONT_SOURCE_MONSTER_AND_ROOTLESS_LEECH_PENDING`

Semantic certificate:

```text
8d3f383e362f30e58d8c482f48e2ac2b77414922366ba8053633859d6206313a
```

This packet was originally reserved under 3751–3768. A parallel agent subsequently published a distinct 3751–3758 packet. The overlapping reservation was retired, every active artifact was moved to 3769–3786, and the semantic certificate was recomputed from the unchanged mathematical payload.

## Inputs and evidence model

The verifier reconstructs the complete finite model rather than importing numerical tables:

- the six-dimensional minus quadratic space over `F2`;
- its 27 singular and 36 nonsingular nonzero vectors;
- all projective lines of `PG(5,2)`;
- the quadrangles `GQ(2,4)` and `GQ(4,2)`;
- the full order-51,840 orthogonal action and its order-25,920 even subgroup;
- all Lagrangian three-spaces;
- all exact-cover ovoids;
- the rank-24 primitive projector and its coordinate algebra;
- integral representation matrices for the lattice calculations.

Published terminology is used only after the finite structures are regenerated. In particular, the literature identifies:

- `V(GQ(2,4))` with `PG(5,2)` and gives the `45+216+270+120` line split;
- the 200 ovoids of `GQ(4,2)` as 40 plane ovoids and 160 tripods;
- `U4(2):2` as an order-51,840 group with a maximal `W(F4)` subgroup of order 1,152 and index 45.

The executable additions below are repo results, not claims copied from those references.

---

# Front 1 — complete the 45-axis association algebra

## The dual generalized quadrangles

The singular vectors form `GQ(2,4)`:

```text
points = 27
lines = 45
points per line = 3
lines through a point = 5
point graph = SRG(27,10,1,5)
```

Dualizing gives `GQ(4,2)`:

```text
points = 45
lines = 27
points per line = 5
lines through a point = 3
point graph = SRG(45,12,3,3)
spectrum = 12^1, 3^20, (-3)^24
```

The verifier enumerates every maximal clique and finds exactly 27 maximal `K5`s, precisely the 27 quadrangle lines.

## Bose–Mesner multiplication

Let `A0=I`, `A1=A`, and `A2=J-I-A`. Then

```text
A1^2  = 12 A0 + 3 A1 + 3 A2
A1 A2 =          8 A1 + 9 A2
A2^2  = 32 A0 + 24 A1 + 22 A2.
```

The primitive idempotent ranks are

```text
1, 20, 24.
```

In the order `(E0,E20,E24)`, the nontrivial Krein rows are

```text
q11 = (20,10,15/2)
q12 = (0,9,25/2)
q22 = (24,15,21/2).
```

Every Hadamard-product identity is checked entrywise with exact rational arithmetic.

## Terwilliger closure

At any point, the three subconstituents have sizes

```text
1, 12, 32.
```

The algebra generated by the adjacency matrix and the three dual idempotents has

```text
dimension = 16
center dimension = 5.
```

A generic central element acts on the standard 45-dimensional module with block multiplicities

```text
2, 3, 8, 14, 18.
```

The dimension and center data give the semisimple type

```text
M3 ⊕ M2 ⊕ C ⊕ C ⊕ C.
```

This closes the previously queued association-algebra front.

---

# Front 2 — resolve the complete triality/incidence tower

## Veldkamp `PG(5,2)`

There are 63 nonzero six-bit vectors. The quadratic form splits them into

```text
27 singular vectors
36 nonsingular vectors.
```

All 651 projective lines `{x,y,x+y}` split as

```text
45  = three singular vectors
216 = two singular + one nonsingular
270 = one singular + two nonsingular
120 = three nonsingular vectors.
```

The 120 all-nonsingular lines coincide objectwise with the 120 Norton/Fischer triples from Passes 3722–3728.

## Incidence ranks

The exact rational and binary ranks are:

| incidence matrix | shape | rank over Q | rank over F2 |
|---|---:|---:|---:|
| singular points × GQ lines | 27×45 | 21 | 21 |
| Lagrangian frames × GQ lines | 135×45 | 45 | 45 |
| Lagrangian frames × nonsingular points | 135×36 | 36 | 29 |
| Norton lines × nonsingular points | 120×36 | 36 | 30 |
| plane ovoids × GQ lines | 40×45 | 25 | 15 |

The regularity identities are

```text
3 frames per GQ line
4 nonsingular points per frame
15 frames through each nonsingular point
10 Norton lines through each nonsingular point
9 GQ lines in each plane ovoid
8 plane ovoids through each GQ line.
```

This places the repo’s `27`, `36`, `40`, `45`, `120`, and `135` carriers inside one finite incidence system.

---

# Front 3 — refine the Monster descent target

The internal target is now the full order-51,840 group

```text
U4(2):2 ≅ O6-(2),
```

not merely its order-25,920 even subgroup.

A future computational embedding must simultaneously reproduce:

```text
36 Fischer involutions
45 GQ points
27 GQ lines
45 W(F4) stabilizers of order 1152
135 W(D4) frames of order 192
40 plane ovoids carrying the W33 graph
120 all-nonsingular Veldkamp/Norton lines.
```

Two literature-supported descent environments are retained as search routes:

1. a route through the Monster maximal subgroup `2^2.^2E6(2):S3`, then a suitable `U6(2):2` local subgroup containing `U4(2):2 × S3`;
2. a Fischer route through `Fi22` or `2.U6(2)`, using the listed `2.2^(1+8):(U4(2):2)` subgroup.

These are search routes, not executed subgroup chains. The strict candidate slot remains `PENDING` and contains no promoted elements.

The promotion harness requires independent group order, class correspondence, incidence digests, and a content hash. Matching one or two order relations is insufficient.

---

# Front 4 — minimally symmetry-broken positive lattice sections

## W(D4) obstruction

For a representative order-192 frame stabilizer, the invariant alternating forms on the rank-24 integral module have binary dimension seven.

Enumerating all 128 forms gives ranks

```text
0^1, 4^3, 6^4, 8^12, 10^12, 14^40, 16^56.
```

No invariant form has full rank 24. Therefore every `W(D4)`-invariant even integral Gram matrix is singular modulo two.

Hence:

```text
No W(D4)-invariant even unimodular rank-24 child exists.
```

## Explicit C2-preserving child

The order-25,920 group has 45 involutions with trace eight on the rank-24 module. For a deterministic representative `R`:

```text
R^2 = I
trace(R) = 8
rank(R-I) = 8.
```

An integral determinant-minus-one basis change conjugates `R` to an exchange of two eight-dimensional blocks while fixing a third. Pulling back

```text
E8 ⊕ E8 ⊕ E8
```

produces an explicit positive-definite even-unimodular Gram matrix with

```text
determinant = 1
maximum absolute entry = 24
exact U4(2) stabilizer = C2.
```

Gram SHA-256:

```text
1fee7de2ab3affc3d343e4856495c8e5fb480adcb17496b65bf9c1cf3280b660
```

This executes controlled symmetry breaking beyond the earlier trivial-stabilizer child.

It does **not** solve the rootless problem: `E8^3` has roots. An explicit rootless Leech basis and its exact intersection with the rank-24 group remain pending.

---

# Front 5 — move beyond the failed 2A target

Let

```text
P=(A-12I)(A-3I)=90 E_{-3}.
```

Its entries are

```text
diagonal    48
adjacent   -12
nonadjacent  3.
```

On the rank-24 image define

```text
x*y = E_{-3}(x coordinatewise y)
```

and normalize the 45 columns by

```text
a_i = P e_i / 21.
```

Every `a_i` is an idempotent. Its left-multiplication spectrum is

```text
1^1, (1/7)^14, (-1/3)^9.
```

The fusion law is

```text
1·1                 -> 1
1·(1/7)             -> 1/7
1·(-1/3)            -> -1/3
(1/7)·(1/7)         -> 1, 1/7, -1/3
(1/7)·(-1/3)        -> 1/7, -1/3
(-1/3)·(-1/3)       -> 1, 1/7, -1/3.
```

The algebra is positive Frobenius. Its multiplication operators generate all

```text
M24(F_1000003), dimension 576,
```

which proves simplicity over characteristic zero.

Every derivation kills each idempotent because

```text
D(a)=D(a^2)=2 a D(a)
```

and `1/2` is absent from the axis spectrum. The 45 axes span the rank-24 space, so the derivation algebra is zero.

The fusion law is not nontrivially `Z2`-graded, so this is not a Miyamoto/Monster-2A algebra. It is a different exact positive axial algebra selected by `GQ(4,2)`.

Pair multiplication has closed geometric formulas.

For collinear axes:

```text
a_i a_j = -(a_i+a_j)/3,
```

and the five axes on every quadrangle line sum to zero.

For noncollinear axes with three common neighbors `c1,c2,c3`:

```text
a_i a_j = (a_i+a_j)/7 + (5/42)(a_c1+a_c2+a_c3).
```

---

# Bonkers 1 — W33 is the 40-plane-ovoid graph

The exact-cover search finds 200 ovoids of the dual quadrangle. The full order-51,840 group splits them into orbits

```text
40 + 160.
```

The 40-object orbit is the published plane-ovoid orbit; the 160-object orbit is the tripod orbit.

Among the 40 plane ovoids, two objects share either one or three quadrangle lines:

```text
540 pairs share one line
240 pairs share three lines.
```

Declare adjacency when the intersection has size three. The resulting graph is

```text
SRG(40,12,2,4).
```

An explicit graph-isomorphism computation identifies it with the repo’s independently reconstructed `W(3,3)` point graph.

If `M` is the 40×45 plane-ovoid incidence matrix, then

```text
M M^T = 8 I + 2 A_W33 + J,
```

with spectrum

```text
72^1, 12^24, 0^15.
```

This is an objectwise reconstruction of W33 from the plane ovoids of `GQ(4,2)`.

---

# Bonkers 2 — the 120 Norton triples are Veldkamp type IV

The prior 120 Norton/Fischer triples were constructed from the rank-15 spread algebra.

The new six-bit projective reconstruction shows that they are precisely the 120 lines of `PG(5,2)` containing three nonsingular points. In the published Veldkamp language, these are exactly the type-IV lines consisting of three `GQ(2,2)` hyperplanes.

Thus the Fischer three-transposition geometry is not an isolated graph artifact. It is one entire line class of the Veldkamp space of `GQ(2,4)`.

---

# Bonkers 3 — 135 frames are not 135 flags

There are two natural transitive 135-object actions:

1. the 135 Lagrangian three-spaces, interpreted as `D4` frames;
2. the 135 ordinary point-line flags of `GQ(4,2)`.

Both map three-to-one onto the same 45 quadrangle points/lines, and both have stabilizer order 192.

They are nevertheless inequivalent.

Frame stabilizer census:

```text
1^1, 2^43, 3^32, 4^84, 6^32.
```

Ordinary-flag stabilizer census:

```text
1^1, 2^27, 3^32, 4^36, 6^96.
```

The permutation-character norms and cross-inner product are

```text
<frame,frame> = 9
<flag,flag>   = 8
<frame,flag>  = 4.
```

Therefore `135=45×3` supports two different actions: a triality-twisted Lagrangian frame action and the ordinary quadrangle flag action.

---

# Evidence boundary

## Proved by this source packet

- complete `GQ(4,2)` association, Krein, and Terwilliger data;
- the full `PG(5,2)` line census;
- exact identification of the 120 Norton triples with all-nonsingular Veldkamp lines;
- the complete 200-ovoid census and `40+160` orbit split;
- an explicit W33 graph on the 40 plane ovoids;
- exact inequivalence of the two 135-object actions;
- the 45-axis rank-24 positive Frobenius algebra;
- the `W(D4)` modulo-two unimodularity obstruction;
- an explicit `C2`-preserving `E8^3` child.

## Not proved

- explicit words in the Monster or a verified Monster class fusion;
- a rootless, canonical, or symmetry-optimized Leech basis;
- a Majorana, Griess, or VOA realization;
- remote CI or successful PDF compilation until observed;
- any hardware, laboratory, or physical mechanism.

## Principal sources for terminology and search routes

- M. Saniga et al., *The Veldkamp Space of GQ(2,4)*, arXiv:0903.0715.
- M. Saniga, *On the Veldkamp Space of GQ(4,2)*, arXiv:1001.0659.
- ATLAS/CTblLib entries for `U4(2).2`, `U6(2):2`, and `Fi22`.
- H. Dietrich, M. Lee, A. Pisani, and T. Popiel, *Explicit construction of the maximal subgroups of the Monster*, arXiv:2411.12230.
