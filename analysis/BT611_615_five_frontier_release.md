# Passes 611–615: spectral boundary, homotopy, equivariance, fault coding, and sequential inference

This release executes the five non-sequential directions following Pass 610. Every result is backed by a deterministic script, immutable JSON certificate, and focused regression.

## Pass 611 — spectral Smith boundary

The exact twisted Singer characteristic polynomial splits into a 24-dimensional integral packet and a square-free 256-dimensional arithmetic core. The integral eigenvalues are

`9^1, 10^6, 12^1, 13^3, 14^3, 15^4, 18^5, 21^1`.

The integral packet contributes low-prime determinant valuations `(16,18,10,4,3)` at `(2,3,5,7,13)`, while the 256-dimensional core contributes `(60,45,5,3,2)`. The block `A5` Gram matrix `I5+J5` symmetrizes the operator, proving reality and diagonalizability.

A same-spectrum/different-Smith counterexample—`diag(2,2)` versus `[[2,1],[0,2]]`—proves that spectral information alone cannot reconstruct the integral Smith lattice. The exact Smith form therefore genuinely requires the lattice calculations of Passes 597, 601, and 606.

## Pass 612 — complete Johnson clique homotopy type

The full clique complex of `J(8,3)` has maximal simplices

- 28 star simplices of dimension five,
- 70 top simplices of dimension three,

and f-vector

`(56,420,840,490,168,28)`.

A deterministic sequence of 518 elementary simplicial collapses reduces it to a 2-complex with f-vector `(56,420,490)`. A spanning-tree presentation then has 365 free edge generators. Exactly 365 elementary Tietze eliminations remove them and leave 125 trivially attached 2-cells. Hence

`Cl(J(8,3)) ≃ wedge_{125} S^2`.

Its integral homology is `H0=Z`, `H2=Z^125`, and zero otherwise.

## Pass 613 — equivariant groupoid replacement

The strict stabilizer in `S8` of the least-exterior-pair connection is the Klein four group generated by `(0 1)` and `(6 7)`. Its core in `S8` is trivial, so the connection orbit has

`40320/4 = 10080`

sectors and carries a faithful `S8` action. Inducing the 280-dimensional twisted Laplacian over this orbit gives a 2,822,400-dimensional block groupoid operator with a genuine non-scalar `S8` action and scaled torsion filtrations.

This resolves the scalar-commutant boundary correctly: symmetry is restored by moving between connection sectors, not by pretending one fixed 280-dimensional block has a hidden commuting action.

## Pass 614 — fault-detecting oriented selector code

The selector is upgraded to a length-20 constant-weight-three code:

- four source one-hot rails,
- four target one-hot rails,
- twelve oriented-edge checksum rails.

The 12 oriented tetrahedral edges become 12 codewords with minimum Hamming distance four. Exhaustive enumeration verifies that every corruption of weight one, two, or three is detected and every single-bit rail loss, false activation, or detector inversion is corrected. The oriented checksum also catches every endpoint permutation that changes the selected transporter.

A fully correlated relabeling of all banks remains information-theoretically invisible without an external absolute rail reference.

## Pass 615 — sequential third-trace falsifier

A four-stage controller samples `Tr(U)` and `Tr(U^2)` at cumulative budgets `(21,4)`, `(42,8)`, `(63,12)`, and `(84,16)`. It stops when posterior confidence exceeds `0.99`; otherwise poor primary fit or failure to decide by 100 photons invokes a 48-photon `Tr(U^3)` audit.

Under the Pass-610 calibration fixture, nominal mean total use ranges from 25.4 to 56.0 photons, worst-class accuracy exceeds 0.995, and the third trace is invoked on fewer than four percent of trials.

A trace-three-only departure is exactly invisible to any policy observing only the first two traces. For a shift of 1.5 and a 48-photon audit, conditional detection power is about 0.893. A sparse sentinel audit is therefore the minimal remedy for this information-theoretic blind spot.

## Validation

The five scripts pass 49 of 49 internal checks, regenerate byte-identical certificates under `--check`, compile, and pass the focused regression.
