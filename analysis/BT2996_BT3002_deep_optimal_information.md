# Passes 2996–3002 — adaptive, locality-aware, self-synchronizing information closure

## Evidence ladder

- **Exact finite mathematics:** Passes 2996, 2997, 2998, 3001.
- **Exact decision tree with explicit synthetic prior:** Pass 2999.
- **Exact conditional rewrite ledger:** Pass 3000.
- **Exact predictive information ledger for the frozen model:** Pass 3002.
- **Source-complete RTL:** Pass 3001 cyclic synchronizer.
- **Still open:** global fixed-schedule minimality of 29, laboratory optical loss, noisy/erasure decision policies, placement/timing, and physical reset energy.

## System-level result

The optimal live architecture is not a large fixed diagnostic panel. It is an adaptive reversible controller:

1. retain the native mixed-radix state;
2. measure the exact common 23-triangle base;
3. stop immediately for 44,848 of 48,826 hypotheses;
4. request one hypothesis-dependent triangle for almost every remaining case;
5. request a second extra triangle only for 412 cases;
6. route addresses by at most two native symplectic transvections;
7. maintain permutations, Pauli byproducts, gauge choices, and terminal phases in software/frame state whenever exact covariance permits;
8. retain only the next action and residual risk when the past transcript is no longer needed.

This converts the machine from a fixed test-and-correct pipeline into a predictive information system whose physical work is triggered only by future distinguishability.

## Pass 2996 — static and adaptive nonabelian localization

The exact fault universe contains 48,826 hypotheses: no fault, one faulty edge carrying one of seven nonidentity elements of `D4`, or two such faulty edges. The full 120-triangle group-valued syndrome is injective.

The exact 23-triangle single-edge-optimal base already separates 44,848 hypotheses. It leaves 1,436 collision classes, each of size at most three. Collision types by support size are

```text
(1,2)       126
(1,2,2)     168
(2,2)       204
(2,2,2)     938
```

where `1` and `2` denote one-edge and two-edge hypotheses.

### Conditional fixed optimum

Holding the frozen 23-row base fixed, an exact binary MILP proves that six additional triangles are necessary and sufficient:

```text
(0,1,3), (0,7,9), (1,6,8),
(2,4,5), (2,8,9), (5,6,7).
```

Thus the frozen base has an exact 29-row completion. This does **not** prove that no different 28-row schedule exists; the global static bound remains

\[
23\le m_{\rm fixed}^{\star}\le29.
\]

### Exact adaptive dominance

A collision-specific information-gain tree has depth distribution

```text
23 triangles: 44,848 hypotheses
24 triangles:  3,566 hypotheses
25 triangles:    412 hypotheses
```

so the worst case is 25 and the uniform mean is

\[
23.0899111129.
\]

Only thirteen possible first escalation tests and ten possible second tests are needed. Operationally this dominates every fixed 29-row strategy regardless of whether the unresolved global fixed optimum is 28 or 29.

## Pass 2997 — the natural 540-object identification is obstructed

Both earlier constructions contain 540 objects, but their natural order-three actions disagree.

For the W33 skew-line frames, the symplectic transvection associated with `a=(1,2,0,0)` has no fixed frame and partitions the 540 frames into 180 three-cycles. Its permutation character is therefore zero.

On the D12 clock-orbit labels, the corresponding translation acts within each twelve-cycle and fixes all 540 orbit labels. Its character is 540.

Consequently no bijection intertwining these natural `C3` actions exists. The counting bridge

\[
2\cdot27\cdot10=40\cdot27/2=540
\]

survives, but it is a shared quotient count rather than a natural equivariant identification.

## Pass 2998 — exact locality-aware address routing

All forty projective W33 addresses and all forty rank-one symplectic transvections were reconstructed. Exact breadth-first search over ordered source/target pairs gives

```text
word length 0:   40
word length 1: 1080
word length 2:  480
```

Therefore every address transfer needs at most two transvections.

A one-step transfer exists exactly when the two projective points are noncommuting. Distinct commuting W33-adjacent pairs require exactly two. The complete 1,600-pair canonical word library is regenerated by the exact source.

At a modeled primitive survival of 0.995, one- and two-step survival values are 0.995 and 0.990025. Those are sensitivity values, not measured optical losses.

## Pass 2999 — Bayesian adaptive calibration and routing policy

For the explicit sparse synthetic prior

```text
P(no fault)       = 0.995
P(one fault)      = 0.0045
P(two faults)     = 0.0005
```

uniform within each fault class, the exact adaptive tree has

\[
P(\text{escalation})=0.00423797155
\]

and expected triangle count

\[
E[m]=23.0062407751.
\]

Stopping after the common 23 rows gives Bayes residual error

\[
2.6200783\times10^{-5},
\]

whereas the exact adaptive policy has zero residual error in the noiseless D4 model.

The next-action alphabet contains only fourteen actions: `STOP` plus thirteen first escalation tests. It therefore needs four fixed bits, and under the frozen sparse prior its Shannon entropy is only

\[
H(A)=0.0517343817\ \text{bits}.
\]

The cost ratio at which exact escalation and immediate stopping are indifferent is

\[
c_{\rm triangle}/c_{\rm error}=0.00419832199.
\]

Erasures and noisy optical outcomes require posterior updates using component-resolved calibration rather than this noiseless decision tree.

## Pass 3000 — fake-gate elimination audit

Eight exact rewrite classes are frozen:

1. terminal permutation to detector/output relabeling;
2. terminal diagonal phase to discard or phase-frame update;
3. Pauli byproduct to a software Pauli frame;
4. D4 gauge conjugation to conjugated tables and syndrome labels;
5. reverse route to inverse lookup;
6. three affine support outputs to one trit decoder;
7. binary ranking to an archive/export/reset boundary;
8. global ternary negation to a label inversion when all downstream uses are covariant.

The exact M36 example already removes two SWAPs, hence six CNOTs. The governing rule is:

> No operation is a physical gate until it changes a future observable in every permissible frame representation.

The rule is conditional. It does not make arbitrary non-Clifford phases or nonlocal permutations free.

## Pass 3001 — optimal self-synchronizing curvature code

The balanced four-symbol cyclic word is

```text
102332001123
```

with each symbol occurring three times. Its twelve cyclic shifts are distinct, its maximum out-of-phase autocorrelation is three, and its minimum cyclic Hamming distance is nine.

Thus it corrects four arbitrary slot-symbol substitutions and detects eight.

The distance is optimal: balanced multiplicities give total out-of-phase autocorrelation

\[
4\cdot3\cdot2=24
\]

over eleven nonzero shifts, so some shift has correlation at least three and no balanced word can have cyclic distance above nine. The displayed word attains nine.

At tick `t`, omit slot `w[t]` and transmit the already-required three distinct curvature pilots in the remaining slots. The omitted slot carries the D12 synchronization symbol while the three transmitted pilots continue to detect every nonidentity `S4` edge permutation. The synchronization channel therefore costs zero additional pilot modes.

## Pass 3002 — predictive thermodynamic controller

A raw hypothesis index requires sixteen fixed bits. The next-action variable requires four fixed bits, and only 0.0517343817 bits of Shannon entropy under the frozen prior.

The exact two-point rate-distortion frontier is

| Policy | Expected extra triangles | Residual Bayes error |
|---|---:|---:|
| stop after 23 | 0 | 2.6200783e-5 |
| exact adaptive | 0.0062407751 | 0 |

The thermodynamic design decision is therefore not merely to compress the past transcript. It is to retain the smallest statistic sufficient for the next decision and its residual risk.

This is a logical Shannon/Landauer ledger. Physical energy requires a temperature, a reset protocol, finite-time constraints, and a hardware implementation.

## Selected architecture

```text
LIVE STATE
  semantic mixed-radix frame, route, slot, sector, and frame-tracking data

BASE DIAGNOSTIC
  exact 23-triangle nonabelian syndrome panel

ESCALATION
  one of 13 first tests, then at most one of 10 second tests
  worst case 25 triangles

ADDRESS CONTROL
  zero, one, or two native symplectic transvections

CLOCK + PILOTS
  optimal word 102332001123
  omitted slot = phase symbol
  remaining three slots = existing curvature pilots

CLASSICAL/REVERSIBLE TRACKING
  permutations, Pauli frames, gauge conjugations, route inverses, ternary labels

IRREVERSIBLE BOUNDARY
  retain only next action/risk during operation
  archive full rank only when storage/export/reset requires it
```

This packet strengthens the current Pareto architecture. It does not establish global optimality over all physically realizable optical platforms or noisy adaptive policies.
