% BEGIN PASS 3193-3204 MONTH AUDIT SEVEN FRONT
\section{Month-audit closure: curvature state, proof envelopes, and evidence debt}
\label{sec:pass3193-month-audit}

A month-wide claim genealogy now treats corrections as first-class theorem dependencies rather
than relying on append-only memory.  In particular, the canonical machine contract distinguishes
frame connectivity from full affine universality, support readout from deterministic execution,
bounded edit tracking from blind acquisition, and source completion from observed digital or
physical evidence.

For the frozen twenty-three-test $D_4$ inference policy, attaching the exact output label
$K\in\{\mathrm{none},\mathrm{flat},\mathrm{curved}\}$ and minimizing the resulting acyclic Moore
machine refines the previous $470$ recursive states to
\[
  876=770\text{ initial classes plus their distinct recursive descendants}.
\]
Thus curvature typing costs one fixed state bit under this contract, not a dense
$48{,}826$-hypothesis register.

The larger-ISA information census is complete for all $194$ universal five- and six-opcode
libraries, but the full $4{,}199{,}040$-state runtime census is not: only three records are frozen.
Accordingly the runtime--information artifact is explicitly typed
\texttt{PARTIAL\_RUNTIME\_3\_OF\_194}; no global winner is asserted.

For four adversarial substitutions, insertions, or deletions, twelve payload-disjoint constant
markers $M_p=u_p^9$ attain the lower bound $d_{\min}=2t+1=9$.  Their exact radius-four ball has
$536{,}484{,}991$ traces per phase.  The marker directly identifies phase, although continuous
stream acquisition and physical symbol confusion remain separate engineering gates.

The candidate-ingestion path is now proof-carrying.  Authorization requires twelve typed fields,
complete engine/source/shard provenance, independent certification, all accepted witnesses, and
matching expected and computed SHA-256 digests.  The committed RTL is a fail-closed envelope gate
and digest comparator; it is not itself a SHA-256 engine and does not assert an accepted
$M_{36}$ candidate.

For $k$ concurrent depth-$n$ addresses in the recursive $40$-ary Holonet, live context storage is
exactly the non-root prefix-trie size.  Hence
\[
 N_{\min}(k,n)=\sum_{d=1}^{n}\left\lceil\frac{k}{40^{n-d}}\right\rceil,
 \qquad
 N_{\max}(k,n)=\sum_{d=1}^{n}\min(k,40^d).
\]
At depth six, sixty-four concurrent paths require between $70$ and $360$ live contexts, or
$3{,}640$ to $18{,}720$ bits at fifty-two bits per context.

Finally, the tempting direct identification of the forty-five chromatic blocks with the
forty-five curvature-route edges fails equivariantly.  The canonical block relation has spectrum
$32^1,2^{24},(-4)^{20}$, whereas none of the three degree-thirty-two orbital unions in the
exceptional $S_6$ edge action satisfies its polynomial identity.  The common cardinality is
therefore not a relation-preserving bridge; any surviving construction must add structure or
abandon equivariance.

\begin{erratumbox}
Passes 3193--3204 do not promote queued or failed workflows.  The active evidence chain
\#242$\to$\#243$\to$\#244, independent front \#245, and older overlapping chain
\#236$\to$\#239 remain separately gated.  No FPGA timing, area, power, PDF, or laboratory claim
follows from source completion.
\end{erratumbox}
% END PASS 3193-3204 MONTH AUDIT SEVEN FRONT
