\subsection{Coxeter--Todd orientation and an exact finite observer}
\label{sec:k12-oriented-observer}
The reproducible audit \texttt{analysis/w33\_coxeter\_todd\_oriented\_observer.py}
constructs an explicit signed intertwiner from the 40-dimensional deck-odd
local Coxeter--Todd cover to the oriented $H_2$ of the W33 two-skeleton.
It credits Holotrade's K12 construction and BT862/866's previously certified
$5_\omega\oplus5_{\omega^2}\oplus30$ module. These cycles disappear when
one includes the tetrahedral three-cells of the full clique complex.
For the signed adjacency $S$, let $P_{10}=(3I-S)/12$. An explicit integral
operator satisfies
\[
 K^{\mathsf T}=-K,\qquad K^2=-48P_{10},\qquad SK=-9K.
\]
Thus $K/4$ realizes the Eisenstein structure on the real ten-dimensional
summand. For externally chosen real $g,\eta$, the finite Hermitian model
$gS+i\eta K$ has bands $3g$ (multiplicity 30) and
$-9g\pm4\sqrt3\eta$ (multiplicity 5 each). The sign of $K$ remains a
conjugate orientation choice, not a selected physical handedness.
On the 126-line adjacency dynamics, observing the 45 neighbors of one line
leaves exactly 55 amplitude modes invisible for all time. The certificate
supplies 45 additional coordinate outputs attaining full observability;
fewer cannot suffice because the invisible eigenvalue-3 space has dimension
45. Moreover, 90 total outputs are globally minimal among fixed linear
observers, since the full adjacency has a 90-dimensional eigenvalue-3 space.
This is a theorem about linear amplitude outputs, not intensity-only
measurement, quantum tomography, physical masses, or a continuum TOE.
The accompanying Markdown report records prior ownership, proofs, and the
incomplete scope of the current corpus rereading.
