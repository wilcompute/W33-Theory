# Five v3 closures: formal rank artifact, full discriminant action, native E6 objects, tolerance compiler, and optical admission loop

**Status: PASS.** The aggregate witness is `analysis/w33_levi_next5_v3.py`; its machine certificate is `data/PART_2026_07_10_LEVI_NEXT5_V3_results.json`. The focused regression suite reports **8 passed in 22.83s**.

## 1. Lean 4 rank artifact

The repository now contains a self-contained Lean project in `formal/`:

- `formal/lean-toolchain`
- `formal/lakefile.toml`
- `formal/W33/OddQRank.lean`

The module has no `sorry` declarations and introduces no axioms. It formalizes the arithmetic layer of the odd-prime-power binary Levi theorem:

\[
\operatorname{rank}_2 M_q=\frac{q(q+1)^2+2}{2},\qquad
\operatorname{rank}_2 A_P=\frac{q(q^2+1)}2+1,\qquad
\operatorname{rank}_2 A_L=q^2+1,
\]

from the trivial/nontrivial Fourier-block ranks. It also proves parity of the four odd-`q` numerators and closes the nilpotent rank ladder

\[
D_q\sim J_4^2\oplus J_3^{(q^3+2q^2+q-4)/2}\oplus J_1^{q(q-1)^2/2}.
\]

The file is pinned to `leanprover/lean4:v4.32.0-rc1`. The Python mirror verifies every polynomial identity, theorem inventory, dependency hash, and absence of placeholders.

**Honest boundary.** Lean and Lake are not installed in the execution container, and outbound package installation is unavailable. Therefore the source was not kernel-compiled in this run. The committed file is a complete formal arithmetic artifact, but the finite-geometry Fourier decomposition remains the executable theorem input and kernel verification remains a separate environment check.

Lean source SHA-256:

```text
0d9c522ad676073569831ea32d9f1708b84077aa4205124c1961c11830b4d4ff
```

## 2. Full action on the two-primary discriminant module

Let \(L_{-4}=\ker_{\mathbb Z}M\) be the rank-15 chiral trade lattice. Exact Smith coordinates give

\[
A_2(L_{-4})\cong (\mathbb Z/2)^{14}\oplus\mathbb Z/8.
\]

The witness builds a saturated integral basis, computes the integral action of the eight projective symplectic transvections and the multiplier-minus-one outer similitude, dualizes each action, and transports it into the canonical Smith generators.

Every generator preserves the full discriminant quadratic form. On the 2-torsion subgroup the images have orders

\[
|\langle g_1,\ldots,g_5\rangle|=25920,
\qquad
|\langle g_1,\ldots,g_5,\tau\rangle|=51840.
\]

Thus the discriminant action is faithful for both \(\PSp(4,3)\) and its outer extension.

Let \(h\) be the canonical order-eight generator, with

\[
q(h)=\frac{11}{8}\pmod{2\mathbb Z}.
\]

The invariant line \(\langle4h\rangle\) is fixed by every inner and outer generator. The order-eight generator itself is **not** fixed or simply inverted. In the canonical coordinates \(e_0,\ldots,e_{13},h\), the outer involution acts by

\[
\boxed{
\tau(h)=5h+e_0+e_1+e_2+e_5+e_8+e_{10}+e_{12}+e_{13}.
}
\]

The map is an involution and a quadratic isometry. The \(\PSp(4,3)\)-orbit of \(h\) has size 2880. This closes the previous fixed/inverted/mixed question: the outer action is genuinely **mixed**, while its fourfold multiple collapses to the fixed isotropic line.

Action digest:

```text
79168fed71a4819d45fe546b88f35cfd48f4b858ea12206bc754e36fa7d14718
```

## 3. Native maps to the cubic-surface E6 geometry

The same eight transvections acting on line homology isolate the six-dimensional minus-type module \(U_6^-\). It contains exactly 27 nonzero singular vectors. Their orthogonality graph has degree 10 and is explicitly identified with the intersection graph of the classical cubic-surface lines

\[
E_i,\quad L_{ij},\quad Q_i
\qquad (0\le i<j\le5).
\]

The induced group orders are

\[
|\PSp(4,3)|=25920,
\qquad
|\PSp(4,3):2|=51840=|W(E_6)|.
\]

No count-only identification is used: the exact native generators induce permutations on every object set.

The line graph reconstructs:

- 27 cubic-surface lines;
- 45 tritangent planes, as intersection triangles;
- 72 sixes of pairwise skew lines, interpreted as oriented double-sixes / the 72 \(E_6\) roots;
- 36 antipodal double-sixes;
- 540 unordered noncollinear point pairs of \(W(3,3)\).

The regular 51,840-state runtime maps equivariantly with exact fiber sizes

\[
\begin{array}{c|c|c}
\text{target}&\text{size}&\text{runtime fiber}\\\hline
\text{line}&27&1920\\
\text{tritangent plane}&45&1152\\
\text{oriented double-six/root}&72&720\\
\text{noncollinear pair}&540&96
\end{array}
\]

Resolving chirality splits every 96-element pair fiber into two canonical 48-element middleware fibers:

\[
51840=2\cdot540\cdot48.
\]

Generator digests:

```text
line27       ddcae709e82d443fb4d4b14a2bf311d202140afbd02a29957ec93f4e69631875
tritangent45 cd8aef14f8a7009ca3eefc7ea6b58b069b51cd7d4089c95bbe8dee4c8d0c49c4
root72       e39e9710321259a40236908663296f25cea3b936438261213a7c1ea094bfba98
```

## 4. Tolerance-aware 16-mode photonic compiler

The primitive eight-bin active matrix remains unimodular with determinant \(-1\). After normalization it is embedded in a 16-mode Halmos dilation. Exact eigensquare-root construction and Givens elimination give

- 16 modes;
- 120 two-mode rotations;
- 16 output phases;
- unitarity residual \(6.43\times10^{-15}\);
- reconstruction residual \(5.06\times10^{-15}\).

The tolerance model includes static splitter and phase fabrication error, calibration uncertainty, dynamic drift, depth-correlated phase drift, 40 dB finite extinction, correlated/local insertion loss, output phase drift, and 50 ps FWHM detector jitter in a 100 ps bin.

A deterministic calibration-gain search selects

\[
\boxed{g_{\rm cal}=1.025}.
\]

Before calibration:

\[
\bar F=0.989727,
\qquad F_{5\%}=0.988111.
\]

After calibration:

\[
\boxed{\bar F=0.999297},
\qquad
\boxed{F_{5\%}=0.998749},
\]

with minimum sampled fidelity \(0.997359\). The modeled nearest-neighbor jitter misbin probability is \(0.01853\). Thus the depth-16 calibrated implementation remains above 0.99 fidelity throughout the sampled tolerance tail.

Netlist digest:

```text
094f2aaefff2ecebded70807e81ad18e19287f81eb1f3a18676c3e25b2319046
```

## 5. End-to-end optical packet emulator

The optical network now carries the eight-bit point-homology syndrome itself. A BPSK time-bin word is transformed by the compiled 16-mode network. Balanced detector click streams estimate the eight output quadratures, and maximum-likelihood decoding selects one of the 256 exact optical codewords.

The decoded syndrome reconstructs a valid 40-bit point cycle from the homology basis and a boundary mask. Admission then executes, in order:

1. optical confidence / retry;
2. keyed authentication;
3. 40-context sentinel syndrome on the payload displacement;
4. immutable transition and type provenance;
5. exact homology-cycle and syndrome validation;
6. native \(W(E_6)\) runtime-coordinate assignment.

The noiseless path exhaustively admits all 256 homology words.

The deterministic noisy run sends 2,500 frames with loss variation, 50 ps jitter, Poisson click noise, and dark counts:

\[
\boxed{2495\ \text{accepted}},
\qquad
\boxed{5\ \text{retries}},
\qquad
\boxed{0\ \text{wrong frames admitted}}.
\]

Every admitted frame receives

\[
(\text{chirality},\text{line}_{27},\text{tritangent}_{45},
\text{root}_{72},\text{pair}_{540}).
\]

The adversarial layer injects 448 authenticated errors of weights 1 through 7, all 45 minimum dark-code words of weight 8, one authenticated raw type retag, and one invalid authentication tag. The results are exact:

\[
448\ \text{sub-eight errors}\to\text{sentinel rejection},
\]

\[
45\ \text{dark-code errors}\to\text{provenance rejection},
\]

\[
1\ \text{retag}\to\text{type-confusion rejection},
\qquad
1\ \text{bad tag}\to\text{authentication rejection}.
\]

No injected attack is admitted. Accepted-runtime digest:

```text
dcc5ef2117568897bae8b6169673b2182accbccf946e236dbac862b35b31fff9
```

## Validation and external anchors

The focused suite reports `8 passed in 22.83s`. All Python modules compile. The current mathlib toolchain was read from the official repository. The photonic tolerance model is informed by primary work on fault-tolerant programmable meshes, high-extinction redundant interferometers, and measured SNSPD efficiency/jitter/dark-count tradeoffs. These sources motivate the engineering ranges; every new numerical and mathematical claim above is generated by the committed witnesses.
