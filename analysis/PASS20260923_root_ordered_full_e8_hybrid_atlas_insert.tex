% PASS20260923 -- root-ordered full graded E8 hybrid atlas.
\subsection*{The hybrid matter chart extends to a root-ordered graded $E_8$ vector atlas}

The physical FI element gives the exact grading
\[
E_8=\mathfrak g_0\oplus\mathfrak g_1\oplus\mathfrak g_2,
\qquad
248=86+81+81,
\]
with branching
\[
(78,1)\oplus(1,8)\oplus(27,3)\oplus(\overline{27},\overline3).
\]
Let $B_{81}$ be the exact hybrid chart consisting of seventy-three cubic
pivots and the eight Fourier/magic dark modes.  The landed address certificate
orders the grade-one roots by the same $K=H_{27}\times C_3$ addresses used by
$B_{81}$.  Negating those roots fixes the grade-two row order, and field
conjugation fixes its coefficients.  Hence the graded vector-space atlas is
\[
\boxed{I_{86}\oplus B_{81}\oplus\overline{B}_{81}},
\qquad \operatorname{rank}=248.
\]
Its FI-center trace is
\[
86+81\omega+81\omega^2=5.
\]
The certificate stores all eighty-one addresses and both ordered root lists,
so the conjugate block is no longer merely a formal copy.

\paragraph{Scope firewall.}
This closes the graded vector-space row ordering.  The neutral $I_{86}$ block
is still relative to the prior $E_6+A_2$ grading basis, and the Chevalley maps
$\mathfrak g_1\times\mathfrak g_1\to\mathfrak g_2$,
$\mathfrak g_2\times\mathfrak g_2\to\mathfrak g_1$, and
$\mathfrak g_1\times\mathfrak g_2\to\mathfrak g_0$ have not yet been
transported.  Thus this is not yet a full Lie-algebra compiler and does not
select a physical CP orientation.

\paragraph{Subsequent executable transport.}
The signed current-H27 Chevalley compiler now evaluates all six graded bracket
products using explicit source permutations, phases, and a labeled neutral
basis. This separate coordinate convention is recorded in its certificate;
it is not silently identified with the negated-root row convention above.
