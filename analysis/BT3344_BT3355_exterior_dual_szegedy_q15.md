# Passes 3344–3355 — Exterior cover strata, nonseparable defect templates, explicit Szegedy oracle, and Q15 guard geometry

## Status

This packet executes the five requested continuations plus two independent high-risk constructions. Every promoted result is an exact finite computation regenerated by `analysis/bt3344_3353_exterior_dual_szegedy_q15.py`. The focused regression passes **5/5** locally.

The exact chromatic frontier remains

\[
\boxed{10\leq\chi(H)\leq 11}.
\]

No queued proof job, source-level circuit, numerical phase display, or manuscript workflow is promoted into a solver theorem, physical speedup, FPGA result, successful PDF, laboratory calibration, or spacetime claim.

---

## 3344–3345 — Exterior exact-cover signature strata

The complete exact census contains

\[
3{,}547{,}800
\]

covers in 327 `PSp(4,3)`-orbits. The known Hamming switch component contains 1,574,640 covers in 135 orbits with stabilizer histogram

\[
2^{108}4^{27}.
\]

The nonlinear 45-coordinate signature census has four strata. Its leading stratum contains 270 cover orbits and 3,149,280 covers, with histogram

\[
2^{216}4^{54},
\]

which is exactly twice every promoted census field of the known Hamming component.

The remaining three signature strata form an exact exceptional cap:

\[
\boxed{57\text{ cover orbits and }398{,}520\text{ covers}},
\]

with stabilizer histogram

\[
\boxed{2^{12}4^{30}8^{15}}.
\]

Subtracting this cap and the known Hamming component leaves an exterior residual of

\[
\boxed{135\text{ orbits and }1{,}574{,}640\text{ covers}},
\]

again with stabilizer histogram `2^108 4^27`. Thus the global census satisfies the exact decomposition

\[
\boxed{327=135+135+57},
\]

\[
\boxed{3{,}547{,}800=1{,}574{,}640+1{,}574{,}640+398{,}520}.
\]

This is a count-and-signature-stratum theorem. It does **not** yet prove that the second 135-orbit residual is connected, isomorphic to the known switch graph, or canonically paired orbit by orbit. Those require the readable orbit ledger or an independently reconstructed switch graph.

---

## 3346 — Exact nonseparable balanced defect templates

The parallel Passes 3332–3343 computation proves that an identical separable local ansatz

\[
A_{\rm block}\otimes K,
\qquad K\succeq0,
\]

cannot improve the Hoffman bound nine. The present packet therefore moves to genuinely colour-profile-dependent defect matrices.

For the balanced hypothetical ten-colouring, every class has size 54. The integer defect Gram has

\[
K_{ii}=324,
\qquad K\mathbf1=0,
\qquad K\succeq0,
\qquad \operatorname{rank}K\leq9,
\]

and every entry obeys

\[
K_{ij}\equiv9\pmod{15}.
\]

Exhausting all matrices invariant under a two-block colour split

\[
S_m\times S_{10-m},\qquad 1\leq m\leq5,
\]

gives the exact split-tagged census

| split | templates |
|---|---:|
| `1+9` | 1 |
| `2+8` | 1 |
| `3+7` | 5 |
| `4+6` | 3 |
| `5+5` | 5 |

There are 15 split-tagged templates, ten genuinely symmetry-broken templates, and

\[
\boxed{11\text{ nonisomorphic arithmetic-semidefinite template types}}.
\]

Every template has exact integral colour-pair edge counts summing to 8,640, exact nonnegative rational spectrum, zero row sum, and rank at most nine.

These are necessary quotient candidates. They are not vertex colourings, and they are not rational dual certificates excluding ten colours.

---

## 3347 — Tiny proof-status sidecars and fail-closed aggregation

The previous chromatic proof lane stored multi-gigabyte CNF/proof artifacts, making the tiny terminal status difficult to inspect independently. This packet separates the evidence into:

1. a tiny canonical status sidecar containing shard identity, base-CNF hash, solver exit code, terminal status, model/proof digest, independent-check flags and proof-artifact reference;
2. a separate large DRAT/LRAT or model artifact.

The frozen depth-four tree contains 100 leaves

\[
(0,3,c,d),\qquad c,d\in\{0,\ldots,9\},
\]

with Merkle root

```text
fd9c8d2ce559ac09fa145799af85dd0ff6480f2b137ea419e3d1bd9452a71b14
```

and 404 canonical bytes in the sample sidecar.

The parent composition law is exhausted over all

\[
3^{10}=59{,}049
\]

assignments of `SAT`, `UNSAT` and `UNKNOWN` child statuses:

- SAT parent assignments: 58,025;
- UNSAT parent assignments: 1;
- UNKNOWN parent assignments: 1,023.

`tools/aggregate_bt3347_status_sidecars.py` checks the record content hash, base formula, shard identity, independent model/proof check and duplicate behavior. Missing, duplicate, stale, timed-out, crashed, digest-mismatched or unavailable evidence remains `UNKNOWN`.

This repairs the evidence contract. It changes no currently unresolved shard to SAT or UNSAT.

---

## 3348 — Explicit reversible Szegedy token oracle

The 135-state Hamming-orbifold walk has weighted degree ten, 1,242 distinct nonzero directed destinations and 1,350 weighted arc tokens.

The packet now freezes every token explicitly. For a quotient species `i`, token `t` and eight-bit target register `z`, the oracle is

\[
O_f:(i,t,z)\longmapsto(i,t,z\oplus f(i,t)).
\]

The full table has

\[
\boxed{135\times10=1{,}350\text{ rows}},
\]

and its involution law is exhausted on

\[
135\times10\times256=\boxed{345{,}600}
\]

register cases.

The two exact row types are:

- 27 fixed-orbit rows: six distinct destinations, comprising two weight-one and four weight-two destinations;
- 108 paired-orbit rows: ten distinct weight-one destinations.

A logical walk step

\[
U=SAR_0A^\dagger SAR_0A^\dagger
\]

uses:

- four transition-oracle calls;
- four uniform-ten prepare/unprepare calls;
- two zero-state phase reflections;
- two source-target swaps.

The persistent arithmetic interface uses 25 logical bits: ten guarded source bits, four token bits, ten guarded target bits and one canonicalization flag. An eight-bit compressed species index is sufficient; temporary comparison ancillas are not minimized.

The smallest positive phase is

\[
2\arccos(7/10).
\]

Three phase bits resolve below half this gap, while four resolve below one quarter. This is an exact reversible logical resource contract, not optimized gate synthesis or a quantum-speedup theorem.

---

## 3349 — Exact Q15 code theory

Let `C` be the 243-word block-one-hot code in `Q15`, with one of `100`, `010`, `001` in each of five blocks. Then

\[
(n,|C|,d,w)=(15,243,2,5).
\]

Coordinate co-occurrence is zero inside a block and 27 across distinct blocks, so the five triples are recovered intrinsically. Therefore the full coordinate-permutation automorphism group is

\[
\boxed{S_3\wr S_5},
\qquad |S_3\wr S_5|=\boxed{933{,}120}.
\]

The affine hull has binary dimension ten, while the linear span has dimension eleven. The span dual is the exact code

\[
\boxed{[15,4,6]},
\]

with weight enumerator

\[
1+10z^6+5z^{12}.
\]

The covering radius in `Q15` is ten. Its complete distance-to-code distribution is

\[
243,1620,4725,7920,8430,5944,2810,880,175,20,1,
\]

the coefficient sequence of

\[
(3+4z+z^2)^5.
\]

The code is not completely regular: two distance-one ambient types have respectively three and two nearest codewords.

Fifteen binary coordinates are minimal for a coordinate-separable one-hot realization, since each ternary `K3` factor needs three binary cut coordinates. No unrestricted global 14-bit impossibility is claimed.

---

## 3350 BONKERS — PG(3,2) emerges in the Q15 dual

The nonzero dual words correspond to nonzero even subsets of the five blocks. These form a four-dimensional vector space over `F2`, whose projectivization is

\[
\boxed{PG(3,2)}.
\]

The exact incidence census is:

\[
15\text{ points},\qquad35\text{ lines},\qquad15\text{ planes}.
\]

The point weights split into ten weight-six and five weight-twelve codewords. Line types are:

- ten `(6,6,6)` lines;
- fifteen `(6,6,12)` lines;
- ten `(6,12,12)` lines.

Plane types are:

- ten planes with four weight-six and three weight-twelve points;
- five planes with six weight-six and one weight-twelve point.

The induced block permutation action has order 120. Under the affine involution `tau`, the projective geometry has:

- three fixed points and nine point orbits;
- seven fixed lines and 21 line orbits;
- three fixed planes and nine plane orbits.

This identifies a precise location for the projective three-space already prominent elsewhere in the repository: it is the parity-check geometry of the Q15 host, not the 243-state cover adjacency graph itself.

---

## 3351–3353 BONKERS — H(5,4) odd-parity guard completion

Add the guard word `111` to the three one-hot symbols in each binary block. The four odd-parity words in `Q3` form `K4` under binary distance two, so five blocks produce

\[
\boxed{H(5,4)=K_4^{\square5}}
\]

inside the distance-two graph of `Q15`.

This graph has

\[
1024\text{ vertices},\qquad15\text{-regular},\qquad7680\text{ edges}.
\]

The shell populations by the number of `111` blocks are

\[
243,405,270,90,15,1.
\]

The edge partition is

\[
1215\text{ valid-valid},\quad1215\text{ valid-guard},\quad5250\text{ guard-guard}.
\]

After quotienting by `tau`, the six shell populations are

\[
\boxed{135,207,144,48,9,1}.
\]

The full `H(5,4)` spectrum is

\[
15^1,\;11^{15},\;7^{90},\;3^{270},\;(-1)^{405},\;(-5)^{243}.
\]

The `tau`-invariant multiplicities are

\[
\boxed{1,9,48,144,207,135}.
\]

Thus the packet obtains the exact shell-spectrum reversal

\[
\boxed{m_j=\text{shell}_{5-j}}.
\]

This is an exact guard-state/Hamming-scheme duality. It is not a fault-tolerant memory or a physical ququart construction.

---

## Reproduction

```bash
python analysis/bt3344_3353_exterior_dual_szegedy_q15.py
python -m pytest -q tests/test_bt3344_3355_exterior_dual_szegedy_q15.py
python tools/aggregate_bt3347_status_sidecars.py /path/to/tiny-sidecars \
  --out evidence/pass3344_3355/chromatic_status_aggregate.json
python tools/integrate_bt3344_bt3355.py
python tools/integrate_bt3344_bt3355.py --check
```

Expected exact-generator status:

```text
PASS_EXACT_SEVEN_FRONT_EXTERIOR_DUAL_SZEGEDY_Q15_PACKET
```

## Evidence boundary

Open items remain:

- an objectwise pairing or switch-graph proof for the Hamming-sized exterior residual;
- a checked ten-colour model or complete UNSAT certificate;
- an exact rational dual excluding ten colours;
- observed simulator, synthesis, placement and manuscript-PDF results;
- optimized fault-tolerant Szegedy gate synthesis;
- physical quantum speedup, optical calibration, detector performance, power, heat, fabrication or spacetime interpretation.
