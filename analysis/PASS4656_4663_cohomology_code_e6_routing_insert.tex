\section*{Passes 4656--4663: cover cohomology, code closure, internal $E_6$, and routing structure}
\paragraph{A nonzero apartment deck class.}
Choose the labelled Schreier graph on the 810 selected point--line flags for the five deterministic $PSp(4,3)$ transvections used throughout the reconstruction.  It has 4050 labelled edges and first Betti number $4050-810+1=3241$.  Its 1620-apartment lift is connected.  With the lexicographically sorted local section the $\mathbb F_2$ voltage cochain has 636 one-edges, and an explicit length-six closed base word
\[
 g_3g_0^{-1}g_1g_2^{-1}g_4g_3^{-1}
\]
lifts from one apartment sheet to the other.  Hence the regular $C_2$ cover of Pass~4650 represents a nonzero class in $H^1$ of this canonical Schreier graph.

\paragraph{The order-216 triality intersection.}
The W33 point stabilizer has derived-series orders
\[
648,216,54,27,3,1.
\]
The order-27 term is extraspecial of exponent three.  Modulo it, the order-216 derived subgroup has quotient $Q_8$, whereas the full order-648 point stabilizer has quotient $SL(2,3)=2A_4$.  Combining this with the Passes~4649 and 4654 action-level identification gives
\[
G_0\cap G_1\cong 3^{1+2}\!:\!Q_8,
\qquad
3^{1+2}\!:\!Q_8<3^{1+2}\!:\!2A_4<PSp(4,3).
\]

\paragraph{The selected binary code closes on its dual and full symmetry.}
For $C=\ker_{\mathbb F_2}(N^T)=[135,16,30]_2$, MacWilliams gives
\[
C^\perp=[135,119,3]_2,
\]
with exactly 270 weight-three words.  Since $C^\perp=\operatorname{row}(N^T)$, these are precisely the 270 selected lines: the code reconstructs its own $135_6$--$270_3$ geometry.  A code-intrinsic joint-Jacobi relation on the 36 minimum words splits their 630 pairs as $270+360$; the 270 relation is the spread/double-six $\operatorname{SRG}(36,15,6,6)$.  Distinct coordinate signatures make this action faithful, so the independently certified graph automorphism order 51840 is an upper bound for $\operatorname{Aut}(C)$.  The explicit $PGSp(4,3)$ outer similitude preserves the selected triples and supplies the matching lower bound:
\[
\operatorname{Aut}(C)=PGSp(4,3),\qquad |\operatorname{Aut}(C)|=51840.
\]

\paragraph{Internal $27$--$36$--$45$ cubic-surface closure.}
The 27 maximal singular generators and 36 code minima reproduce the zero-intersection matrix $R$ of rank 21.  The recovered meeting graph on the 27 has exactly 45 triangles, five through each line.  If $T$ is line--triangle incidence and $D$ is triangle--double-six disjointness, then every triangle/double-six intersection contains zero or two lines, with census $0^{540}2^{1080}$, and
\[
T^TR=2(J-D).
\]
The triangle stabilizer has order 576 and fixes exactly one protected 16-line support; hence this internal 45 is $PSp(4,3)$-equivariantly the protected center-quad/$E_6$ tritangent carrier.

\paragraph{Topology-aware router.}
Exact fractional all-shortest-path flow separates the 2025 edges of the selected 270-router into 1620 edges of load $38.377\ldots$ and 405 edges of load $66.155\ldots$.  The 405 high-load edges are exactly 27 disjoint Petersen graphs, one on the ten selected lines contained in each internal 27-carrier object.  Deleting the entire Petersen shortcut orbit leaves a connected 12-regular graph of diameter four and edge-connectivity 12.  Component-mixed photonic sensitivity calculations retain the earlier Pareto conclusion; they do not constitute measured Holonet performance.

\paragraph{Three outside-box closures.}
First, the 1120 anisotropic binary two-planes split under $PSp(4,3)$ as $40+1080$.  The 40-orbit partitions the 120 nonsingular vectors into $40\times3$ and carries a four-class association scheme of valencies
\[
1,2,36,27,54
\]
whose complete intersection tensor equals the historical 120-selector algebra.  The actions are nevertheless inequivalent: the new quotient is W33 points, while the historical selector quotient is W33 lines.

Second, the code alone reconstructs the protected 45.  Its intrinsic 36-vertex Jacobi graph has 135 maximal $K_4$'s; their anticompleteness graph is exactly $45C_3$.  The three $K_4$'s in each component unite to a 12-subset of the 36 carrier, and the resulting 45-object orbit has stabilizer 576 and is equivariantly the protected/$E_6$ tritangent 45.

Third, the apartment duo bit does \emph{not} descend to a 36-spread signing.  An apartment stabilizer of order 16 fixes no spread.  Its spread-stabilizer intersection orders have profile $1^{16}4^{16}8^4$; transporting the four order-eight cases yields a deck-blind incidence
\[
135_4\text{--}36_{15},
\]
rowwise disjoint from the code-minimum incidence $135_8$--$36_{30}$.  Thus the proposed binary spread signing fails closed rather than being promoted from numerology.

\paragraph{Evidence boundary.}
Every statement above is an exact finite group, graph, code, incidence, or graph-cover calculation.  The routing numbers use explicitly stated component-mixed sensitivity assumptions.  No particle, generation, gauge-field, optical-phase, or measured-hardware identification is inferred.
