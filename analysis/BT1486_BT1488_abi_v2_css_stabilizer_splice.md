# BT1486-BT1488: ABI v2 Retwined CSS, V4 Stabilizers, Splice v2

## BT1486

BT1486 reruns the BT1471 CSS join from the ABI v2 packet coordinates.  It uses
the BT1425 retwined frame rule and verifies all `72` ABI v2 value rows against
both `X` and `Z` syndromes.

The row layer keeps both axes:

```text
P0 = P1 = P2 = 24 rows
T0 = T1 = T2 = T3 = 18 rows
```

The active/guard split is still:

```text
24 active value rows + 48 guard value rows = 72
```

All active columns are fixed by the retwined guard-tail permutation.  Exactly
`24` guard value rows move, and every moved row remains syndrome-equivariant.

## BT1487

BT1487 classifies the branch symmetries behind the four `V4` triangle classes.
There are two useful stabilizer levels:

```text
S4 = all permutations of four V4 triangle classes, order 24
D4 = square subgroup generated by tau4 and a bit-swap reflection, order 8
```

This makes the Fano reading of the `168` active detector bins explicit in two
equivalent ways:

```text
7 Fano points * 24 S4 point-stabilizer states = 168
21 Fano flags * 8 D4 flag-stabilizer states = 168
```

So the side note is right: `168` is not merely an optical detector count.  It is
Fano-native.  The optical port is carrying the Fano point/flag stabilizer
arithmetic.

## BT1488

BT1488 upgrades the paper splice cascade.  The v2 rule is:

```text
BT1484 replaces BT1472 as the preferred E6-aware claim table.
BT1474, BT1480, BT1482, BT1484, BT1486, BT1487
form the preferred exact finite insert packet.
```

Formula-level Golden Quartic/Mobius-ball claims remain blocked pending rendered
formula transcription.  The manifest assumes no public source-code repository
for Otto's paper; any equation replication should be done from the paper text
and local scripts, not from an external codebase.

## Current synthesis

```text
C3 x V4 ABI v2 rows
  -> retwined W33 CSS syndrome equivalence
  -> S4/D4 branch-triangle stabilizer classifier
  -> Fano-native 168 active-bin interpretation
  -> E6-aware paper splice v2 manifest.
```
