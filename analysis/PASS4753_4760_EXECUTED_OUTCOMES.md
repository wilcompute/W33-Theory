# Passes 4753–4760 — executed outcomes

## 4753 — complete residue orbital algebra

The transitive PSp(4,3) action on the 270 involution residues has rank 12.  Its exact orbital algebra has rational dimension 12 and center dimension 9.  A central generator has algebra characteristic polynomial

`(x-616)^4 (x-436)(x+68)(x+74)(x+500)(x+578)(x+584)(x^2-308x+23824)`.

The quadratic factor has discriminant `-432=-144*3`, hence center field `Q(sqrt(-3))`.  The 616 block is 4-dimensional as an algebra.  Inside it the degree-3 hot/Petersen orbital produces a rational rank-20 idempotent, so the block splits over Q.  Therefore

`A_Q ~= Q^6 x Q(sqrt(-3)) x M_2(Q)`,

and over C,

`A_C ~= C^8 x M_2(C)`.

The unique noncommutative block is exactly the multiplicity-two degree-20 constituent that Pass 4747 identified as the source of the `1 +/- sqrt(13)` cold-router eigenvalues.

A primitive rank-24 central projector has orbital coefficients

`(4/45, 2/135, 1/30, 2/135, -1/45, -1/270, 2/135, -1/45, 2/135, 2/135, -1/45, -8/135)`.

Its normalized projected-orbit inner products are

`1, 1/6, 3/8, 1/6, -1/4, -1/24, 1/6, -1/4, 1/6, 1/6, -1/4, -2/3`.

## 4754 — the odd-q distance theorem and the actual mod-4 branch

For every odd prime power q,

`d(ker_F2 A_*(W(3,q))) = q+1`.

The lower bound is a direct generalized-quadrangle double count.  Equality forces a pairwise-skew `(q+1)`-set satisfying the exact `0/2` transversal law.  In the dual `Q(4,q)`, an anisotropic 2-space has a nondegenerate 3-dimensional orthogonal complement; its projective plane cuts the parabolic quadric in a conic of `q+1` points.  Polarity meets that conic in tangent/secant/external lines and gives the required binary kernel word.

The canonical projective `J`-involution is what branches:

* `q = 3 mod 4`: it fixes exactly `q+1` isotropic lines and is minimum;
* `q = 1 mod 4`: it fixes `q+3` isotropic lines and sits two above minimum.

Thus q=5 was not a failure of the distance law; it was the first visible quadratic-character branch of the involution shell.

**Prior-art firewall.** Bagchi–Brouwer–Wilbrink (1991) determined dimensions of related binary line/neighbourhood codes for Sp(4,q) and O(5,q), and Key–Moori–Rodrigues (2005) studied adjacency codes of odd-characteristic symplectic rank-3 actions.  This pass records the explicit line-intersection-kernel proof and involution comparison required here.  No publication-novelty claim is made without full textual comparison.

## 4755 — non-coordinate 24D bridge survives; canonical Leech lattice does not

Pass 4753 canonically extracts a rational 24-dimensional PSp(4,3) constituent from the 270 residues.  ATLAS independently provides a 24-dimensional integral characteristic-zero representation of `U4(2)=S4(3) ~= PSp(4,3)`, so the representation-level bridge is real and does not require a Golay coordinate frame.

Scale the 270 projected coordinate vectors to squared norm 24.  Their integral Gram values are

`24, 9, 4, -1, -6, -16`.

The lattice generated by all 270 vectors has exact determinant

`2^10 * 3^10 * 5^23`.

Because an inner product `1` occurs, any scalar rescaling that remains integral has integral scalar.  Its determinant then retains odd 5-adic valuation, whereas every full-rank sublattice of an integral unimodular lattice has square determinant.  Therefore this **canonical projected residue lattice cannot embed as a full-rank sublattice of the Leech lattice under any invariant integral rescaling**.

This is not a representation no-go: another commensurable U4(2)-invariant lattice in the same rational module could still be the correct Conway/Leech lattice.  The canonical projector lattice is what is closed.

## 4756 — the dependency code has a second triangle species and higher shells

The cold residue graph has 1080 graph triangles, not 540:

* 540 are XOR-zero weight-3 dependency circuits;
* 540 are nondependency triangles.

Every one of the 1620 cold edges lies in exactly one triangle of each species.

The `[270,240,3]` dependency code begins

* `A3=540`,
* `A4=3645`, splitting into PSp orbits `1620+1620+405`,
* `A5=29376`.

The residue-pair XOR multiplicity census is

`1^25650, 2^3240, 3^135, 4^540, 6^270`.

## 4758 — bonkers: the two triangle species assemble into 135 cubes and reconstruct selected135

Make a bipartite graph whose left vertices are the 540 dependency triangles, whose right vertices are the 540 nondependency triangles, and join two triangles when they share a cold residue edge.  It is 3-regular on both sides, but it is not connected:

`triangle-incidence graph = 135 Q_3`.

Each cube contains four triangles of each species and uses six residues.  Those six residues form `K6 - 3K2` in the cold graph.  Their W33 line supports have union size 8, giving 135 distinguished weight-8 words of `H10^perp`; they form one PSp orbit with stabilizer order 192.

The graph on these 135 words defined by support intersection 4 is PSp-equivariantly **exactly the Pass-4716 selected135 graph**.  Under the compatible Pass-4737 residue-to-selected270 bijection, the six residues in each cube map to the six selected270 lines incident with that selected135 vertex.  Thus the binary dependency geometry alone reconstructs the full selected135–selected270 incidence layer.

## 4759 — bonkers: cube sums collapse 135 -> 45 and produce the 24D GQ frame

Use the rank-24 projector of Pass 4753 and its integral norm-24 residue vectors.  Sum the six vectors belonging to each dependency cube.  All 135 sums have norm 144, but they collapse in packets of three to exactly 45 distinct vectors.

Divide their Gram matrix by 9.  The 45 vectors then have

* squared norm 16,
* inner product `-4` on 270 pairs,
* inner product `1` on 720 pairs.

If `A_45` is the `-4` relation, then

`G_45 = 15 I - 5 A_45 + J`,

and `A_45` is `SRG(45,12,3,3)`.  Consequently `G_45` has rank 24 and its only nonzero eigenvalue is 30.  This is exactly the canonical 24-dimensional spherical embedding of the 45-point GQ quotient, derived here from the 270 residue dependencies.

## 4760 — bonkers: the cube unions resolve the old binary 24-dimensional overlap

Let `C_star` be the binary code on the forty W33 line coordinates generated by the forty point-stars.  It has rank 25.  Let `K=H10^perp`, rank 30.  Their combined rank is 31, so the intersection has dimension 24.

The 135 cube-union weight-8 words span rank 24 and lie in both codes.  Hence

`span(cube unions) = C_star intersect H10^perp`.

This is a `[40,24,6]` binary code.  Enumerating its 16-dimensional dual and applying MacWilliams gives the complete weight enumerator

`1 + 240 z^6 + 1485 z^8 + 27792 z^10 + 169600 z^12 + 707760 z^14 + 1909170 z^16 + 3491280 z^18 + 4162560 z^20 + ... + z^40`,

symmetric under `w <-> 40-w`.  Its 240 minimum weight-6 words are exactly `star_p + star_q` for the 240 collinear W33 point pairs.  The 135 cube words form a distinguished PSp orbit inside the larger weight-8 shell of size 1485.

No identification between this characteristic-two 24-dimensional module and the characteristic-zero 24-dimensional constituent of Pass 4753 is inferred from dimension alone.

## 4757 — exact radius-six Hamiltonian sectors and stochastic response

For the commuting parity model

`H = - sum_R prod_{i in R} Z_i`,

every syndrome has 1024 spin configurations.  The complete syndrome/coset-leader census is now explicit through original error weight six.  In particular:

* one line: 40 syndromes at violated-check weight 27;
* two lines: 540 at weight 48 (skew pair), 240 at weight 54 (meeting pair);
* three lines: weights `63,67,69,75,81` with counts `2160,1080,4320,2160,160`;
* weights four through six are frozen in the JSON certificate.

At error weight six the raw `C(40,6)=3,838,380` errors collapse to exactly `3,819,900` distinct syndromes.  There are 18,480 doubleton classes and no triples; their syndrome-weight census is `84:9720, 90:4320, 96:4320, 108:120`.

The skew two-defect sector has energy cost 96 versus 108 for two isolated defects, i.e. effective binding `-12` in this finite model.  Meeting defects cost 108 and have zero pair binding.

For independent line flips of probability `p`, set `a=1-2p`.  Exact residue-overlap counting gives

`E[H] = -270 a^4`,

`Var(H) = 270(1-a^8) + 21600(a^6-a^8) + 3240(a^4-a^8)`.

This is a classical independent-noise ensemble.  It is not claimed to be a thermal Gibbs law or a microscopic fundamental Hamiltonian.
