# BT647 Synthesis Bridge

The current stack has a single descent chain.

W33 incidence gives the SRG(40,12,2,4) point graph.

The directed nonbacktracking carrier has 480 arcs and outdegree 11.

The natural fold maps those arcs to 160 Levi flags.

The Levi flag graph is a 4-class distance scheme on 160 flags.

Its protected idempotent E4 has rank 81.

The endpoint recurrence has polynomial

P(x)=(x-11)(x-1)(x+1)(x^2-2x+11)(x^2+4x+11).

Removing the minus sheet leaves 24(-1)^n.

BT644 internalizes this 24 sheet: an S4 subgroup of PSp(4,3) has Levi flag orbit profile

8,8,24,24,24,24,24,24.

BT645 records the protected clock:

E4 F_n E4 = (2+(-1)^n)E4.

Thus the synthesis is:

Ihara growth -> folded flag flow -> Bose-Mesner flag scheme -> Hodge E4 -> period two clock plus internal S4 sign packets.
