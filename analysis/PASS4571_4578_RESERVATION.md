# Passes 4571--4578 execution ledger

This packet executed the five explicit frontier attacks queued after Passes 4544--4551 plus three independent outside-the-box probes, while avoiding the concurrently advancing 4560+ lanes.

## Primary fronts

- **4571 — dual middle-module composition:** CLOSED at the composition-factor level. The unique 186D block has composition factors `40^3 + 14^2 + 8 + 6^5`; a visible 120D submodule contributes `40^2 + 14^2 + 6^2` and the 66D quotient contributes `40 + 8 + 6^3`. The 64D quotient `S250/S186` is irreducible. Small factors 6/8/14 are exhaustively spin-certified; 40/64 use full-degree irreducible group-algebra characteristic-polynomial witnesses. The complete unlabeled Loewy extension lattice below S186 is not claimed enumerated.
- **4572 — executed apartment enumerator mass:** exact labeled support spectra are now complete for supports 8 and 9, beyond Pass4520's support-7 frontier. Support 8 evaluates all `76,904,685` subsets and support 9 all `273,438,880`, for `350,343,565` newly evaluated subsets. Support-8 weights range `528..960` and support-9 `582..1026`. A support-10 monolithic run was attempted but exceeded the execution window. The complete numerical enumerator therefore remains OPEN; no `2^39` or `10,789,604` full-run checksum is claimed.
- **4573 — general C8 selector:** the universal extrapolation is REFUTED exactly. In `W(3,2)=GQ(2,2)`, all 90 apartments and 60 induced `K1,3` non-apartments share primitive C8 degree-four coefficient 36. In `Q^-(5,2)=GQ(2,4)`, a certified apartment has coefficient 60 while a `K1,3` has 36, so the collision is not forced by `s=2`. The universal replacement remains Pass4523's `C6 -> line adjacency -> induced C4 apartments`; W33's C8-alone selector is a special sufficiency theorem.
- **4574 — Q(5,3) global prism action:** CLOSED by an independent observed computation with no pynauty dependency. Four explicit unitary transvections on the repo's GF(9) `H(3,9)` construction generate `PSU(4,3)`; adjoining `diag(3,1,1,1)` gives projective order `13,063,680` on the 112 dual Q53 points. A noncollinear pair has pointwise/setwise stabilizers `1440/2880`; the ten-rung setwise image has order `1440`, kernel two, and one 120-orbit on 3-subsets. Therefore all `544,320` prisms form one orbit with stabilizer 24.
- **4575 — binary cubic incidence:** CLOSED. The actual 27x36 cubic-line/double-six incidence matrix has GF(2) rank six and, through the frozen Pass4525/4527 coordinates, is exactly `R[s,a]=B(s,a)` between singular and anisotropic vectors of the natural `O^-(6,2)` six-space. The row/column codes are `[36,6,16]` and `[27,6,12]`, both self-orthogonal; dual kernels are `[36,30,3]` and `[27,21,3]` with low relation counts `(A3,A4)=(120,945)` and `(45,270)`.

## Outside-box probes

- **4576 — protected/cubic nonlinear shortcut:** NEGATIVE through unary Boolean degree two. Full equivariance systems, including constants, have ranks `222/222` for `V8 -> U6` and `176/176` for `U6 -> V8`; the only degree-<=2 unary equivariant maps are zero. This is consistent with the later Pass4583 pair-valued exterior-square bridge: `(v,w)->v wedge w` has two independent inputs and a larger intermediate module.
- **4577 — apartment-fiber polarity:** POSITIVE. For two distinct 12-apartment fibers, let `n2` count cross-fiber pairs sharing exactly two W33 lines. `n2=12` iff the associated singular classes are nonorthogonal; orthogonal pairs have `n2 in {0,2,6,48}`. This reconstructs `SRG(135,70,37,35)` directly from the apartment quotient and its cross-fiber intersection data.
- **4578 — protected 120 vs cubic 120:** NEGATIVE. The 36 double-sixes have one 120-orbit on unordered triples, but it is not the protected 120-state anisotropic G-set. Both stabilizers have order 216, yet suborbit signatures are `[1,1,1,27,27,27,36]` versus `[1,2,27,36,54]`; direct generator propagation finds no equivariant bijection. The count match is explicitly rejected.

## Release state

- The theorem insert `analysis/PASS4571_4578_composition_enumerator_c8_q53_binary_bonkers_insert.tex` is chained after Passes 4552--4559.
- Public card/page are registered through the safe restored-site registry; `docs/index.html` is not directly overwritten.
- Frozen semantic regression tests and a read-only exact-evidence workflow were added. The workflow reruns all eight executable witnesses, including the complete support-8/9 census, dry-runs the site materializer, and compiles the three canonical manuscripts.
- Remote workflow/PDF status has not yet been observed through the available status surface, so CI green is not claimed here.

Evidence discipline remains strict: exact theorem or exact negative result where executed; bounded OPEN frontier for the complete enumerator; no physical interpretation from finite incidence, code, zeta, or group-action coincidences.
