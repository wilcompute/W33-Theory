% Passes 4865--4872: exact continuation after the rational/Steiner/O5/adjoint packet.
% Idempotent because all three root manuscripts consume W33_CURRENT_FRONTIER_MANIFEST,
% while w33_paper.tex also carries the packet explicitly at the historical frontier.
\ifdefined\WDDPassFourEightSixFivePacketLoaded
  \expandafter\endinput
\else
  \gdef\WDDPassFourEightSixFivePacketLoaded{1}%
\fi

\section{Intrinsic ternary filtration, Steiner clique homology, and the closed cut enumerator}
\label{sec:pass4865-4872}

This section records the exact finite results of Passes 4865--4867, 4869,
4871, and 4872.  The evidence boundary is important: all statements below are
finite graph, code, module, Lie-algebra, or compiler theorems.  They do not by
themselves identify a continuum gauge field, a particle multiplet, or a
physical coupling.  In particular, the covering radius of the binary
$[360,36,20]_2$ code is \emph{not} closed here; the exact bound from the prior
packet remains $124\leq\rho\leq179$.

\subsection{The $64$-dimensional ternary Levi homology has a canonical radical}

Let $H_1$ denote the $64$-dimensional cycle space of the $GQ(4,2)$ Levi graph
over $\mathbb F_3$, and let $K_{54}\subset H_1$ be the $54$-dimensional module
generated by the oriented local $K_{3,3}$ witnesses.  Restrict the standard
edge dot product to $H_1$.  Its Gram matrix has rank $35$, hence radical
\[
  R_{29}=\operatorname{rad}(H_1),\qquad \dim R_{29}=29.
\]
The exact reconstruction proves
\[
  R_{29}\subset K_{54},\qquad
  \operatorname{rank}\langle\ ,\ \rangle|_{K_{54}}=25.
\]
Thus the previously observed $54+10$ quotient is controlled by a canonical
bilinear-form defect rather than by an arbitrary choice of complement.

Under both $PSp(4,3)$ and $PGSp(4,3)$, the radical has the invariant diamond
\[
  0<S_{14}<M_{19},M_{24}<R_{29},
  \qquad M_{19}\cap M_{24}=S_{14},
  \qquad M_{19}+M_{24}=R_{29}.
\]
The successive irreducible dimensions are
\[
  R_{29}:14,5,10,\qquad
  K_{54}:14,5,10,25,\qquad
  H_1:14,5,10,25,10.
\]
Exact splitting systems show that both
\[
  0\longrightarrow R_{29}\longrightarrow K_{54}\longrightarrow Q_{25}
  \longrightarrow0
\]
and
\[
  0\longrightarrow K_{54}\longrightarrow H_1\longrightarrow Q_{10}
  \longrightarrow0
\]
are nonsplit for both $PSp$ and $PGSp$.  After quotienting by the radical, the
pairing is nondegenerate and gives the canonical orthogonal decomposition
\[
  H_1/R_{29}\cong Q_{25}\perp Q_{10}.
\]
The resulting $10$-space is explicitly intertwined with the previously
certified adjoint quotient.

\subsection{The Steiner triples are exactly the maximal triangles}

Let $H_{36}$ be the double-six graph, $\operatorname{SRG}(36,20,10,12)$.  Its
clique complex has
\[
  f=(36,360,1200,1080,216).
\]
Its maximal cliques are exactly $216$ copies of $K_5$ and $120$ maximal
triangles.  Those $120$ maximal triangles are precisely the Steiner trihedral
pairs.  Equivalently, a double-six triangle is Steiner exactly when it has no
common neighbor.  Every other triangle has exactly four common neighbors and
lies in exactly two $K_5$'s.  This removes cubic-surface coordinates from the
definition of the Steiner subset itself.

Over both $\mathbb F_2$ and $\mathbb F_3$, the boundary ranks are
\[
  (\operatorname{rank}d_1,\operatorname{rank}d_2,
    \operatorname{rank}d_3,\operatorname{rank}d_4)
  =(35,325,755,216),
\]
with Betti numbers
\[
  (1,0,120,109,0).
\]
There is nevertheless a sharp characteristic boundary.  The $1080$
non-Steiner triangle boundaries span rank $324$ over $\mathbb F_2$ but rank
$325$ over $\mathbb F_3$.  Hence the one-dimensional even-triangle defect that
defines the binary Steiner switching parity is genuinely characteristic two.
Over $\mathbb F_3$, $H_2$ identifies with the $120$-dimensional permutation
module on the Steiner triangles, and exact common-generator calculations give
\[
  \operatorname{Hom}_{PSp}(H_2,Q_{10})=0,
  \qquad
  \operatorname{Hom}_{PSp}(Q_{10},H_2)=0.
\]
Thus there is no linear $PSp$-equivariant Steiner-$H_2$/adjoint bridge.  Any
such bridge must use a nonlinear operation, a change of characteristic, or
additional structure.

\subsection{The ordinary cut polynomial and the complete binary enumerator}

Marking one double-six exposes six opposite columns.  The other $35$
double-sixes become the $15$ duads and $20$ triads of this six-set.  A cut,
after fixing the marked vertex on one side, is therefore specified by $15$
duad bits and $20$ triad bits.  The duad bits are precisely a labeled graph on
six vertices.  Quotienting them by $S_6$ leaves only the $156$ unlabeled
six-vertex graph classes.  Exhausting the $2^{20}$ triad states for each class
and restoring the exact orbit multiplicity gives the complete ordinary cut
enumerator without enumerating all $2^{35}$ cuts individually.

The cut code has $82$ occupied weights, minimum nonzero weight $20$, maximum
weight $216$, and exactly $120$ maximum cuts.  Combining this enumerator with
the already frozen nontrivial switching-coset enumerator closes the complete
weight enumerator of
\[
  K=[360,36,20]_2,
  \qquad |K|=2^{36}.
\]
A MacWilliams transform gives
\[
  A_0^\perp=1,\quad A_1^\perp=A_2^\perp=0,\quad
  A_3^\perp=1080,
\]
recovering the $1080$ even-triangle minimum checks independently.

\subsection{A marked double-six produces a literal $K_6$ symplectic chart}

For a marked double-six, its twelve lines canonically pair into six opposite
columns.  The $15$ non-neighboring double-sixes are exactly the duads
$\binom{[6]}2$, while the $20$ neighboring double-sixes are the triads
$\binom{[6]}3$.  Writing the corresponding subsets as weight-$2$ and
weight-$3$ vectors in $\mathbb F_2^6$, the entire $35$-vertex residue adjacency
is
\[
  x\sim y
  \quad\Longleftrightarrow\quad
  B(x,y)=x\cdot y+\operatorname{wt}(x)\operatorname{wt}(y)=1\pmod2.
\]
The matrix of $B$ is $I_6+J_6$, a nondegenerate alternating matrix of rank
six.  The residue automorphism group has order $1440$ and is explicitly
\[
  S_6\times C_2,
\]
where $S_6$ permutes the six columns and the central $C_2$ fixes duads while
complementing triads.  In particular, the earlier $15$-coordinate $K_6$-duad
carrier is now incidence-derived once a double-six is marked.  The marking is
extra data, and the $35$-shell is not being identified with the whole
$63$-point three-qubit symplectic polar space.

\subsection{The adjoint bracket is forced by Levi incidence}

The $10$-dimensional quotient $Q_{10}$ can be reconstructed using only the
Levi cycle space and the oriented $K_{3,3}$ span.  Solving directly for
equivariant alternating products gives
\[
  \dim\operatorname{Hom}_{PSp}(\Lambda^2Q_{10},Q_{10})
  =\dim\operatorname{Hom}_{PGSp}(\Lambda^2Q_{10},Q_{10})=1.
\]
The unique nonzero map has rank $10$, satisfies Jacobi on every basis triple,
has zero center, and has full derived algebra.  Therefore the finite Lie
algebra structure
\[
  Q_{10}\cong\mathfrak{sp}_4(\mathbb F_3)
\]
is determined by the Levi incidence action up to multiplication of the bracket
by a nonzero scalar in $\mathbb F_3$.  The earlier $O_5(3)$ exterior-square
construction is consequently an independent cross-certificate, not a required
definition of the bracket.

\subsection{Exact qutrit-native compilation of the port-matching datum}

The minimal local datum that removes the $S_3^{45}$ sheet ambiguity is a
bijection between three sheet cells and three incident line ports at each of
$45$ recovered points.  Each local $S_3$ element has the exact affine form
\[
  i\longmapsto(-1)^b i+r\pmod3,
  \qquad r\in\mathbb F_3,\ b\in\mathbb F_2.
\]
Thus the native representation is one trit plus one bit per point.  The full
port table has $6^{45}$ states and information
\[
  45\log_2 6\approx116.3233\ \text{bits},
\]
so an arbitrary globally packed binary table requires at least $117$ bits,
whereas independent three-bit local words require $135$ bits.  Adding the
already-certified global chirality bit raises the globally packed optimum to
$118$ bits.  For logically irreversible reset, the information-theoretic
Landauer lower bound is $45k_BT\ln6$ (plus $k_BT\ln2$ for chirality); reversible
routing itself need not dissipate this amount.

\paragraph{Evidence firewall.}
The executable producers and frozen JSON certificates for this section are
\texttt{w33\_pass4865\_4866\_ternary\_filtration\_steiner\_clique.py},
\texttt{w33\_pass4867\_cut\_ising\_full\_enumerator.py},
\texttt{w33\_pass4869\_marked\_double\_six\_k6\_symplectic\_residue.py},
\texttt{w33\_pass4871\_intrinsic\_levi\_adjoint\_bracket.py}, and
\texttt{w33\_pass4872\_port\_matching\_information\_compiler.py} together
with their \texttt{PART\_W33\_PASS...json} outputs.  Count equalities are not
promoted to identifications without an explicit equivariant map.
