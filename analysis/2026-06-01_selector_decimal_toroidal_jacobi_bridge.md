# Selector / Decimal / Toroidal / Jacobi Bridge

Date: 2026-06-01

## Executive result

The decimal/mod-12/toroidal material is not a side quest away from the current
selector/Jacobi frontier. It sharpens the exact packet that already governs the
live qutrit bridge at `q=3`.

The strongest exact shell is

$$
(6,7,12,84),
$$

with readings

$$
6 = 2q = q! = \operatorname{ord}_7(10),
$$

$$
7 = \Phi_6 = \text{Heawood/Fano/toroidal shell},
$$

$$
12 = q(q+1) = \text{local codec} = -1/\zeta(-1),
$$

$$
84 = 12\cdot 7 = 14\cdot 6 = 21\cdot 4.
$$

Here base ten is **not** taken as ontology. It is the readable shadow

$$
\Phi_4 = q^2+1 = 10
$$

at `q=3`, while the structural statements remain qutrit-native.

## Exact packets already certified upstream

### Local/global selector packet

The DCLXXXVI-DCLXXXIX surfaces already certify:

$$
162 = 6\cdot 27 = 2\cdot 81 = 81+81.
$$

This is the common selector / photonic / host packet.

### Decimal/toroidal shell

The decimal and toroidal bridges already certify:

$$
84 = 12\cdot 7 = 14\cdot 6 = 21\cdot 4.
$$

So the same shell simultaneously records:

- codec times Heawood shell,
- Heawood/G2 times selector order,
- Fano/Heawood edge packet times tetrahedral fixed point.

### Local selector average

The local three-branch qutrit selector is exact `S_3`, and the normalized group
average is literally

$$
\frac{1}{6}\sum_{g\in S_3}\rho(g).
$$

So the coefficient $+1/6$ has an exact selector-theoretic source.

## New arithmetic splice

Using the repo-native cyclotomic atoms

$$
\Phi_3=q^2+q+1,\qquad \Phi_4=q^2+1,\qquad \Phi_6=q^2-q+1,
$$

one gets the exact identity

$$
q^3-(\Phi_3+2\Phi_6)=(q-3)\Phi_4.
$$

Hence at `q=3`:

$$
27 = 13 + 14.
$$

This already splices the qutrit cubic packet to the Heawood/G2 side.

Two downstream exact consequences are then:

$$
81 = 39 + 42,
$$

where

$$
39 = 3\Phi_3
$$

is the exact-gradient packet from the CSS hinge surface, and

$$
42 = V+E+F
$$

is the certified toroidal cell chart for the Csaszar packet.

Doubling gives the common packet split

$$
162 = 78 + 84.
$$

This is the sharp new splice:

- `78 = 6\Phi_3`, matching the exact `E_6` adjoint dimension,
- `84` is the exact decimal/toroidal shell.

So the common packet may already be read arithmetically as

$$
162 = 78 + 84,
$$

in addition to the already-certified readings

$$
162 = 6\cdot 27 = 2\cdot 81 = 81+81.
$$

## Fraction dictionary on the shell 84

Because

$$
84 = 14\cdot 6 = 12\cdot 7,
$$

the live reciprocal coefficients sit on the same shell:

$$
\frac16 = \frac{14}{84},
$$

$$
\frac17 = \frac{12}{84},
$$

$$
\frac1{12} = \frac{7}{84}.
$$

This is the cleanest way to state the decimal/toroidal hint without promoting
base 10 to a fundamental status.

## What is exact vs what remains frontier

### Exact

- the local qutrit selector is exact `S_3` of order `6`;
- the common packet is exact `162 = 6\cdot27 = 2\cdot81 = 81+81`;
- the decimal/toroidal shell is exact `84 = 12\cdot7 = 14\cdot6 = 21\cdot4`;
- the arithmetic splice
  $$
  27=13+14,\qquad 81=39+42,\qquad 162=78+84
  $$
  follows exactly from already-certified packets;
- the `Z_3`-graded `E_8` Jacobi checker closes with the explicit scales
  $$
  +\frac16,\qquad -\frac16.
  $$

### Frontier

The exact local three-point permutation representation has **zero** sign
projector. So the negative coefficient `-1/6` is **not** explained by a literal
local sign projection.

The remaining live problem is therefore:

> derive the negative dual coefficient from the oriented / contragredient
> `g_2` side of the selector packet, not from the visible three-point branch law
> itself.

## Best next theorem target

A good next target is a focused dual-orientation bridge proving:

1. `+1/6` comes from exact local selector averaging;
2. `-1/6` comes from the dual/oriented `81`-sector and the antisymmetric
   epsilon-twisted `g_2` bracket.

That would turn the current Jacobi coefficients from a successful fit into a
true selector theorem.
