# BT1507--BT1509: Aut(W33) orbit firewall, route-trace CSS replay, and rebuild gate

## BT1507 -- Aut(W33) orbit validation

BT1504 remains a quotient/SAT scaffold, not a canonical automorphism-orbit theorem.  The undecorated W33 skew-line pair geometry has 540 skew pairs and is expected to be one automorphism orbit, while BT1504 splits decorated residual data into 7 point classes, 21 flag classes, and 3 fiber classes.

The next exact test is to compute automorphism orbits on decorated triples: left line, right line, and residual key.

## BT1508 -- route-trace CSS replay

The eight native D4 traces replay through the BT1486 retwined CSS row classes.  Each trace has 72 symbolic ticks, split as 24 active and 48 guard.  Across all eight native generators this gives 576 symbolic ticks, all inheriting X/Z syndrome legality from the CSS layer.

This is not an optical-noise simulation.

## BT1509 -- release rebuild gate manifest

The release gate sequence is prepared but not executed here.  It begins with the BT1506 splicer, regenerates BT1504, BT1505, and BT1508 artifacts, then rebuilds photonic_holonet.pdf and runs the release-lock and photonic-qec focused tests in checkout.

## Current synthesis

BT1504 is firewalled as scaffold rather than canonical orbit theorem; BT1505 traces are CSS-legal symbolically; BT1509 provides the exact local checkout gate for the next PDF rebuild.
