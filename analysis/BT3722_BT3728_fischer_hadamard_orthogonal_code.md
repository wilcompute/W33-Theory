# Passes 3722–3728 — Fischer closure, a Hadamard multiport, and the six-bit orthogonal core

## Release status

The exact verifier `analysis/w33_pass3722_3728_fischer_hadamard_orthogonal_code.py` reconstructs W(3,3), its forty isotropic lines, all thirty-six spreads, and the rank-fifteen Norton algebra from first principles. It reproduces the frozen semantic certificate

`dd23c386c7dee92be7a91817018bd737e9efb7fe050354be6bbf91952c59e525`.

Passes 3694–3700 already proved that centered line–spread incidence is an ETF(15,36), that its Norton algebra has thirty-six primitive axes, and that its 120 distinguished triples partition three-per-W33-line. This packet determines the group, code, cover, and rigidity structures forced by those objects.

## Seven-front result map

| front | exact outcome |
| --- | --- |
| 120-triple classification | the triples are precisely the Fischer lines of a 36-involution 3-transposition system |
| Naimark materialization | `K=2A-J` is a symmetric regular Hadamard matrix and `H=K/6` is the exact 36-port orthogonal completion |
| Monster class fusion | the existing GAP/CTblLib target is retained; no unobserved multiplicity is promoted |
| Monster words | exact short standard-generator words are found in Miyamoto involutions; serialized `mmgroup` words remain pending |
| axial envelope | multiplication operators generate all M15 modulo 1,000,003; the algebra is simple and has zero derivations over Q |
| bonkers I | the triple-incidence kernel is a six-dimensional minus-type binary orthogonal code whose 27 and 36 sectors recover the Schläfli-complement and spread graphs |
| bonkers II | the 120 triple blocks form a connected nonregular three-cover with full S3 holonomy and trivial deck group |

# I. The Norton triples are Fischer lines

For each primitive axis `a_i`, the Z2 grading of the Norton fusion law defines a Miyamoto involution `tau_i`. Every `tau_i` fixes sixteen of the thirty-six axes.

For distinct axes:

- if their spreads share four W33 lines, the involutions commute;
- if their spreads share one W33 line, their product has order three, and conjugation produces the unique third axis in the Norton triple.

The complete pair census is therefore

\[
270\text{ commuting pairs}+360\text{ order-three pairs}.
\]

The 120 Norton triples are exactly the Fischer lines of this 3-transposition space. Seven explicit Miyamoto involutions generate a group of order

\[
\boxed{51,840}.
\]

Its exact order census is

\[
1^1 2^{891} 3^{800} 4^{5940} 5^{5184} 6^{12960}
8^{6480} 9^{5760} 10^{5184} 12^{8640}.
\]

The subgroup generated by even products has order 25,920 and exactly the previously certified U4(2) census. Hence the internal action is

\[
\boxed{O_6^-(2)\cong U_4(2){:}2},
\qquad
\boxed{\Omega_6^-(2)\cong U_4(2)}.
\]

This is a finite-group identification inside the exact thirty-six-point action. It is not a Monster embedding.

# II. Compressed standard-generator targets

The Miyamoto involutions yield short standard-generator words. For U4(2), define

\[
a=\tau_0\tau_1\tau_2\tau_9,
\qquad
b=\tau_0\tau_3\tau_4\tau_9\tau_3\tau_5.
\]

Then `a,b,ab` have orders `2,5,9`; the required commutators have orders `3,2`; and `a,b` generate the complete order-25,920 subgroup.

For U4(2):2, define

\[
c=\tau_0,
\qquad
d=\tau_1\tau_2\tau_3\tau_4\tau_9\tau_3\tau_5\tau_9.
\]

Then `c,d,cd` have orders `2,9,10`, while `cd^2` has order eight and `[c,d^2]` has order two; `c,d` generate all 51,840 elements. These are exact abstract filters for a future `mmgroup` search, not serialized Monster words.

# III. The Naimark completion is a symmetric Hadamard multiport

Let `A` be the adjacency matrix of the spread graph SRG(36,15,6,6). Define

\[
K=2A-J.
\]

The verifier proves

\[
K=K^T,
\qquad
K K^T=36I,
\qquad
K\mathbf1=-6\mathbf1,
\]

with entries in `{+1,-1}` and diagonal `-1`. Thus `K` is a symmetric regular Hadamard matrix of order 36. The normalized matrix

\[
H=K/6
\]

is a rational orthogonal involution, and

\[
E_{15}=\frac{I+H}{2},
\qquad
E_{21}=\frac{I-H}{2}.
\]

The earlier 15+21 signal/guard decomposition is therefore implemented by one explicit uniform-amplitude, two-phase, thirty-six-port transform. This is an exact transfer-matrix certificate, not a fabricated-device claim.

# IV. Axial rigidity

In a rational basis of the fifteen-dimensional carrier, the associative algebra generated by left multiplication has dimension 225 modulo 1,000,003. It is the full matrix algebra M15 over that field. Consequently no nonzero proper rational ideal survives:

\[
\boxed{\text{the Norton algebra is simple}.}
\]

The complete derivation equations also have rank 225 modulo the same prime, hence

\[
\boxed{\operatorname{Der}(V)=0\text{ over }\mathbf Q.}
\]

Adjacent axes generate a two-dimensional algebra with idempotents

\[
0,\ a,\ b,\ \frac34(a+b).
\]

Nonadjacent axes generate a three-dimensional algebra containing the unique third Fischer axis `c`, with idempotents

\[
0,\ a,\ b,\ c,\ a+b+c.
\]

The fusion spectrum remains non-Majorana and non-Griess.

# V. Bonkers I: one six-bit code contains both the 27 and 36 carriers

Let `D` be the 36 by 120 axis–Fischer-line incidence matrix. Its ranks are

\[
\operatorname{rank}_{\mathbf F_2}D=30,
\quad
\operatorname{rank}_{\mathbf F_3}D=35,
\quad
\operatorname{rank}_{\mathbf F_5}D=
\operatorname{rank}_{\mathbf F_7}D=36.
\]

The binary left kernel is a `[36,6]` code with complete weight enumerator

\[
\boxed{1+27z^{16}+36z^{20}}.
\]

The quadratic form

\[
q(x)=\frac{\operatorname{wt}(x)}4\pmod2
\]

has nondegenerate rank-six polar form and exactly 28 singular vectors including zero, so it is minus type. The 36 weight-twenty words are exactly the Miyamoto supports and reproduce SRG(36,15,6,6). The 27 weight-sixteen words reproduce

\[
\operatorname{SRG}(27,10,1,5),
\]

the Schläfli complement. Thus the recurring 27- and 36-object carriers are the singular and nonsingular nonzero vectors of one explicit six-bit quadratic code.

# VI. Bonkers II: the 120 triples form a nonregular three-cover

Join two Fischer lines when they share an axis. The resulting graph has degree 27, spectrum

\[
27^1\oplus9^{20}\oplus3^{15}\oplus(-3)^{84},
\]

and distance distribution `1+27+90+2` from every vertex. The two vertices at distance three, together with the root, are precisely the three Norton triples through one W33 line. Hence the graph has forty fibers of size three.

The quotient is the complement of the W33 line-intersection graph,

\[
\operatorname{SRG}(40,27,18,18).
\]

Between adjacent fibers, the lifted edges form a perfect matching, so this is an exact three-cover. Its transport permutations generate all of S3, while the centralizer supplies no nontrivial global deck transformation. Therefore

\[
\boxed{\text{the cover is connected and nonregular, with S3 holonomy and trivial deck group}.}
\]

This explains why the local ternary structure does not globalize to a uniform C3 sheet symmetry.

# VII. Evidence boundary

Proved here:

- the Fischer 3-transposition interpretation of all 120 triples;
- the exact U4(2):2 Miyamoto action and U4(2) even subgroup;
- explicit symmetric regular Hadamard/Naimark completion;
- simplicity and zero derivations of the Norton algebra;
- the six-dimensional minus-type binary code and its 27+36 decomposition;
- the nonregular S3-holonomy three-cover;
- short abstract standard-generator words.

Still pending:

- serialized Monster words;
- an executed Monster 196883 class-fusion restriction;
- any Majorana, Griess, or VOA realization;
- a physical optical implementation;
- remote CI and canonical-PDF results.
