% PASS20260923 dark-eight Strange bridge and complete hybrid matter-81 chart.
\subsection*{The rank-$73$ root chart completes as $73$ cubic modes plus an exact Fourier--magic dark eight}

The eight-dimensional complement of the full $81\times270$ cubic-incidence
transform is
\[
D_8\cong
\chi_{\rm ext}\oplus\chi_{\rm ext}^{\,2}
\oplus V_\omega\oplus V_{\omega^2}.
\]
The two one-dimensional summands are the nontrivial external-$C_3$ Fourier
characters.  The two three-dimensional summands admit a sharper description.
For the four selected noncentral Hesse directions
\[
\omega X,\quad \omega Z,\quad ZX,\quad \omega^2ZX^2,
\]
the stacked matrices
\[
I+\rho(d)+\rho(d)^2
\]
have exact rank two.  Their unique common multiplicity ray is
\[
\boxed{m=(1,-\omega,0)^T}.
\]
If $|S\rangle=(0,1,-1)^T$ denotes the repository's qutrit Strange reference,
then
\[
\boxed{m=ZX^2|S\rangle}.
\]
Thus the $V_\omega$ dark multiplicity direction is a Clifford image of qutrit
Strange magic, and the $V_{\omega^2}$ direction carries its complex conjugate.
This is a single-qutrit statement and is not the distinct
\cmd{M36\_Q4\_RAW} ququart/two-qubit resource.

A deterministic set of $73$ pivot columns from the $270$ cubic instructions,
together with the two external Fourier characters and six
Strange/conjugate-Strange matrix-coefficient columns, forms an exact
$81\times81$ matrix over $\mathbf Q(\omega)$ with
\[
\boxed{\operatorname{rank}=81}.
\]
The dark columns are Hermitian-orthogonal to every cubic-incidence column and
have Gram diagonal
\[
81,\ 81,\ 54,\ 54,\ 54,\ 54,\ 54,\ 54.
\]
Hence the frozen matter/address carrier has the complete hybrid coordinate law
\[
\boxed{81=73_{\rm cubic}+8_{\rm Fourier/magic}}.
\]


The completed chart also makes the address--operator mismatch quantitative.
Under the abstract \(K=H_{27}\times C_3\) identification, the visible
\(73\)-space can map equivariantly to at most \(24\) operator dimensions and
the dark eight to at most \(3\).  Hence
\[
\boxed{\operatorname{rank}_{K\text{-equivariant}}\le27,\qquad
81-27=54.}
\]
Of the dark eight, only the \(V_\omega\) summand is operator-compatible in the
frozen central orientation; \(\chi_{\rm ext}\), \(\chi_{\rm ext}^2\), and
\(V_{\omega^2}\) lie outside the operator irrep support.  Thus the remaining
compiler must convert representation type on a \(54\)-dimensional deficit.
The equality with the separate \(54\) fiber-instruction count is recorded only
as a count collision, not an objectwise identification.

\paragraph{Scope firewall.}
This is an invertible coordinate chart on the root/address carrier.  It is not
an equivariant address--operator conjugacy; the earlier H27 and
$H_{27}\times C_3$ rank obstructions still forbid that interpretation.  The
remaining problem is to match this complete hybrid chart to the
$C^9_{\rm mult}\otimes C^3_{\rm int}\otimes C^3_{\rm ext}$ trinification
operator chart by an explicitly symmetry-changing compiler.
