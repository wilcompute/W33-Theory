# Five v4 closures: kernel CI, cohomology, the W33–E6–E8 functor, foundry calibration, and HIL runtime

**Status: PASS.** The aggregate runner is `analysis/w33_levi_next5_v4.py`; the aggregate ledger is `data/PART_2026_07_10_LEVI_NEXT5_V4_results.json`. All five fresh witnesses passed, all Python modules compile, and the focused certificate/interface suite reports eight passing tests.

## 1. Kernel-grade formal verification path

The `formal/` package now has a root module and two proof layers:

- `W33/OddQRank.lean` proves the arithmetic rank and Jordan identities;
- `W33/FourierBlocks.lean` exposes a kernel-checked `OddQFourierCertificate q` interface from the trivial and nontrivial additive-character blocks to the global odd-`q` theorem.

It closes the native case

\[
\operatorname{rank}_2 M_3=25,\qquad
\operatorname{rank}_2 A_P=16,\qquad
\operatorname{rank}_2 A_L=10,
\]

and

\[
D_3\sim J_4^2\oplus J_3^{22}\oplus J_1^6.
\]

The correction from the earlier provisional value 15 is forced arithmetically:

\[
\frac{3^3+2\cdot3^2+3-4}{2}=22,
\qquad 2\cdot4+22\cdot3+6=80.
\]

`.github/workflows/lean-formal.yml` requires `lake build --wfail`, an explicit scan rejecting `sorry` and `admit`, and an independent official `leanchecker` pass through `leanprover/lean-action@v1`.

**Boundary.** Lean and Lake were unavailable in the local execution container. The local Python witness verifies source structure and every polynomial identity; kernel and independent-checker validation run in GitHub Actions.

## 2. Cohomology of the mixed order-eight rail

Let

\[
A=A_2(L_{-4})\cong(\mathbb Z/2)^{14}\oplus\mathbb Z/8,
\]

let `h` generate the order-eight factor, and let `tau` be the outer involution. The exact action is

\[
\tau(h)=5h+u,
\]

where

\[
u=e_0+e_1+e_2+e_5+e_8+e_{10}+e_{12}+e_{13},\qquad q(u)=1.
\]

Because `tau^2=1`, the displacement lies in `ker(1+tau)`. Replacing `h` by `h+v`, with `v` in the two-torsion, changes `u` by `(1+tau)v`; therefore the obstruction lives in

\[
H^1(C_2,A[2])=\ker(1+\tau)/\operatorname{im}(1+\tau).
\]

Exact binary linear algebra gives

\[
\dim\ker(1+\tau)=9,\qquad
\dim\operatorname{im}(1+\tau)=6,\qquad
\boxed{\dim H^1=3}.
\]

The computed representatives are `0x3f`, `0x5e`, and `0x118`.  Relative
to the scalar-5 normal form, the displacement and fixed isotropic line
have identical quotient coordinates:

\[
\boxed{[u_5]=[4h]\ne0}.
\]

This obstructs only a pure scalar-5 form.  Relative to a fixed rail,

\[
u_1=\tau(h)-h=u_5+4h,
\qquad [u_1]=0.
\]

There are exactly `512` order-two shifts making `h+v` fixed, and `256`
of them preserve (q(h+v)=11/8).  Thus the action does admit a fixed
order-eight generator; the earlier absolute nonremovability reading was
false.  Pass 174 extends this correction to the route module and identifies
the corresponding route-hull (E_8/2E_8) shadow.

## 3. One explicit W(3,3)–E6–E8 incidence functor

The witness constructs all 240 E8 roots in doubled integral coordinates. An explicit A2 pair has a 72-root orthogonal E6 subsystem, and six computed simple roots generate the native Weyl action. The restriction to the complete E8 root set has orbit decomposition

\[
\boxed{240=72+6+6\cdot27}.
\]

This consists of the 72 E6 roots, six fixed A2 roots, and six minuscule 27-orbits. A 27-orbit is identified objectwise with the Schläfli line graph. For each E6 root `alpha`, the six minuscule weights with pairing one form one skew six, giving a generator-equivariant bijection

\[
\{72\ E_6\text{ roots}\}\longleftrightarrow
\{72\text{ oriented double-sixes}\}.
\]

A breadth-first lift through 45,518 Weyl states writes every native generator as a word in the six standard E6 reflections and produces its explicit permutation on all 240 roots. The eight inner words have length 24; the outer word has length 11.

The following incidence squares commute generator by generator:

1. 40 W33 points versus 40 W33 lines;
2. 27 cubic lines versus 45 tritangent planes;
3. 27 lines versus 72 roots/oriented double-sixes;
4. the 72-root subsystem included in all 240 E8 roots;
5. 40 W33 points versus 540 noncollinear pairs.

The stabilizer of the base pair `{0,4}` has exact order 48, yielding one native diagram on

\[
40,40,27,45,72,240,540,48.
\]

## 4. Foundry-targeted self-calibration

The concrete engineering model uses an 800-nm-class foundry-compatible Si3N4 passive core, 1550 nm center wavelength, 2.6 dB/m propagation loss, thermo-optic heaters, 14-bit phase controls, dense thermal crosstalk, fabrication offsets, wavelength dispersion, insertion loss, and dynamic drift.

The 16-mode mesh has 120 MZI rotations, 16 output phases, and 376 effective control variables. Inverse-crosstalk calibration reduces RMS phase error from

\[
0.40635\rightarrow0.03253\rightarrow0.002608\rightarrow0.000321\rightarrow\cdots\rightarrow0.000222
\]

radians, with final deterministic phase RMS `0.0001703`. Nominal process fidelity improves from `0.1951758` to

\[
\boxed{0.999998964}.
\]

Across 96 modeled dies,

\[
\bar F=0.999970663,\qquad
F_{5\%}=0.999959653,\qquad
F_{\min}=0.999954119.
\]

Across 64 drift epochs, tracked mean fidelity is `0.999997768` and tracked minimum is `0.999994253`, versus open-loop mean `0.998003149`.

The depth-16 path has 0.04992 dB passive propagation loss, 0.288 dB modeled MZI excess loss, and 0.33792 dB on-chip loss. The thermal upper-bound estimate is **7.81 W**. Thus the optical fidelity is viable, but a fully thermo-optic high-duty implementation is power-limited; piezoelectric or electro-optic control is the natural next hardware substitution.

## 5. Bounded-memory hardware-in-the-loop runtime

The HIL adapter accepts line-oriented records of the form

```json
{"timestamp_ps":12345,"channel":7,"frame":12}
```

on 16 balanced channels plus synchronization. It performs bounded-memory frame reduction, shot-noise-weighted likelihood decoding over 256 codewords, online drift/gain estimation, retry control, authentication, sentinel evaluation, provenance validation, exact homology reconstruction, and native W(E6) replay.

The deterministic run processed

\[
\boxed{3,164,709\text{ individual timestamps}}
\]

across 128 frames:

\[
\boxed{126\text{ accepted}},\qquad
\boxed{2\text{ retried}},\qquad
\boxed{0\text{ wrong admitted}}.
\]

The adversarial replay rejected 224 authenticated errors of weights one through seven at the sentinel layer, all 45 minimum dark-code displacements at provenance, one raw retag at type-confusion, and one invalid tag at authentication. No attack was admitted.

## Installed commands

```bash
holonet formal-rank-v4
holonet discriminant-cohomology-v4
holonet e8-incidence-functor-v4
holonet foundry-calibrate-v4
holonet hil-runtime-v4
holonet levi-next5-v4
```

## Validation and source boundaries

Fresh executions produced PASS certificates for all five tracks. `python -m py_compile` succeeds for every v4 module and the installed CLI. The focused suite reports eight passing tests. The Python workflow recomputes each heavy witness in its own process to avoid retaining the SymPy lattice, E8 group closure, Monte Carlo model, and timestamp state simultaneously.

External engineering anchors set the model ranges; every new algebraic and numerical conclusion is produced by the committed witnesses. The foundry heater efficiency, MZI excess loss, layout length, and facet losses remain explicit assumptions until a specific PDK corner deck is selected.
