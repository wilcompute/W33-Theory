# Execute-all-five continuation — constructive W33 closures

Four of the five targets close constructively on the W33 side.

1. **Independent W33 matter parity.**  The native cubic 27-line carrier alone gives
   (27=1+10+16) relative to a reference line: itself, ten meeting lines, sixteen
   skew lines.  With the separately certified (Q_\psi=(4,-2,1)), odd parity is
   exactly skewness.  The 45 tritangents are exactly
   (5(1,10,10)+40(10,16,16)).

2. **Physical FI (A_2\to) canonical family (A_2).**  Both six-root systems were
   rebuilt in one doubled-coordinate (E_8).  A BFS over the 1,120 (A_2)
   subsystems found the frozen 23-reflection Weyl word
   ([2,1,4,6,0,1,2,3,7,1,0,2,1,3,4,5,6,0,7,1,2,3,4]).
   It sends all root grades (78+81+81) grade-for-grade, not merely the six
   neutral roots.

3. **Objectwise (Z_6) sectors.**  The exact (E_6) minuscule orbit is anchored
   to the cubic 27 by a Schläfli graph isomorphism.  This gives
   (48=16\times3), (33=(10+1)\times3=30+3), and makes
   (32=16_{-3}+\overline{16}_{+3}) objectwise: every matter-odd minuscule
   weight differs from the unique (1_4) weight by a genuine (E_6) root,
   and root negation supplies the conjugate channel.

4. **Two (D_8) involutions.**  The physical (SU(9)) theta-cubed parity root
   model is transported exactly into the standard E8 root gauge.  A
   10-reflection Weyl word
   ([0,3,2,1,4,3,2,5,4,3]) sends its 112 fixed roots to the 112
   (Q_\psi)-even roots and all 128 odd roots to odd roots.  The involutions
   are explicitly Weyl-conjugate even though their order-six extensions remain
   distinguished by the previously frozen trace fingerprints.

The fifth target—the exhaustive matter-even D-flat cone—is owned by the
Holotrade/model side.  Its exact source-data gate is recorded there because the
current Git tree contains neither the per-model `.sp` charge dumps nor
`unbroken2.py`; no class-wide proof is fabricated from summary counts.
