# BT1812 W(E6) hinged-path orbit result

Executed the Schlaefli/W(E6) hinged-path orbit test locally using the BT1806 Schlaefli graph and tritangent support export.

## Reproduction method

Rather than relying on slow VF2 enumeration, the computation uses the standard 27-line cubic-surface model:

```text
6 exceptional lines E_i
6 conic lines Q_i
15 pair lines L_ij
```

The standard Schlaefli graph is matched once to the exported BT1806 graph. The W(E6) action is then generated by:

```text
S_6 index permutations
one Cremona involution on a triple of blown-up points
```

This generates exactly:

```text
|Aut(Schlaefli)| = 51840
```

## Result

For the transported BT1795 image:

```text
image size = 18 support lines
image stabilizer size = 36
```

The defect support is:

```text
T010 -> support 10
T210 -> support 22
T222 -> support 44
```

Under the image stabilizer, the defect orbit has:

```text
orbit size = 36
```

The stabilizer of the specific defect inside the image stabilizer is therefore trivial.

## Crucial hinge refinement

The Hesse-side hinge class has 54 supports. Under the W(E6) image stabilizer, those 54 hinges break into:

```text
10 stabilizer slices
8 slices of size 6
2 slices of size 3
```

The defect lies in a 6-hinge slice. Its orbit through the full image stabilizer has 36 triples, but only 6 of those 36 are Hesse hinges.

The six Hesse hinges in the defect slice are:

```text
(5,10,41)
(7,34,40)
(10,22,44)
(12,34,42)
(18,40,42)
(30,41,44)
```

## Interpretation

W(E6) does not fix the defect uniquely. It distinguishes a 6-hinge slice containing the defect.

That is exactly the count of edges in a 4-state quartet:

```text
C(4,2) = 6
```

So the Schlaefli/E6 orbit result points directly back to the hidden quartet in the `12 = 3 x 4` fibre law. The W(E6) test does not select one hinge; it selects the six possible quartet-pair hinges. The observed table defect is one oriented edge inside that hidden K4/quartet.
