# BT1859-BT1861 summary

Executed the three requested moves after BT1856-BT1858, with repo and paper searches against QEC, Holonet, UTM tape, Rule-110-style local rules, and TeX integration terms.

## Search/read anchors

Useful anchors:

```text
proofs/bt1348_gf3_qec_holonet_integration.py
papers/BT1347_photonic_holonet_journal.tex
papers/BT1857_holonet_k12_compiler_patch.tex
data/bt1856_code_distance_falsifier.json
data/bt1858_utm_tape_witness.json
```

## BT1859 — CSS subsystem split search

The split is aligned with the existing GF(3) qutrit QEC language from BT1348.

Proposed split:

```text
X-type checks = 44 face-current rows
Z-type checks = 6 cyclic-distance parity rows, with H6 as antipodal sheet row
gauge generators = distance-class row sums and antipodal sheet flips
logical candidates = cycles not generated by face rows or distance rows
```

Search result:

```text
raw check rank = 6
face row count = 44
candidate subsystem rank upper bound = 50
logical edge-space lower bound = 16
single-edge detection = true
two-edge same-distance cancellation remains = true
distance theorem status = not proven
```

Verdict: a structured CSS/subsystem candidate exists, but BT1856 remains binding.  No quantum distance theorem yet.

## BT1860 — Holonet TeX integration sanity

BT1860 checked the patch against the Holonet REVTeX environment.

A real issue was found:

```text
\begin{enumerate}[nosep]
```

requires `enumitem`, but the Holonet paper does not load it.  The patch was updated to standard REVTeX-compatible `enumerate`.

Result:

```text
patch file = papers/BT1857_holonet_k12_compiler_patch.tex
status = insertion-ready and TeX-sanitized
```

The original long Holonet journal file was not overwritten through the connector; the safe path is patch file plus integration manifest until a local TeX build applies and compiles the splice.

## BT1861 — UTM transition-rule compiler

BT1858 supplied the 12-symbol alphabet:

```text
{S,L} x Z/6Z
```

BT1861 adds a local transition rule:

```text
T((a,h_a),(b,h_b),(c,h_c)) = (rule110(a,b,c), h_b+1 mod 6)
```

Binary gap-track table:

```text
111 -> 0
110 -> 1
101 -> 1
100 -> 0
011 -> 1
010 -> 1
001 -> 1
000 -> 0
```

Coverage:

```text
12^3 = 1728 local neighborhoods
12 output symbols
radius = 1
```

Verdict: BT1858 supplied the executable tape alphabet; BT1861 supplies the transition-rule compiler.  Full physical universality remains open.
