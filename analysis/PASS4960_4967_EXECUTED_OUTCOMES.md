# Passes 4960–4967 — executed outcomes

Status: **EXECUTED ON MASTER; dedicated exact replay/publication workflow queued at close time.**

This packet executed the five queued attacks after Pass4958 plus three independent outside-box probes. Two attacks close as structural negatives, which are retained as results rather than forced into positive claims.

## Pass 4960 — degree-7 moment barrier at 173

Using frozen `A3..A7(K^perp)` and exact Krawtchouk/centered-moment identities, the one-sided degree-7 truncated moment relaxation was tested at leader distance 173.

Exact feasible signed shell values:

`(T3,T4,T5,T6,T7)=(-1080,-1936,75316,830590,-37193040)`.

They obey every independent absolute/parity shell constraint. Both the ordinary degree-3 moment matrix and the `(X+7)` localizing matrix are positive definite.

Therefore this relaxation cannot improve the current theorem

`134 <= rho(K) <= 173`.

This is not an actual distance-173 coset. It proves that a further moment attack needs correlations among signed shells, additional coset identities, or stronger structure than independent `|Tj|<=Aj` bounds through `j=7`.

## Pass 4961 — ordinary equivariant dark-15 bridge is impossible

The PSp action on `40 points × 40 lines` has exactly two orbitals:

- incident: 160,
- nonincident: 1440.

Hence every equivariant point-line matrix is `a Z + b(J-Z)`. Since `J` vanishes on nonconstant constituents and `Z` kills the line `-4` eigenspace,

`Hom_PSp(V15_line,V15_point)=0`

and the reverse Hom space is also zero.

Thus signs/orientations inside an ordinary equivariant incidence matrix cannot restore the missing 15↔15 channel.

## Pass 4962 — triangle-filled Q43 connection has H¹(F2)=0 and local curvature generates S3

The Q43 disjointness graph has:

- 40 vertices,
- 540 edges,
- 3240 triangles.

The triangle-edge boundary rank over F2 is 501, exactly `540-40+1`, the graph cycle rank. With vertex-edge rank 39,

`H^1 = 540 - 39 - 501 = 0`.

The sign curvature is:

- 1080 flat triangles,
- 2160 reflected/odd triangles.

After rooting all triangle holonomies to one fiber, the curvature set is the identity plus all three S3 transpositions. The transpositions generate S3.

Therefore full Pass4945 holonomy is already generated by local triangle curvature; there is no residual flat abelianized C2 sector after triangle relations are imposed.

## Pass 4963 — Witting carrier correction and exact Pancharatnam center law

Direct Eisenstein arithmetic on the forty Witting/Penrose rays proves:

- ambient carrier: C^4 / CP^3;
- orthogonality graph: standard W(3,3) POINT graph;
- not isomorphic to the Q(4,3) Steiner/W33-line graph.

The old direct phase-exponent ray→F3^4 encoder was invalid: it sent 40 rays to only 19 tuples and mapped 8 rays to the zero tuple.

On all 3240 nonorthogonal triples, exact Bargmann phase obeys:

- one W33 common center: `+pi/6` 1440, `-pi/6` 1440;
- four W33 common centers: `+pi/2` 180, `-pi/2` 180.

Thus phase magnitude exactly detects the W33 point-triad 1/4-center dichotomy.

The old Pass4882 direct Witting=Steiner/Q43 claim is withdrawn and the legacy producer/tool/report are hardened against recreating it.

## Pass 4964 — unique full-group double-six ↔ spread bridge

For the full order-51840 W(E6)=PGSp(4,3) action:

- 36 cubic-surface double-sixes,
- 36 W33 spreads,
- stabilizer of one double-six: order 1440,
- fixed spreads under that stabilizer: exactly one.

This produces a unique full-group equivariant bijection.

Pair relations transport exactly:

- double-six intersection 6 ↔ spread overlap 1: 360 pairs;
- double-six intersection 4 ↔ spread overlap 4: 270 pairs.

Therefore the double-six `SRG(36,20,10,12)` is literally the one-overlap graph on W33 spreads.

## Pass 4965 — outside-box: canonical local nine-spread chart

Transporting all 120 Steiner triangles through Pass4964 gives:

- every Steiner triangle maps to 3 spreads with exactly one common W33 line;
- every W33 line lies in 9 spreads;
- exactly 3 Steiner triangles lie over each W33 line;
- those 3 triangles partition its 9 incident spreads as `3+3+3`.

Within a triple spread overlap is 1; across triples it is 4. Therefore:

- local one-overlap graph: `3 K3`;
- local four-overlap graph: `K3,3,3`.

This is a finite incidence theorem, not automatically a qutrit hardware theorem.

## Pass 4966 — outside-box: Witting oriented phase realizes PGSp/PSp sign

Five PSp generators transferred to the Witting rays preserve oriented phase on every one of the 3240 nonorthogonal triples.

The explicit similitude

`M = diag(1,2,1,2) mod 3`

satisfies `M^T J M = -J`, extends the action from order 25920 to 51840, and negates every oriented phase:

- `+pi/6 ↔ -pi/6`, 1440 each;
- `+pi/2 ↔ -pi/2`, 180 each.

Thus oriented Witting phase realizes the finite `PGSp/PSp ≅ C2` character.

## Pass 4967 — outside-box: point + double-six complete W33-line readout

Pass4964 turns the old anonymous line-spread incidence into a canonical double-six/line matrix `C` of shape 36×40.

- row weight 10,
- column weight 9,
- rank 16,
- line-side nonzero sector `1+15`, dark sector `24`.

Point-line incidence `Z` has rank 25 and transmits `1+24`, dark `15`.

Together:

`18 I_40 = 3 Z^T Z + C^T C - 3 J_40`

and

`rank [Z;C] = 40`.

Every W33-line coordinate can therefore be reconstructed exactly from point and cubic-surface double-six readouts.

Prior-art firewall: Part CXXVI owns the spread-line spectrum and Pass4958 owns point+anonymous-spread complementarity. Pass4967's new content is the canonical double-six naming of those spread rows via Pass4964.

## Integration / publication

Integrated into all shared paper wrappers through:

- `analysis/PASS4960_4967_moments_dark15_curvature_witting_spread_insert.tex`
- `analysis/W33_CURRENT_FRONTIER_MANIFEST.tex`

Published source:

- `docs/pass4960-4967-moments-witting-double-six.html`
- `analysis/PASS4960_4967_index_insert.html`
- `tools/integrate_pass4960_4967_public.py`

Witting correction hardening:

- `analysis/w33_pass4882_pancharatnam_steiner_f3_cocycle.py`
- `data/PART_W33_PASS4882_PANCHARATNAM_STEINER_COCYCLE.json`
- `tools/pancharatnam_symplectic_invariants.py`
- `docs/pancharatnam_symplectic_invariants.md`
- `docs/PASS4878_4882_PERPLEXITY_FRONTIER.md`

Regression / replay:

- `tests/test_w33_pass4960_4967_eight_front.py`
- `.github/workflows/w33_pass4960_4967_eight_front.yml`

At close time the dedicated workflow run is **queued**, not observed green or failed. Do not report remote replay or public-root materialization as completed until the runner executes it.

## Hard boundaries

1. The exact covering radius remains open; Pass4960 proves a relaxation barrier, not a 173-distance coset.
2. The dark-15 zero-Hom theorem is linear and equivariant; nonlinear/twisted/symmetry-broken bridges remain possible.
3. The Witting result lives on the W33 point action; the Steiner fibers live on Q43/W33 lines.
4. The finite outer phase sign is not automatically spacetime parity or CP.
5. The canonical 3+3+3 line chart is finite incidence and does not by itself certify a physical qutrit device.
