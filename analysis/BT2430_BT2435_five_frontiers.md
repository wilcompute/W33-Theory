# Passes 2430–2435 — labelled tomotope obstruction, U6 identifiability, signature-pair no-go, commutative shell fusions, and the first split-subgroup Hom

## Executive result

All five requested fronts were executed. The packet closes four exact finite questions and turns the remaining global U6 computation into a sharply specified union statistic.

The central corrections are:

1. the canonical curved-event `192` reproduces the tomotope's count package but not its archived labelled symmetry group;
2. global pair-collision data determines the second syndrome-multiplicity moment but cannot determine singleton syndromes;
3. the particular nine-signature capacity witness is literally impossible because two of its exact cover fibers have no disjoint pair;
4. the rank-22 full-`PGSp` shell algebra has fifteen exact commutative fusions, with a unique finest rank-nine scheme;
5. the central E8/coexact obstruction disappears on a distinguished `C5`, where the Hom space has dimension 144.

## Pass 2430 — the selected 192 is not the archived labelled tomotope

Pass 2410 selected the two event-duad-valency-two layers, giving a canonical local carrier of `96+96=192` at each residual duad. The fixed-duad stabilizer is `C2 x S4` of order 48 and acts in four regular 48-element orbits, two per layer.

The new search builds the complete intrinsic ordered-pair relation on the 192 events from:

- layer pair;
- event intersection size;
- equality and cross-incidence of the distinguished event centers;
- leaf intersection and cross-adjacency counts;
- equality and intersection of the secondary residual duads.

There are exactly 199 such relations. Every automorphism of the order-48 stabilizer was tested in every regular-orbit exchange. The result is:

- 20 relation-preserving extensions;
- every extension has order 96;
- every extension has two flag orbits of 96;
- every extension is `C2^2 x S4`;
- every extension has order spectrum `1^1 2^39 3^8 4^24 6^24`.

The archived labelled tomotope symmetry has order spectrum

`1^1 2^15 3^32 4^24 8^24`.

Therefore no complete-intrinsic-relation extension is the archived `W(D4)^+`-type tomotope symmetry. The exact `192=96+96=8x24` and unordered `4+4` correspondences survive, but they do not define a labelled rank-four maniplex isomorphism.

## Pass 2431 — pair counts cannot determine the global U6 singleton coefficient

Let `n_m` be the number of weight-six syndromes of multiplicity `m`. The exact global data gives

`N = sum m n_m = 249,219,381,880`

and

`E = sum C(m,2)n_m = 1,724,138,884,380`.

Hence the exact second moment is

`sum m^2 n_m = N + 2E = 3,697,497,150,640`.

This implies a Cauchy lower bound of `16,797,930,540` occupied weight-six syndromes, but it does not determine `n_1`. The finite witness

- `n_2=3`, with `N=6`, `E=3`, `n_1=0`;
- `n_3=1,n_1=3`, with the same `N=6`, `E=3`;

proves non-identifiability from the first two factorial moments.

The missing computation is therefore an idempotent union, not another pair sum. A deterministic 4096-shard contract partitions all `6,230,484,547` fixed-chart combinadic ranks into disjoint intervals of 60,844,575 or 60,844,576 bits. Every mapper emits a bitset of ranks marked by any collision partner or lower-shadow syndrome; reduction is bitwise OR followed by exact popcount.

No global U6 coefficient is promoted.

## Pass 2432 — the selected nine signatures are incompatible at depth two

Pass 2416 proved that all nine signatures selected by the Pass-2309 capacity witness are individually realized by exact covers. Their fiber sizes are

`11,664, 11,664, 864, 11,664, 2,808, 288, 2,808, 864, 288`,

for a total of 42,912 covers.

The new exhaustive worker independently reconstructs signature fibers 1 and 8 from the literal 540-frame/240-edge exact-cover geometry:

- fiber 1: 11,664 covers, 90,497 search nodes;
- fiber 8: 288 covers, 34,014 search nodes.

It then checks all

`11,664 x 288 = 3,359,232`

pairs and finds

`0`

frame-disjoint pairs.

Thus no transversal can choose one pairwise-disjoint cover from each of the nine selected fibers. The selected balanced signature tuple is refuted before higher-order packing begins.

This does not decide `chi(H)=9`: another signature multiset could still support a resolution. The parallel full-group argument that no nine-colouring can be group-equivariant further confirms that future searches must remain asymmetric.

## Pass 2433 — fifteen commutative fusions and a unique finest rank-nine scheme

The rank-22 `PGSp(4,3)` coherent configuration has 17 nonidentity transpose-orbit variables. Every one of the `2^16-1=65,535` binary symmetric seeds, modulo complement, was closed exactly under transpose and multiplication.

The result is:

- 16 distinct coherent closures;
- 15 commutative association-scheme fusions;
- rank distribution `3^3,4^5,5^3,6,7,8,9`.

The three rank-three schemes are the imprimitive clique systems:

- `135 K4`, with parameters `(540,3,2,0)`;
- `45 K12`, with parameters `(540,11,10,0)`;
- `36 K15`, with parameters `(540,14,13,0)`.

The unique finest commutative fusion has rank nine and valencies

`1,256,24,128,48,48,8,3,24`.

Its complete structure-constant stream has SHA-256

`e440020943e56bb5bfa9cbf42e1baaf31e251b33ed050cfc6c535e8a6b46d6e2`.

This exhausts commutative fusions generated by binary symmetric unions of the 22 orbitals. It does not claim classification of arbitrary non-binary relation partitions.

## Pass 2434 — the first distinguished split-subgroup Hom is 144-dimensional

The full lifted groups are obstructed by opposite central characters: the faithful real `E8` carrier belongs to `2.U4(2)` with the central involution acting as `-I`, while the coexact 90 is inflated from `U4(2)` and has central action `+I`.

Restrict to the distinguished order-five subgroup. It avoids the center and the unique order-five class partitions the 40 W33 points into eight pentagons.

For one faithful degree-four constituent, the ATLAS standard order-five generator has characteristic polynomial

`Phi5(t)=t^4+t^3+t^2+t+1`.

Therefore the real eight-dimensional carrier, the sum of the conjugate degree-four constituents, restricts to `C5` with multiplicities

`(0,2,2,2,2)`

on `(1,zeta,zeta^2,zeta^3,zeta^4)`.

The literal signed-edge projector has rank 90 and trace zero on all four nonidentity powers of the order-five class. Hence its restriction has multiplicities

`(18,18,18,18,18)`.

Consequently

`dim Hom_C5(E8,90) = dim Hom_C5(90,E8) = 4 x 2 x 18 = 144`.

This proves existence, not canonicity. Choosing a specific `C5` and one vector in a 144-dimensional Hom space breaks the full symmetry.

## Pass 2435 — evidence boundary

The packet separates:

- exact count-scale tomotope correspondences from a false labelled-maniplex identification;
- exact pair moments from the missing singleton-union statistic;
- individual signature realizability from compatibility of those fibers;
- the complete noncommutative rank-22 algebra from its exact commutative fusion lattice;
- existence of proper-subgroup intertwiners from a canonical full-symmetry coupling.

No code count, tomotope flag, subgroup character, shell relation, or syndrome multiplicity is identified with a measured particle, charge, generation, coupling, or spacetime degree of freedom.
