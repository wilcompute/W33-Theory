# Pass 10946 — clock-code-cone objectwise theorem

The new temporal clock proposal can now be pinned to the existing repository objects without a counting argument. Take the four projective rays of `P1(F3)` and choose the oriented representatives `(1,0), (0,1), (1,1), (2,1)`.

Evaluating the linear clock function `ell_(a,b)(x,y)=a*x+b*y` gives `(a,b,a+b,2a+b)=(a,b,a+b,b-a)`. This is **exactly** the frozen repository tetracode generated by `(1,0,1,2)` and `(0,1,1,1)`: no coordinate permutation or post-hoc code equivalence is required.

## The sign that looked arbitrary is an orientation lift

The usual normalized representatives are `(1,0), (0,1), (1,1), (1,2)`. Only the fourth differs: `(2,1)=-(1,2)`. Projectively these are the same point, but evaluation notices the sign. That single oriented representative is exactly why the repository convention has `b-a` rather than `a-b` in the fourth coordinate.

So the projective clock ray determines the clock **frame**, while choosing a representative of that ray supplies a binary algebraic orientation. The code sign is not extra bookkeeping glued on after the fact; it is the lift from projective clock geometry to an oriented evaluation code.

Every nonzero form vanishes on exactly one projective clock ray. The eight nonzero tetracode words consequently split into four opposite pairs, one pair for each omitted clock. Adding the three possible qutrit origins gives `4*2*3=24` complete finite clock calibrations.

## The same four rays are the already frozen null cone

Apply the symmetric-square map `nu(x,y)=(x^2,x*y,y^2)`. Every image satisfies `q(a,b,c)=a*c-b^2=0`. More importantly, the four images match the existing M36/Hesse temporal atlas **coordinate by coordinate**:

- tetracode 0: `[1:0] -> (1,0,0) -> M36 family B`
- tetracode 1: `[0:1] -> (0,0,1) -> M36 family A`
- tetracode 2: `[1:1] -> (1,1,1) -> M36 family C`
- tetracode 3: `[1:2] -> (1,2,1) -> M36 family D`

Those family records already carry the Bell-line point, Hesse striation and temporal A2 component. The clock-code correspondence therefore plugs directly into an objectwise chain already frozen in the repository rather than creating a parallel dictionary.

## Symmetry

All 48 elements of `GL(2,3)` induce exact monomial automorphisms of the oriented evaluation tetracode. Their permutation image on the four projective rays has order 24, `PGL(2,3) ~= S4`. The two lifts `g` and `-g` give the same projective clock permutation but opposite global codeword signs.

This is the clean algebraic separation we needed: `GL(2,3)` is the signed clock lift, while `PGL(2,3)=S4` acts on the projective clock frames. It explains why the finite-null/Hesse S4 and tetracode clock symmetry can be the same action while still retaining a distinct reversal sign.

## E8 consequence and boundary

The older E8 glue certificate already proves `240 = 4*6 + 8*27` for the A2^4 skeleton plus the eight nonzero tetracode sectors. Pass 10946 upgrades the reading of its four glue coordinates: they are the four projective clock rays, and the eight nonzero sectors are their eight oriented nonzero linear clock functions.

This remains a theorem of finite F3 projective geometry, coding theory and the existing finite determinant cone. The sign lift is not by itself a thermodynamic arrow, and the determinant cone is not being identified with continuum Lorentz spacetime. Irreversible time still requires the open-system layer already separated elsewhere in the temporal program.

Producer: `analysis/w33_pass10946_clock_code_cone_objectwise.py`

Certificate: `data/w33_pass10946_clock_code_cone_objectwise.json`

Regression: `tests/test_w33_pass10946_clock_code_cone_objectwise.py`
