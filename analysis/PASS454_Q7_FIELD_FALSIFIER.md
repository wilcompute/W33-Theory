# Pass 454 — q=7 decisive field falsifier

A deterministic census of 80 random inverse-closed q=7 sections was executed using the six nonzero central-Fourier blocks rather than the full 343-vertex graph.

Results:

\[
80\text{ samples}\longrightarrow80\text{ distinct nonlinear spectra}.
\]

Pass 447's quadratic integer-trace/norm detector finds no quadratic pairs. Instead, for every sample the three representatives \(t=1,2,3\) produce an irreducible trace-cube polynomial whose number field has discriminant

\[
49.
\]

Hence the q=5 quadratic atlas does not persist at q=7. It upgrades to the real cubic cyclotomic field

\[
\mathbb Q(\zeta_7+\zeta_7^{-1}).
\]

This is the decisive falsifier for a universal \(\sqrt5\) law across odd q, while positively confirming the cyclotomic covariance theorem of Pass 453.
