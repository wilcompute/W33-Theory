# BT1483--BT1485: ABI v2 consumer, E6 DAG table v2, and D4/V4 branch audit

## BT1483

The BT1482 ABI v2 is executable. The consumer regenerates 12 packets, 48 events, and 72 rows.

The dual-axis row balances are P0=P1=P2=24 rows and T0=T1=T2=T3=18 rows. Thus the C3 channel axis and the V4 triangle axis both survive expansion to the decoder rows.

## BT1484

The paper claim table is regenerated from the E6-merged DAG. It now includes E6 provenance for the oriented 72-sector, the H1/CSS 81 closure, and the C3 x V4 strand grid.

Blocked formula and physical claims remain downstream and cannot support exact finite claims.

## BT1485

The branch action audit shows that D4 shear fixes each V4 branch class and therefore each gauge triangle. Tau4 acts on branches by [0,1,2,3] -> [2,3,0,1], an order-2 translation on V4. Tau4 preserves the four-triangle partition as a set, but does not fix each triangle individually. Tau4 and shear still fail to commute on full branch/phase states, so the retwined-frame rule remains necessary.

## Current synthesis

C3 x V4 -> ABI v2 rows -> E6-aware claim DAG -> D4/V4 branch-action firewall.
