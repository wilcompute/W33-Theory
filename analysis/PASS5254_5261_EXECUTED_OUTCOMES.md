# Pass5254–5261 executed outcomes — reconciled continuation

**Status:** executed on 2026-08-14. These results were developed concurrently with the authoritative Pass5238–5245 packet. Rather than overwrite that packet's numbering, this stream is authoritatively renumbered 5254–5261. The original development filenames are preserved as provenance and are mapped explicitly in `data/PART_W33_PASS5254_5261_RESULTS.json`.

The authoritative q5 state entering this packet is already strong: `C_F=[325,65,25]_2`; the 156 minimum footprint words are exactly W(3,5) point footprints; every q5 apartment-code word of weight 625 is one of the 936 chamber stars; and any strict word below 625 has zero P-component parity and at most 15 active P components. The full q5 distance theorem is **not** yet closed because the zero-P strict residual remains.

## Pass5254 — q3 weight-eight false-center echo family

The radius-seven provenance theorem has a sharp structured first-correction obstruction at weight eight. Every apartment center lies in four local charts, and in each chart there are exactly two two-error masks on the other coordinates whose unique local leader is the center. Choosing one poison pair independently in each chart gives `2^4=16` configurations per center. Across 1620 centers these are 25,920 distinct weight-eight errors.

For every one, maximum vote followed by maximum singleton provenance returns the false center alone. Nevertheless the **full** deterministic decoder self-heals:

`8 -> 9 -> 1 -> 0`

in exactly three sweeps. Thus the true-only/monotone first-correction guarantee is exactly radius seven, while this complete obstruction family does not falsify eventual radius eight. An orbit-complete census of every weight-eight error remains open.

Development source: `analysis/w33_pass5239_q3_radius8_false_center_echo.py`.

## Pass5255 — exact modular Jordan form of the connected odd-q L graph

Pass5221's q3/q5 GF(2) nullities are now resolved into complete Jordan chains.

At q3,

`chi_A(x) mod 2 = x^100 (x+1)^440`.

The zero-primary sector is `J1^100`. The one-primary sector is

`J1^70 + J2^22 + J3^26 + J4^62`.

At q5,

`chi_A(x) mod 2 = x^3716 (x+1)^6034`.

The zero-primary sector is

`J1^2156 + J3^520`,

while the one-primary sector is

`J1^804 + J2^1471 + J3^48 + J4^536`.

So the characteristic-zero irreducible decomposition is strongly nonsemisimple after reduction mod two; the large radicals are not just collections of reduced real zero-eigenvalues.

Development source: `analysis/w33_pass5240_connectedL_mod2_jordan_structure.py`.

## Pass5256 — characteristic>3 root outer shell is a uniform two-root torus

In canonical U(q) coordinates the commuting subgroup `<H1,H3>` has order q^2 and its right cosets are exactly the fixed `(a,c)` fibers. Thus the quotient is coordinatized by `F_q^2`.

Combining this quotient with the symbolic root-metric theorem gives a family statement in every finite field of characteristic greater than three: distance four projects exactly onto

`(F_q^*)^2`,

and every nonzero `(a,c)` fiber contains exactly

`(q-4)^2`

distance-four states. Hence the shell size is `(q-1)^2(q-4)^2`. At q5 Pass5217 identifies those 25 fibers objectwise with the 25 P atoms of a chamber star, giving the previously observed 16-atom nonzero torus plus nine-atom coordinate cross.

Development source: `analysis/w33_pass5241_root_outer_shell_two_root_torus_family.py`.

## Pass5257 — complete q5 minimum-dual shell is a real frame with one binary blind mode

Let H8 be the 24,375-by-325 incidence matrix of the complete q5 weight-eight dual shell. The rank-four pair scheme gives

`H8^T H8 = 600 I + 25 R1 + 5 R2`.

Its real Gram spectrum is

`4800^1 + 900^90 + 425^104 + 500^130`,

so H8 has full real column rank 325. Over GF(2), however, its rank is only 259 and its nullity is 66. Since the 65-dimensional footprint code lies in the kernel and the all-ones vector is also killed by every even weight-eight check, while the independent odd weight-nine dual check excludes the all-ones vector from the footprint code, one gets the exact identity

`ker_F2(H8) = C_F direct-sum <1>`.

Thus the entire minimum dual shell is information-complete over R but misses exactly one binary bit: global parity. One odd dual check supplies that bit and raises the parity-check rank to 260.

Development source: `analysis/w33_pass5242_q5_weight8_frame_constant_mode.py`.

## Pass5258 — Hoffman-13 shortening is a doubly-even 52-dimensional residual

The Hoffman 13-cover gives 13 systematic footprint coordinates and a shortening kernel of dimension 52. Because the 13 cover carriers partition all 156 W-points, zero projection on those coordinates forces even point-input parity. Point-input parity is representation-independent because `ker(F^T)` is generated by even weight-six W-lines. Hence the shortened code lies in the doubly-even hull.

With `d(C_F)=25`, the shortened code has

`[312,52,d_sh]_2`, `28 <= d_sh <= 40`,

and `d_sh` must be one of `28,32,36,40`. Weight-40 shortened words are supplied by sums of two collinear point footprints in one cover carrier.

Development source: `analysis/w33_pass5243_hoffman13_shortened_doubly_even_code.py`.

## Pass5259 — complete low even shell of one q5 P component

A q5 P component is `Cut(K6) tensor Cut(K6)=[225,25,25]_2`. Exhaustion of all `2^25=33,554,432` states gives the low even shell

`40^180, 48^450, 64^3825, ...`.

The minimum nonzero even weight is exactly 40. Every weight-40 word is exactly a simple tensor of a weight-five factor cut and a weight-eight factor cut, in either order; there are

`6*15*2=180`

of them.

Since every q5 strict apartment word has zero P parity, every active P component is a nonzero even local word and therefore costs at least 40 apartments. Hence a strict sub-625 word can activate at most

`floor(624/40)=15`

of the 325 P components.

Development source: `analysis/w33_pass5244_q5_zero_footprint_even_tensor_shell.py`.

## Pass5260 — parity/syndrome exact sequence isolates a 560-dimensional strict residual

The full P-side solution space has dimension `325*25=8125`. Its even-block subspace has dimension `325*24=7800`. The connected-L compatibility map has rank 7500 on the full P side and kernel the 625-dimensional apartment code.

The parity image of that apartment code is the 65-dimensional footprint code, so the zero-footprint apartment residual has dimension

`625-65 = 560`.

Rank-nullity then gives connected-L rank 7240 on the even-block P space. Therefore the missing syndrome quotient has dimension

`7500-7240 = 260 = 325-65`,

and there is a canonical exact sequence

`0 -> C_F -> F2^325 -> Im(L|V_P)/Im(L|E) -> 0`.

The dual of this quotient is the 260-dimensional footprint dual code. This explains structurally why the sparse footprint checks are precisely the parity information not already present in the even-block connected-L image.

The strict q5 problem is now equivalent to one finite block-code target:

**prove the 560-dimensional zero-footprint residual has P-block distance at least 16.**

Pass5259 shows any strict sub-625 word would have block support at most 15. Therefore a block-distance-16 theorem would finish the strict side and, together with the already closed equality shell, prove q5 apartment-code distance 625.

Development source: `analysis/w33_pass5245_zero_footprint_parity_syndrome_exact_sequence.py`.

## Pass5261 — the exact footprint minimum shell is a two-distance tight frame

The 156 exact weight-25 footprint minimum words are the W(3,5) point footprints. Their incidence Gram is

`F F^T = 24 I + 4 A_W + J`,

with spectrum `300^1 + 40^90 + 0^65`. Every coordinate occurs in 12 minimum words, so the row centroid is `(1/13) 1_325`. After centering the Gram spectrum becomes

`40^90 + 0^66`.

Therefore the 156 centered minimum-shell vectors form an equal-norm tight frame in R^90, with squared norm `300/13` and frame bound 40. After unit normalization the two off-diagonal inner products are

`2/15` for collinear W-points and `-1/25` for noncollinear W-points.

So the binary minimum shell itself recovers the W(3,5) collinearity relation as a real two-distance tight frame.

## Evidence boundary

The q5 **equality shell is closed**: all weight-625 apartment-code words are chamber stars. The q5 **strict** side is not yet closed. Any strict counterexample must lie in the 560-dimensional zero-footprint residual and occupy at most 15 P blocks. Proving residual block distance at least 16 is now a sufficient strict-side closure target. The q3 decoder has guaranteed radius seven; the complete 25,920 weight-eight false-center echo family self-heals, but global eventual radius eight remains open. The Hoffman shortened exact minimum remains open. The connected-L Jordan forms are exact only for q3 and q5.
