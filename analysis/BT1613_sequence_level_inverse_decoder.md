# BT1613 Sequence-level Inverse Decoder Prototype

Detector bin alone gives local Fano fields, not frame identity.

The decoder uses ordered tick context to recover the Witting frame, then checks detector-bin consistency.

It reconstructs 1600 frames, 520 control frames, 1080 contextual-fuel frames, and the 168-bin usage profile.
