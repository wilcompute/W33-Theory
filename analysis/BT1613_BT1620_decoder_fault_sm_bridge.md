# BT1613-BT1620 Decoder, Fault Integration, and SM Bridge Firewall

## BT1613

The sequence-level inverse decoder uses ordered tick context plus detector-bin consistency to reconstruct Witting source-target frames. Bin-only injection remains blocked.

## BT1614

The fault-aware integration promotes decoded frames to PASS, CORRECTED, or ABORTED outcomes with CSS row and Pauli-frame update fields, following the parallel fault-path ABI shape.

## BT1620

The Standard Model bridge is moved to the BT1620 namespace to avoid parallel BT1615 collisions. It remains a firewall scaffold: algebraic SM sectors are linked to required ABI observables, but no agreement claim is allowed until canonical parameter JSON, decoded click streams, sector observables, and tolerances exist.

## Current synthesis

The architecture now has an inverse-decoder scaffold, a fault-state stream, and a claim-safe path toward testing algebraic SM packets against ABI output statistics.
