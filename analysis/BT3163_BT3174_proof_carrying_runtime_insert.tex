\section{Proof-carrying epoch-aware execution}
\label{sec:bt3163-bt3174}

The factorized noisy controller now closes into a complete streamed decision path while
preserving the architecture's typed separations.  The selector clock, mirror transport bus,
address sheets, temporal extension, protected memory and Bayesian observer are not identified
with one another merely because they share counts or control the same photon.

The exact hypothesis universe contains
\[
1+45\cdot7+\binom{45}{2}7^2=48{,}826
\]
objects.  A two-pass stream reconstructs every logweight from one baseline, at most two unary
factors and at most one shared-triangle correction, then accumulates the normalizer and all
$23\times8$ predicted-outcome bins.  No dense posterior RAM is required.  The complete
schedule is $98{,}387$ cycles, including the $528$-cycle factor load and entropy/argmax scan.
For the frozen Q12/exponential-LUT reference, all twelve deterministic next actions agree
with floating point; the maximum posterior and action-score errors are respectively
$4.09\times10^{-7}$ and $4.44\times10^{-5}$ bits.  Clock rate remains a placement question.

The epoch code is also strengthened.  Twelve payload-unused symbols are assigned to the twelve
phases, and phase $p$ is transmitted as five repeats of its assigned symbol.  Distinct markers
and marker-to-payload words have Levenshtein distance at least five.  Exhaustion of all
$298{,}140$ radius-two received traces proves unique phase recovery under any two edits, with
zero clean payload symbols required after the marker.  Length five is optimal for two-edit
correction.

The runtime now carries three exact affine decoders.  In the frozen path-plus-collision model,
\texttt{fast6} strictly dominates the current four-opcode ISA for every nonnegative collision
price and crosses the $36$-collision four-opcode ISA at $16.7016424928$.  This is not yet a
physical promotion: the six-opcode decoder must be calibrated, fit and meet timing, while the
current decoder remains the fail-closed fallback.  A separate $32$-shard workflow computes the
complete growth series of all $194$ universal larger ISAs before any global optimum is claimed.

Two new structural diagnostics accompany the controller.  First,
$[D_4,D_4]=\{1,r^2\}$, so one central commutator bit is a complete conjugation-invariant
curvature syndrome for every shared-triangle correction; the $3{,}381$ factors split as
$1{,}725$ flat and $1{,}656$ curved.  Second, an ISA is also a lossy control channel from
opcode to destination.  Uniform frame/opcode averaging gives $1.84952$, $1.90508$ and
$2.41057$ bits per dispatch for the current, low-collision and fast-six ISAs.  Absolute
capacity and normalized collision efficiency are therefore different optimization objectives.

Finally, the rank-three $M_{36}$ question is launched as a complete $256$-shard experiment over
the canonical Pass-3125 engine.  An empty partial shard is not a no-go, and no aggregate is
complete without all shard identities under one engine digest.  Candidate promotion remains
fail-closed behind the independent trace-eight projector, first-order rejection, Pauli,
Wigner, stabilizer-fidelity and error-slope gates.  No laboratory optical, heat, coherence or
physical edit-rate conclusion follows from these finite and digital results.
