# BT1859 — CSS Subsystem Split Search

BT1859 searches for the correct quantum-code interpretation of the BT1853/BT1856 optical incidence compiler.

## Repo anchors searched

The key existing QEC anchor is:

```text
proofs/bt1348_gf3_qec_holonet_integration.py
```

That file already verifies:

```text
GF(3) arithmetic
qutrit Pauli X,Z with X^3 = Z^3 = I
Weyl relation
[[3,1,2]]_3 qutrit repetition code
stabilizer syndrome detection
Holonet routing compatibility
W(3,3) gauge/matter QEC partition
```

So BT1859 does not invent a new code language.  It attaches the K12/F12 compiler surface to the existing GF(3) qutrit stabilizer language.

## Proposed subsystem split

```text
X-type checks: 44 face-current rows on K12 triangular faces
Z-type checks: 6 cyclic-distance parity rows, with H6 = antipodal sheet row
gauge generators: distance-class row sums and antipodal sheet flips
logical candidates: cycles not generated by face rows or distance rows
```

## Search result

```text
raw check rank = 6
face row count = 44
candidate subsystem rank upper bound = 50
logical edge-space lower bound = 16
single-edge detection = true
two-edge same-distance cancellation remains = true
distance theorem status = not proven
```

## Verdict

A CSS/subsystem candidate exists as a structured split, but the protected-distance theorem is still open.  BT1856 remains binding: the raw incidence compiler alone is not a quantum code-distance theorem.

The next falsifier must compute the true homology/logical basis of the combined `44 + 6` check complex.

Boundary: subsystem split search only; no protected quantum memory or distance theorem is claimed.
