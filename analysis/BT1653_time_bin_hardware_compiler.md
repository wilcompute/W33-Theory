# BT1653 Time-bin Hardware Compiler

The 2048-bin envelope is lowered into a component schema: source, 11-bit time-bin encoder, binary delay tree, active Witting analyzer, guard router, dark/loss/parity gates, detector bank, and CSS retry controller.

All components carry loss placeholders and calibration trigger timing.

This is a compiler schema, not a measured hardware design.
