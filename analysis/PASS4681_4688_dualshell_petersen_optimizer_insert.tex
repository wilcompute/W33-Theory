\section*{Passes 4681--4688: dual-shell reconstruction, complementary incidence algebra, and the Petersen routing bundle}

This block continues the selected $135_6$--$270_3$ geometry and its binary code while preserving a strict finite-evidence boundary.  All identifications below are explicit code, incidence, graph, group-action, graph-cover, or parameterized-routing statements.

\paragraph{The dual weight-132 shell is the protected 45.}
For the selected code $C=[135,16,30]_2$, the dual $C^\perp=[135,119,3]_2$ has exactly $45$ words of weight $132$.  Their three-coordinate complements are pairwise disjoint and partition the $135$ coordinates as
\[
135=45\cdot3.
\]
These $45$ triples are literally the singular-fibre triples attached to the previously certified protected $16$-line supports.  They form one $PSp(4,3)$ orbit with stabilizer $576$, and one $PGSp(4,3)$ orbit with stabilizer $1152$.  Projecting the $270$ dual weight-three selected lines through these packets gives $270$ distinct triples on the $45$ packets.  Every occurring packet pair occurs three times, and the packet-pair graph is
\[
\operatorname{SRG}(45,12,3,3),
\]
the complement of the protected $\operatorname{SRG}(45,32,22,24)$.

\paragraph{The complementary $135\times36$ incidences close an exact algebra.}
Let $Q$ be the Pass-4663 incidence $135_4$--$36_{15}$ and let $M$ be the code-minimum/spread incidence $135_8$--$36_{30}$.  Their rows are disjoint, both have rational rank $36$, and their combined rational column space still has dimension $36$.  If $A$ is the spread/double-six graph $\operatorname{SRG}(36,15,6,6)$, then
\[
Q^{T}Q=15I+3A,\qquad M^{T}M=24I+6J,
\]
\[
Q^{T}M=M^{T}Q=6(J-I-A),
\]
and therefore
\[
(Q+M)^T(Q+M)=9(3I+2J-A).
\]
Thus $Q+M$ has rank $21$ and annihilates exactly the $15$-dimensional $+3$ eigenspace of $A$.  Its nonzero Smith factors are $1^{14}3^7$.  Individually,
\[
\operatorname{SNF}(Q)_{\ne0}=1^{29}2^6 4,
\qquad
\operatorname{SNF}(M)_{\ne0}=1^{15}2^{14}4^6 8.
\]
On the $135$ side the Gram operators generate a commutative four-dimensional algebra with joint multiplicities $1,15,20,99$ and exact relation
\[
MM^T=2QQ^T+QM^T.
\]

\paragraph{The apartment cohomology class has only a $C_2$ presentation normalizer.}
The nonzero Pass-4656 deck class lives in the first cohomology of a particular five-factor labelled Schreier multigraph.  The natural normalizer of those five cyclic transvection factors is trivial inside $PSp(4,3)$ and has order two inside $PGSp(4,3)$.  The nonidentity normalizer element lies in the outer coset, lifts to the apartment cover, and therefore fixes the deck class.  Full $PSp(4,3)$ does not preserve this frozen labelled base.  Consequently a $PSp$ orbit or $PSp$ submodule generated by this class inside that fixed $3241$-dimensional $H^1$ is not defined.  An equivariant module question requires a different, $PSp$-invariant base complex.

\paragraph{The selected-270 edge split is a homogeneous bundle over the Schlaefli 27.}
The $405$ Petersen shortcut edges form one $PSp(4,3)$ orbit with stabilizer $64$ and map $15$-to-$1$ onto the $27$ internal Schlaefli objects, whose stabilizer has order $960$.  The $1620$ base edges form one orbit with stabilizer $16$ and map $12$-to-$1$ onto the $135$ edges of $\operatorname{SRG}(27,10,1,5)$, whose edge stabilizer has order $192$.  Hence there are exact homogeneous quotient maps
\[
G/H_{64}\longrightarrow G/H_{960},\qquad
G/H_{16}\longrightarrow G/H_{192}.
\]
The $27$-vertex quotient module has rational spectral dimensions $1+20+6$.  Locally the order-$960$ component stabilizer acts on the ten Petersen vertices through $A_5$ with kernel $16$; its edge stabilizer image is $V_4$.  Under $PGSp(4,3)$ the component stabilizer doubles to $1920$, the local image becomes $S_5$, and the edge stabilizer image becomes $D_8$.  Thus the shortcut module is literally induced from the local Petersen-edge action:
\[
\mathbb Q[405]
=\operatorname{Ind}_{H_{960}}^{PSp}\mathbb Q[A_5/V_4]
=\mathbb Q[PSp/H_{64}],
\]
with the analogous $S_5/D_8$ statement for $PGSp$.

\paragraph{Mixed base/shortcut technologies have exact routing breakpoints.}
Write $c_b>0$ and $c_s>0$ for arbitrary scalar costs assigned to base and shortcut edges and set $r=c_s/c_b$.  Exact all-pairs two-objective path enumeration gives only four policies, separated by
\[
r=1,\qquad r=\frac32,\qquad r=2.
\]
The per-source $(\hbox{base traversals},\hbox{shortcut traversals})$ totals are
\[
(420,239),\ (548,111),\ (620,63),\ (746,0)
\]
in the four consecutive regions.  The same breakpoints apply to independent-edge reliability after $c=-\log q$.  A component-mixed sensitivity example places a four-stage degree-12 MZI fabric on base edges and a two-stage degree-3 EO fabric on the Petersen shortcuts.  Under those explicit assumptions the loss ratio lies in $1<r<3/2$ while the latency ratio lies in $r<1$, so the mixed and shortcut-favoring policies are the two Pareto choices.  These component numbers come from separate demonstrations and are not a measured integrated Holonet stack.

\paragraph{Two fail-closed outside-box tests.}
The $45$ weight-132 complement triples alone form nothing more than $45$ disjoint three-sets.  Their full hypergraph automorphism group is
\[
S_3\wr S_{45}=S_3^{45}:S_{45},
\]
so that shell alone cannot select the protected $PGSp$ action.  Adding the dual weight-three shell yields the $270$ packet triples and reduces the full automorphism group to $PGSp(4,3)$ of order $51840$.

Likewise, the nonzero apartment voltage does not directly sign either selected-270 routing-edge orbit.  Every one of its $4050$ labelled Schreier edges projects to a pair of selected lines at graph distance three.  The projection has $1275$ distinct distance-three pairs, with multiplicity profile $3^{1200}6^{75}$, and has zero intersection with both the $1620$ base edges and the $405$ Petersen shortcuts.  Any routing signing therefore requires an additional choice of a length-three path/transgression; no canonical hot/cold bias follows directly from the deck class.

\paragraph{Evidence boundary.}
These results certify finite code shells, incidence algebras, group actions, graph-cover scope, homogeneous permutation modules, and parameterized routing.  They do not identify particle generations, gauge fields, optical phases, or measured device performance.
