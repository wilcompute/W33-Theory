# Passes 7171–7186 — E8 association algebra, D4 atlas, H27 matter cover, and q=9 local exclusion

## Claim boundary

This packet contains exact finite computations and algebraic consequences.  It does **not** prove `alpha(W(3,9))=51`: the global target-48 residual-clique problem remains open.  The E8, D4, H27, voltage, coding, and lattice statements are finite mathematical identifications; they are not particle, continuum, gauge-field, or hardware claims.

Several ingredients predate this packet in the repo: the six-to-one E8-root/W33 fibration, the `[40,16,8]_2` W33 adjacency code, the 90 center-quads and their `90 -> 45 -> 27` quotient, the binary tritangent V20, the Steiner/K3,3 V24, and an abstract/local H27 Heisenberg model.  The new content is the objectwise assembly of those ingredients and the new exact constraints stated below.

## Pass 7171 — the complete colored E8 root association algebra

On all 240 roots, normalized inner product takes the five values `2,1,0,-1,-2` with valencies

`1, 56, 126, 56, 1`.

The positive-inner-product adjacency `A_+` has spectrum

`56^1 + 28^8 + 8^35 + (-2)^112 + (-4)^84`.

Because it has five distinct eigenspaces, `A_+` already generates the full five-dimensional Bose–Mesner algebra.  With relation order `I,A_+,A_0,A_-,A_antipode`, the simultaneous eigenmatrix is

```
[1, 56, 126,  56,  1]
[1, 28,   0, -28, -1]
[1,  8, -18,   8,  1]
[1, -2,   0,   2, -1]
[1, -4,   6,  -4,  1].
```

This completes the Pass7164 C6 Fourier picture: W33 plus the cyclic fibre phase determines every E8 root relation, not only the +1 graph.

## Pass 7172 / 7179 — the selected 90 D4s form a rank-five association scheme

There are 3150 D4 root subsystems in E8.  The W33-selected 90 form one structured subfamily.  Their four nontrivial relation degrees are

`1, 32, 32, 24`,

with eigenmatrix

```
[1,  1, 32, 32, 24]
[1, -1,  8, -8,  0]
[1,  1, -4, -4,  6]
[1,  1,  2,  2, -6]
[1, -1, -4,  4,  0]
```

and multiplicities `1,15,20,24,30`.

The unique valency-one relation is orthogonal complementation `D4 <-> D4^perp`.  The degree-32 relation “share one W33 fibre” has exactly 80 maximal 9-cliques: 40 point-stars and 40 neighborhood-stars.  Orthogonal complementation swaps the two families.  Quotienting those 80 stars by this involution reconstructs W33 itself.  Consequently

`Aut(D_90) = Aut(W33) x C2`,

of order `103680`.

The full Krein table is nonnegative, but no ordering of the four nontrivial primitive idempotents is Q-polynomial.

## Pass 7173 / 7180 — q=9 target 48 is excluded through local deletion radius eight

For canonical anchor type `(1,3,5)`, the known residual 47-clique is

`42 invertible + 5 rank-one`.

The exact maxima after deleting exactly `d=0,...,5` members of the 42-state invertible core, followed by arbitrary compatible additions from **all** remaining normalized states, are

`47, 46, 47, 46, 46, 46`.

A direct exact target-48 branch-and-bound additionally proves infeasibility for deletion radii 6, 7, and 8.  Therefore any hypothetical residual 48-clique in this anchor graph must delete at least nine members of the known 42-state invertible core; equivalently its overlap with that core is at most 33.

This is a large certified basin around the known 51-point W(3,9) witness, but it is local.  It is **not** a global 48-clique exclusion.

## Pass 7174 — the E8 Z12 phase and Pauli commutator phase are inequivalent

The Pass7168 E8 C6-fibre cocycle has complement-triangle holonomies only at odd residues

`1,3,9,11 mod 12`.

For the canonical two-qutrit Weyl/Pauli commutator cochain, every edge phase lies in `4 Z/12`, so every triangle holonomy lies in `{0,4,8}`.  Since adding a coboundary does not change triangle holonomy, the two cocycles cannot be cohomologous.

A subtle correction is important: the detailed 4-versus-8 histogram depends on the chosen projective Pauli representative section.  Only the subgroup/parity obstruction is promoted as canonical.

## Pass 7175 / 7181 — E6 x A2 and the corrected 27-fibre matter structure

Choose one E8 C6 fibre.  Its six roots form A2.  The W33 shell around its base point is `1+12+27`.

- the chosen fibre gives the 6 A2 roots;
- the 12 W33 neighbors contribute `12*6=72` roots orthogonal to A2, forming E6;
- the 27 W33 nonneighbors contribute 162 roots, splitting by A2 charge as `81+81`;
- either 81-sector projects to 27 E6 minuscule weights, each with three A2 charges, and the 27 projected weights give the Schläfli `srg(27,16,10,8)`.

Correction: the 27 nonneighbor C6 fibres are **not** one-to-one with the 27 minuscule weights.  Each fibre contains three opposite E6 weight pairs, and each weight class runs across three fibres.

Those 27 fibres themselves have a different canonical structure: they group into nine classes of three, and every pair of classes is joined by one perfect matching.  Thus their induced graph is a regular C3 voltage cover of K9 with triangle-holonomy histogram

`0^12 + 1^36 + 2^36`.

## Pass 7183 — the C3 voltage is the affine-area cocycle on AG(2,3)

After switching so one K9 spanning star has zero voltage, the 12 zero-holonomy K9 triangles are exactly the 12 affine lines of AG(2,3).

There are 48 affine-plane isomorphisms fixing the chosen origin.  Under all of them the voltage is, up to global sign,

`psi(u,v) = det(u,v) in F3`.

Therefore triangle holonomy is the oriented affine area

`det(v-u,w-u)`.

The class is nontrivial because 72 of the 84 K9 triangles have nonzero holonomy.

## Pass 7186 — the E8 matter-fibre graph is the repo's H27 Cayley graph

Choosing the sign convention already used by the repo's H27 model gives the nonabelian group law

`(u,z)*(v,w) = (u+v, z+w-det(u,v))`

on `F3^2 x F3`.  Its center has order three.  The E8-derived 27-vertex matter-fibre graph is exactly

`Cay(H27, {(u,0): u != 0})`.

Every ordered adjacency pair is verified from the Cayley law.  The graph has

- 27 vertices, degree 8, 108 edges;
- diameter 3 and distance spheres `1,8,16,2`;
- intersection array `{8,6,1;1,3,8}`;
- spectrum `8^1 + 2^12 + (-1)^8 + (-4)^6`.

The explicit semidirect action `H27 : GL(2,3)` has order `27*48=1296`.  Independent graph-automorphism enumeration also gives exactly 1296 automorphisms.  The vertex stabilizer of order 48 is transitive on every distance sphere, so the graph is distance-transitive.

This does not rediscover the repo's old local H27 model; rather, it identifies the new E8/A2-derived 27-fibre object **with** that old H27 model objectwise.

## Pass 7176 — each selected D4 contains its own three-frame triality packet

Every selected D4 lift contains 24 roots with positive-inner-product graph spectrum

`8^1 + 4^4 + 0^9 + (-2)^8 + (-4)^2`.

Its 12 antipodal root lines contain exactly three mutually orthogonal 4-frames.  Every such frame uses one antipodal pair from each of the D4's four E8 C6 fibres.  This gives a concrete three-frame triality realization inside each selected D4.

## Pass 7177 / 7182 — support codes and D4 lattice glue

The 90 four-point D4 base supports span the full even-weight binary code

`[40,39,2]_2`.

The 45 orthogonal-pair eight-point supports span

`[40,15,8]_2`.

For selected D4 pairs, the W33 relation determines the integral E8 lattice behavior:

- share one base point: span rank 6;
- disjoint cross-count 4: rank 8, index 1, hence generates E8;
- disjoint cross-count 7: rank 8, index 1;
- orthogonal complement: rank 8, index 4.

For every orthogonal pair the index-four discriminant glue is the diagonal `(Z2)^2`

`{(0,0),(v,v),(s,s),(c,c)}`.

There is no ordinary `D4^10` direct-sum glue code inside rank-eight E8.  The ten-D4 spreads are highly redundant atlases, not rank-40 decompositions.

## Pass 7178 / 7182 / 7184 — 27 ten-D4 spreads produce the missing 1+20 code

There are 27 selected ten-D4 spreads.  Each contains five orthogonal D4 pairs and partitions all 240 E8 roots.

On the 45 orthogonal-pair coordinates, the 27 spread incidence rows generate

`[45,21,5]_2`,

with exactly 27 minimum words.  Its dual is

`[45,24,6]_2`,

with exactly 120 minimum words; these are objectwise the 120 induced K3,3 / Steiner circuit supports already known in Pass5019.

The all-ones vector belongs to the 21-space.  Its even subcode has dimension 20.  After matching the intrinsic 45 coordinates to the canonical cubic-surface tritangent coordinates, the complete PSp intertwiner space from this even subcode to the previously certified binary tritangent-selector V20 is one-dimensional; its unique nonzero map has rank 20 and also intertwines the outer PGSp generator.

Hence, as full-group binary modules,

`[45,21,5] = 1 + V20_trit`,

and its orthogonal complement is the canonical Steiner `V24=[45,24,6]`.

## Pass 7185 — 1080 explicit unimodular E8 charts and a flat transition groupoid

Every selected-D4 pair in relation `(0,4)` supplies eight deterministic D4 simple roots whose lattice is E8 itself.  Expressed relative to a fixed E8 simple-root basis, the resulting 8x8 matrix lies in `GL(8,Z)`.

There are exactly 1080 such charts.  The 27 ten-D4 spreads partition them into 40 charts each, with multiplicity one.

Ordinary chart transitions are

`T(j<-i)=X_j X_i^{-1}`,

and therefore satisfy exact composition and telescope to the identity around every closed path.  This kills a tempting but incorrect interpretation: honest basis changes alone cannot produce nontrivial triality/GL8(Z) holonomy.  Such a holonomy would require an additional frame quotient or identification.

## Synthesis

Three previously separate repo threads now meet objectwise:

1. `E8 -> W33` six-root fibres give the complete E8 association algebra and the selected D4 subsystems.
2. A chosen A2 fibre turns the W33 `1+12+27` shell into `A2 + E6 + 81 + 81`; the 27 matter fibres form an AG(2,3) determinant-voltage cover and hence the H27 Cayley graph.
3. The 90 D4s produce 45 orthogonal pairs and 27 ten-D4 spreads; their binary incidence code is exactly `1+V20`, dual to the old Steiner V24.

The q=9 side remains deliberately separate: it gains a radius-eight exact local exclusion but no global optimality theorem.
