# Pass 10958 — exact Albert carrier for the missing mu4 clock modes

Producer: `analysis/w33_pass10958_exact_albert_mu4_missing_mode_intertwiner.py`

Certificates:

- `data/w33_pass10958_exact_albert_mu4_missing_mode_intertwiner.json`
- `data/w33_pass10958_mu4_intertwiner_matrix.json`

Regression: `tests/test_w33_pass10958_exact_albert_mu4_missing_mode_intertwiner.py`

## 1. The Albert half-turn is exact, rational, and order four

Pass 10956 produced exact rational matrices on the Peirce 16: the Spin(8) chirality involution (Chi), a rotation generator (R), and the normalizer half-turn

[
U=R/2.
]

The new exact calculation proves

[
R^2=-4I_{16},qquad U^2=-I_{16},qquad U^4=I_{16},
]

and

[
UChi=-Chi U.
]
So the Albert normalizer half-turn is not merely a numerical matrix exponential. It is an exact rational operator whose square is the same central sign identified in Pass 10950 as the (2pi) Spin(9) matter-parity action on the Peirce 16.

## 2. Its spectrum is exactly eight +i modes and eight -i modes

Over (mathbb Q(i)),

[
chi_U(x)=(x^2+1)^8.
]

The (+i) and (-i) eigenspaces both have dimension eight. Applying the exact chirality involution sends the (+i) eigenspace to the (-i) eigenspace because of the anticommutation relation.

Thus the half-turn eigenmodes are not chirality eigenmodes. Chirality pairs the two conjugate (mu_4) phases.

## 3. The missing Pass-10954 two-mode module embeds explicitly

Pass 10954 isolated the two missing clock characters

```text
k = 2,6  ->  +i,-i.
```

Define their two-dimensional generator

[
A_2=operatorname{diag}(i,-i)
]

and the chirality-exchange matrix

[
X_2=egin{pmatrix}0&1\\1&0end{pmatrix}.
]
The producer constructs an explicit rank-two matrix

[
W_2:mathbb Q(i)^2ightarrow mathbb Q(i)^{16}
]

such that

[
UW_2=W_2A_2,qquad
Chi W_2=W_2X_2.
]

So the missing two-character clock module is now an actual submodule of the executable Albert Peirce carrier.

## 4. The full Albert 16 is eight copies of that module

Pair an exact (+i) eigenbasis with its images under (Chi). This gives an invertible (16	imes16) change of basis (W) satisfying

[
UW=W,operatorname{diag}(A_2,ldots,A_2),
]

and

[
Chi W=W,operatorname{diag}(X_2,ldots,X_2),
]

with eight identical (2	imes2) blocks.

Therefore, as a (C_4) module over (mathbb Q(i)),

[
	ext{Albert Peirce }16
cong
8ig(chi_{+i}opluschi_{-i}ig).
]
This is the first carrier-level bridge in the Pass 10954-10958 sequence: the previously missing (pm i) clock sector is no longer only phase-matched or cocycle-matched to the Albert geometry.

## 5. The central square is objectwise the same relation

On the missing clock module,

[
A_2^2=-I_2.
]

On the Albert carrier,

[
U^2=-I_{16}.
]

Pass 10950 identifies (-I_{16}) with the central (2pi) Spin(9) matter-parity sign. Thus the cyclic-module relation itself is now shared objectwise:

```text
missing clock mode:   (+/- i)^2 = -1
Albert half-turn:     U^2       = -I16
matter-parity reading:           2pi Spin(9) central sign.
```

Pass 10957 had already shown that the same (pm i) phases are the (mu_4) square roots needed to convert the plus/minus (S_4) Schur cocycles. Pass 10958 upgrades that projective statement to an explicit carrier embedding for the cyclic clock subgroup.
## 6. What remains open

This does **not** yet give a representation of the full order-eight clock group inside Spin(9). The exact intertwiner is only for the (C_4) missing-mode quotient generated by (operatorname{diag}(i,-i)).

The next hard question is whether the other six clock Fourier characters and the nonabelian (S_4) quotient can be realized compatibly on the Albert carrier, or whether there is a provable obstruction forcing the clock to live in a larger module than the Peirce 16.

## Boundary

This is an exact cyclic-module theorem over (mathbb Q(i)). It does not identify the (pm i) eigenmodes with physical particles, observed chirality, or literal time states. It also does not embed all of (GL(2,3)) into Spin(9).
