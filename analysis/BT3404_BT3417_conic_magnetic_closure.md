# Passes 3404–3417 — Minimum-defect radius, null conic, magnetic search, and matrix-valued ternary torus

## Status

This packet executes the five requested continuation fronts and two deliberately high-risk constructions on a collision-free namespace after reconciling the reserved Passes 3390–3403 lane.

The exact source verifier is designed to certify the finite statements below. The magnetic search and literal 720-edge character decomposition are separate executed evidence targets. Until their generated JSON files are frozen, the live chromatic boundary remains

\[
\boxed{10\leq\chi(H)\leq11}.
\]

No finite quadratic form is promoted to physical spacetime, no quantum speedup is claimed, and no Clifford+T count is meaningful without an approximation tolerance.

## 3404 — coboundary-aware minimum-defect radius

The earlier sphere count proves

\[
D_{H^1}\geq389.
\]

The scalar cohomology has dimension 436 and is generated by the 720 minimum-support patterns. Therefore some 436 minimum-support patterns form a basis. Tensoring by the coefficient module \(\mathbb F_3^5\) does not multiply the word length: one arbitrary nonzero coefficient vector attached to one selected support is still one minimum defect.

Every switching class consequently has a representative using at most one labeled defect on each of the 436 basis supports:

\[
\boxed{389\leq D_{H^1}\leq436}.
\]

This improves the previous upper bound 480 by 44, exactly the scalar coboundary rank. The exact covering radius inside the interval remains open.

## 3405 — geometric classification of the null modes

The shifted hidden operator has Fourier eigenvalue

\[
3\bigl([k_1\ne0]+[k_2\ne0]-[k_3\ne0]\bigr).
\]

Over \(\mathbb F_3\), the nonzero indicator is \(x^2\). Hence the eight nonconstant null modes are exactly the nonzero solutions of

\[
\boxed{k_1^2+k_2^2-k_3^2=0}.
\]

Opposite vectors define the same point of \(PG(2,3)\), so the eight modes projectivize to four points. Exhaustive enumeration of \(PGL(3,3)\) gives a projective stabilizer of order 24 acting as every permutation of these four points:

\[
\boxed{\operatorname{Stab}(Q)\cong PGL(2,3)\cong S_4}.
\]

The earlier two four-mode “rulings” are therefore the two affine lift families of one nondegenerate projective conic, not two projective lines.

## 3406 BONKERS — the null-conic MDS code

Use the four affine representatives

\[
(1,0,1),\ (2,0,1),\ (0,1,1),\ (0,2,1)
\]

as columns of a ternary generator matrix. They span a code with parameters

\[
\boxed{[4,3,2]_3},
\]

weight enumerator

\[
1+12z^2+8z^3+6z^4,
\]

and dual generated by

\[
(1,1,2,2),
\]

which is a signed \([4,1,4]_3\) repetition code. Thus the finite light-cone directions themselves form a maximally distance-separable error-detection object.

This is a code attached to a finite Fourier null set. It is not a spacetime code or a physical causal structure.

## 3407 — profile-sensitive Hermitian chromatic search

`analysis/bt3407_magnetic_chromatic_search.py` constructs the live 45-block graph and exhausts all 5,280 graph triangles with all three omitted-edge patterns.

For each candidate it evaluates two edge-supported Hermitian families:

1. the ternary magnetic phase pattern \((\omega,\bar\omega,1)\);
2. the real signed pattern \((-1,-1,+1)\).

The search records extremal eigenvalues, generalized Hoffman ratio, traces, and eigenpair residuals. It intentionally searches the full graph-triangle set, which is broader than the selected 240 filled faces. A winning candidate must therefore be cross-walked to the filled-face orbit before it is called a cohomological minimum defect.

No numerical result is promoted in this report until the workflow artifact is frozen.

## 3408 — literal 720-edge representation atlas

The 720 minimum-support patterns are the 720 undirected edges of the \(\operatorname{SRG}(45,32,22,24)\). The existing literal action constructor builds the two-generator \(PSp(4,3)\) permutation action and sends it to GAP.

The Pass 3404–3417 evidence workflow executes that decomposition and records:

- all irreducible multiplicities;
- orbital rank;
- stabilizer subdegrees;
- degree and multiplicity-square consistency.

Until the generated JSON is present, only the exact permutation-module resolution

\[
0\to\mathbb F_3[240\text{ faces}]\to\mathbb F_3[720\text{ edges}]\to Z^1\to H^1\to0
\]

is promoted.

## 3409 — exact 108-state fibre factorization

Every ambiguous barycentre already determines a center in the fixed affine flat \(\mathbb F_3^3\). The same center map extends canonically to all barycentric fibres:

- fixed singleton: label \((0,0)\);
- distance-two change in the first paired coordinate block: \((1,0)\);
- distance-two change in the second paired block: \((0,1)\);
- ambiguous double fibre: \((1,1)\).

Every center carries exactly these four labels. Therefore

\[
\boxed{108\cong\mathbb F_3^3\times\mathbb F_2^2}
\]

as a set, with shell weights \((0,1,1,2)\) in each center fibre.

The exact verifier then tests that the barycentric adjacency depends only on the center displacement. If so, it is a \(4\times4\) matrix-valued Cayley operator over \(\mathbb F_3^3\), and three qutrit Fourier transforms reduce it to 27 four-dimensional symbols.

## 3410 BONKERS — the full 135-state walk as a five-channel ternary torus

The hidden antisymmetric sector is already a scalar signed Cayley operator on the same \(\mathbb F_3^3\). Combining it with the four barycentric channels gives

\[
\boxed{135=27\cdot5}.
\]

After symmetric/antisymmetric splitting, the entire 135-state operator becomes a five-channel matrix-valued operator on the ternary torus. Fourier transform reduces the spectrum to 27 internal blocks of size five.

This is a substantive compression, not cardinality numerology: the verifier reconstructs the integer translation kernel and checks that the 27 Fourier symbols reproduce the complete known spectrum

\[
10^1,\ 7^6,\ 4^{22},\ 1^{44},\ (-2)^{42},\ (-5)^{20}.
\]

## 3411 — mixed-radix Szegedy compilation contract

The torus form gives an exact logical organization:

- center register: three qutrits;
- internal register: five levels;
- binary fallback: eight state qubits;
- Hamming-neighbour label: four qubits for ten choices;
- walk step: two Szegedy reflections;
- spectral reduction: three qutrit Fourier transforms and 27 controlled internal symbols.

The state relabeling and operator decomposition are exact. Uniform preparation over ten neighbours and synthesis of arbitrary controlled five-dimensional symbols are approximation-dependent, so a gate-optimal Clifford+T total is intentionally refused until an error tolerance and synthesis model are supplied.

## 3412 — the 436 = 4 × 109 resonance falsifier

The identity

\[
436=4(108+1)
\]

is recorded because the same packet exposes four null-conic channels and 108 barycentric states. It is not promoted to a module isomorphism. The scalar cohomology is a \(PSp(4,3)\)-module, while the barycentric construction is organized by the centralizer of the affine involution \(\tau\). An explicit common-group intertwiner is required before the dimension identity has structural content.

## Reproduction

```bash
python analysis/bt3404_3417_conic_torus_closure.py --json /tmp/conic_torus.json
python analysis/bt3407_magnetic_chromatic_search.py --json /tmp/magnetic.json
python analysis/bt3381_minimum_defect_edge_character.py
python -m pytest -q tests/test_bt3404_3417_conic_torus_closure.py
```
