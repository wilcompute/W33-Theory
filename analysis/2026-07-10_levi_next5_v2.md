# Five deeper closures: formal ranks, sentinel admission, mod-8 lift, native runtime, and photonic compiler

**Status: PASS.** The executable witness is `analysis/w33_levi_next5_v2.py`; the machine-readable certificate is `data/PART_2026_07_10_LEVI_NEXT5_V2_results.json`.

## 1. Exact symbolic certificate for every odd prime power

Let `M_q` be the binary point-line incidence matrix of `W(3,q)`, with

\[
A_P=M_qM_q^T,\qquad A_L=M_q^TM_q,
\qquad D_q=\begin{pmatrix}0&M_q\\M_q^T&0\end{pmatrix}.
\]

The proof is now emitted as a dependency-checked symbolic certificate. Extending scalars to a splitting field for the odd additive translation group separates one trivial character block and `q-1` nontrivial blocks. On each nontrivial block,

\[
A_{P,\chi}:Y\mapsto Y+Y^T,
\]

whose image has basis `E_ij+E_ji`, `i<j`, and rank `q(q-1)/2`. The transformed incidence columns span `Sym_q`, with basis `E_ii` and `E_ij+E_ji`, so their rank is `q(q+1)/2`. Their line Gram form retains only diagonal coordinates in characteristic two and has rank `q`.

The trivial block is the affine-plane block, with ranks

\[
q^2+1,\qquad q^2+q+1,\qquad q+1.
\]

Summing the blocks gives

\[
\operatorname{rank}_2M_q=\frac{q(q+1)^2+2}{2},
\]

\[
\operatorname{rank}_2A_P=\frac{q(q^2+1)}2+1,
\qquad
\operatorname{rank}_2A_L=q^2+1.
\]

Solving the nilpotent rank ladder with `rank D^3=2` and `D^4=0` yields

\[
D_q\sim J_4^2\oplus
J_3^{(q^3+2q^2+q-4)/2}\oplus
J_1^{q(q-1)^2/2},
\]

with no `J_2` blocks. The JSON contains the proof DAG, all symbolic equalities, and a canonical SHA-256 digest.

## 2. Sentinel-aware authenticated packet admission

The Pass-159 dark-mode code is now a live admission invariant. The line-point readout kernel is the exact binary code

\[
\mathcal S=[40,15,8]_2,
\]

with weight enumerator

\[
1+45z^8+720z^{12}+6930z^{16}+17376z^{20}
+6930z^{24}+720z^{28}+45z^{32}+z^{40}.
\]

For every authenticated envelope, the admission layer reconstructs the expected native or mirror packet from immutable provenance and applies the 40 context checks to the payload displacement

\[
e=\text{received payload}-\text{expected payload}.
\]

If `0 < wt(e) < 8`, then `e` cannot lie in `S`, so at least one context syndrome fires. This is an exact distance theorem, not a sampled security claim. The executable stress layer additionally checked 448 authenticated errors of weights one through seven, and every one was rejected at the sentinel layer.

The two remaining ambiguity classes are also closed:

- a minimum dark-code displacement of weight eight is sentinel-invisible but is rejected by immutable transition provenance;
- an authenticated raw point/line retag has zero payload displacement but is rejected as type confusion.

Thus no sub-eight corruption, dark-code displacement, or raw type retag can simultaneously evade sentinel, homology, authentication, and provenance checks.

## 3. Exact two-adic lift of `1 + U14-`

For the integral chiral lattice `L_-4`, the complete 2-primary discriminant group is

\[
A_2(L_{-4})\cong(\mathbb Z/2)^{14}\oplus\mathbb Z/8.
\]

The chiral Gram matrix is entrywise even, so the natural map

\[
L_{-4}/2L_{-4}\longrightarrow A_2(L_{-4})[2],
\qquad x\longmapsto x/2,
\]

is an isomorphism. Let `h` generate the depth-three cyclic factor. Exact Smith-transform arithmetic gives

\[
q(h)=\frac{11}{8}\pmod{2\mathbb Z}.
\]

The unique fixed line in `L_-4/2L_-4` maps exactly to

\[
\langle4h\rangle,
\]

and `q(4h)=0`. Its complementary module is not guessed: direct enumeration of `h^perp` gives 8,127 nonzero isotropic vectors and 8,256 anisotropic vectors, exactly the minus-type orthogonal module

\[
h^\perp\cong U_{14}^-.
\]

The Brown invariants split as

\[
\operatorname{Br}(\langle h\rangle)=7,
\qquad
\operatorname{Br}(U_{14}^-)=4,
\qquad
7+4=3\pmod8,
\]

which reproduces the full Pass-162 two-adic phase. Therefore

\[
L_{-4}/2L_{-4}\cong\langle4h\rangle\oplus h^\perp
\cong\mathbf1\oplus U_{14}^-
\]

with an explicit integral/discriminant lift.

## 4. Native action on all 51,840 runtime states

Eight projective symplectic transvections generate a permutation group of order

\[
25920=|\PSp(4,3)|
\]

on the 40 points. The multiplier-minus-one diagonal similitude normalizes this group, lies outside it, and generates the full extension of order

\[
51840=|\\PSp(4,3):2|,
\]

the standard `W(E6)` extension.

The regular action on all 51,840 group elements is now explicit. Under the native `PSp(4,3)` subgroup it splits into two regular chirality sheets of size 25,920. Adding the outer similitude swaps the sheets and gives one regular orbit of size 51,840. Every runtime-state stabilizer is therefore trivial.

The internal object decomposition is also exact. A point stabilizer has order 648 and center order three; its quotient has order 216, the projective single-qutrit Clifford size. A noncollinear point-pair stabilizer has order 48 and orbit size 540. Consequently

\[
25920=40\cdot648=120\cdot216=540\cdot48,
\]

\[
51840=2\cdot40\cdot3\cdot216=2\cdot540\cdot48.
\]

Every runtime state receives a unique coordinate

\[
(\text{chirality},\ \text{projective point},\ C_3\text{ phase},\ \text{Clifford quotient class}),
\]

providing a genuine native replacement for the earlier product-coordinate surrogate. Generator permutations and SHA-256 digests are included in the certificate.

## 5. Photonic compilation of the integral Levi-to-E8 map

The exact `8 x 80` intertwiner has a much smaller hardware support than its ambient dimensions suggest. It uses only the eight time bins

```text
point[0], point[4], point[7], point[13],
line[0],  line[1],  line[4],  line[5].
```

The active `8 x 8` integer matrix has determinant `-1`, so these eight bins form a unimodular readout of the primitive control lattice. Eight is also minimal because the map has rank eight.

The active matrix is

\[
\begin{pmatrix}
-1&-1&0&2&1&0&1&-3\\
-1&-2&-1&4&2&-2&4&-6\\
-1&-3&-3&6&2&-2&5&-8\\
-1&-2&-2&4&1&-1&4&-6\\
-1&-1&-1&3&0&0&2&-4\\
-1&0&0&1&0&0&1&-2\\
0&-1&-1&2&0&0&0&0\\
-1&-1&-2&3&1&-1&2&-3
\end{pmatrix}.
\]

### Exact coherent compiler

After scaling by its largest singular value, the active map is embedded in a 16-mode Halmos unitary dilation. The certificate verifies unitarity to residual

\[
2.45\times10^{-15}.
\]

A fully materialized triangular Givens/Reck netlist contains 120 two-mode rotations and 16 output phases and reconstructs the dilation to residual

\[
1.56\times10^{-15}.
\]

An equivalent Clements layout uses the same 120 Mach-Zehnder elements with lower rectangular depth. The unreduced `80 -> 8` dilation would require 88 modes and 3,828 mesh elements, so the primitive eight-bin selector reduces mode count by `5.5x` and mesh count by `31.9x`.

### Sparse readout compiler

The support graph contains 51 weighted input-output edges. A binary fanout/fanin realization requires the exact minimum

\[
43+43=86
\]

tunable couplers and 31 pi-phase elements, with maximum support depth six. The integer-copy realization has row `L1` optima

```text
9, 22, 30, 21, 12, 5, 4, 14
```

and the certificate includes exact dual witnesses proving these optima; its balanced-tree depth ten is therefore optimal under binary copy/add primitives.

### Loss and dark-count execution

The engineering run uses measured anchors of `0.55 dB/cm` silicon-nitride propagation loss, `78%` detector efficiency, and `158 cps` dark rate, with explicitly labeled component-layout assumptions. For a 100 ps time bin:

- sparse path: depth 6, modeled loss 0.65 dB, detected-signal probability 0.6716;
- 16-mode coherent path: depth 16, modeled loss 1.70 dB, detected-signal probability 0.5273;
- unreduced 88-mode path: depth 88, modeled loss 9.26 dB, detected-signal probability 0.0925.

In ten million modeled sparse-network shots, 3,283,781 loss events became retries and four detector dark events were generated. The sentinel/provenance theorem admitted zero dark-count corruptions.

## Validation

The focused regression suite reports `8 passed`. External technical anchors used for engineering context are Chandler-Sin-Xiang on symplectic incidence modules, Bouchard et al. on programmable ultrafast time-bin circuits, Clements et al. on loss-robust universal interferometer meshes, low-loss silicon-nitride measurements, and high-speed SNSPD performance. All new mathematical claims are carried by the explicit executable certificates rather than attributed to those sources.
