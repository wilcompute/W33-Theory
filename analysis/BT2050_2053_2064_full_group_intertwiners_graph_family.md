# Passes 2050–2053 and 2064 — full-group schedules, explicit mixers, and the regular-spread family

The five fronts close with **77/77** frozen checks.

## 2050 — local schedule subgroups fuse globally

The 33 positive subgroup classes inside one four-line-pair stabilizer fuse into
14 PGSp(4,3)-conjugacy types.  The first deterministic exact-cover witness from
each local class gives 32 distinct frame sets in 12 full-group schedule orbits.
The subgroup fusion is complete; the first-witness orbit count is not a census of
all schedules.

## 2051 — quadratic connectivity is now explicit

Seven geometry-derived bilinear maps reach every rational signed-edge target
15, 24, 30 and 81.  Vertex-energy gradients give symmetric maps to 15 and 24.
Skew edge-matrix commutators give antisymmetric maps to 30 and 81.  Twisting by
the canonical complex structure produces chirality-odd symmetric maps to 30 and
81.  The maps have verified ranks, equal nonzero singular values, PSp
equivariance, chirality parity and mu6 phase response.

## 2052 — integrated executable reference architecture

The D8 witness expands from twelve subgroup orbits to sixty frames and covers all
240 edges once.  The same reference program implements the 36-lane rank-three
mixer and the 18-lane rook-double crossbar.  It is a software and bit-count model,
not synthesized hardware.

## 2053 — the spread graph is NO_6^-(2)

The literal four-line spread graph is the standard rank-three `NO_6^-(2)` graph,
equivalently `NO_5^{-perp}(3)`.  Its local graph is Kneser K(6,2), its second
subconstituent is Johnson J(6,3), and its full automorphism group has order
51,840.  This identification does not rely on the highly nonunique parameter set
(36,15,6,6).

## 2064 — a q=3,5,7 regular-spread rank-three family

Complete regular-symplectic-spread orbits have sizes 36, 300 and 1,176.  In all
three cases distinct spreads meet in exactly 1 or q+1 lines, and the q+1
relation is strongly regular with the frozen parameter formulas.  The resulting
all-odd-q formula is conjectural beyond the complete q=3,5,7 computations;
non-Desarguesian spreads remain outside the census.

## Boundaries

`chi(H)=9` remains open.  Explicit intertwiners are not physical coupling
constants.  The schedule prototype is not fabricated hardware.  The graph name
and standard rank-three framework retain their literature ownership.  The
uniform larger-q formulas are not promoted as an all-q theorem.
