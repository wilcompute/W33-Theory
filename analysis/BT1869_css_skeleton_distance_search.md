# BT1869 — Distance Search for the Global-Clock CSS Skeleton

BT1868 produced the first clean commuting CSS-compatible skeleton:

```text
payload symbols = 66
X checks = 44 face rows, rank 42
Z checks = 1 global all-distance clock-sum row
candidate k = 23
```

BT1869 tests whether that skeleton is protected.

## Result

The skeleton is not protected.  A weight-2 X-type logical defect exists:

```text
edge(0,1) + 2*edge(0,2)
```

It commutes with the global Z clock row because:

```text
1 + 2 = 0 mod 3
```

and it is not generated by the 44 face-row X stabilizer span in the exact low-weight search.

## Distance verdict

```text
X logical minimum weight = 2
quantum CSS distance upper bound = 2
```

So BT1868 is a valid commuting skeleton but not a protected memory.

## Interpretation

The failure is informative.  One global Z stabilizer is too weak to control local two-edge X defects.  A protected K12/F12 code needs one of:

```text
dual face checks
local Z checks
subsystem gauge pairing of distance contrasts
```

Boundary: this falsifies the current BT1868 skeleton as a protected code.  It does not falsify all gauge-extended K12/F12 subsystem constructions.
