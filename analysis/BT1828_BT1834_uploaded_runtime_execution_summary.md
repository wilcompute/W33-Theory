# BT1828-BT1834 Uploaded Runtime Execution Summary

## Uploaded bundle read

The uploaded artifacts establish two linked tracks.

1. E8 selector track: BT950 gives the rank-8 mod-2 E8 shadow; BT951 proves exact support minimum 60; BT953 classifies the support-60 certificate graph; BT954 selects minimizer 2 in the BT929 vertex metric gauge.
2. Runtime track: the W33 interrupt controller, defect-aware placement, defect walk, and packet-kernel JSONs confirm edge migration at 3 rays, constant 9-point page bill, 1023 relocation walk steps, and the 1600-input packet-kernel pass.

## BT1828

Added the replay output-path witness for `analysis/bt1824_executable_packet_replay.py`, anchored to the uploaded packet-kernel baseline: 1600 inputs, 46400 hops, 90 escalations, 15 relocations, average migration cost 3.

## BT1829

Added the generated-aperture CSV artifact note. The full 1440-row table remains generated by running `analysis/bt1825_aperture_shot_table_exporter.py`.

## BT1830

Added the compiled-controller telemetry trace schema. It extends the uploaded 1023-step walk trace with the BT1823 `compiled_phase` field.

## BT1831

Added a public ledger supplement with the runtime rows. The full ledger in-place update was blocked in this connector pass, so the rows are committed as a merge-ready supplement.

## BT1832

Added the static TeX build-check witness and summary for `holonet_machine.tex`. This checks the inserted runtime-stack result and basic delimiter/environment balance. It is not a full PDF build.

## BT1834

Added the E8-runtime selector bridge: BT954 metric winner 2 is the E8-side selector to attach to the compiled W33 aperture/runtime stack, while the tetracode quotient remains open.

## Honest boundary

Raw uploaded artifacts are represented by the BT1833 checksum manifest rather than all being copied byte-for-byte. The largest JSONL trace is recorded by SHA-256 and line count in the manifest.
