\ifdefined\WPassFiveZeroNineZeroLoaded\else
\gdef\WPassFiveZeroNineZeroLoaded{1}
\subsection*{Passes 5090--5097: derivative root sheets, $q=4$ defect rigidity, and the integral $\sqrt{17}$ order}
A collision-reconciliation against the concurrently executed Passes 5074--5081 leaves six genuinely new finite results plus two synthesis theorems.  At $q=4$, if every active opposite-pair chart is a minimum $1|4$ cut, fixing one selected apartment gives sixteen local seeds; exact propagation leaves eight solutions and eight contradictions, and the eight solutions are precisely the eight chamber stars through that apartment.  Hence any non-chamber weight-$256$ word must contain a heavier $2|3$ chart.

The old factor-$17$ bridge is stronger than rational similarity.  With
\[
 A=\begin{pmatrix}1&4\\1&0\end{pmatrix},\quad
 B_2=\begin{pmatrix}4&2\\2&5\end{pmatrix},\quad
 P=\begin{pmatrix}2&-1\\-1&1\end{pmatrix},
\]
one has $\det P=1$ and $AP=P(B_2-4I)$.  Thus the common integral lattice is the maximal order $\mathbb Z[(1+\sqrt{17})/2]$.  The $q=3$ recurrence generator $\mu=10+4\lambda$ spans the conductor-four order $\mathbb Z+4\mathcal O_K$, whose discriminant is $272=16\cdot17$.  This is an arithmetic order/lattice theorem, not a new projective-group intertwiner.

The raw point-local $V_{24}$ compiler that reaches the Smith denominator floor $780$ is an exhaustive strict one-swap local optimum for conditioning among denominator-$780$ raw bases: among $3289$ nonsingular one-apartment swaps, only eight remain at denominator $780$, and all seven alternatives are worse than the frozen basis.  A sampled two-swap search gives additional Pareto evidence but no global optimum theorem.

The formal derivative noticed in the apartment distance reduction has an exact discrete meaning.  A chamber lies on $q^4$ apartments.  Their active opposite-pair charts split into four incidence roles---point endpoint, point interior, line endpoint and line interior---and each role contains exactly $q^3$ charts.  Hence
\[
 q^3+q^3+q^3+q^3=4q^3=\frac{d}{dq}q^4.
\]
This is a finite coarea/codimension-one derivative, not infinitesimal calculus in the prime-power parameter $q$.  At $q=3$ a normal unipotent subgroup of order $81$ acts regularly on the $81$ chamber-star apartments and its local switches split into four root directions.

Those directions are not Cartesian.  At $q=3$ the six pair-generated subgroup orders are $9,9,9,9,27,81$; at $q=2$ they are $4,4,4,4,4,8$.  Correspondingly the chamber-star switch graph has $q^4$ vertices and degree $4(q-1)$, but is spectrally non-isomorphic to $H(4,q)$ for $q=2,3,4$.  The derivative carrier is therefore root-curved rather than a four-coordinate Hamming cube.

For a weight-$256$ q=4 word let $A_1$ and $A_2$ count $1|4$ and $2|3$ charts.  Writing $A_2=2t$, exact coarea gives
\[
 A_1=256-3t,\qquad A_{\rm active}=256-t,
\]
and the supported local Tanner-triangle count is $1024-8t$.  Pass5090 forces $t\ge1$ for every exotic minimum word, so the unresolved shell is naturally graded by a discrete curvature defect.

Finally, the coarea and Fourier distance attacks are exactly the same functional in two coordinate systems.  If $\mu$ is uniform on apartment boundaries and $N_A=q^4(q+1)^2(q^2+1)/8$, then
\[
 \widehat\mu(y)=1-\frac{1}{2N_A}\sum_O s_O(q+1-s_O).
\]
Thus minimizing local chart energy and maximizing nontrivial Fourier bias are equivalent formulations of the remaining all-$q$ distance theorem.
\fi
\input{analysis/PASS5098_5101_root_coset_supplement_insert}%
