# 2026-09-24 — class-field closure of the Pass-409 trialitarian cubic

## Status

**PASS — exact arithmetic bridge, with independent database cross-check.**

This note extends the Pass-409 cubic-Jacobi / trialitarian D4 frontier. The key improvement is qualitative: the S3 that permutes the three split A1 factors is not merely an abstract Galois-group match. Its order-three part is the Artin class-field quotient of a specific quadratic resolvent.

The result is arithmetic and representation-theoretic. It is **not** promoted to a Standard-Model, spacetime, or coupling derivation.

## 1. The cubic field is fundamental and squarefree

Let f(t) = t^3 - t^2 - 53 t - 120. Exact symbolic computation gives

    Disc(f) = 94557 = 3 * 43 * 733,

with all three prime exponents equal to one. Hence the cubic order generated by a root is maximal and the field discriminant is exactly 94557.

The polynomial has three real roots and is irreducible over Q, so K is totally real and non-Galois. Its normal closure has Galois group S3. The quadratic resolvent is F = Q(sqrt(94557)), with defining polynomial x^2 - x - 23639.
At the three ramified rational primes the cubic reduces as

    p=3:   t (t+1)^2
    p=43:  (t-6)^2 (t+11)
    p=733: (t-182)^2 (t+363).

Thus each inertia group in the S3 normal closure is a transposition. There is no hidden totally-ramified cubic prime.

## 2. The quadratic resolvent has class number six

The continued fraction is

    sqrt(94557) = [307; overline(1,1,204,1,1,614)],

and the first Pell solution of norm +1 is

    252151^2 - 94557 * 820^2 = 1.

Therefore a fundamental unit is epsilon = 252151 + 820 sqrt(94557).

The executable reconstructs the class number independently from the real quadratic class-number formula. Using the primitive quadratic character chi_D and the log-sine formula for L(1,chi_D), it obtains

    h(F) = 5.9999999999997...

hence the integer class number is exactly h(F)=6.

LMFDB independently records Cl(F) = C6 with GRH flag zero for this computation.
The exact database records are:

- 2.2.94557.1: F = Q(sqrt(94557)), class number 6, class group [6].
- 3.3.94557.1: the exact cubic f, class number 1.
- 6.6.845436619026693.1: the S3 normal closure.

The first two class-number records are unconditional. The normal-closure class number is irrelevant below.

## 3. Class-field triality theorem

Let N be the splitting field of f. Then

    F subset N,  [N:F]=3,  Gal(N/F)=C3.

Because the cubic discriminant is squarefree, the S3 inertia at every ramified prime is generated by a transposition. Its intersection with the normal subgroup A3=C3 is trivial. Therefore N/F is unramified at every finite prime. Both fields are totally real, so there is no infinite ramification either.

The independent discriminant-tower check is

    Disc(N) = 845436619026693 = 94557^3,

so

    Norm_F/Q(d_{N/F}) = Disc(N) / Disc(F)^3 = 1.

Since h(F)=6, the 3-primary part of the class group is exactly C3. There is therefore exactly one unramified cyclic cubic extension of F. Consequently

    N = H_3(F),

the Hilbert 3-class field of Q(sqrt(94557)).
## 4. Why the full group is S3, not C6

The ideal class group C6 is abelian; it must not be conflated with triality S3. The mechanism is

    1 -> C3 -> Gal(N/Q) -> C2 -> 1.

Quadratic conjugation sends an ideal a to its conjugate, and [a][conj(a)] is the principal norm ideal. Thus conjugation acts by inversion on Cl(F)[3]=C3. Therefore

    C3 : C2 = S3.

This gives the Pass-409 descent datum an arithmetic origin:

- C3: Artin reciprocity on the unique 3-primary ideal-class direction.
- C2: Galois conjugation of the quadratic resolvent.
- S3: the resulting nonabelian semidirect product.

Over the normal closure,

    Res_{K/Q} SL2 tensor_Q N  =  SL2^3.

The three factors are indexed by the three embeddings K -> N. The same S3 acts on those embeddings and therefore on the three outer nodes of the D4 triality diagram.

So the class-field S3 is not merely isomorphic to the triality group: it is the actual Galois descent action that permutes the three split A1 factors.
## 5. Frobenius triality decoder

For any rational prime p not dividing 94557, factor f modulo p. The factorization type is the Frobenius conjugacy class in S3:

    1+1+1  -> identity       -> chi_94557(p)=+1
    3      -> 3-cycle       -> chi_94557(p)=+1
    1+2    -> transposition -> chi_94557(p)=-1.

Class field theory refines the even branch. When chi_D(p)=+1, the prime splits in F, and its Artin symbol in Gal(H_3(F)/F)=C3 decides between identity and a three-cycle.

Thus the exact decoder is a quadratic sign bit plus a three-class Artin state, together determining the S3 Frobenius conjugacy class.

The factorization type cannot distinguish the two nontrivial elements of C3, because they are conjugate in S3. Choosing one as +1 and the other as -1 requires an orientation of the cubic class-field generator. That is a real gauge boundary, not missing computation.

The executable checks every unramified prime below 10^4:

    1226 primes, 0 parity mismatches,

with census

    203 identity, 394 three-cycles, 629 transpositions.

These proportions are already close to the Chebotarev targets 1/6, 1/3, 1/2.
Examples:

    p=2,5,13 -> transposition
    p=7,11,31 -> 3-cycle
    p=23,107 -> identity.

## 6. Why this is a useful TOE-side advance

This is not another equality of integers. It supplies a chain of natural maps:

    Cl(F)[3] --Artin--> Gal(N/F) -> Gal(N/Q)
              -> Emb_Q(K,N) -> SL2^3.

Every arrow has an established mathematical meaning. This upgrades the trialitarian label from a split-prime fingerprint to a global arithmetic descent mechanism.

What it does NOT do is turn the class group into a physical generation index, particle family, or qutrit register. Those readings require an additional physical map and are intentionally not asserted.

## 7. Literature anchors

- G. Henniart and L. Lomeli, Asai cube L-functions and the local Langlands conjecture, arXiv:1701.01516.
- M.-A. Knus and J.-P. Tignol, Triality and etale algebras, arXiv:0912.3405.
- R. Evans, F. Lemmermeyer, Z.-H. Sun, M. van Veen, Ring class fields and a result of Hasse, J. Number Theory 266 (2025), 33-61, arXiv:2403.04986.
- LMFDB records 2.2.94557.1, 3.3.94557.1, and 6.6.845436619026693.1, fetched 2026-09-24.

## 8. Artifacts

- analysis/w33_20260924_triality_class_field_closure.py
- data/w33_20260924_triality_class_field_closure.json
- analysis/w33_20260924_frobenius_triality_decoder.py
- data/w33_20260924_frobenius_triality_decoder.json
- tests/test_w33_20260924_triality_class_field_closure.py
