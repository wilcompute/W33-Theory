# BT819 — Beacon Heptads and the Clock Trichotomy

Physics of the universal computer/network, built on BT818's alpha = 7.

## The beacon mesh

The maximum classical-witness sets of the photonic substrate are
HEPTADS: seven states, pairwise overlap exactly 1/3 = 1/q — a complete
K7 mesh with uniform interference visibility.  This is the Csaszar
toroidal node (Phi6 = 7, K7 routing, diameter 1) realized as a
constellation of quantum states inside a single photon: a network
phase-reference frame in which every beacon pair interferes identically.

## Census and symmetry (exact)

```text
heptads:            2880          (new substrate invariant)
PSp(4,3)-orbits:    1             (transitive)
stabilizer order:   9 = q^2       (a 3-group; 7-free)
```

Since 7 does not divide 25920, no heptad carries an internal Z7: the
Csaszar clock can synchronize the beacon mesh only from OUTSIDE the
symplectic world.

## Frame physics

The mesh's frame operator has spectrum

```text
{ (5 - sqrt13)/2,  1,  1,  (5 + sqrt13)/2 }     (sum 7, det 3)
```

— equiangular at 1/3 but NOT tight (the (4,7)-ETF angle would be 1/8):
a LOOSE mesh whose impedance discriminant is sqrt(13).  Both external
clock numbers materialize in one object: 7 beacons, sqrt(13) spread.

## The Clock Trichotomy

```text
Z12  rectangle/D12 clock (BT746)   12 | 25920    INTERNAL
Z7   Csaszar/F42 clock  (BT803)     7 !| 25920   EXTERNAL
Z13  Singer/PG clock    (BT807)    13 !| 25920   EXTERNAL
```

The universal computer has exactly one internal gauge clock (k = 12,
running the selector/duo machinery) and two external synchronization
standards (Phi6 = 7 and Phi3 = 13 — the reptend pair, both of decimal
period 6 = q!, BT774).  Architecturally this is how real networks keep
time: a local oscillator plus external references that the local
symmetry group cannot generate.  Contextuality (BT818: no ovoid,
KS deficit >= 4 = mu contexts) is the computational fuel; the heptads
are the largest classically coherent islands; the spreads (36 schedules,
BT817) are the measurement timetables; and the three clocks partition
timekeeping into gauge time and reference time.

## Boundary

Open: the stabilizer-9 structure (Z9 or Z3xZ3? which 3-group);
2880 = 2 x 1440 = 2 x |Stab_Sp(spread)| - a heptad/spread duality?;
the (5 +- sqrt13)/2 spectrum vs the Singer orbits; and the heptad as a
physical synchronization protocol (7-beacon interferometry).
