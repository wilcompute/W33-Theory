# Passes 3163–3174 — proof-carrying photonic runtime

## Executive result

The prior factor engine becomes a complete proof-carrying execution contract:

```text
194-ISA exact growth census
  -> physically gated tri-ISA decoder
  -> 256-shard rank-three M36 intake
  -> phase-carrying two-edit epoch
  -> streamed 48,826-hypothesis Bayesian action
  -> one-bit non-Abelian curvature telemetry
  -> information-aware runtime selection
```

This packet was designed after rereading both canonical manuscripts.  It preserves the
Photonic Holonet's distinction between state, operator, address, route, selector clock,
mirror bus and temporal extension, while following the machine blueprint's rule that every
promoted figure is typed and every unbuilt component remains visible.

## 3163 — all 194 larger ISAs, not only eight representatives

Pass 3154 evaluated the eight locally non-dominated designs exactly.  The new engine divides
all 194 universal five/six-opcode subsets into 32 resumable shards.  Every record carries its
complete growth series, diameter, mean distance and reached order.  Aggregation fails unless
there are exactly 194 unique records and each reaches

\[
81\cdot 51{,}840=4{,}199{,}040.
\]

Only that aggregate may promote a global mean-distance optimum.  Source completion is not
reported as census completion.

## 3164 — the current four-opcode ISA is a fallback, not the optimum

For the frozen additive objective

\[
J_i(c)=\bar L_i(1+c p_i),
\]

`fast6` has both lower mean length and lower collision probability than `current4`.
Consequently their algebraic crossing is negative,

\[
c_{\rm current,fast}=-2.3597031587,
\]

so `fast6` strictly dominates `current4` for every nonnegative collision price.  The real
runtime competition is therefore

\[
\texttt{fast6}\quad\hbox{versus}\quad\texttt{low4},
\]

with exact crossing

\[
c=16.7016424928.
\]

The RTL implements all three maps separately.  Mathematical dominance does not authorize
promotion: `fast6` must also be calibrated, fit the target and pass timing.  `current4`
remains the fail-closed implementation.

## 3165–3166 — the M36 question becomes a complete sharded experiment

The canonical Pass-3125 engine is materialized byte-for-byte and hashed.  Its 256 logical
shards are scheduled as 32 workflow buckets of eight.  Each normalized shard records the
engine digest, detected CLI, elapsed time and every candidate object.  Aggregation refuses to
complete unless shard identities are exactly `0..255`.

Any aggregate candidates are then consumed by the independent projector/monotone pipeline:
rank three, commutation, trace-eight idempotence, nine single-error annihilation, nonzero
success, non-stabilizer output, complete Pauli spectrum, frame-explicit Wigner witness,
product-stabilizer fidelity lower bound, logical symplectic frame and accepted-error slopes.

No candidate observed before completion is a theorem, and no empty partial shard is a no-go; no input is not a no-go theorem.

## 3167–3168 — the epoch marker carries its own phase

The payload uses nine of the 24 omission-times-pilot symbols, leaving fifteen unused.  Select
twelve unused symbols

```text
1, 3, 4, 5, 6, 8, 9, 10, 12, 13, 14, 17
```

and encode phase `p` as five repeats of its assigned symbol.  Distinct markers have
Levenshtein distance five, and every marker has distance at least five from every length-five
payload word.  Thus their radius-two balls are pairwise disjoint.

The complete radius-two census contains

\[
12\times 24{,}845=298{,}140
\]

received traces and no collisions.  The decoder corrects any two substitutions, insertions
or deletions and recovers phase immediately:

\[
\boxed{\text{post-marker clean payload symbols required}=0.}
\]

Length five is optimal because a two-edit code requires minimum distance at least five.

## 3169–3170 — factor compression closes into a Bayesian action engine

The exact hypothesis universe is

\[
1+45\cdot7+\binom{45}{2}7^2=48{,}826.
\]

Each logweight is reconstructed from the baseline, at most two unary factors, and one of the
3,381 shared-triangle corrections.  The controller performs two streams:

1. find the maximum logweight;
2. accumulate the normalizer and all `23×8` predicted-outcome bins after max subtraction.

No 48,826-entry posterior RAM is required.  Including factor loading, entropy terms and
argmax, the exact schedule is

\[
528+48{,}826+48{,}826+184+23=98{,}387\text{ cycles}.
\]

The Q12/exponential-LUT reference preserved all 12 deterministic next actions, with maximum
posterior error `4.09e-7` and maximum action-score error `4.44e-5` bits.  At a stated 100 MHz
input the model gives 1,016.39 decisions/s; this is not placed timing.

## 3171 — outside-box result: belief curvature is one central bit

Write a nonidentity D4 label as `r^a s^b`.  Every commutator lies in the derived subgroup

\[
[D_4,D_4]=\{1,r^2\}.
\]

Because both elements are central, the commutator is conjugation invariant.  Therefore one
bit is a complete gauge-invariant syndrome for whether a shared-triangle interaction is flat
or curved.  Among 49 ordered nonidentity label pairs, 25 are flat and 24 curved; across all
3,381 sparse corrections this becomes

\[
1{,}725\text{ flat}+1{,}656\text{ curved}.
\]

This is a genuine finite gauge diagnostic for the posterior factor graph, not a physical
field-strength measurement.

## 3172 — outside-box result: the ISA is also a control channel

At a fixed frame, opcode collisions make the observed destination a lossy channel from the
opcode choice.  Uniform frame/opcode averaging gives:

| ISA | absolute bits/dispatch | ideal bits | normalized |
|---|---:|---:|---:|
| current4 | 1.8495219522 | 2 | 0.9247609761 |
| low4 | 1.9050775077 | 2 | 0.9525387539 |
| fast6 | 2.4105697281 | 2.5849625007 | 0.9325356663 |

`fast6` carries 0.5610477759 more absolute bits per dispatch than `current4`, a 30.3347%
gain.  The result also separates two objectives: `low4` has the best normalized efficiency,
while `fast6` carries the most total control information.

## Manuscript consequences

The Photonic Holonet insert places the new runtime after the predictive architecture without
identifying the epoch with the selector clock or the posterior with protected memory.  The
machine blueprint receives an engineering table, an erratum-style correction of the old
clean-post-marker assumption, and an updated “what is not built” entry for the still-pending
M36 census and physical tri-ISA promotion.

## Evidence ladder

- **Exact finite, locally observed:** phase code, D4 curvature, ISA channel statistics,
  tri-ISA algebra, hypothesis/factor map and stream schedule.
- **Exact for explicit numerical model:** Q12/LUT action regressions and additive runtime cost.
- **Source-complete:** 32-shard all-ISA gate, 256-shard M36 gate, RTL, tests, manuscript
  integrator and workflows.
- **Pending:** 194-record aggregate, 256-record M36 aggregate, accepted candidate,
  unrestricted stabilizer/Clifford gates, RTL simulation/synthesis/place, manuscripts,
  PDFs and laboratory evidence.
