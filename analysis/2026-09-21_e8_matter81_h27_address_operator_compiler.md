# E8 matter 81: Clifford-lift uniqueness and Qpsi commutant audit

The latest H27 results contain two copies of the same abstract extraspecial
group, but they do different jobs and cannot be silently identified.

The **regular Payne H27** acts simply transitively on the 27 cubic-surface
objects. It is an address group. The new **trinification H27** has commutator
equal to the scalar center of E6. It is an operator group. The first has a
fixed-point-free center on its 27 labels; the second has a scalar center that
fixes every basis label. That fixed-point census rules out a permutation-level
conjugacy between the two embeddings.

The broad separation theorem and its executable construction are owned by
`analysis/w33_address_operator_h27_roles.py`.  The compiler here independently
replays every shared observable against that landed certificate, then adds two
audits that it does not contain: uniqueness of the Clifford-gauge direction
lift and the exact intersection of the Qpsi clock with the execution
commutant.

## 1. The 270 cubics are ten coset instruction families

Write the regular Heisenberg group in normal form

\[
(a,b,c)=Z^aX^b\omega^c,\qquad ZX=\omega XZ.
\]

The newly materialized physical Clifford-648 dictionary removes a gauge
choice that was still hidden in the first version of this compiler. Its fixed
\(H_{27}:SL(2,3)\) complement has suborbits

\[
1,1,1,8,8,8
\]

on the 27 affine H27 addresses. Joining the two nonidentity central elements
to each eight-orbit gives three ten-neighbor Cayley graphs. Exactly one is
\(\operatorname{SRG}(27,10,1,5)\); the other two fail the
constant-common-neighbor test. The unique surviving orbit selects the five
order-three subgroups generated by

\[
\omega,\quad \omega X,\quad \omega Z,\quad ZX,\quad \omega^2ZX^2.
\]

Mapping this address incidence structure to the frozen 27-frame carrier is
still not canonical: after fixing the identity address at frame zero there are
exactly (1920=|W(D_5)|) incidence isomorphisms.  The executable compiler picks
the first only as deterministic bookkeeping.  This measured gauge ambiguity
is why no root-gauge equivariant intertwiner is inferred from the graph match.

have \(5\cdot9=45\) right cosets. Their collinearity graph is exactly

\[
\operatorname{SRG}(27,10,1,5)=GQ(2,4),
\]

and an explicit anchored graph isomorphism sends those cosets to the 45
tritangents on the 27 complete two-qutrit factorisation frames.

All 648 explicit affine permutations preserve this selected 45-line geometry.
Their line orbits have sizes

\[
45=9+36.
\]

If the external phase labels are held fixed, the induced scheduler action on
the lifted cubic instructions has the exact orbit decomposition

\[
270=27+27+216,
\]

with stabilizer orders \((24,24,3)\). The two small orbits are the two
combinatorial orientations above the nine-line orbit in this fixed gauge.
This census is an address permutation, not the complex action of a physical
Clifford gate on the external qutrit basis.

Now adjoin the external phase shift (C_3). Above each of the five base
directions, use the two nonzero slopes (+1) and (-1). This gives ten
order-three subgroups of

\[
K=H_{27}^{\mathrm{address}}\times C_3^{\mathrm{external\ shift}},
\qquad |K|=81.
\]

Each subgroup has 27 cosets, so there are (10\cdot27=270) lifted lines.
The verifier checks equality of sets, not only equality of counts:

\[
\boxed{\text{270 cosets in }K
=\text{270 zero-sum E8 matter-root cubic triples}.}
\]

Every coset projects to one base tritangent and uses the external phases
(0,1,2) once each. Every root lies on ten cosets, and every pair lies on at
most one. The certificate also gives all 81 frozen E8 roots explicit
(((a,b,c),p)) addresses.

This supplies a finite instruction geometry: ten instruction directions,
27 translated instances per direction, and ten incident instructions at each
of 81 addresses.

## 2. The center-correct H27 exposes an active two-qutrit unit

For the trinification branching

\[
27=(3,\bar3,1)+(1,3,\bar3)+(\bar3,1,3),
\]

use the center-correct generators

\[
X_{\rm int}=(X,X,I),\qquad Z_{\rm int}=(Z^2,Z,I).
\]

The verifier constructs an explicit chart ((m,q)), with (m=0,\ldots,8)
and (q\in\mathbf F_3), in which

\[
X_{\rm int}|m,q\rangle=|m,q+1\rangle,
\qquad
Z_{\rm int}|m,q\rangle=\omega^q|m,q\rangle.
\]

Thus the E6 27 restricts to nine copies of the three-dimensional
Schrodinger representation of H27. Tensoring with the external A2 qutrit
gives

\[
\boxed{\mathbf C^{81}\cong
\mathbf C^9_{\rm multiplicity}\otimes
\mathbf C^3_{\rm internal}\otimes
\mathbf C^3_{\rm external}.}
\]

The physical central product (H_{27}\circ H_{27}=3_+^{1+4}) therefore acts
as nine copies of its irreducible nine-dimensional two-qutrit representation.
The 81 projective displacement operators are Hilbert--Schmidt orthogonal, so

\[
\mathcal A_{\rm exec}=I_9\otimes M_9(\mathbf C),
\qquad
\mathcal A_{\rm exec}'=M_9(\mathbf C)\otimes I_9.
\]

Both algebras have dimension 81. The multiplicity factor is consequently an
exact noiseless subsystem for the restricted Pauli243 error/operator algebra.
That statement does not extend to arbitrary E8 interactions or laboratory
noise.

## 3. Only the FI Z3 part of the Qpsi clock commutes

On the E6 27, the exact Qpsi spectrum is

\[
27=1_{4}+10_{-2}+16_{1}.
\]

The external qutrit repeats these eigenspaces, so on the matter 81 their
dimensions are (3,30,48). Every eigenspace of a diagonalizable operator in
the commutant (M_9\otimes I_9) has dimension divisible by nine. Exhausting
all twelve powers of

\[
D_{12}=\zeta_{12}^{Q_\psi}
\]

leaves exactly

\[
k=0,4,8.
\]

All three are scalars. Hence

\[
\boxed{\langle D_{12}\rangle\cap\mathcal A_{\rm exec}'
=\langle D_{12}^{4}\rangle\cong C_3,}
\]

which is precisely the FI/common-H27 center. Matter parity (D_{12}^{6})
and the mod-four shadow (D_{12}^{3}) lie outside the certified execution
commutant.  The subsequent complete Steiner/trinification atlas certificate
also proves that neither shadow normalizes the execution algebra in any of
the 51,840 Weyl-compatible monomial charts.  Thus changing that finite chart
cannot rescue either operation.

## 4. Virtual-machine reading and exact open boundary

The resulting architecture has three distinct layers:

| layer | exact object | role |
|---|---|---|
| address/scheduler | (H_{27}^{\rm regular}\times C_3), order 81 | addresses roots; ten coset families encode the 270 cubic incidences; Clifford address orbits are (27,27,216) |
| execution | (3_+^{1+4}), order 243 | two-qutrit Pauli algebra on the internal and external qutrits |
| latent/control | (M_9\), the commutant | transformations of the nine-dimensional multiplicity subsystem |

The parallel linear-intertwiner audits now sharpen the remaining object.  On
the 27-dimensional carriers every H27-equivariant map from the regular
address module to the operator module has rank at most 9.  On the full
81-dimensional scheduler/execution carriers every
K=(H27 x C3)-equivariant map has rank at most 27.  Both bounds are sharp and
exclude an invertible equivariant basis conjugacy, even after automorphism
twists.  The frozen-root compiler must therefore be non-equivariant, covariant
only under a proper common subgroup, or formulated after replacing or
enlarging one of the actions.  It must record which symmetry it changes.

That compiler would select the physically realized chart and compile each
cubic coset instruction into an explicit operator on active and multiplicity
registers.  It is no longer needed for the monomial normalizer yes/no question:
the all-chart certificate closes that question uniformly and leaves only the
FI/common-H27 (C_3).  The phase-held-fixed Clifford scheduler census does not
say that Fourier or quadratic-phase gates permute the frozen root basis. The
result is finite incidence and restricted representation theory; it does not
identify generations, compute Yukawa coefficients, select a vacuum, or
establish hardware protection.

## External cross-checks

The component structures are established independently in the literature:

* Babu, Bajc and Susič analyze the regular
  (E_6\supset SU(3)\times SU(3)\times SU(3)) trinification stage in
  [*Trinification from E6 symmetry breaking*](https://arxiv.org/abs/2305.16398).
* Saniga, Green, Lévay, Pracna and Vrana record the 27-point/45-line
  (GQ(2,4)) carrier and its E6 role in
  [*The Veldkamp Space of GQ(2,4)*](https://arxiv.org/abs/0903.0715).
* Holbrook, Kribs and Laflamme give the operator-algebra basis for reading a
  commutant multiplicity factor as a noiseless subsystem in
  [*Noiseless subsystems and the structure of the commutant in quantum error
  correction*](https://arxiv.org/abs/quant-ph/0402056).

Those sources validate the ingredients and terminology. They do not establish
the repository's exact finite certificates. Internally, the landed
`w33_address_operator_h27_roles` packet owns the five-to-ten direction lift,
the \(9+36\) and \(27+27+216\) scheduler orbits, and the matter-81
factorization; older Passes 371 and 386 own the abstract Clifford naturality
and phase-aware orbital menu. The incremental content here is the exhaustive
three-candidate uniqueness audit in the fixed Clifford gauge and the exact
\(Q_\psi\)-commutant intersection.  The rank-9 and rank-27 compiler no-goes
are owned by `w33_address_operator_h27_intertwiner_obstruction.py` and
`w33_scheduler_operator_k81_intertwiner_obstruction.py`; they are integrated
here to prevent the older open-boundary wording from inviting an impossible
equivariant search.
