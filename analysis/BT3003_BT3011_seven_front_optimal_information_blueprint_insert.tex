\section{Passes 3003--3011: symmetry-reduced M36 and typed golden control}

The six non-CSS projectors found by the general-isotropic M36 pilot form one orbit of size $729$ under $\operatorname{Stab}(m)^3\rtimes S_3$, a group of order $34{,}992$. The $495$ RREF pivot patterns reduce to $98$ copy-permutation types. This classifies the pilot false leads but does not replace the pending $213{,}648{,}435$-subspace sweep.

A new set of $28$ triangles separates all $48{,}826$ no-, one-, and two-edge nonidentity $D_4$ fault hypotheses. Thus the previous $29$-triangle construction was not optimal. No $27$-triangle witness was found, but minimum optimality remains open.

Every skew isotropic line pair lies in exactly three spreads and has exactly four isotropic transversals. Therefore the $540$ skew pairs carry a natural $3\times4$ fiber and produce $6{,}480$ flags. The action of $PSp(4,3)$ is transitive on these flags. Nevertheless the two order-$12$ classes of $PGSp(4,3)$ have cycle profiles $6^{48}12^{516}$ and $6^4 12^{538}$; no projective automorphism realizes the controller's semiregular $12^{540}$ clock.

The abelian syndrome shell is implemented as $A_4=V_4\rtimes C_3$, with the order-three action supplied by Fibonacci reduction modulo two. Synthesizable RTL places the $D_4$ core behind a separate authorization-gated write block. An $89/233$ Christoffel accumulator supplies a finite hardware approximation to the golden Sturmian schedule: $89$ nonadjacent expensive events per $233$ ticks.

Crossing that scheduler with the $12$-phase clock gives a $2796$-tick calendar. Every phase receives exactly $89$ events, the global gap alphabet is $\{2,3\}$, and time reversal is a cyclic shift by $89$ scheduler positions.

Finally, component-resolved survival, OAM, slot, and dark-count statistics feed a Bayesian finite-horizon chirality receiver. Under the repository's synthetic reference stack, copy cost $10^{-3}$ gives modeled success $0.996589$ at $7.710$ expected copies. These values are synthetic policy diagnostics, not laboratory performance.

The four $V_4$ syndrome labels also admit the tetrahedral qubit SIC geometry with orientation-preserving group $A_4$. This is proposed only as a compact shell readout; it is not an encoding of the protected nonabelian route core.
