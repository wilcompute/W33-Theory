# Pass 10953 — the minimal doubled Pin clock and its trace-Choi visibility

Producer: `analysis/w33_pass10953_minimal_doubled_pin_clock_visibility.py`

Certificate: `data/w33_pass10953_minimal_doubled_pin_clock_visibility.json`

Regression: `tests/test_w33_pass10953_minimal_doubled_pin_clock_visibility.py`

## Exact theorem

Pass 10952 showed two facts that initially looked like alternatives:

- on a bare qutrit, odd powers of the (Z_8) clock are not completely positive;
- on (mathcal H_3\oplus\overline{mathcal H}_3), the same tick has a unitary (6\times6) lift.

This pass proves that the doubled carrier is **minimal**, identifies its extra (C_2) degree of freedom exactly, and turns the construction into an interferometric prediction.
## Why six dimensions are forced

Let (X,Z) be the qutrit Weyl generators. The faithful Schrödinger representation has central commutator

```text
Z X Z^-1 X^-1 = omega I.
```

Its conjugate representation has central character (omega^{-1}). On the doubled carrier define

```text
R(X) = diag(X, X*),
R(Z) = diag(Z, Z*).
```

The Pass-10952 unitary tick

```text
D = [[0,U_S],[U_S^*,0]]
```

implements the determinant-odd clock automorphism and exactly inverts the center,

```text
D C D^dagger = C^-1.
```

Therefore any complex-linear implementer of this automorphism maps the (omega)-eigenspace of (C) bijectively onto its (omega^{-1})-eigenspace.
Retaining one faithful qutrit sector already requires a three-dimensional (omega)-eigenspace. The conjugate eigenspace must have the same dimension. Hence

```text
dim >= 3 + 3 = 6.
```

The explicit (D) attains the bound. The doubling is not padding; it is the minimum complex-linear carrier of the determinant-odd qutrit clock.

## The determinant bit becomes a literal sector bit

Let

```text
Gamma = diag(I3,-I3).
```

Then

```text
Gamma D Gamma = -D,
[Gamma,R(X)] = [Gamma,R(Z)] = 0.
```

Consequently

```text
Gamma D^n Gamma = (-1)^n D^n.
```

Odd clock powers are exactly sector-swapping; even powers are exactly sector-preserving. The abstract determinant character of (GL(2,3)) has become an explicit (C_2) orientation/conjugate-sector register.
## Exact spectrum and polynomial

The unitary (D) has the six eighth-root eigenvalues

```text
zeta8^k,  k in {0,1,3,4,5,7}.
```

The missing exponents are (2,6), i.e. the pair ({+i,-i}). Hence its minimal polynomial is

```text
(x^8-1)/(x^2+1) = x^6-x^4+x^2-1.
```

The producer verifies the matrix polynomial to machine precision.

Its exact eight-step character is

```text
Tr(D^n) = [6,0,2,0,-2,0,2,0].
```

Thus the normalized trace visibility is

```text
|Tr(D^n)|/6 = [1,0,1/3,0,1/3,0,1/3,0].
```
The zero on every odd tick has a structural explanation: an odd tick is block off-diagonal and swaps orthogonal sectors, so its trace vanishes. This is independent of the Choi non-CP statement on the bare qutrit.

## The physical qutrit C4 is in the Fourier class

The bare-qutrit physical clock uses the even subgroup generated by

```text
V = U_S U_S^*.
```

Its symplectic matrix is (g^2), an order-four element of (SL(2,3)). Exhaustion finds exactly four symplectic conjugators carrying (g^2) to the qutrit Fourier matrix. In the actual Clifford lift a canonical conjugator gives

```text
H V H^dagger = phase * X F3
```

for the frozen gauge used by the producer: a Pauli-displaced Fourier gate.

The qutrit trace sequence over the physical (C_4) is

```text
Tr(V^n) = [3,1,-1,1]
```

up to numerical phase convention, so
the existing qutrit Bell-overlap calibration predicts

```text
|Tr(V^n)|/3 = [1,1/3,1/3,1/3].
```

This is exactly the (1/3) intermediate visibility already used by the repo's BT820 temporal self-entanglement protocol for the qutrit Fourier calibration. The new content is that the freshly derived clock lands in that calibrated visibility class objectwise.

## Direct Bell-overlap check

For the doubled carrier the producer also constructs the normalized six-dimensional maximally entangled vector and verifies directly for all eight powers

```text
<Omega6 | (I tensor D^n) | Omega6> = Tr(D^n)/6.
```

So a hypothetical sector-resolved six-dimensional Choi interferometer would see

```text
1, 0, 1/3, 0, 1/3, 0, 1/3, 0.
```

No such six-dimensional hardware is claimed to exist in the current blueprint. The existing Bell-qutrit interferometer can test the even (C_4) sequence without that extension.
## Boundary

The (C_2) sector is a **conjugate-representation/orientation register** forced by complex-linear realization of the determinant-odd automorphism. It is not identified here with literal past/future, particle/antiparticle, physical chirality, a thermodynamic arrow, or spacetime time orientation.

The trace-Choi visibility formula is standard Bell-state channel overlap and is already executable in this repository. The novelty here is the exact clock spectrum, minimality proof, sector grading, and objectwise Fourier-class weld.
