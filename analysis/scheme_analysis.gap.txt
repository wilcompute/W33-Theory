## Selector Design: GAP Association Scheme Analysis
# Use this in GAP to find the intersection matrices and eigenvalues.

# The design has 120 blocks of size 108.
# Intersection sizes: [108, 54, 12, 4, 2]
# Frequencies (valencies): [1, 2, 36, 27, 54] (Total 120)

# Let R0, R1, R2, R3, R4 be the relations for overlaps 108, 54, 12, 4, 2.
# R0 = Identity (valency 1)
# R1 = 54-overlap (valency 2)
# R2 = 12-overlap (valency 36)
# R3 = 4-overlap (valency 27)
# R4 = 2-overlap (valency 54)

# Load the association scheme into GAP
LoadPackage("grape");
LoadPackage("design");

# Since the design is transitive, we can use the intersection matrices P_i where (P_i)_jk = number of blocks z such that (x,z) in Rj and (z,y) in Ri.

# The valencies sum to 120. 1 + 2 + 36 + 27 + 54 = 120.
# The ranks and eigenvalues will tell us if this corresponds to a known geometric structure (like a W33 derivative).
