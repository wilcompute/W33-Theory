# Passes 651–655 — closed 2-adic tree, integral descent defects, gauge-complete optics, correlated dropout, and policy stability

## Pass 651 — Closed-form 2-adic solution tree

The scalar deformation equation

\[
S^2=4S\pmod{2^n}
\]

now has a complete closed form. For every \(n\ge5\),

\[
\boxed{S=a2^{n-2}+4\varepsilon},\qquad a\in\{0,1,2,3\},\quad \varepsilon\in\{0,1\}.
\]

Hence every level has exactly eight scalar points. Only \(S=0\) and \(S=4\) form compatible \(2\)-adic branches. The other six points are finite-level phantoms: four have no child, two have children, and every such child dies at the next reduction. Thus every non-character persistence bar has length at most one edge.

## Pass 652 — Integral \(D_8\) descent-defect signature

For the seven-dimensional arithmetic relation space \(V\), define

\[
\delta(g)=\dim(V+gV)-7.
\]

The exact defect histogram on the unmarked-frame stabilizer \(D_8\) is

\[
\boxed{0^1,\;1^2,\;2^1,\;3^4}.
\]

The edge-axis reflections have defect one, the half-turn has defect two, and quarter-turns plus vertex-axis reflections have defect three. The ranks agree over \(\mathbb Q\) and modulo \(2,3,5,7,11,13\), proving that the obstruction is integral. The relation-space stabilizer in \(D_8\) is exactly the identity.

## Pass 653 — Gauge-complete 16-mode optical tomography

The existing 256 input probes do reconstruct all sixteen row effects

\[
E_k=U^*|k\rangle\langle k|U,
\]

but intensity-only data determine \(U\) only modulo independent output phases \(U(1)^{16}\). That gauge is unavoidable: arbitrary left diagonal rephasings preserve every output intensity.

A single generic coherent input and two interference quadratures between output 0 and each of the remaining fifteen outputs fix the fifteen relative phases. Therefore

\[
\boxed{256+2\cdot15=286}
\]

configurations reconstruct the full \(16\times16\) coherent transfer matrix up to one global phase. The deterministic reconstruction error is below \(10^{-14}\).

## Pass 654 — Correlated-dropout matrix e-process

Channel independence is no longer required when predictable pair-inclusion propensities are known. The unbiased increment is

\[
Y_{ii}=\frac{O_i z_i^2}{\pi_i},\qquad
Y_{ij}=\frac{O_iO_jz_iz_j}{\pi_{ij}}\quad(i\ne j).
\]

For common-gate dropout with probability \(g\), the independence formula is biased off diagonal by exactly \(1/g\). In the deterministic replay, \(g=0.9\), so the misspecified inflation is \(10/9\), while the pairwise estimator detects the covariance change, contains the true covariance in its PSD upper model, and retains a valid whitener and positive selector separation.

## Pass 655 — Minimax policy stability and order degeneracy

The unordered optimal action set is unique, but the ordered policy is not. Both tagged traces have root value 12:

\[
\boxed{Q(\operatorname{Tr}(U)_{\rm guard})=Q(\operatorname{Tr}(U^2)_{\rm cov})=12}.
\]

Either can be executed first. The next-best root value is 13, giving a one-block optimality margin. The two-action policy remains optimal for science quotas 7 through 10 and across the integer cost box

\[
c_1\in\{4,5,6\},\qquad c_2\in\{5,6,7\}.
\]

The covariance-tagged cost \(c_2=7\) is a sharp boundary: raising it to 8 changes the optimal policy and raises the minimax value to 13.

## Verification and boundaries

The five deterministic ledgers are regenerated by their scripts and checked byte-for-byte. The focused regression verifies every certificate and the principal invariants above.

- Pass 651 classifies scalar points in the fixed commutant order, not arbitrary module deformations.
- Pass 652 is the defect signature of the present seven arithmetic relations.
- Pass 653 requires coherent output interference and a nonvanishing reference probe.
- Pass 654 requires valid predictable pair propensities and matrix tail/variance envelopes.
- Pass 655 is exact only for the declared finite scenario, outcome, science, and cost model.
