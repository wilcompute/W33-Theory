# BT1570-BT1572 Internal Clifford, Lens Calibration, and Protocol Packet

## BT1570

The internal Clifford action generated by I, X, Z, F3, and S has 216 elements. All preserve the state/operator Choi split by construction. Only identity stabilizes the identity Choi state itself. The 24 zero-translation frame changes preserve centered OAM opposition, while the 192 translated elements move the OAM origin and require recentering.

## BT1571

The S/lens calibration becomes exact at the finite phase-signature level. On the centered OAM basis ell=-1,0,+1 with qutrit labels 2,0,1, qutrit S has omega-exponent signature [1,0,1]. A centered quadratic lens phase omega^(ell^2) has the same signature.

## BT1572

The passive-vs-active experimental protocol is specified with centered OAM preparation, axial time-bin support, operator off/on controls, I/X/Z/F3/S settings, leakage checks, basis covariance, and external-reference comparison.

## Current synthesis

The operator-on-photon model now has an orbit census, an exact finite S/lens phase calibration, and a concrete lab protocol distinguishing passive labels from active internal operators.
