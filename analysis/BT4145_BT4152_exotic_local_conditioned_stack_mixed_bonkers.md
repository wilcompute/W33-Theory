# BT4145–BT4152 — exotic anomaly repair, local relational gates, conditioned seven-fault sensing, hybrid-stack materialization, mixed equilibria, and three outside-box probes

## Status

`PASS_EXACT_BOUNDED_EXOTIC_OPTIMUM_LOCAL_RELATIONAL_LIFT_CONDITIONED_T7_STACK_LEDGER_NEWTON_MIXED_DEMON_SUSY_ECHO`

This packet executes the five implementation targets left open by Passes 4129–4136 and adds three independent finite constructions. All promoted claims are regenerated by `analysis/w33_pass4145_4152_exotic_local_conditioned_stack_mixed_bonkers.py`.

## Pass 4145 — bounded exotic anomaly optimization

The explicit 145-state carrier has scaled anomaly vector
\[
(-28,-28,-69,-222,-3594)
\]
in the coordinates
\[
\left(A_{SU(3)^3},\,12A_{SU(3)^2Y},\,12A_{SU(2)^2Y},\,6A_{\mathrm{grav}^2Y},\,216A_{Y^3}\right).
\]

A mixed-integer search was run over all 72 species
\[
R_3\in\{1,3,\bar3\},\qquad \dim R_2\in\{1,2\},\qquad Y=n/6,\quad 0<|n|\le6,
\]
with exact cancellation of all five local anomalies and odd added weak-doublet parity so that the original Witten anomaly is repaired. The primary objective minimizes one-particle dimension; the secondary objective minimizes the number of multiplets at the dimension optimum.

The exact optimum adds 107 states in 30 multiplets, producing a 252-state anomaly-free signed spectrum inside this bounded family. One optimal spectrum is

- \(1(1,1)_1\);
- \(11(1,2)_1\);
- \(1(3,1)_{1/3}\);
- \(4(3,1)_{1/2}\);
- \(3(3,1)_{2/3}\);
- \(2(3,2)_{-1}\);
- \(1(3,2)_{-2/3}\);
- \(4(3,2)_{1/3}\);
- \(3(3,2)_{1/2}\).

It adds 21 weak doublets, flipping the global \(SU(2)\) parity. Relative to the 261-state standard-charge optimum, bounded exotic hypercharges save nine states.

**Boundary.** This is an exact optimum only in the stated bounded fundamental/singlet family. It does not establish a phenomenological spectrum, asymptotic freedom, masses, decoupling, or a globally unrestricted representation optimum.

## Pass 4146 — exchange-local relational-gate lift

The compressed 32-mode relational code pairs occupation words separated by Hamming distance 32. Therefore any one-shot operator supported on fewer than 32 occupation modes has zero direct matrix element between paired clock words. This is an exact locality obstruction to the compressed complement swap.

A local lift uses

- 28 dual-rail payload qubits: 56 modes and 28 particles;
- one system clock mode \(q\);
- one reservoir mode \(r\);
- exactly one particle in the \(q,r\) pair.

The lifted code has 58 modes, 29 particles, one clock qubit, 28 payload qubits, and dimension \(2^{29}\). The phase reference sees 57 system modes. Its two clock branches have system weights 28 and 29, so the reference noise remains the one-use \(z_1\) channel on the clock and the identity on all payload qubits.

The exact bounded-support generating Hamiltonians are

\[
X_j=a_j^\dagger b_j+b_j^\dagger a_j,\qquad
Z_j=n_{a_j}-n_{b_j},
\]
\[
H_j=(X_j+Z_j)/\sqrt2,
\]
\[
H_{\mathrm{CNOT}(i\to j)}
=n_{b_i}(a_j^\dagger b_j+b_j^\dagger a_j),
\]
plus the analogous exchange \(q^\dagger r+r^\dagger q\) for clock \(X\). The largest primitive support is three modes. \(H,S,\mathrm{CNOT}\) generate the payload Clifford group, while clock-controlled payload gates use \(n_qH_{\rm payload}\).

A 28-qubit GHZ encoder uses one Hadamard and 27 CNOTs in a five-layer binary tree: 28 primitives and depth six. If every primitive has diamond error at most \(\epsilon\), a \(G\)-primitive word has implementation error at most \(G\epsilon\), plus the exposed clock dephasing. At \(K=256\), the latter is \(3.6819664457\times10^{-5}\).

**Boundary.** The compilation is exact on the ideal code. Hardware geometry, pulse calibration, leakage under imperfect blockade, and fault tolerance remain open.

## Pass 4147 — conditioned seven-fault measurements

The prior moment rows \(e^3,e^5\) reached \(160^5=104{,}857{,}600{,}000\). Two new deterministic rows are generated over the prime field \(\mathbb F_{24001}\) and centered as integers. Their maximum coefficient magnitude is only 11,992.

The verifier exhaustively checks

- 1,620 eight-cycles;
- 5,184 ten-cycles;
- 43,200 twelve-cycles;
- 336,960 fourteen-cycles;
- 133,920 rank-two theta cores.

Every one-dimensional cycle response is nonzero modulo 24,001, and every theta response matrix has nonzero determinant modulo 24,001. Hence the same statements hold over the integers. Since every incidence nullspace supported on at most 14 edges has dimension at most two, the stacked measurement has spark at least 15 and uniquely identifies every seven-sparse error in the noiseless model.

Quantitative conditioning data:

\[
\max |w_e|=11{,}992,
\]
\[
\text{dynamic-range reduction}=8.7439626\times10^6,
\]
\[
\min_{\rm cycles}\frac{\|(w_1\cdot c,w_2\cdot c)\|_2}{\|c\|_2}
=2.8410259716,
\]
\[
\min_{\rm theta}\sigma_{\min}^{\rm generalized}
=0.08869240414.
\]

**Boundary.** Exact injectivity and incidence-nullspace conditioning are certified. End-to-end noisy restricted singular values, ADC resolution, calibration drift, and physical sensor realization remain open.

## Pass 4148 — hybrid stack materialization

The four point-to-line permutations are partitioned into increasing, noncrossing layers of depths
\[
8,\ 8,\ 9,\ 8.
\]
The exact ledgers contain all 160 point-line routes and all 33 planar layers. No same-plane crossing remains.

The shared active island requires six selector/recombiner MZIs and 24 common branch phase plates: 30 active controls. Ten interlayer transitions are occupied over five signal uses.

The missing storage cost is now explicit. Equalizing nine temporal slots requires eight delay units per route, or 1,280 route-slot units over 160 routes. With slot width \(\tau\) and group index \(n_g\),
\[
L_{\max}=8c\tau/n_g,\qquad
L_{\rm aggregate}=160(8c\tau/n_g).
\]
At \(n_g=2\) and \(\tau=5\) ps, this is 6 mm maximum delay per path and 0.96 m aggregate routed delay. At 0.5 dB/cm, the added five-use delay loss is 1.5 dB, taking the provisional internal budget from 9.12 to 10.62 dB. Keeping this added loss below 1 dB requires
\[
\tau<3.34\ {\rm ps}.
\]

**Verdict.** The zero-crossing stack and control ledger are materialized, but ordinary static-delay serialization is not a practical low-loss closure unless ultrafast slots are available. Resonant storage, frequency-bin serialization, or additional physical routing planes are required.

## Pass 4149 — Newton-refined mixed-equilibrium catalogue

Finite-time endpoints were refined with full 80-variable Newton solves and canonicalized under all 25,920 projective symplectic permutations and simultaneous global field inversion.

For the nominal 24-sector selector:

- 64 deterministic seeds;
- 42 pure seeds and 22 mixed seeds;
- 18 distinct Newton-refined mixed roots;
- all 18 are linearly stable.

For the nominal 15-sector selector:

- an enlarged 200-seed deterministic corpus;
- 160 pure seeds and 40 mixed seeds;
- five distinct Newton-refined mixed roots;
- four linearly stable roots;
- one weak index-one saddle.

This corrects the earlier finite-time rounded count of eight selector-15 mixed classes: after Newton refinement, those endpoints collapse to five actual roots.

The root-stabilizer equitable quotient
\[
A_Q=\begin{pmatrix}
0&12&0\\
1&2&9\\
0&4&8
\end{pmatrix}
\]
has one nonzero mixed solution for each selector. Lifted to the full graph, these are saddles of Morse indices 23 and 14. Thus the most symmetric mixed branches are not hidden attractors.

**Boundary.** The deterministic corpora and the complete root-stabilizer quotient are certified. No claim is made that every full-space mixed equilibrium has been found.

## Pass 4150 — bonkers: an exact W33 Maxwell demon

Every maximum cut divides W33 into 20 low and 20 high vertices. The cut graph is 8-regular bipartite and has exactly
\[
56{,}260{,}624{,}960
\]
perfect matchings.

Give high vertices energy \(\Delta\) and low vertices energy zero. At inverse temperature \(\beta\), the high probability is
\[
p=\frac1{1+e^{\beta\Delta}}.
\]
A one-bit demon measures the high/low class and, if high, uses any perfect matching to lower the particle and extract \(p\Delta\) average work. Its memory entropy is
\[
h(p)=-p\ln p-(1-p)\ln(1-p).
\]
Landauer erasure then gives
\[
p\Delta-kT h(p)
=-kT\ln(1+e^{-\beta\Delta})<0.
\]
Reversible restoration of the particle can at best recover the missing free energy and bring the full closed-cycle work to zero.

## Pass 4151 — bonkers: signed-incidence supersymmetry

For the oriented Levi incidence matrix \(D\in\mathbb R^{80\times160}\), define
\[
Q=\begin{pmatrix}0&D\\D^T&0\end{pmatrix}.
\]
Then
\[
Q^2=\operatorname{diag}(DD^T,D^TD).
\]
Because \(\operatorname{rank}D=79\), there are one vertex zero mode and 81 edge zero modes: 82 total. With vertices positively graded and edges negatively graded,
\[
\operatorname{ind}Q=1-81=-80=V-E.
\]

The nonzero spectrum is paired:
\[
\pm\sqrt{4-\sqrt6}^{\,24},\quad
\pm2^{\,30},\quad
\pm\sqrt{4+\sqrt6}^{\,24},\quad
\pm2\sqrt2.
\]
This is exact finite supersymmetric quantum mechanics and the index is the Levi graph Euler characteristic.

## Pass 4152 — bonkers: exact W33 echo crystal

Let
\[
H_-=L=12I-A,\qquad H_+=16I-L=4I+A.
\]
Both are one-hop graph Hamiltonians, they commute, and
\[
H_-+H_+=16I.
\]
Therefore, for every \(\tau\),
\[
e^{-i\tau H_+}e^{-i\tau H_-}=e^{-i16\tau}I.
\]
At
\[
\tau=\pi/8
\]
the two-half cell is exactly the identity and has duration \(\pi/4\). Repeating the cell gives perfect analytic refocusing. If the second half becomes \(H_++E\) with Hermitian \(E\), the one-cell operator error is bounded by
\[
\tau\|E\|.
\]

**Boundary.** This is an exact switching identity, not a measured material echo or time crystal.
