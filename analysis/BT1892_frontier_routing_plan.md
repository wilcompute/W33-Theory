# BT1892 — R1/R2/R3 Frontier Routing Plan

BT1892 turns the Holonet completeness boundary into a concrete attack plan using the BT1891 decomposition.

## Starting point

The new Holonet paper frames the finite architecture as complete modulo three physical residuals:

```text
R1 = integral E8 definite lift
R2 = moonshine quintic / 2A-2B module boundary
R3 = continuum / Einstein-Hilbert coefficient
```

BT1891 adds:

```text
Phi3 = 13 = 8 + 5 = rank(E8) + F5
```

via:

```text
[[66,13,3]]_3 -> [[66,8,3;5]]_3
```

## Routed frontiers

### R1 — integral E8 lift

Object:

```text
8 protected logical qudits of [[66,8,3;5]]_3
```

Task:

```text
construct the canonical integral positive-definite gauge-fixed lift
```

Avoid:

```text
+2 eigenlattice search; BT926 already ruled it out
```

### R2 — moonshine quintic

Object:

```text
5 gauge-clock qudits
```

Task:

```text
test whether the quintic 2A/2B obstruction is the transport law on the F5 gauge sector
```

### R3 — continuum/EH coefficient

Object:

```text
detector-time/noise action of the 8+5 split
```

Task:

```text
derive the continuum coefficient as an effective action of repeated syndrome cycles
```

## Breakthrough claim

The three residuals are not unrelated loose ends.  They route to three layers of the same finite code decomposition:

```text
E8 logicals
F5 gauge clocks
decoder-time effective action
```

Boundary: routing plan only; no residual frontier is claimed solved.
