# Passes 4681–4688 executed outcomes

Canonical continuation after the occupied Pass4680 frontier. All five requested fronts and all three outside-box probes are materialized on `master` with executable witnesses, frozen certificates, focused regression/workflow, a shared theorem insert in all three maintained manuscripts, and registered public card/page surfaces.

## 4681 — the 45 dual weight-132 complements are literally the protected 45 packets
For the selected code

`C=[135,16,30]_2`, `C^perp=[135,119,3]_2`,

the dual has exactly 45 words of weight 132. Their three-coordinate complements are 45 pairwise-disjoint triples partitioning all 135 coordinates as

`135 = 45 x 3`.

These triples are object-for-object equal to the three singular fibers attached to each protected 16-line support from Pass4585/4624. They form one PSp orbit with stabilizer 576 and one PGSp orbit with stabilizer 1152.

Each of the 270 dual weight-three selected lines meets three distinct complement packets. Projecting those three coordinates to packet labels gives 270 distinct triples on the 45 packets. Every occurring packet pair appears in exactly three projected triples; the packet-pair graph is

`SRG(45,12,3,3)`,

the complement of the protected `SRG(45,32,22,24)`. Thus the dual weight-132 shell supplies a second code-internal realization of the protected/E6 45 carrier.

## 4682 — the complementary 135x36 incidences close a four-dimensional algebra
Let `Q` be the Pass4663 `135_4-36_15` incidence and `M` the code-minimum/spread `135_8-36_30` incidence. Their rows are disjoint. Both have rational rank 36 and together span the same 36-dimensional rational column space.

For `A=SRG(36,15,6,6)` on the spread/double-six carrier,

`Q^T Q = 15 I + 3 A`,

`M^T M = 24 I + 6 J`,

`Q^T M = M^T Q = 6 (J-I-A)`.

Hence

`(Q+M)^T(Q+M) = 9 (3 I + 2 J - A)`.

The sum has rational rank 21 and its 15-dimensional kernel is exactly the `+3` eigenspace of `A`. The integral data are:

- `SNF(Q)_nonzero = 1^29 2^6 4`, with ranks `(Q,F2,F3,F5)=(36,29,36,36)`;
- `SNF(M)_nonzero = 1^15 2^14 4^6 8`, with ranks `(36,15,36,36)`;
- `SNF(Q+M)_nonzero = 1^14 3^7`, with ranks `(21,21,14,21)`.

On the 135 side, `QQ^T`, `MM^T`, and `QM^T` are symmetric commuting operators with exact relation

`MM^T = 2 QQ^T + QM^T`.

Together with `I,J` they form a four-dimensional commutative algebra with joint multiplicities `1,15,20,99`.

## 4683 — the frozen apartment H1 does not carry a full PSp action
Pass4656 uses a particular labelled Schreier multigraph generated by five frozen cyclic transvection factors. Its nonzero C2 deck class lives in a 3241-dimensional `H^1` of that chosen labelled base.

The natural normalizer of those five cyclic factors is:

- order 1 inside PSp(4,3);
- order 2 inside PGSp(4,3), hence `C2`.

The nontrivial normalizer element has order two, lies in the outer coset, lifts to the apartment double cover and therefore fixes its deck class. The standard bare outer similitude is not itself the normalizer element; an outer-coset conjugate is.

Crucially, full PSp does not preserve this fixed five-factor labelled base. Therefore a PSp orbit or PSp submodule generated by `[alpha]` inside this particular 3241-dimensional `H^1` is **not defined**. An equivariant cohomology-module question requires a new PSp-invariant base complex. This is a scope correction, not a negative theorem about such future complexes.

## 4684 — the 405/1620 routing split is a homogeneous bundle over the Schlaefli 27
The 405 Petersen shortcut edges form one PSp orbit with edge stabilizer 64. They map 15-to-1 onto the 27 internal Schlaefli objects, each with stabilizer 960:

`G/H64 -> G/H960`.

The corresponding 27x405 component-edge incidence has Gram `15 I27`; its pushforward kernel has dimension 378.

The 1620 robust-base edges form one PSp orbit with edge stabilizer 16. They map 12-to-1 onto 135 unordered pairs of the 27 components. Those quotient pairs are exactly the edges of

`SRG(27,10,1,5)`,

whose edge stabilizer has order 192:

`G/H16 -> G/H192`.

The first pushforward kernel has dimension 1485. The unsigned 27x135 quotient edge-to-vertex incidence has

`B B^T = 10 I + A27`

and Smith profile `1^26 2^1`; its secondary kernel has dimension 108. The 27-vertex rational module splits spectrally with dimensions `1+20+6`.

## 4685 — mixed base/shortcut technology has exactly four routing policies
Assign positive scalar costs `c_b,c_s` to base and Petersen shortcut edges and set `r=c_s/c_b`. Exact two-objective path enumeration from every one of the 270 sources gives the same destination frontier. The only policy breakpoints are

`r = 1, 3/2, 2`.

The four consecutive regions have per-source `(base traversals, shortcut traversals)` totals

`(420,239), (548,111), (620,63), (746,0)`.

Across unordered pairs these become

`(56700,32265), (73980,14985), (83700,8505), (100710,0)`.

For independent-edge survival probabilities, setting `c=-ln(q)` gives exactly the same reliability breakpoints.

The sensitivity example uses explicit switch-depth assumptions: degree-12 base routing gets four binary stages and local degree-3 Petersen routing gets two. With the same separate-platform component anchors used previously, the resulting loss ratio is about `1.377`, hence the mixed region `1<r<3/2`, while the latency ratio is about `4.62e-5`, hence the shortcut-favor region `r<1`. Under those anchors the shortcut-favor and mixed policies are the two Pareto choices. These numbers remain component-mixed sensitivity anchors, not a demonstrated integrated Holonet stack.

## 4686 — outside box: the weight-132 hypergraph alone does NOT select E6
The 45 complement triples by themselves are simply 45 disjoint 3-sets. Their full 3-uniform-hypergraph automorphism group is

`S3 wr S45 = S3^45 : S45`,

of order

`12434208011101142745800297866258209857527581172828914008585963468573071374035189760000000000`.

So the weight-132 shell alone is vastly too symmetric to select the protected PGSp action.

Adding the dual weight-three shell changes the answer. Its 270 projected packet triples have two-section `SRG(45,12,3,3)`, giving an automorphism upper bound 51840, while the explicit PGSp code action supplies the matching lower group. Thus the pair of dual shells at weights 132 and 3 has full automorphism group PGSp(4,3), order 51840. The weight-three shell is precisely the additional relation that breaks the wreath symmetry to the protected action.

## 4687 — outside box: the Petersen shortcut module is induced from A5/S5
Fix one of the 27 Petersen components. Its PSp stabilizer has order 960. On the ten Petersen vertices its image has order 60 and element-order census

`1^1 2^15 3^20 5^24`,

hence `A5`; the kernel has order 16. On the 15 Petersen edges the local edge stabilizer is `V4`, giving upstairs edge stabilizer order 64.

Under PGSp the component stabilizer doubles to 1920. Its local image doubles to `S5` of order 120, still with kernel 16. The local edge stabilizer becomes `D8`, giving upstairs edge stabilizer order 128.

Therefore the hot-edge permutation module is literally induced from the local Petersen-edge action:

`Q[405] = Ind_{H960}^{PSp} Q[A5/V4] = Q[PSp/H64]`,

and under PGSp,

`Q[405] = Ind_{H1920}^{PGSp} Q[S5/D8] = Q[PGSp/H128]`.

This is an exact finite permutation-module statement; no physical A5/S5 symmetry is inferred.

## 4688 — outside box: the apartment voltage and routing edges live on different graph scales
Project the 4050 labelled edges of the frozen Pass4656 flag-Schreier graph to the selected-line endpoints in the 270-router. Every single projected pair is at selected270 graph distance three.

Thus there are

- zero hits on the 1620 base routing edges;
- zero hits on the 405 Petersen shortcut edges.

The 4050 labelled edges collapse to 1275 distinct distance-three selected-line pairs, with exact multiplicity profile

`3^1200, 6^75`.

Consequently the apartment C2 voltage does not canonically induce a hot/cold routing-edge signing. Such a coupling would require an additional choice of a length-three routing path/transgression for each projected Schreier edge. No bias can be promoted without specifying and testing that extra data.

## Integration and evidence
- Executable witnesses: `analysis/w33_pass4681_dual132_protected45.py` through `analysis/w33_pass4688_voltage_routing_projection_obstruction.py`.
- Frozen certificates: `data/PART_W33_PASS4681_*` through `data/PART_W33_PASS4688_*`.
- Regression: `tests/test_w33_pass4681_4688_dualshell_petersen_optimizer.py`.
- Exact-regeneration workflow: `.github/workflows/w33_pass4681_4688_dualshell_petersen_optimizer.yml`.
- Shared theorem insert: `analysis/PASS4681_4688_dualshell_petersen_optimizer_insert.tex`, integrated into `w33_paper.tex`, `photonic_holonet.tex`, and `holonet_machine_blueprint.tex`.
- Public card/page: `analysis/PASS4681_4688_dualshell_petersen_optimizer_index_insert.html` and `docs/dualshell-petersen-module-routing.html`, registered in the public frontier manifest.
- `docs/index.html` was not rewritten directly; publication uses the registered-card route because connector reads of the giant file truncate.

Evidence discipline: all promoted statements are exact finite group, graph/hypergraph, graph-cover/cohomology-scope, binary-code, incidence/module, or parameterized-routing statements. No particle, generation, gauge-field, optical-phase, or measured-hardware identification follows from them alone.
