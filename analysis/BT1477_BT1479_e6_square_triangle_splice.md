# BT1477--BT1479: E6/ABI square, triangle-channel bridge, and splice cascade

## BT1477

The E6 firewall square and the ABI/CSS decoder square are now one diagram:

36 -> 72, 24 + 48 = 72, and 72 + 9 = 81.

Here 36 is the affine-triad skeleton, 72 is the oriented sector, 24+48 is the active/guard ABI realization of that sector, and 81 is the CSS/H1 closure after adding the q-squared firewall gap.

## BT1478

The 12 closure strands admit two transverse partitions.

Three channels of size four:

P0 = (0,1,2,3), P1 = (4,5,6,7), P2 = (8,9,10,11).

Four triangles of size three:

T0 = (0,4,8), T1 = (1,5,9), T2 = (2,6,10), T3 = (3,7,11).

Every channel meets every triangle in exactly one strand. The Szilassi/Fano three-channel view and the E6 gauge-shell four-triangle view are the two axes of the same 3-by-4 grid.

## BT1479

The splice cascade is ordered as claim section, claim table, CSS table, acquisition plan, and rendered equation fill after acquisition and audit.

Current synthesis:

(36 -> 72 -> 81) = (24 + 48 = 72) inside (72 + 9 = 81) = 3 by 4 strand grid.
