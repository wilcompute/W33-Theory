# BT1574 Exact S/F3 Optical Calibration Pair

On the centered OAM basis ell=-1,0,+1 with qutrit labels 2,0,1, S has phase signature [1,0,1].

The F3 mixer uses the same centered qutrit labels and is the finite three-mode Fourier mixer.

This is exact finite calibration, not a loss or aberration model.
