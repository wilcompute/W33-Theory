% Passes 7305--7320: intrinsic double-six decoder, complementary Naimark carrier,
% proof-carrying q=7 validator, and the extension-field Pauli trace firewall.
\subsection{An intrinsic double-six decoder and a trace-corrected finite Pauli validator}
\label{sec:pass7305-7320-intrinsic-naimark-pauli-scope}

\paragraph{The spread code now recovers the double-sixes intrinsically.}
Let
\[
 C_{\mathrm{spread}}=[45,21,5]_2
\]
be the code generated by the twenty-seven ten-$D_4$ spread words, and let
$\mathcal M_5$ be its intrinsic shell of twenty-seven minimum words.  Exact
enumeration of all $2^{21}$ codewords finds $21{,}168$ words of weight fifteen.
Exactly thirty-six of them have the minimum-shell intersection profile
\[
 \boxed{\bigl\{|\operatorname{supp}(x)\cap\operatorname{supp}(m)|:
 m\in\mathcal M_5\bigr\}=0^{12}3^{15}.}
\]
This is the unique profile class of size thirty-six.  Only after this
code-intrinsic selection is complete do we compare with the cubic-surface
coordinates: the selected words are exactly the thirty-six columns of the
$45\times36$ disjoint-tritangent/double-six incidence matrix $N$.  Joining two
selected words when their supports meet in six coordinates gives, objectwise,
\[
 \boxed{H_{36}=\operatorname{SRG}(36,20,10,12)},
\]
with $360$ intersections of size six and $270$ intersections of size three.

The ownership boundary is important.  Passes 7182 and 7184, in
\path{analysis/w33_pass7182_d4_glue_spread_code.py} and
\path{analysis/w33_pass7184_spread_code_v20_v24_module.py}, own
$C_{\mathrm{spread}}$, its twenty-seven minimum words, and the
$\mathbf 1\oplus V_{20}$ statement.  Passes 7225--7248, in
\path{analysis/w33_pass7225_7232_spread_code_doily_puncture.py} and
\path{analysis/w33_pass7241_7248_double_six_slice_generator.py}, own the
externally constructed thirty-six double-six words and
$\operatorname{col}(N)=C_{\mathrm{spread}}$.  The new result here is the
converse intrinsic selector and the reconstruction of $H_{36}$ from its pair
intersections.

\paragraph{The two visible carriers resolve one orthogonal $15+20+1$ decomposition.}
Let $B$ be the $40\times36$ W33-line/spread incidence matrix and align the
columns of $N$ to those of $B$ using the explicit Pass4992
spread/double-six scheme isomorphism.  If $A$ is the
$\operatorname{SRG}(36,15,6,6)$ overlap-four graph, define
\[
 E_{15}=\frac{6I+2A-J}{12},\qquad
 E_{20}=\frac{9I-3A+J}{18},\qquad E_1=\frac{J}{36}.
\]
Then exact integer linear algebra gives
\[
 E_{15}+E_{20}+E_1=I_{36},
\]
\[
 \left(B-\frac14J\right)^{\!T}\left(B-\frac14J\right)=18E_{15},
 \qquad
 \left(N-\frac13J\right)^{\!T}\left(N-\frac13J\right)=18E_{20},
\]
and the cross-Gram matrix vanishes:
\[
 \boxed{\left(B-\frac14J\right)\left(N-\frac13J\right)^{\!T}=0.}
\]
Thus, with $u=2^{-1/2}\mathbf 1_{36}$,
\[
 T=\begin{bmatrix}B-J/4\\N-J/3\\u\end{bmatrix},
 \qquad \boxed{T^TT=18I_{36}}.
\]
The tritangent lanes realize the rank-twenty part of the old rank-twenty-one
Naimark complement; the common scalar row supplies the final dimension.

The same composition has a fully integral lowering.  Set
\[
 C_4=4B-J_{40,36},\qquad D_3=3N-J_{45,36},
\]
and
\[
 K=\begin{bmatrix}3C_4\\4D_3\\6J_{2,36}\end{bmatrix}.
\]
Then
\[
 \boxed{K^TK=2592I_{36}},\qquad
 K\in\{-4,-3,6,8,9\}^{87\times36}.
\]
These coefficients are shift--add realizable, but no synthesis cost or timing
is inferred for $K$.  Pass3694
\path{analysis/w33_pass3694_3700_spread_etf_axial_closure.py} already owns the
abstract $\operatorname{ETF}(15,36)$ and its rank-twenty-one Naimark
complement; Pass4992
\path{analysis/w33_pass4992_4999_common.py} owns the alignment; Pass7241 owns
the literal $N$; and
Pass7249--7304
\path{analysis/PASS7249_7304_eight_frontier_attacks.md} already owns the
centered rank-twenty two-distance frame.  What is new is their exact joint
orthogonal composition and the integer transform $K$.

\paragraph{The intrinsic shells descend through the $27$-line $E_6$ carrier.}
Let $T$ be the $27\times45$ minimum-shell/tritangent incidence and let $R$
be the $27\times36$ cubic-line/double-six incidence.  Once the Pass7305
thirty-six-set is selected, zero intersection between the twenty-seven minimum
words and those thirty-six words reconstructs $R$ objectwise, without importing
root coordinates.  The three incidence carriers obey
\[
 \boxed{T^TR=2(J-N)}.
\]
After centering, all three nonconstant sectors have rank twenty and the exact
factorization is
\[
 \boxed{T_0^TR_0=-2N_0},\qquad
 -\frac{N_0}{\sqrt{18}}=
 \left(\frac{T_0}{\sqrt6}\right)^{\!T}
 \left(\frac{R_0}{\sqrt{12}}\right).
\]
Thus the same rank-twenty Naimark shadow factors through the minuscule
$27$-line $E_6$ carrier by named matrices.

The graph $H_{36}$ has $1200$ triangles.  Exactly $120$ have empty common
$R$-intersection and $1080$ have four common cubic lines.  The resulting
triangle--edge parity system over $\mathbb F_2$ has rank $325$ and recovers a
switching class of signed Gram matrices $G$ satisfying
\[
 \boxed{\operatorname{rank}G=6,\qquad G^2=12G},
\]
so $\operatorname{spec}(G)=12^6\oplus0^{30}$.  A direct cross-check chooses
one $A_2$ anchor in the current 240-root $E_8$ model.  Its orthogonal
complement contains $72$ roots, or $36$ projective root lines, and one frozen
$H_{36}$ isomorphism maps the intrinsic thirty-six-set to them with
off-diagonal Gram profile $(-1)^{120},0^{270},1^{240}$.  This is an actual-root
cross-certificate but not a canonical map: it depends on the chosen $A_2$
anchor and $H_{36}$ gauge, and the thirty-six-set is not asserted to be an
invariant subset of the code embedding's transitive anisotropic 120-set.
Pass7163--7170
\path{analysis/w33_pass7163_7170_e8_hexagonal_lift.py} owns the ambient
$E_8/A_2$ realization; Pass7317--7320 owns this chosen-gauge composition.

The same witness also closes a tempting but false mod-twelve reading of $K$.
Every column of $K$ is identical modulo twelve, with row residues
$9^{40},8^{45},6^2$.  Removing those row constants and dividing by twelve gives
\[
 Z^TZ=18I_{36}+42J_{36},
\]
while column differences satisfy
\[
 D^TD=18(I_{35}+J_{35}),\qquad \operatorname{rank}D=35,
\]
a scaled $A_{35}$ difference lattice.  The constant column residue therefore
rules out identifying $K\bmod12$ with the nontrivial $E_8$ $\mathbb Z_{12}$
grading.

\paragraph{A proof-carrying q=7 certificate validator.}
The frozen q=7 witness contains thirty-three projective labels and $528$
unordered pairs, all with nonzero symplectic product.  Independent projective
rescaling changes the six nonzero residue populations but never the
zero/nonzero predicate, so the RTL exposes only \texttt{inputs\_valid} and
\texttt{noncommute}.  Yosys SAT proves the Mersenne-fold pair core equal to
both the signed symplectic formula and a naive remainder circuit for every one
of the $2^{24}=16{,}777{,}216$ raw input assignments.

The measured \texttt{synth\_ice40} frontier is
\[
\begin{array}{c|r|r|r|r|r}
\text{design}&\text{LUT4}&\text{carry}&\text{FF}&\text{BRAM}&\text{cycles}\\ \hline
\text{naive pair core}&528&262&0&0&\text{combinational}\\
\text{Mersenne pair core}&129&40&0&0&\text{combinational}\\
\text{register serial}&1637&63&814&0&562\\
\text{synchronous-BRAM serial}&196&63&48&1&1618
\end{array}
\]
and exact simulation accepts the frozen witness and rejects a deterministic
duplicate-point corruption in all three complete-check architectures.  A
seeded, unconstrained-IO nextpnr run on iCE40HX8K-CT256 reports $2439$ and
$230$ routed logic cells and final proxy frequencies $31.80$ and $41.31$ MHz
for the register and BRAM serial designs respectively.  These are synthesis
and place-and-route proxies, not board timing, IO validation, power
measurements, or a fabricated device.

\paragraph{Typed stabilizers and the extension-field trace firewall.}
The exact GAP witness deliberately separates the linear symplectic group, its
projective image, and projective symplectic similitudes:
\[
\begin{array}{c|r|r|r}
q&|\operatorname{Stab}_{\mathrm{Sp}}|&
|\operatorname{Stab}_{\mathrm{PSp}}|&
|\operatorname{Stab}_{\mathrm{PCSp}}|\\ \hline
3&18&9&18\\
5&24&12&12\\
7&2&1&2\\
9&4&2&2
\end{array}
\]
For the frozen q=5 order-three-invariant eighteen-set only, the linear
stabilizer is $\mathrm{SL}(2,3)$ and both projective stabilizers are $A_4$.
This is not a classification of all q=5 maxima and not an upper bound for
$\alpha(W(3,5))$.  For the packet-owned q=7 witness, the standard projective
Clifford/$\mathrm{PSp}$ stabilizer is trivial, whereas the larger
commutation-preserving $\mathrm{PCSp}$ stabilizer is $C_2$.

For $q=p^r$, the standard Galois--Pauli commutator is controlled by
\[
 \operatorname{Tr}_{\mathbb F_q/\mathbb F_p}B(x,y),
\]
not merely by whether $B(x,y)$ vanishes in $\mathbb F_q$.  Consequently one
$\mathbb F_9$ projective point becomes a four-point totally isotropic line in
$PG(7,3)$.  The $820$ such lines partition all $3280$ projective points.  The
frozen fifty-one-point construction becomes $204$ physical classes in
fifty-one commuting $K_4$ blocks; between every two blocks the commuting
cross-graph is a perfect matching $4K_2$.  The resulting commuting graph is
$53$-regular with $5406$ edges.  Therefore
\[
 \boxed{\text{the fifty-one objects are mutually nonorthogonal spread lines,
 not fifty-one pairwise-noncommuting physical Pauli classes}.}
\]
The q=9 construction remains a finite-geometric lower bound; the value of
$\alpha(W(3,9))$ is not determined.

Finally, the Lean module
\path{formal/W33/Pass7316AntisymplecticRescaling.lean} proves the
carrier-independent scalar identity that rescaling a bilinear similitude of
multiplier $\mu$ by $c$ changes its multiplier to $c^2\mu$.  In particular an
antisymplectic map becomes symplectic when $c^2=-1$.  This formalizes the
square-class mechanism separating q=7 from q=9; the finite-field enumeration
and the stabilizer orders remain GAP-owned.

\paragraph{Evidence boundary.}
All promoted statements are exact finite code, incidence, frame, finite-field,
group, RTL-equivalence, synthesis, place-and-route-proxy, or Lean-kernel
statements with the scopes above.  They do not establish quantum state
preparation, laboratory hardware, device performance, loss or noise budgets,
particle assignments, masses or energies, continuum dynamics, or a physical
theory.  The $E_8$ root cross-check is chosen-gauge, and the mod-twelve
residue of $K$ is explicitly not the $E_8$ $\mathbb Z_{12}$ grading.
