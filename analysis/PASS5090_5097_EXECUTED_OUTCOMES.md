# Passes 5090–5097 — collision-reconciled derivative/root continuation

**Status:** EXECUTED 2026-08-14. A concurrent master packet claimed Passes 5074–5081 while this continuation was in progress. Its Pass5074 independently corroborated the same coarea tester reduction and its Pass5079 independently closed the all-q Tanner six-cycle law. Those results are treated as prior/corroborated ownership and are not duplicated here. Passes 5090–5097 contain only the unique remainder plus two synthesis theorems exposed by the reconciliation.

## 5090 — q=4 local-minimum shell rigidity
The q=4 apartment code is `[13600,256,256]_2`. Assume every active opposite-pair chart is a minimum `1|4` cut of its local K5. Fix one selected apartment. It lies in four charts and each chart has two singleton roots compatible with that selected K5 edge, giving 16 seeds. Exact chart-state propagation and DFS gives exactly eight satisfiable seeds and eight contradictions. The eight satisfiable supports are exactly the eight chamber stars through the fixed apartment.

Hence every hypothetical non-chamber minimum word must contain a heavier `2|3` chart. This is stronger and more local than the existing one/two/three-chamber-generator census, but it does not yet classify the heavy-chart sector.

## 5091 — the sqrt(17) bridge is integral and is a quadratic-order statement
On the irrational twisted-Hecke sector let `A=[[1,4],[1,0]]`. For the corrected historical transfer block `B2=[[4,2],[2,5]]`, the unimodular matrix

`P=[[2,-1],[-1,1]]`, `det P=1`

satisfies `A P=P(B2-4I)`. Thus the q=2 transfer lattice and the twisted-Hecke quadratic lattice are integrally conjugate after the scalar shift; the earlier rational-similarity statement was not optimal.

If `lambda=(1+sqrt(17))/2`, then `lambda^2-lambda-4=0` and `O_K=Z[lambda]` is the maximal order of `Q(sqrt(17))`, discriminant 17. The q=3 recurrence generator is `mu=10+4 lambda`, whose minimal polynomial is `x^2-24x+76`; therefore `Z[mu]=Z+4 O_K` is the conductor/index-four order and has discriminant `16*17=272`. This explains the corrected q=2/q=3 discriminants through one quadratic order. It does not revive the retracted p=5 discriminant claim. No PSp-equivariant geometric intertwiner is asserted because the historical recurrence space was not supplied with such an action.

## 5092 — denominator-780 compiler Pareto frontier, one-swap exact
The raw 24-apartment V24 basis that reaches the point-star Smith floor has inverse denominator 780 and condition number `~72382.4833`. Its complete nonsingular one-apartment-swap neighborhood contains 3289 distinct bases. Exactly eight retain denominator 780, and the best *other* denominator-780 neighbor has condition `~74483.1939`. Therefore the frozen floor basis is a strict one-swap local optimum for numerical conditioning subject to exact denominator 780.

A seeded two-swap screen tested 24547 candidates; exact arithmetic was evaluated on the 1200 numerically best, and none had denominator 780. The best sampled condition without the denominator constraint was about 102.57. This is evidence of a sharp arithmetic/analog Pareto wall, not a global proof over all denominator-780 bases. The denominator-3120 raw basis with condition `~105.4622` remains the balanced control.

## 5093 — the user's derivative observation becomes a four-sheet coarea theorem
For a fixed chamber, exactly `q^4` apartments contain it. Each such apartment supplies one active opposite-pair chart in each of four incidence roles: point-endpoint, point-interior, line-endpoint and line-interior. Within any one role, each active chart contains exactly q chamber-star apartments because its local cut is a `1|q` star. Double-counting the q^4 apartment-role incidences gives exactly q^3 charts in each role:

`q^3+q^3+q^3+q^3=4q^3`.

Thus the formal identity `d(q^4)/dq=4q^3` has a literal finite-geometric shadow: q^4 is a chamber-star volume and its four codimension-one/root-direction sheets each have size q^3. This is a discrete coarea derivative, not infinitesimal calculus in q. Exact role counts are `8+8+8+8`, `27+27+27+27`, `64+64+64+64` at q=2,3,4.

At q=3 the chamber stabilizer contains a normal unipotent subgroup U of order 81 acting regularly on the 81 chamber-star apartments. U has element-order census `1^1 3^44 9^36`, center order 3 and derived subgroup order 9; the local switch neighbors split into four order-three root directions. This is the group-theoretic anchor behind the four-sheet count.

## 5094 — outside box: a noncommutative discrete Hessian
The four root directions do not behave like four Cartesian coordinates. At q=3 the six subgroups generated by pairs of directions have orders

`9,9,9,9,27,81`.

Thus four pairs close at the naive q^2 size, one grows to q^3 through an extra commutator layer, and one generates the full q^4 unipotent carrier. At q=2 the corresponding exact fingerprint is `4,4,4,4,4,8`. The subgroup data provide a finite-difference/root-commutator analogue of an anisotropic Hessian: second-direction composition detects C2-building curvature. The Hessian language is interpretive; the subgroup orders are exact.

## 5095 — outside box: same derivative counts, not the Hamming four-cube
Form the chamber-star switch graph by joining two star apartments when they differ inside one active root chart. Its vertex count is q^4 and its degree is `4(q-1)`, exactly matching the Hamming graph `H(4,q)`, but its spectrum does not.

- q=2: `chi=(x^2-16)(x^2-4)^2 x^2 (x^2-2)^4`, so the 16-vertex degree-four graph is not Q4.
- q=3: `chi=(x-8)(x-5)^4(x-2)^10(x+1)^18(x+4)^12(x^2-4x+1)^6(x^2+2x-2)^6(x^2-x-8)^6`, so it is not H(4,3).
- q=4: `chi=(x-12)(x-8)^6(x-6)^24(x-4)^9(x-2)^24 x^84(x+4)^72(x^2-8)^18`, so it is not H(4,4).

The derivative geometry really has four directions, but its local q^4 carrier is root-curved/non-Cartesian. This is also a firewall against reviving the old hypercube connection from the q=2 count `16 vertices / degree 4` alone.

## 5096 — q=4 heavy-chart defect conservation
For a weight-256 q=4 word, a nonzero local K5 cut has weight 4 (`1|4`) or 6 (`2|3`). Let A1 and A2 be the numbers of those two chart types. The coarea identity gives

`1024=4 A1+6 A2`.

Hence A2 is even; write `A2=2t`. Then

`A1=256-3t`, `A_active=256-t`.

The local supported Tanner-triangle count is `4 A1+2 A2=1024-8t`. A chamber star is exactly `t=0`. By Pass5090 any exotic minimum word must have `t>=1`, so it necessarily has strictly fewer than 256 active charts and at most 1016 supported local Tanner triangles. This turns the unresolved shell into a defect-graded problem: `t` simultaneously measures heavy-chart pairs, deficit from the derivative support, and an eight-at-a-time Tanner curvature loss.

## 5097 — reconciliation theorem: coarea and Fourier are the same energy
The concurrent Pass5080 writes apartment weight through the Fourier coefficient of the uniform apartment-boundary measure `mu`:

`wt(c_y)=N_A(1-muhat(y))/2`.

The coarea chart identity writes the same weight as

`wt(c_y)=(1/4) sum_O s_O(q+1-s_O)`.

Therefore, exactly,

`muhat(y)=1-(1/(2N_A)) sum_O s_O(q+1-s_O)`,

where `N_A=q^4(q+1)^2(q^2+1)/8`. For a chamber/edge character this gives

`muhat_edge=1-16/((q+1)^2(q^2+1))`.

Thus the active-chart tester route and the Fourier-extremality route are not competing conjectural approaches: they are local and harmonic coordinate systems for the same energy functional. The remaining all-q distance theorem can be phrased equivalently as either minimum chart energy or maximum nontrivial Fourier bias.

## Boundary
Pass5090 classifies the q=4 local-minimum chart sector, not the heavy-chart shell. Pass5092 is exhaustive for one-swap floor neighbors but sampled for two-swap/global search. Pass5093 makes the derivative analogy exact as discrete coarea/root-sheet counting, not as continuous variation of a prime-power field size. The all-q minimum-distance theorem remains open, but Pass5097 collapses two previously separate strategies into one exact functional identity.
