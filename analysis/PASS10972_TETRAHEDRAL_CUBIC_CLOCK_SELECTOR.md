# Pass 10972 — The minimal cubic clock selector

Producer: `analysis/w33_pass10972_tetrahedral_cubic_clock_selector.py`
Certificate: `data/w33_pass10972_tetrahedral_cubic_clock_selector.json`
Regression: `tests/test_w33_pass10972_tetrahedral_cubic_clock_selector.py`

## Result

Pass 10971 proved that a linear Hamiltonian on the four clock amplitudes cannot preserve the full S4=PGL2(3) clock symmetry and select one preferred direction.

The minimal spontaneous-symmetry-breaking escape is already present in the invariant theory of the same clock module.
Let

[
A={x=(x_1,x_2,x_3,x_4)inmathbb R^4:sum_i x_i=0},
]

the three-dimensional augmentation representation, with (S_4) permuting coordinates.

The cubic

[
p_3(x)=sum_i x_i^3
]

is (S_4)-invariant. On the unit sphere of (A),

[
|p_3(x)|le 1/sqrt3.
]
The positive equality cases are exactly the four permutations of

[
(3,-1,-1,-1)/sqrt{12},
]

and the negative equality cases are their opposites. Thus the four extrema are not inserted
clock labels: they are the orbit of the standard tetrahedral augmentation vector, each with
stabilizer (S_3).

## Exact extremum proof

Extremize (p_3) subject to (p_1=0) and (p_2=1). Lagrange multipliers give

[
3x_i^2=alpha+2eta x_i,
]

so every coordinate is one of at most two values.
If (m) coordinates equal (a) and (4-m) equal (b), the constraints force

[
b=-{mover4-m}a.
]

The only multiplicities are (1+3), (2+2), and (3+1). The exact squared cubic values are

[
1/3,quad0,quad1/3.
]

The sign separates the two opposite tetrahedra. The verifier carries this classification with
exact rational arithmetic.

## An S4-invariant Landau selector

Take

[
V(x)=lambda(p_2(x)-v^2)^2-g,p_3(x),
qquad lambda,g,v>0.
]
The potential is fully (S_4)-invariant. At fixed radius its minima maximize (p_3), so its
angular vacuum set is exactly the four positive tetrahedral clock directions. Therefore

[
S_4longrightarrow S_3
]

is spontaneous, not explicit.

Along a vacuum ray the positive radius is the unique positive solution of

[
4lambda(r^2-v^2)=sqrt3,g r,
]

namely

[
r_*={sqrt3 g+sqrt{3g^2+64lambda^2v^2}over8lambda}.
]

The radial second derivative there is (4lambda(r_*^2+v^2)>0).
Changing the sign of g exchanges the two opposite tetrahedra.

For the S4=W(A3) reflection representation, symmetric cubics on four coordinates are generated by p1^3, p1 p2, and p3. On the augmentation space p1=0, so p3 is the unique nonzero cubic generator up to scale.

## What this changes

The dynamical problem is narrower. A symmetric linear selector is impossible, while a symmetric nonlinear selector can choose one of four equivalent vacua spontaneously. The next question is which certified W33 quantity could supply this augmentation field and its cubic coefficient.

## Native-cubic firewall

The repository already has a signed 45-term E6 cubic controlling the exact Hesse-clock 54->81->83 bracket ladder. That makes a native-cubic weld an obvious target, but it is not claimed here.

The temporal 27-history carrier and the E6/H27 27-carrier have distinct certified structures, and the existing gauge bridge is explicitly a coordinate choice. An objectwise S4-equivariant embedding of the augmentation module into the cubic carrier is still required before the two cubics can be identified.

## Boundary

This packet constructs an exact finite-symmetry Landau model. It does not derive a physical field, energy scale, continuum dynamics, or a physical vacuum from W33. Its value is to give the missing dynamics problem a concrete representation-theoretic target and a sharp weld to test next.
