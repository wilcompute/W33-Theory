# BT874 — The Texture Triality R Is the Heisenberg Center (a Long-Root Transvection)

**Status: PROVEN (machine-verified, `analysis/bt874_texture_triality_is_heisenberg_center.py`, data `data/bt874_texture_triality_heisenberg.json`)**

Pinning Pillar 68's mass-texture generation map R — the order-3 element acting
on the 27-element matter shell with no fixed points and 9 orbits of 3, which
grades the Yukawa tensor (the CKM/PMNS origin). BT858 made the point shell a
Heisenberg torsor; BT874 identifies R exactly, and **refines the BT864 guess.**

## The identification

- **T1:** the center Z (order 3) of the point-stabilizer's Heisenberg group
  3^{1+2} acts on the 27 non-collinear points of p₀ with exactly **9 free
  orbits of 3, no fixed points** — this *is* Pillar 68's R.
- **T2 (refinement of BT864):** R's global fixed-point count is **13 = p₀ +
  its 12 neighbors = the perp-hyperplane p₀^⊥**, with 4 fixed lines. So R is a
  **long-root transvection** (the 40-class), NOT the 240-class tentatively
  flagged in BT864. This is textbook-correct: the center of a point-parabolic's
  unipotent radical is exactly the long-root transvection group with axis p₀^⊥.
- **T3:** R fixes the entire local gauge perp-plane (the 13-point PG(2,3))
  pointwise and acts freely only on the 27 matter shell — the canonical
  "fix the gauge, stir the matter" element.

## Why this is the right element

A generation/Yukawa map should leave the gauge structure invariant and
rotate only matter — which is precisely a transvection's action: it fixes the
13-point perp-plane (the local gauge sector, p₀ and its 12 collinear
neighbors) and acts as the Heisenberg center (9 free orbits) on the matter
shell. The texture triality is therefore the **long-root element** — tying
the three generations to the root/unipotent structure of Sp(4,3), not to a
generic semisimple class. BT864's gauge split 22+9+9 for the transvection now
reads: 22 on the fixed gauge plane's span, 9+9 the nontrivial generation
rotation.

## One element, two roles

The same order-3 transvection R:
- splits the **Steinberg matter register** (H₁) as 27+27+27 (BT863,
  matter-blind generation count — true for any order-3 element);
- acts on the **point matter shell** (the 27 non-neighbors) as 9 free orbits
  (the Yukawa/texture grading, Pillar 68).

So the generation *count* (register eigenspaces) and the generation *texture*
(shell orbits) are driven by one long-root transvection — the count is the
representation degeneracy, the texture is the geometric orbit structure, and
both are the q=3 long-root element acting.

## Correction logged

BT864 tentatively proposed the 240-class as the physical triality (almost
free on points, fixes 6=q! schedules). BT874 corrects this: R is the
40-class long-root transvection (fixes the 13-point gauge plane, 9 free orbits
on the matter shell). The transvection reading is more substrate-natural —
the long-root element is the canonical generation generator.

## Open

- The Yukawa tensor's exact grading under R (Pillar 68's T[a,b,v]=0 unless
  g=−(a+b)) re-derived from the transvection's 9-orbit structure.
- The three transvection classes' joint action (the full long-root torus) as
  the generation + weak-isospin structure.
