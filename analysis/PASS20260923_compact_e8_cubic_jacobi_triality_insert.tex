% Pass 409 — compact E8, cubic Jacobi residual, and triality routing.
\subsection*{Compact-$E_8$ two-control switch and cubic-Jacobi boundary algebra}

Let $\sigma_c$ be the compact anti-linear conjugation of the committed
Chevalley basis, with
\[
\sigma_c(h_i)=-h_i,\qquad
\sigma_c(e_\alpha)=
-\operatorname{sgn}K(e_\alpha,e_{-\alpha})e_{-\alpha}.
\]
All $30628$ unordered basis-bracket identities are satisfied exactly.  The
fixed root planes have Killing value $-120$, while the split Cartan Killing
matrix is positive definite, so the fixed real form is compact.

For either diagonal H27 background $x$, the pair
\[
A=x+\sigma_c(x),\qquad B=i(x-\sigma_c(x))
\]
generates $\mathfrak e_{8(-248)}$; the equivalent complex pair
$x,\sigma_c(x)$ closes exactly to all $248$ Chevalley directions over
$\mathbb Q$.

Moreover, the compact-conjugate architecture has an exact minimum of eight
root channels.  A witness supported on
\[
\{16,19,23,31,153,190,216,228\}
\]
with amplitudes $(3,2,1,2,1,3,1,1)$ closes to all $248$ dimensions over
$\mathbb Q$.  Its selected-root matrix has determinant $-1$.  Fewer than eight
root directions cannot generate rank eight, hence
\[
\boxed{k_{\min}=8}.
\]

The non-diagonal center/external branch instead has a $24$-dimensional perfect
algebra $L_{24}$ with radical
\[
\operatorname{rad}L_{24}\cong\mathfrak h_7.
\]
It contains $\mathfrak h_4\subset\mathfrak h_7$, and the Levi quotient is
\[
L_{24}/\mathfrak h_7
\cong\operatorname{Res}_{K/\mathbb Q}\mathfrak{sl}_2(K),
\]
where
\[
K=\mathbb Q(\theta),\qquad
\theta^3-\theta^2-53\theta-120=0,qquad
\operatorname{Disc}K=94557.
\]

At the completely split primes $107$ and $151$, the Levi factor separates into
three commuting $A_1$ factors.  On the nested Heisenberg quotients,
\[
\mathfrak h_4/Z\cong(2,2,2),\qquad
\mathfrak h_7/\mathfrak h_4
\cong(2,1,1)\oplus(1,2,1)\oplus(1,1,2).
\]
The $(2,2,2)$ sector has no polynomial invariants in degrees $1,2,3$ and a
one-dimensional invariant space in degree $4$, the Cayley
$2\times2\times2$ hyperdeterminant line after scalar splitting.

Finally, for the corrected local Weil carrier $\rho_6$ of the $1296$-element
W33 point stabilizer $H$,
\[
\operatorname{Ind}_{H}^{W(E_6)}\rho_6
=10\oplus60\oplus80\oplus90,
\]
whereas the signed edge chain is
\[
15\oplus24\oplus30\oplus81\oplus90.
\]
Thus the equivariant Hom space is one-dimensional and its nonzero image is
exactly the unique degree-$90$ constraint constituent.  The local qutrit
carrier therefore routes canonically to the global constraint sector, not to
the harmonic degree-$81$ logical sector.


The $18$-dimensional core has the exact $D_4$ contact-grading fingerprint:
grading by the central simple root gives root counts $1,8,6,8,1$,
$g_0^{\mathrm{ss}}=A_1^3$, and the eight $g_{-1}$ weights are all
$(\pm1,\pm1,\pm1)$.  Thus the derived contact parabolic is
$\mathfrak{sl}_2^3\ltimes\mathfrak h_4$, matching the split residual core.

The cubic centroid has squarefree discriminant
$94557=3\cdot43\cdot733$.  At each ramified prime its reduction contains a
dual-number factor $\mathbb F_p[\epsilon]/(\epsilon^2)$, so the $9$-dimensional
Levi reduction acquires a canonical $3$-dimensional square-zero ideal and its
Killing rank drops from $9$ to $6$.

The unique degree-$90$ overlap is realized explicitly over $\mathrm{GF}(103)$
by an invertible $90\times90$ matrix intertwining all three frozen
$W(E_6)$ generators.  Finally, the minimum eight-root compact frame has exact
chromatic number three for each of the two controls, so one full $AB$ cycle
requires exactly six $72$-tick microframes, or $432$ ticks.  Five such cycles
fit exactly in the existing $30$-microframe $E_8$ Coxeter bus.

\paragraph{Boundary.}
These are exact finite Lie-algebra and representation results.  They do not
derive a continuum action, Standard-Model parameters, black-hole entropy, or
laboratory control energies.
