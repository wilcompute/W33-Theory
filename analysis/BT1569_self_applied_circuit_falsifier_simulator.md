# BT1569 Self-applied Circuit Falsifier Simulator

The simulator compares passive labels, internal-operator behavior, operator-independent behavior, leakage failure, and label-dependence failure.

Only the internal-operator scenario passes the symbolic criteria: trace-Choi pattern, low leakage, basis covariance, and operator activation.

Tolerances are placeholders, not lab data.
