# 24 September 2026 — history compactification, reduced H1, and temporal orientation

Status: **PASS with explicit point/line and qutrit-operator firewalls**.

This packet begins from the Bell-line temporal chart but does not assume the
interpretive claims suggested by its dimensions. Every promoted bridge below
has an executable incidence, character, or group-action certificate.

## 1. The 27-history atlas is the big cell of Q(4,3)

Fix a W33 Lagrangian line B. Every Lagrangian line disjoint from B is uniquely

    L_S = {(S y,y) : y in F3^2}

for one symmetric 2x2 matrix S. Hence there are 3^3=27 histories.

The complete Lagrangian Grassmannian contains 40 lines. In Pluecker coordinates

    [x0,x1,x2,x3,x4] = [p34,p14,p24,-p23,p12]

they lie on

    x0*x4 - x1*x3 + x2^2 = 0.

The 27 affine points are S=(a,b;b,c) -> [1,a,b,c,ac-b^2].
The remaining 13 points have x0=0. They are not merely a PG(2,3)
"hyperplane at infinity": intrinsically they form the tangent-cone section of
Q(4,3), with B as cone vertex and four three-point generators indexed by the
four projective rank-one directions.

Thus the exact completion is

    27 big-cell histories + 13 Bell-boundary points = 40 Q(4,3) points.

Q(4,3) is the repository's W33 line-side carrier. The odd-q point/line
firewall remains in force: this does not identify Q(4,3) with the
nonisomorphic W33 point graph.

## 2. The null graph and spectral clock

Two histories meet iff det(S-T)=0. The induced null-history graph has

    |V|=27, degree=8, |E|=108, cycle rank=82,
    Spec(A)=8^1 2^12 (-1)^8 (-4)^6,
    Spec(L)=0^1 6^12 9^8 12^6.

At t*=2*pi/9 the walk U=exp(-i t* L) satisfies U^3=I, with phase
multiplicities 1^9, omega^12, (omega^2)^6. Fourier transform over F3^3
identifies the fixed nine modes exactly as the constant mode plus the eight
nonzero dual-null vectors.

The 9=1+8 count is real but is not the single-qutrit operator module. The
natural Bell-line S4 action splits the eight null modes as 4+4, while SL(2,3)
is transitive on the eight nonidentity projective qutrit Pauli labels. The
actual fixed-nine action is faithful of order 648, not the order-216
projective qutrit Clifford group.

## 3. Coherent temporal chirality and mu_12

The two PSp(B) orbits of size four are the two rank-one Weil-phase classes
4_+:+i and 4_-:-i. The full PGSp(B) order-1296 extension fuses them.

For qutrit Fourier F and chirp G=diag(1,omega,omega),

    (F G)^3 = i F^2.

The Weyl commutator supplies omega, so <i,omega>=mu_12. Across all 27
quadratic history forms the Weil-phase census is

    1^13, i^4, (-1)^6, (-i)^4.

Complex conjugation sends omega to omega^-1 and i to -i. This supplies a
reversible coherent orientation mechanism, distinct from entropy production.

## 4. The unique global orientation cycle

The history graph has 36 edge-disjoint triangles, one for each W33 point
outside B. Every temporal edge lies in exactly one such triangle.

A signed sum of the 36 triangle boundaries gives a primitive cycle supported
on all 108 history edges with coefficients +/-1. The inner 648 fixes this
cycle; the outer 648-element coset negates it. Hence

    82 = 1_orientation + 81_reduced

is an objectwise statement.

Filling the 36 local same-event triangles makes their boundaries contractible
and leaves

    b1 = 82 - 36 = 46.

The signed 36-triangle module has one invariant line, so its reduced sector has
dimension 35. Thus the reduced history module splits as

    81 = 35 + 46.

## 5. Point/line firewall survives the repeated 27/108/36/46 package

The point-side H27 graph also has 27 vertices, 108 edges, 36 triangles and
filled b1=46, but it is not isomorphic to the line-side history graph. Even
their complete unlabeled induced four-vertex profiles agree. The first
separator in this audit is the independent-triad common-neighbor law:

    history: 0^405 1^216 2^324,
    point H27: 0^225 1^648 3^72.

This is a deep dual shadow, not an objectwise identification.

## 6. Global W33 H1=81 is temporal core plus Bell-boundary relative homology

The temporal incidence core is

    36 W33 points off B <-> 27 W33 lines skew B,

with 108 incidences, so b1(core)=108-63+1=46.

The full W33 Levi graph has 80 vertices and 160 incidences, so b1(full)=81.
Restoring the Bell boundary adds 4 Bell points, B itself, and the 12 other
lines meeting B: 17 vertices and 52 incidences. Therefore

    81 = 46_core + 35_boundary-relative.

On all 648 Bell-line stabilizer elements, the relative-35 character equals the
reduced signed-triangle-35 character exactly.

There is also an explicit chain map for the 46. For an oriented temporal edge
L->M meeting at p, each of the three Bell points b not collinear with p
selects a canonical W33 quadrilateral

    b -> x_L(b) -> p -> x_M(b) -> b.

Summing those three quadrilaterals is equivariant under every Bell-line
stabilizer element. Its induced homology rank is 46 modulo each of
5, 7, 11 and 101. Its kernel on the 82-dimensional temporal cycle space is
exactly the 36-dimensional temporal-triangle boundary space.

Therefore filled temporal H1 injects explicitly into global W33 H1. Separately,
the reduced 81-dimensional unfilled temporal cycle representation has exactly
the same character as global W33 H1 on all 648 stabilizer elements.

## 7. Finite present, unbounded history

The unfilled null-history graph has pi_1=F_82 and H1=Z^82. Its universal cover
is the infinite 8-regular tree, with shell sizes

    N_0=1,
    N_n=8*7^(n-1), n>=1.

The same 27-state present can therefore recur at infinitely many lifted
locations after inequivalent reduced path words and windings.

This supports a precise emergent-time statement: instantaneous state is a
finite label, while operational history is a lifted path. Step count,
free-group word, homology winding, U(1) phase, thermodynamic affinity and
classical record strings are different temporal observables on path space.

## 8. Dual parabolics are architectural

PSp(4,3) has two nonconjugate order-648 parabolics already separated by the
repository:

- point side: 3^(1+2):2A4, center C3, derived order 216;
- line side: 3^3:S4, trivial center, derived order 324.

The qutrit Heisenberg/Clifford operator algebra selects the point-side 648.
The 27-history big cell selects the line-side 648. Therefore the emerging
Holonet architecture has a dual-parabolic separation:

    point parabolic = operator/data transport,
    line parabolic  = temporal/history/control transport.

The obstruction to identifying the two is useful: it prevents the data
register and history/control register from collapsing into the same carrier.

## 9. The Hermitian 3+1 completion is an elliptic section, but the direct J weld fails

Write an F9-Hermitian event as X=[[a,z],[zbar,c]], with z=x+u*y and
u^2=-1. Its determinant is

    Q_H(a,c,x,y)=a*c-x^2-y^2.

The 81 affine events split exactly as 21+30+30 by determinant value, and the
40 projective directions split 10+15+15. The explicit projective map

    Phi(a,c,x,y) = [a,x,y,-x,-c]

lands in the already-certified Q(4,3) model and obeys

    Q_43(Phi(v)) = -Q_H(v).

Hence the ten Hermitian projective null directions are literally the elliptic
hyperplane section x1+x3=0 of Q(4,3). Under the Pluecker line dictionary they
become ten pairwise-skew W33 lines covering all 40 W33 points: one spread.

Exhausting all projective hyperplanes gives section sizes

    10^36, 13^40, 16^45.

The 36 elliptic ten-sections reproduce exactly all 36 independently enumerated
W33 spreads.

The stronger ansatz Omega(x,y)=B(Jx,y) with J^2=-I does not exist. Exhausting
the full 3^6=729-dimensional B-skew candidate space finds zero solutions.
Thus causal determinant geometry and W33 symplectic geometry meet through the
Q43/W33-line duality, not through one compatible complex structure J.

## 10. The Hesse 12+72 split is a literal 4A2 root subsystem inside E8

Identify the nine history amplitudes with the affine plane AG(2,3). Its 84
three-subsets split under AGL(2,3), order 432, into exactly two orbits:

    12 affine Hesse lines + 72 noncollinear triangles.

In the standard A8 realization of E8, a triple S labels the exterior root

    w_S = sum_(i in S) e_i - (1/3) sum_i e_i,

with inner product w_S.w_T=|S intersect T|-1.

Each of the four Hesse parallel classes contains three disjoint triples. Their
three roots have pairwise inner product -1 and sum to zero, so together with
their negatives they form one A2 root system. Different parallel classes meet
in one cell and are therefore mutually orthogonal.

Consequently the 12 Hesse roots in Lambda^3(9), plus their 12 negatives in
Lambda^6(9), form a reflection-closed rank-eight A2^4 subsystem. The E8 root
shell refines exactly as

    240 = 72_A8 + 24_Hesse-4A2 + 144_noncollinear-triangle,

and at the Lie-algebra level

    248 = 8_Cartan + 72 + 24 + 144 = 80 + 84 + 84.

The A8 branching and the global census of E8 A2^4 subsystems are prior results;
the new content is the objectwise temporal/Hesse identification inside the
nine-cell basis.

## 11. Bell null directions, Hesse striations and the four A2 factors share one S4

The four projective rank-one directions of Sym_2(F3), the four parallel classes
of AG(2,3), and the four A2 components above are all canonically indexed by

    P1(F3) = {(1,0),(0,1),(1,1),(1,2)}.

For direction v, the temporal null representative is the rank-one symmetric
matrix v v^T. For every one of the 24 projective GL(2,3) representatives, the
induced permutation of

- the four projective directions,
- the four Hesse parallel classes, and
- the four A2 components

is identical. The common faithful image is

    PGL(2,3) ~= S4.

The larger groups remain distinct. The history line parabolic 648 has kernel
27 on this four-label carrier, while AGL(2,3), order 432, has kernel 18.
Therefore this is an exact common quotient/intertwiner, not a group equality.

This closes a structural loop: the Bell celestial boundary of the 2+1 history
chart is the same finite S4 carrier that indexes the Hesse/E8 4A2 striations.

## 12. The diagonal phase weld is exactly a Page-Wootters phase-matching constraint

The four existing center/external backgrounds

    c, p, c+p, c-p

are exactly the four projective linear directions of F3^2. Their zero sets are
two factor axes c=0,p=0 and two correlated graph subgroups p=-c,p=c.

For an order-three qutrit evolution U, the two stationary history conventions

    (X_clock tensor U)|Psi_+> = |Psi_+>,
    (X_clock tensor U^-1)|Psi_-> = |Psi_->

become, in the clock X-phase basis, exactly c+p=0 and c-p=0.

The existing 4x4 E8 generation grid obeys this distinction without exception:
axis-only/axis-only pairs close to dimension 24, while a graph-correlated
background on either side generates all 248 dimensions. The cubic quotient
similarly jumps from projection rank 36 on either pure axis to rank 54 on
either diagonal graph.

Thus the diagonal weld has an exact relational-clock interpretation. This is
not a Wheeler-DeWitt or continuum Hamiltonian derivation.

## 13. Four positive null counters quotient to the 27-state present

Use the four integral rank-one positive-semidefinite null updates

    D0, Dinf, Dplus, Dminus.

Their counter map F3^4 -> Sym_2(F3) has rank three and exact kernel

    <(1,1,1,1)>.

Therefore

    Sym_2(F3) ~= F3^4/<1111>,

so the 27 finite histories are four null-direction pulse counters modulo one
common mode.

Over the integer positive cover that common mode does not vanish:

    D0 + Dinf + Dplus + Dminus = 3 I2.

Adding one common winding returns to the same finite mod-three history while
increasing matrix trace by six. Because every positive generator has positive
trace, there are no nontrivial positive loops upstairs.

## 14. The 27-history chart is the relative mode of the four Hesse A2 factors

Index F3^4 by the four common null/Hesse/A2 directions. Sending each coordinate
basis vector to its rank-one symmetric matrix gives a surjection

    A : F3^4 -> Sym_2(F3)

with kernel <1111>. For every projective GL(2,3) element,

    R_G A = A P_G mod 3,

where P_G permutes the four directions and R_G is congruence on Sym_2(F3).

Hence the quotient is not only a vector-space identity but an S4-module
intertwiner:

    Sym_2(F3) ~= F3^{four A2 components}/common mode.

The Bell 27-history atlas is therefore the three relative modes of the four
Hesse A2 striations. The common mode is precisely the finite direction discarded
by the quotient and the positive winding retained by the integer lift.

## 15. The nine-cell A8/E8 process brackets are literal packet operations

On the nine history cells, the standard A8 root-support rules become:

- E_ij replaces cell j by i inside a three-history packet;
- two disjoint Lambda^3 packets bracket to the complementary Lambda^6 root;
- Lambda^3/Lambda^6 packets differing by one cell contract to E_ij;
- opposite packets return a Cartan direction.

The exact support counts are uniform: 1512 nonzero sl9-on-Lambda3 actions,
840 unordered disjoint Lambda3/Lambda3 wedges, 1512 Lambda3/Lambda6 root
contractions, and 84 opposite-root Cartan pairs.

On the Hesse 12, two lines in the same parallel class bracket to the negative
root of the third line; all 54 pairs from different striations have zero
root-sum bracket. This is exactly the four-A2 support closure found above.

Signed Chevalley constants are not rederived here; this packet certifies the
objectwise root-support compiler and leaves signs to the frozen E8 artifact.

## 16. One qutrit's nine operator histories compile W33, sl9 and E8 support

Vectorize the qutrit matrix units |i><j| as a nine-dimensional history space.
The 81 two-sided Weyl transformations

    P_(a,b) tensor conjugate(P_(c,d))

form a Hilbert-Schmidt orthogonal basis of End(C^9). The identity accounts for
one direction; the 80 nonidentity transformations are traceless, independent
and span exactly sl9.

Their labels x in F3^4 obey the two-sided commutator form

    <x,y> = aB-bA-cD+dC mod 3.

Pairing the 80 nonzero labels by sign gives 40 projective rays; commuting rays
reproduce SRG(40,12,2,4) with spectrum 12^1,2^24,(-4)^15.

Adding the 84 Lambda^3 history packets and 84 dual Lambda^6 packets gives the
standard A8 decomposition

    E8 = sl9 + Lambda^3(9) + Lambda^6(9),
    248 = 80 + 84 + 84,

with the objectwise support brackets certified in the preceding section.

## 16. Tetracode glue and temporal common mode meet on the eight-leaf A2^4 fibre

The standard ternary tetracode in the four Hesse/A2 coordinates meets the
temporal common line <1111> trivially. It therefore injects into

    F3^4/<1111> ~= Sym_2(F3)

as a 2D, nine-state history plane. Its four projective rays split by the
history determinant as

    1 null + 3 equal anisotropic.

The signed monomial tetracode automorphism group has order 48. Its orbit on
the temporal common-mode line <1111> has size eight and stabilizer order six.
Turning each sign pattern into four orientation bits modulo simultaneous
inversion gives exactly

    F2^4/<1111> ~= F2^3,

the Pass-7409 orientation fibre of the eight Eisenstein W33 leaves through one
fixed A2^4 subsystem.

After quotienting the central +/-I, the common-mode stabilizer is C3. On the
four tetracode history rays it fixes the unique null ray and cycles the three
anisotropic rays. The projective order-24 tetracode group acts transitively on
all eight common-mode/orientation classes, with the cube-rotation cycle census
1, 9, 8, 6 across the identity, order-two, order-three, and order-four types.

This is a finite code/module/orientation theorem. The C3 is not identified here
with a measured generation symmetry, and the 1+3 projective slice is not a
continuum Lorentz spacetime.

## Evidence boundary

All promoted statements above are finite incidence, homology, Fourier,
operator-matrix or finite-group theorems with executable producers. They do
not derive Lorentzian spacetime, gravity, a measured speed of light or a
laboratory thermodynamic arrow. Those physical identifications remain open.

Primary producers are the 24 September history big-cell, spectral-clock,
fixed-nine chirality, Maslov-mu12, invariant-orientation, 35+46,
point/line-firewall, cycle-81, explicit-H1-intertwiner, relative-Levi,
universal-cover, Hermitian-3+1/spread, temporal-Hesse-4A2, common-S4,
Page-Wootters weld, positive-null-counter cover, four-A2 quotient, temporal
A8/E8 process-bracket, nine-history su9/E8 compiler, and temporal-tetracode
common-mode scripts under analysis/.
Focused regression coverage is in
tests/test_w33_20260924_history_compactification_breakthroughs.py.
