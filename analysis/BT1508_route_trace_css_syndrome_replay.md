# BT1508 Route-trace CSS Replay

All eight native D4 route traces replay into the BT1486 retwined CSS row classes.

Each trace has 72 ticks split as 24 active and 48 guard, for 576 total symbolic ticks.

This is a symbolic CSS replay, not an optical-noise simulation.
