# Pass 499 — product-ring discriminator

For `(Z/9) x F_9`, the exact one-pair parity reduction yields a depth-24 witness.
The separable tensor slice instead lands at depths 60, 66, and 96, showing that
separability is a nongeneric symmetry constraint rather than a route to the
minimum.
