% Passes 3274--3285: twisted transport and reset closure.
\subsection{Twisted port transport and proof-authorized epistemic reset}
\label{sec:bt3274-bt3285}

The filled finite-port complex has the proved homotopy model
$X\simeq\bigvee^{436}S^1$.  For the trivial rank-two local system over
$\mathbb F_3$ one has $(\dim H^0,\dim H^1)=(2,872)$.  Sending one free
generator to the fixed-point-free $D_4$ quarter-turn
\[
J=\begin{pmatrix}0&-1\\1&0\end{pmatrix}\pmod3
\]
and the remaining generators to the identity gives the exact twisted result
\[
\boxed{(\dim H^0,\dim H^1)=(0,870).}
\]
Thus nontrivial transport removes two constant modes; it does not collapse the
large port moduli space to a single phase bit.

An independent controller verifier rebuilds the raw hypothesis-subset DAG and
applies iterative Moore partition refinement rather than recursive signature
interning.  It recovers exactly $770$ initial and $876$ total behavior states,
independently of traversal order and state numbering.

Runtime evidence is separated into three typed universes: the two four-opcode
operational baselines, the $194$ universal five/six-opcode census, and future
larger families.  A baseline may enter a shared-metric comparison but cannot
increment the census completeness count.  Before a complete aggregate exists,
the correct status is one joined census runtime, two exact four-opcode
baselines, and $193$ pending census records.

Finally, the exact controller contains three distinct terminal witnesses,
corresponding to no shared curvature, shared-flat curvature, and shared-curved
curvature.  Every admissible non-reset phase, route, and fail-closed sensing
map fixes those witnesses pointwise.  Consequently every unauthorized word
has transformation rank at least three at arbitrary length.  Rank one is
available only through a separately authorized reset with a matching proof
root; its shortest word length is one.

\paragraph{Evidence boundary.}
These are exact finite-algebra and software-contract statements for the frozen
models.  They do not establish noisy-posterior equivalence, optical phase,
contextuality completeness, FPGA timing, physical reset energy, laboratory
calibration, or device performance.
