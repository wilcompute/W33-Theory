\subsection{Pass 10941: triality VM five-front closure}

The requested rational conjugation of the 18-dimensional core to the
\emph{split} $D_4$ contact parabolic does not exist. Its Levi centroid is the
cubic field $K=\mathbb Q[\theta]/(\theta^3-\theta^2-53\theta-120)$, whereas
the split $A_1^3$ centroid is $\mathbb Q^3$; their idempotents differ. Two
independent rational charts into the correct trialitarian presentation agree
on all $103$ nonzero brackets and all $5832$ ordered Jacobi checks.

The central product
$\mathfrak h_7=\mathfrak h_4*_{Z}\mathfrak h_3$ compiles to a seven-mode
symplectic ABI. Twenty-four lane-local transvections have Lie closure
$\mathfrak{sp}_8\oplus\mathfrak{sp}_6$ of dimension $57$; one bridge
transvection gives $\mathfrak{sp}_{14}$ of dimension $105$, exactly at both
$p=3$ and $p=103$. With Heisenberg translations this is the seven-qutrit
Clifford/stabilizer instruction set. Universal quantum computation still
requires a non-stabilizer resource, but the ideal logical instruction path is
now explicit. Every listed transvection lowers exactly into the
$F_i,P_i,CZ_{ij}$ alphabet furnished by the fixed ancilla--memory interaction.
The programmed branch
\[
 K_m=3^{-1/2}X^{-m}FT,
 \qquad T=\operatorname{diag}(1,\zeta_9,\zeta_9^{-1}),
\]
therefore adds $T$ on any of the seven modes, giving approximately universal
seven-qutrit Clifford+$T$. Moreover its analyzer kets obey the exact identity
$|b_m\rangle=Z^{-m}|M_T^*\rangle$ for $|M_T\rangle=T|+\rangle$; analyzer
programming relocates the ninth-root magic resource rather than erasing it.
This is an ideal-resource compiler theorem. A protected analyzer or endogenous
magic factory, the physical photon--memory interaction, and a fault-tolerance
threshold remain open. Rank-one stabilizer outputs are not used.

The common $W_{90}$ also lifts beyond the earlier $\mathbb F_{103}$ witness.
On the signed-edge lattice the primitive integral central operator $A$
satisfies
\[
 A^2=48A,\qquad \operatorname{tr}(A)=4320,
 \qquad \operatorname{rank}_{\mathbb Q}A=90.
\]
Thus $A\mathbb Z^{240}$ is a $W(E_6)$-stable integral target lattice. Its
ranks modulo $2,3,5,7,103$ are respectively $14,25,90,90,90$, exposing the
bad-prime boundary without a characteristic-zero obstruction.

At each ramified prime $3,43,733$, the centroid word becomes
$(a,\dot a,c)\in\mathbb F_p[\epsilon]/(\epsilon^2)\times\mathbb F_p$.
Exact opcodes implement addition, multiplication, inversion, Horner
evaluation, and composition; $192$ polynomial and $192$ chain-rule checks
pass. Finally, the Killing-normalized compact-root schedule has microframe
weights $(4,5,5,4,5,5)$. For the declared reference area
$\theta_0=\pi/2$, the Duhamel bound gives
$\|\widetilde U-U\|\leq\exp(28\theta_0\rho)-1$; a one-percent target requires
$\rho\leq2.2623490581\times10^{-4}$. The separate declared survival target is
$0.90$ per cycle. These are falsifiable engineering requirements, not device
measurements or predictions.
