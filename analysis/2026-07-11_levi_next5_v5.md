# Five v5 audited witnesses: q=3 matrix algebra, prescribed-action extensions, typed E8 lanes, hybrid model, and hardware runtime

**Status: PASS.** Five independent executable witnesses, compact machine certificates, formal-source additions, a hybrid GDS/Verilog-A package, synthesizable frame-reducer RTL, and focused regression tests are included.

## 1. Exact q=3 matrix algebra and a separate W33 rank census

Over `F₂`, the transpose-sum operator on `Mat₃(F₂)` is

\[
T(Y)=Y+Y^T.
\]

Exact enumeration gives

\[
\ker T=\operatorname{Sym}_3(F_2),\quad |\ker T|=64,
\]

\[
\operatorname{im}T=\operatorname{Alt}_3(F_2),\quad |\operatorname{im}T|=8.
\]

The symmetric space has dimension six, the alternating space dimension three,
and the diagonal map on symmetric matrices has rank three.  No finite Fourier
transform or direct-sum map from this six-dimensional basis into the W33
incidence operator is constructed here.

The full native matrices independently return

\[
\operatorname{rank}_2M_3=25,
\quad \operatorname{rank}_2A_P=16,
\quad \operatorname{rank}_2A_L=10,
\]

and the Levi nilpotent ladder `[80,50,26,2,0]`, hence

\[
D_3\sim J_4^2\oplus J_3^{22}\oplus J_1^6.
\]

`formal/W33/HeisenbergQ3.lean` kernel-checks the finite matrix-space
cardinalities using `native_decide`. `formal/W33/FourierBlocks.lean` and
`OddQRank.lean` prove arithmetic/divisibility identities only; they do not
formalize incidence ranks.  The Fourier-to-incidence bridge and the arbitrary
odd-prime-power geometric rank theorem remain separate targets.

## 2. Periodic H² and the rail transgression

For the two-primary discriminant module

\[
A\cong (\mathbb Z/2)^{14}\oplus\mathbb Z/8
\]

with outer involution `τ`, periodic cyclic cohomology gives

\[
H^1(C_2,A[2])=\ker(1+\tau)/\operatorname{im}(1+\tau),
\]

\[
H^2(C_2,A[2])=\ker(1-\tau)/\operatorname{im}(1+\tau).
\]

Both spaces have dimension three. Therefore there are eight extension classes
for the prescribed, nontrivial (C_2)-action: one split class and seven
nonzero classes whose lifted outer generator has order four. They are not
central extensions. The displayed minimum-Hamming masks, weights, and
quadratic values use one selected Smith-coordinate basis and are not canonical
class labels.

The v4 scalar-five displacement obeys `[u]=[4h]≠0`, so it cannot be removed while preserving `τ(h)=5h+u`. However, in the exact sequence

\[
0\to A[2]\to A\to A/A[2]\to0,
\]

the connecting class of the order-eight rail is

\[
\delta([h])=[\tau(h)-h]=[u+4h]=0.
\]

An explicit minimum-weight gauge `v=0x411` gives a fixed element `h'=h+v` of exact order eight. This resolves the apparent tension: the **fixed-rail formulation exists**, while the **scalar-five formulation carries a genuine nonzero mixing class**.

## 3. Typed runtime realization of `240=72+6+6·27`

Every E8 root now has an executable address:

- 72 `E6_CONTROL` states;
- six pointwise-fixed `A2_REFERENCE` states, whose pairing-one graph is a six-cycle;
- six `MINUSCULE_PAYLOAD` lanes, each with 27 slots carrying
  (operatorname{SRG}(27,10,1,5)), the complement of the conventional
  Schläfli graph (operatorname{SRG}(27,16,10,8)).

The six payload copies are bundled by the same 27 classical cubic-surface line labels. Every payload root has precisely

\[
16\text{ positive controls}+16\text{ negative controls}+40\text{ orthogonal controls}.
\]

All six simple E6 reflections preserve lane identity, complement-Schläfli
adjacency, and the control-routing signature, exhaustively on every object.
A seeded 50,000-step random replay is retained as an integration smoke test;
it adds no proof strength beyond those exhaustive checks.

## 4. Hybrid PZT/Pockels architecture model below 100 mW

The 16-mode, 120-MZI mesh is recompiled from continuously heated phase shifters to:

- PZT piezo-optomechanical coarse trim;
- capacitive Pockels fine control;
- signed 16-bit commands with an explicit `±2π` full scale;
- closed-loop correction in a fixed-seed phase-error model.

The explicit architectural allocation is

\[
\boxed{85.607097\ \mathrm{mW}},
\]

including drivers, TIAs, digital servo, clock/reference, PZT refresh/leakage, and EO switching. In 128 fixed-seed Gaussian phase-error draws with full inverse-crosstalk recalibration, the fifth percentile is `0.999988590`; the 256-epoch tracked minimum is `0.999999673`. These are synthetic phase-model statistics, not die yield or measured foundry corners.

A deterministic GDSII placement-sketch generator and a separate hand-written Verilog-A interface model are supplied. CI parses all 1,968 GDS records, checks 392 boundary elements, regenerates the sketch twice, and requires byte-for-byte equality. The sketch contains reference rails and placement rectangles, not routed couplers or functional MZIs; the Verilog-A evidence is a static source-contract check, not a device simulation.

The mathematical decomposition has 120 Givens rotations and 376 phase
parameters, while the sketch contains 120 abstract interferometer placement
slots.  No one-to-one electrical manifest joining all 376 commands to those
placements is supplied.

## 5. Vendor-normalized adapters, reference reduction, and RTL smoke test

The runtime now includes dependency-injected adapters for:

- Swabian Time Tagger `createTimeTagger` + `TimeTagStream.getData`;
- the quTAG Python wrapper `getLastTimestamps(reset)` plus `getDataLost()` compatibility surface;
- NDJSON replay.

Missed/lost/full-buffer telemetry is rejected by the clean-read path. A 17-channel AXI-style SystemVerilog reducer snapshots bounded 24-bit counts, timestamps, overflow state, and zero-based frame IDs, holding a completed summary stable under output backpressure.

The deterministic **Python cycle reference** processed `1,048,832` events into 256 frames, exactly matching a simpler software reducer while exercising backpressure. The RTL evidence is separate: Icarus compiles the module and smoke-simulates two completed frame snapshots. Clean reference frames produce two parallel address projections:

\[
\boxed{\text{timestamp/count summary}\to W(3,3)\text{ point}_{40}}
\qquad\parallel\qquad
\boxed{\text{complement-Schl\"afli payload}_{162}\to E_6\text{ control}_{72}\to E_8\text{ root}_{240}}.
\]

The replay covers all 40 W33 addresses and all `6×27=162` payload addresses; each selected control has exact inner product `+1` with its payload root. The two projections are not claimed to be a W33-to-Schläfli incidence morphism: the existing 27-vertex guardrail rules out that subgraph interpretation.

## Installed commands

```bash
holonet fourier-geometry-v5
holonet extension-cohomology-v5
holonet e8-lanes-v5
holonet hybrid-compiler-v5
holonet hardware-runtime-v5
holonet levi-next5-v5
```

## Boundaries

The Lean surface proves finite matrix cardinalities and arithmetic identities,
not the Fourier-incidence rank theorem. The H² result classifies the
two-torsion extension problem for a prescribed action, not central or all
integral extensions. The degree-10 lane graph is the complement of the
conventional Schläfli graph, and the seeded replay is only smoke. The power
number is a modeled allocation; corner numbers are synthetic phase
simulations. The GDS is a record-valid placement sketch without PDK layers,
functional couplers, ports, DRC, extraction, or a 376-command device manifest.
The Verilog-A file is not simulated. Vendor APIs use documentation-shaped
dependency-injected doubles; physical instruments are not attached. The
million-event run is Python, while RTL evidence is the independent two-frame
smoke simulation. The aggregate now regenerates all five witnesses and
requires exact JSON-normalized equality with every cached certificate.
