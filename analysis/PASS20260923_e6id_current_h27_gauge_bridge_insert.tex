% Canonical E6-id to current physical-Clifford H27 gauge bridge.
\subsection*{The canonical cubic and current $H_{27}$ compiler gauges are explicitly conjugate}

The canonical signed $E_6$ cubic and the later physical-Clifford $H_{27}$
address chart use two independently anchored coordinate gauges. Their raw
$45$-line label sets are not identical: they overlap in only ten triads,
although all nine firewall center fibers lie in that overlap.

Fix the bridge by requiring
\[
  e6id\ 0\longmapsto (0,0,0)\in H_{27}
\]
and by sending the canonical bad-nine spread to the current pure-center spread
generated by $(0,0,1)$. Deterministic minimum-remaining-values propagation on
the colored $\operatorname{GQ}(2,4)$ incidence graph then fixes a $27$-point
bijection in $27$ search nodes. Under this permutation,
\[
  \{\text{45 canonical cubic triads}\}
  =
  \{\text{45 current five-direction }H_{27}\text{ cosets}\},
\]
and the nine firewall fibers map exactly to the nine current center cosets.

Consequently every Fourier/compiler statement below uses this explicit bridge
before applying the current $H_{27}$ Schrödinger representation. The older
Pass1103 coordinate table remains an exact historical Heisenberg gauge, but
the two gauges are not conflated.
