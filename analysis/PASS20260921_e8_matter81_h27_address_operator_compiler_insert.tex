% PASS20260921 incremental audit of the landed address/operator H27 theorem.
\subsection*{The Clifford gauge uniquely fixes the $H_{27}$ lift and the $Q_\psi$ clock leaves only its center}

The preceding address/operator $H_{27}$ certificate fixes five direction
subgroups and the matter factorization
$\mathbb C^{81}\cong\mathbb C^9\otimes\mathbb C^3\otimes\mathbb C^3$.
An independent replay agrees with every shared count.  It also removes the
remaining direction-lift ambiguity.  In addresses
$(a,b,c)=Z^aX^b\omega^c$, the fixed physical
$H_{27}:SL(2,3)$ dictionary has complement suborbits
\[
1,1,1,8,8,8.
\]
Exactly one of the three eight-orbits, after adjoining the two nonidentity
central elements, gives $\operatorname{SRG}(27,10,1,5)=GQ(2,4)$.  It uniquely
selects the five direction lifts
\[
\langle\omega\rangle,\quad
\langle\omega X\rangle,\quad
\langle\omega Z\rangle,\quad
\langle ZX\rangle,\quad
\langle\omega^2ZX^2\rangle.
\]
This is a uniqueness statement relative to the fixed split-complement gauge,
not an absolute choice without gauge data.  The selected lines and lifted
cubics independently replay the landed $9+36$ and $27+27+216$ orbit counts.
Even after anchoring the address identity at frame zero, there are exactly
$1920=|W(D_5)|$ incidence isomorphisms to the frozen frame carrier.  Selecting
the first map is deterministic bookkeeping, not a canonical physical
root-gauge intertwiner.

For the landed execution algebra
$\mathcal A_{\rm exec}=I_9\otimes M_9(\mathbb C)$ with commutant
$\mathcal A_{\rm exec}'=M_9(\mathbb C)\otimes I_9$, the $E_6$ charge
multiplicities $1_{4}+10_{-2}+16_{1}$ give an additional exact test.
For $D_{12}=\exp(2\pi iQ_\psi/12)$, exhaustive multiplicity checks give
\[
\langle D_{12}\rangle\cap\mathcal A_{\rm exec}'
=\langle D_{12}^{4}\rangle\cong C_3,
\]
the FI/common-$H_{27}$ center; the order-two and order-four shadows lie outside
the certified commutant.

\paragraph{Scope firewall.}
The uniqueness is an address-permutation result with the external phase label
held fixed.  The nine-dimensional factor is noiseless only relative to the
certified Pauli-$243$ algebra.  A full $H_{27}$-equivariant change of basis is
impossible: its rank is at most $9$ on the $27$-dimensional carriers.  On the
full scheduler/execution modules every
$(H_{27}\times C_3)$-equivariant compiler has rank at most $27$ in dimension
$81$.  The surviving frozen-root compiler must therefore change the symmetry
or retain only a proper common subgroup.
The subsequent complete Steiner-chart audit nevertheless closes the monomial
normalizer question uniformly: among all $51840$ compatible charts, only
$D_{12}^{0,4,8}$ normalize the execution algebra.
