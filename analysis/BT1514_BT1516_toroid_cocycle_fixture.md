# BT1514--BT1516: toroidal incidence, gauge cocycle, and fixture validation

## BT1514

The 7/21/3 count bridge passes a Szilassi-style incidence-count test.  There are seven face classes, twenty-one edge/flag classes, six incident edges per face, two incident faces per edge, and three two-edge local sectors per face.  This is not yet a concrete realization theorem; it proves incidence-count compatibility.

## BT1515

The missing residual-key algebra is now recorded as a gauge cocycle:

rho -> R^{-1} rho L.

The action is closed on S3 residual keys, has identity and inverse laws, and composes associatively.  Full decorated Aut(W33) orbit testing still needs transported gauge labels for automorphism generators.

## BT1516

The calibration fixture schema validates against the route-trace and CSS-replay shapes.  The validator regenerates the full 576-row fixture shape: eight native D4 generators, seventy-two rows per generator, 192 active rows, and 384 guard rows.

## Current synthesis

The toroidal 7/21/3 bridge is incidence-compatible, the residual cocycle law is algebraically sound, and the native calibration fixture is structurally validated against route traces and CSS replay.
