# Top five non-sequential continuations after Passes 2974–2983

1. **Complete the 495-shard general-isotropic M36 census.** Aggregate exact shard outputs, classify non-CSS collinearity witnesses by Clifford orbit, and evaluate first-order deep-M36 slope only for genuinely nonstabilizer accepted states.
2. **Minimum nonabelian localization schedule.** The parallel packet has a 29-triangle construction for all one- and two-edge `D4` faults. Prove the optimum or lower it, using the full `D4^36` gauge quotient rather than only support signatures.
3. **Golden-shell controller RTL.** Implement the `A4 = V4 semidirect C3` syndrome shell, the `1/phi^2` balanced event scheduler, and a fail-closed interface that prevents order-three commands from entering the `D4` core.
4. **Objectwise 540×12 clock identification.** Compare the parallel packet's 540 twelve-cycles with the older 540-fiber, spread, and chirality objects; either construct the equivariant bijection or produce a decisive orbit/subdegree obstruction.
5. **Laboratory receiver/router co-calibration.** Combine component-resolved OAM/slot calibration with the adaptive chirality posterior, then determine the optimal stopping rule under measured loss, crosstalk, erasure, dark-count, and operation-cost data.
