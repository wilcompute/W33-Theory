% Passes 4984--4991: residual A4 shell, audit, twin dark15, exact decoder, gauge and bonkers.
\subsection*{Residual $A_4$, complementary readout, and local affine gauge (Passes 4984--4991)}
The weight-four dual shell of the 36-dimensional switching code has the exact $W(E_6)$-orbit decomposition
\[
A_4=10530=810+6480+3240,
\]
where the three classes are the $\sigma$-even simple four-cycles of $H_{36}$ with respectively zero, one, and two chords.  The 9720 words generated by sums of two weight-three checks are precisely the one- and two-chord classes; the residual 810 is a single chordless orbit with stabilizer order 64.  This does not yet improve the rigorous covering-radius interval $134\leq\rho\leq173$.

The residual orbit admits a second, cubic-surface description.  Each residual square is unselected by exactly two tritangents.  The resulting map has image the 270 intersecting tritangent pairs and fibre size three.  For every such pair the six double-sixes missed by both induce the octahedral graph $K_{2,2,2}$, and the three residual checks are its three equatorial squares.  Hence
\[
810=270\cdot3
\]
is an exact three-fold octahedral-equator bundle.

Let $C$ be the $36\times40$ spread--line incidence reader and $M$ the $45\times36$ tritangent selector.  For the stacked readout $R=[C^T;M]$ one has
\[
R^TR=18I_{36}+22J_{36},
\qquad
R^\dagger=\left(\frac1{18}I-\frac{11}{7290}J\right)R^T.
\]
Thus for measurements $y_L=C^Tx$ and $y_T=Mx$,
\[
x=\frac1{18}(Cy_L+M^Ty_T)-\frac{11}{90}\left(\sum y_L\right){\bf1}.
\]
The nonconstant squared singular value is exactly 18.  Thirty-six rows are minimal, and exact full-rank bases exist with compositions $15+21$ and $16+20$ line/tritangent rows.  Centering the line rows by $1/4$ and tritangent rows by $2/3$ yields frame operators $18P_{15}$ and $18P_{20}$, so their union is an equal-bound tight frame $18P_{{\bf1}^{\perp}}$ on the 35-dimensional mean-zero spread carrier.

The point- and line-side dark fifteens remain inequivalent.  Point--line incidence annihilates both; the 80-vertex Levi graph has exact nullspace $15_p\oplus15_\ell$ of dimension 30.  The point and line-intersection graphs have binary adjacency ranks 16 and 10, respectively, so there is no incidence-preserving point--line side swap at $q=3$.  Together with the negative PGSp outer-twist test, the honest symmetry-preserving dark carrier is the twin 30-space rather than a hidden $15\leftrightarrow15$ operator.

Finally, the twelve local $AG(2,3)$ completions remain a single orbit even under the PSp index-two subgroup selected by Witting phase, so the finite outer sign does not choose a qutrit coordinate plane.  Their ambiguity is nevertheless structured: the disjointness graph of completions is $4K_3$, and the four 3-packets are uniquely equivariantly indexed by the four points of the underlying W33 line.  A distinguished line point reduces the ambiguity to a residual triple, but the bare incidence geometry does not select such a point.

Pass4985 also hardens the collided Pass4968--4972 repair: graph automorphisms preserve the 24- and 15-dimensional adjacency eigenspaces; the nontrivial Ihara root fields are $\mathbb Q(i\sqrt{10})$ and $\mathbb Q(i\sqrt7)$; and the exact W33 critical group is $(\mathbb Z/10)^8\oplus\mathbb Z/40\oplus(\mathbb Z/160)^{14}$.  Finite outer sign, critical-group numerology, and hardware coordinate labels are kept separate from physical identifications unless an independent mechanism is supplied.
