# SU(2) Level-12 D8 ADE Bridge Theorem

Date: 2026-05-29

This continues the H7 projector audit. The previous result corrected the uploaded seven-sector TQC lead: the 7x7 matrix is not the full SU(2)_12 modular S-matrix. It is the even-label block, and it satisfies

```text
H H^T = (I + R)/2.
```

The new step is to identify the exact ADE modular-invariant object containing this projector.

For SU(2) level k=12, the ADE case is D8:

```text
k = 12 = 4*3,
D_(2*3+2) = D8,
Coxeter number h(D8)=14=k+2.
```

The D8 modular invariant on labels 0,...,12 is

```text
Z_D8 = |chi_0 + chi_12|^2
     + |chi_2 + chi_10|^2
     + |chi_4 + chi_8|^2
     + 2 |chi_6|^2.
```

Let M be its 13x13 coefficient matrix. Then

```text
M^2 = 2M,
spec(M)=2^4 + 0^9,
rank(M)=4.
```

Thus M/2 is a rank-4 projector.

## The exact H7 bridge

Restrict M to the seven even labels

```text
0,2,4,6,8,10,12.
```

Then

```text
M_even = I + R.
```

Since the H7 audit proved

```text
H H^T = (I+R)/2,
```

we get the new exact identity

```text
M_even = 2 H H^T.
```

So the H7 projector is not an isolated object. It is exactly the even-label restriction of the SU(2)_12 D8 ADE modular invariant.

## Distance splitting

The projector has rank 4 and nullity 3:

```text
rank(H)=4=d_Z,
nullity(H)=3=d_X.
```

Therefore

```text
Phi6 = 7 = d_X + d_Z,
k = 12 = d_X d_Z.
```

This upgrades the prior result:

```text
H7 is the even block of the D8 ADE invariant, and its rank/nullity are the W33 CSS distances.
```

## D8 graph skeleton

The D8 Dynkin graph has 8 vertices and 7 edges. Its degree sequence is

```text
1,2,2,2,2,3,1,1.
```

Therefore

```text
sum degrees = 14 = k+2 = dimG2,
sum degree^2 = 28 = mu = v-k,
edges = 7 = Phi6.
```

The graph gives a second exact bridge:

```text
vertices(D8)=8=trace(M),
edges(D8)=7=Phi6,
degree sum(D8)=14=k+2,
degree-square sum(D8)=28=mu.
```

The D8 adjacency spectrum is generated by Coxeter exponents

```text
1,3,5,7,7,9,11,13
```

via

```text
lambda_m = 2 cos(pi*m/14).
```

The characteristic polynomial is

```text
x^2 * (x^6 - 7*x^4 + 14*x^2 - 7).
```

The zero eigenvalue has multiplicity 2, matching the doubled exponent 7.

## Verified identities

The verifier `analysis/w33_su2_12_ade_bridge.py` checks:

| Identity | Result |
|---|---:|
| level 12 is D8 ADE case | true |
| Coxeter number equals k+2 | 14 |
| M_even = 2 H H^T | true |
| M_even = I+R | true |
| M^2 = 2M | true |
| M commutes with S and T | true |
| rank(M)=d_Z | 4 |
| trace(M)=8 | true |
| entry sum(M)=14 | true |
| Frobenius square sum(M)=16=E2 | true |
| D8 edges=Phi6 | 7 |
| D8 degree sum=k+2 | 14 |
| D8 degree-square sum=mu | 28 |
| D8 spectrum matches Coxeter exponents | true |

## Interpretation

The corrected train of thought is now:

```text
uploaded H7 lead
  -> even SU(2)_12 block
  -> H H^T=(I+R)/2
  -> rank/nullity = 4/3 = d_Z/d_X
  -> SU(2)_12 D8 ADE invariant M
  -> M_even = 2 H H^T
  -> D8 graph skeleton recovers Phi6, k+2, mu.
```

This is the cleanest version of the TQC connection so far. The seven-sector object is not a free-standing seven-anyon model. It is the degenerate even block of the D8 ADE modular invariant, and that degeneracy is precisely the W33 CSS distance split.

## Current honesty boundary

This proves a finite modular-invariant/projector bridge. It still does not construct a physical braiding representation, continuum Hamiltonian, or experimental prediction. The next valid step is not to claim full TQC hardware, but to test whether the D8 nimrep paths act naturally on the already-computed minimal logical X/Z incidence geometry.
