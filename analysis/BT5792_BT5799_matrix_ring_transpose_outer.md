# Passes 5792–5799 — the q=5 Latin carrier is an affine `M_2(F_2)` left/right geometry

## Executive result

Passes 5776–5783 reconstructed the q=5 Reye zero shell as a Klein `V_4` Latin square and exposed the common rank-nine projector shared by the point, heavy, and Reye-line carriers.  The remaining mystery was the source outer involution: why are the point and heavy degree-12 actions permutation-equivalent but not internally conjugate?

There is now a concrete answer.

Choose two copies

\[
V=W=\mathbb F_2^2.
\]

The full centerless order-576 Latin carrier is

\[
\boxed{
G_{576}
=\operatorname{Hom}(W,V)times\bigl(GL(V)\times GL(W)\bigr).
}
\]

After bases are chosen,

\[
\operatorname{Hom}(W,V)\cong M_2(\mathbb F_2),
\]

so

\[
\boxed{
G_{576}\cong
M_2(\mathbb F_2)_{+}:\bigl(GL_2(2)\times GL_2(2)\bigr),
}
\]

with

\[
16\cdot6\cdot6=576.
\]

This is not an order coincidence: the verifier constructs all 576 affine left/right maps explicitly and replays the q=5 point/heavy/line actions from them.

## Pass 5792 — the normal 16-space is literally the additive matrix ring

Write a group element as

\[
g=(X,A,B),
\qquad
X\in M_2(\mathbb F_2),\quad
A,B\in GL_2(2),
\]

acting on the 16 matrices by

\[
\boxed{
M\longmapsto A M B^{-1}+X.
}
\]

The matrix space has exactly

\[
16=6+10
\]

elements split into six invertible matrices and ten singular matrices.  The group generated by arbitrary translations `X` and left/right units has exactly 576 distinct affine permutations.

The six-unit/ten-singular census is classical for `M_2(F_2)`.  Saniga–Planat–Pracna use this same noncommutative ring in their projective-ring model of the two-qubit Pauli geometry (`quant-ph/0611063`).  That is external prior art; the new statement here is the exact coordinate bridge to the q=5 Latin carrier.

## Pass 5793 — the heavy blocks are affine bilinear hyperplanes

The 12 point-carrier objects admit the uniform form

\[
\boxed{
P=\{(w,x):0\ne w\in W,\ x\in V\}.
}
\]

The three nonzero `w` values are the row, column, and symbol classes recovered in Pass 5777; the four `x` values are the Klein labels.

For a nonzero covector `\phi\in V^*` and arbitrary `\psi\in W^*`, define

\[
\boxed{
H_{\phi,\psi}
=\{(w,x)\in P:\phi(x)+\psi(w)=1\}.
}
\]

There are `3*4=12` such sets, each of size six.  Under the explicit q=5 chart from Pass 5778, the verifier proves

\[
\boxed{
\{H_{\phi,\psi}\}
=\{\text{the 12 frozen multiplicity-six heavy supports}\}.
}
\]

The complementary equation

\[
\phi(x)+\psi(w)=0
\]

gives exactly the 12 Klein intercalate supports.  Thus the Pass5779 complement theorem becomes a single affine equation:

\[
\boxed{
\text{intercalate / heavy}=0/1\text{ level sets of one bilinear form.}
}
\]

## Pass 5794 — the affine matrix model replays all three carrier characters

The point action induced by

\[
M\mapsto AMB^{-1}+X
\]

is

\[
\boxed{
(w,x)\mapsto
\bigl(Bw,\,Ax+X(Bw)\bigr).
}
\]

The dual heavy action is

\[
\boxed{
(\phi,\psi)\mapsto
\left(
\phi A^{-1},
\psi B^{-1}+\phi A^{-1}X
\right).
}
\]

The sixteen Reye/Latin triples are the graphs of the sixteen linear maps `W->V` restricted to the three nonzero `w` values.

Exhausting all 576 triples `(X,A,B)` gives faithful permutation images of order 576 on all three carriers.  Both 12-actions have stabilizer order 48 and subdegrees

\[
\boxed{1,3,8}.
\]

The complete permutation-character Gram is

\[
\boxed{
\left(
\langle\pi_i,\pi_j\rangle
\right)_{i,j=P,H,L}
=
\begin{pmatrix}
3&2&2\\
2&3&2\\
2&2&3
\end{pmatrix}.
}
\]

This exactly replays the frozen Pass5667–5674 GAP certificate in a completely different model.

The point permutation sign also reproduces

\[
\boxed{
\bigl(
\langle\varepsilon\pi_P,\pi_P\rangle,
\langle\varepsilon\pi_P,\pi_H\rangle,
\langle\varepsilon\pi_P,\pi_L\rangle
\bigr)
=(0,0,0).
}
\]

So the matrix model independently sees the sign firewall of Pass 5781.

## Pass 5795 — transpose normalizes the group but is not in it

Let

\[
\Theta(M)=M^T.
\]

Conjugating an affine left/right map by transpose gives

\[
\Theta\circ(X,A,B)\circ\Theta
=
\boxed{
\tau(X,A,B)
=
(X^T,B^{-T},A^{-T}).
}
\]

The verifier checks this identity on all

\[
576\times16
\]

group-element/matrix pairs.  It also checks

\[
\boxed{\tau^2=1}.
\]

But transpose itself is **not** one of the 576 maps `M -> A M B^{-1}+X`.  Therefore it is an external normalizer involution of the affine Latin carrier.

Transpose fixes exactly eight of the sixteen matrices, namely the symmetric `2 x 2` binary matrices.

## Pass 5796 — transpose is exactly the point/heavy factor swap

Using the chosen bases, identify a column vector with its transpose row covector and define

\[
\boxed{
F:P\to H,
\qquad
F(w,x)=(w^T,x^T).
}
\]

The exact intertwining law is

\[
\boxed{
F(g\cdot p)=\tau(g)\cdot F(p)
}
\]

for every one of the `576*12` element/point pairs.

Thus the heavy degree-12 action is the point action twisted by the transpose factor-swap automorphism.

The outerness is intrinsic, not merely inferred from `\Theta\notin G`.  Intersect a point stabilizer and a heavy stabilizer with the normal translation group `T=M_2(F_2)_+`.

For a point stabilizer,

\[
T_P=\{X:Xw=0\},
\]

a four-element subspace with fixed **right kernel**.  Its aggregate image spans all of `V`.

For a heavy stabilizer,

\[
T_H=\{X:\phi X=0\},
\]

a four-element subspace with image in one fixed **left-annihilator line**.  Its aggregate image has dimension one.

Conjugation inside the affine group acts on translations as

\[
X\mapsto AXB^{-1},
\]

which preserves these two geometric types.  Hence the two stabilizer classes cannot be internally conjugate.  Transpose exchanges them exactly:

\[
\boxed{T_P^T=T_H.}
\]

This is the concrete finite-algebra mechanism behind the abstract Pass5674 carrier outer involution.

## Pass 5797 — relation to the two-qubit matrix-ring literature

The appearance of `M_2(F_2)` is not itself new.  Saniga, Planat and Pracna, *Projective Ring Line Encompassing Two-Qubits* (`quant-ph/0611063`), use the projective line over the noncommutative ring `M_2(F_2)` to encode the commutation geometry of the 15 nonidentity two-qubit Pauli operators.  Their ring has the same 16 underlying matrices and the same six-unit/ten-zero-divisor split.

The repo-specific theorem is narrower and different:

\[
\boxed{
\text{q=5 Reye/Latin translation space}
\cong M_2(F_2)_+
}
\]

with the exact left/right unit action and transpose outer involution above.

We do **not** identify q=5 W33 carrier points with two-qubit observables, ring-projective points, entanglement classes, or physical states.  Establishing any context-preserving bridge to the two-qubit projective-line geometry would require a separate object map and separate verifier.

## Evidence boundary

This packet is exact finite affine geometry, Latin-square combinatorics, matrix algebra, and permutation representation theory.  It gives a concrete origin for the point/heavy carrier outer involution.  It does not derive a quantum degree of freedom, a qubit embedding, dynamics, gauge interactions, masses/couplings, or physical unification.
