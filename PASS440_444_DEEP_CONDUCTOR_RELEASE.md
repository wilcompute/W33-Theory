# Passes 440–444 — deep conductor release

This release executes the five v1.3 frontier moves as one connected structure.

## Closed fronts

1. **Finite-chain-ring conductor tower.** The length-two field/ring atlas is
   extended to every central-character conductor depth `j=1,...,n`, with exact
   spectra and every prime-to-characteristic Smith layer.
2. **Machine-formal kernel.** The repository's existing Lean/Mathlib package
   proves the constructive paired-block reduction and all conductor/multiplicity
   algebra without `sorry` or custom axioms.
3. **Blind optical protocol.** A sealed-label, transfer-matrix-aware 192-sample
   preregistration rehearsal scores 192/192 with classifier and abstention
   threshold frozen before reveal.
4. **Section-sensitive torsion.** The 81 inverse-closed q=3 sections are exactly
   a 9-element zero-curl orbit and a 72-element nonzero-curl orbit, with distinct
   characteristic polynomials and critical groups.
5. **Hjelmslev geometry.** Conductor-one and primitive spectral magnitudes are
   the residue-plane and within-neighborhood eigenspaces of the affine
   Hjelmslev incidence Gram operator.

## Strongest combined statement

The same filtration is visible in four languages:

- additive-character conductor;
- integral Smith layer;
- section curvature;
- Hjelmslev geometric resolution.

The photonic Nyquist discriminator is therefore testing whether the register
supports only residue-level modes or also the within-neighborhood primitive
layer—not merely detecting an isolated period.

## Validation boundary

All five Python certificates regenerated byte-for-byte locally, and the focused
suite passed 5/5 tests. The Lean source was structurally audited locally and is
integrated into the existing `formal/W33` package; actual Lean compilation is
owned by the committed Actions job because Lean is not installed in the local
execution container. No laboratory-data claim is made.
