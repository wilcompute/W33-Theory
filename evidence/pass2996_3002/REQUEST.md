# Passes 2996–3002 observable evidence request

This branch exists only to retain stable observed evidence for the source-complete packet already published on `master`.

The dedicated workflow must establish:

1. the compressed and materialized-readable generators both report `PASS 7 / 7`;
2. all eight focused regressions pass;
3. the curvature synchronizer passes all 444 clean/single-substitution RTL cases;
4. the decoder synthesizes and places on HX8K, with volatile logs retained only as artifacts;
5. blueprint and site integration are idempotent;
6. the W33 paper, Photonic Holonet, and machine-blueprint PDFs compile;
7. stable hashes and a one-shot completion marker are committed to this branch.

Do not infer laboratory optical performance, deletion/erasure tolerance, physical Landauer energy, or global fixed-schedule minimality of 29 from a green digital workflow.
