# Passes 2960–2966 observed-evidence branch

This branch exists only to obtain and retain observable evidence for the source-complete physical-compiler packet already published on `master`.

The dedicated workflow must materialize the readable exact source, pass the seven focused regressions, simulate all three RTL contracts, synthesize and place the datapaths, integrate the machine blueprint and site idempotently, compile all three canonical PDFs, and commit the resulting logs and hashes back to this branch.

Do not merge this branch until that workflow is green and the generated evidence has been inspected.
