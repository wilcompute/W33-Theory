# PART CCCCXLVII — E8 Dot-Orbit Constant-Rescue Law

This part upgrades CCCCXLVI from sampled class stability to an exact,
class-wide law.

## Construction

Use explicit automorphism generators on doubled E8 roots:

- adjacent coordinate swaps,
- even sign flips (flip coordinate $0$ and $k$).

These induce an action on unordered root pairs $(a,b)$.

## Dot-class pair structure

For doubled inner product $d=a\cdot b$, unordered pairs split as:

$$
\#(d=8)=240,\; \#(d=-8)=120,\; \#(d=4)=6720,\; \#(d=-4)=6720,\; \#(d=0)=15120.
$$

## Orbit decomposition + invariance

- Each dot-class decomposes into generated orbits.
- Third-reference rescue count is invariant under these generators.
- Orbit representatives across the same dot-class all return the same constant.

Therefore all pairs in a fixed dot-class share one exact rescue value.

## Exact rescue law

$$
d=\pm 8 \Rightarrow 126,\qquad d=\pm 4 \Rightarrow 234,\qquad d=0 \Rightarrow 240.
$$

So this is an exact all-pairs law (via orbit decomposition under the generated
automorphism action), not only sampled stability.

## Honesty boundary

This proves exact class-wise constants under the explicit generator group used.
It does not claim to present the full Weyl-group generators.
