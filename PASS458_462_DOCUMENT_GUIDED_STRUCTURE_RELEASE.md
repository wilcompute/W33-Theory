# Passes 458–462 — document-guided structure release

This release executes all five continuations after Pass 457 while treating the four supplied documents as hypothesis generators rather than evidence.

1. **Collision invariant:** first 2-WL rank 19 versus 18; rooted Terwilliger trace 622 versus 650.
2. **Prime-power covariance:** exact theorem over every odd \(q=p^f\), with q=9 and q=25 witnesses.
3. **Switching search:** only the two endpoints survive the 1024-point phase cube; 420 natural switching masks fail.
4. **Integral separator:** discriminant modules `(Z/3)^3` versus `(Z/3)^9 + (Z/9)^9`; center depth 1 versus 3.
5. **Formal L1:** complete q=3 rim inclusion and `4/0` common-neighbor cardinalities in Lean source.

All Python certificates are deterministic. The Lean module contains no `sorry` or custom axiom; remote compilation remains the authoritative type check.
