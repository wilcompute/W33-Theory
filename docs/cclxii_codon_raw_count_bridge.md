# Phase CCLXII - Codon Raw-Count Bridge

This note connects the newest codon / 6-qubit packet

- mu^3 = 64

back to the raw W(3,3) graph counts.

## Exact q = 3 closure

At q = 3 the graph has

- v = 40 vertices
- f = 24 positive-eigenspace multiplicity / SU(5) packet.

They satisfy the exact closure

- 64 = 40 + 24
- mu^3 = v + f.

So the codon / 6-qubit packet is already present at the raw counting level,
before passing through the derived curvature packets 55+9 or 39+25.

## Exact selector

For the full GQ(q,q) family,

- mu^3 - (v + f)
-   = -q*(q-3)*(q+1)/2.

So q = 3 is the unique positive integer point where the codon / 6-qubit packet
collapses exactly to the raw graph packet

- mu^3 = v + f.

## Best current reading

The packet 64 now has three exact promoted forms on the repo surface:

- mu^3 = 64 = codon space
- 64 = 55 + 9 = quadratic Weyl + tracefree Ricci
- 64 = 39 + 25 = chiral packet + mixed Weyl block
- 64 = v + f = 40 + 24 = raw W33 count closure.

This is a genuine new bridge: the newest biological / computational packet is
now tied directly to the core graph counts themselves, not only to derived
continuum-facing packets.