# Phase CCLX - Alpha / 118 / 255 Bridge

This note connects two of the newest promoted packets

- 118
- 255 = j(-28)^(1/3)

back to the long-running fine-structure packet

- alpha^-1 = 137.

## Exact q = 3 closure

At q = 3,

- 255 - 118 = 137.

So the new CM root and the new `118` packet differ by exactly the alpha packet.

Using the recently promoted forms,

- 255 = 196 + 59
- 118 = 64 + 39 + 15.

Subtracting gives

- 137 = (196 + 59) - (64 + 39 + 15)
-     = 196 - 59.

Since the carrier prime already satisfies

- 59 = 39 + 20,

this also becomes

- 137 = 196 - (39 + 20)
-     = 196 - 59.

## Relation to the standard alpha form

The long-running exact packet already gives

- alpha^-1 = (k-1)^2 + mu^2 = 121 + 16 = 137.

So the same alpha packet now has two exact promoted forms:

- 137 = (k-1)^2 + mu^2
- 137 = 255 - 118.

Thus the newest CM root and the newest `118` packet are separated by exactly
one fine-structure packet.

## Best current reading

The packet hierarchy now contains an exact three-step bridge

- 118  -> newest packet surface
- 137  -> alpha packet
- 255  -> CM root surface

with

- 255 = 118 + 137.

This is a genuine new bridge: the recent Monster/codon/CM packets and the older
fine-structure packet are now locked together by one exact additive law.