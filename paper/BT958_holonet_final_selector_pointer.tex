% BT958 -- Holonet pointer to the final W33 E8 selector theorem

\paragraph{Canonical E8 selector status.}
The heavy-math companion \texttt{w33\_paper.tex} now contains the final
BT957 support--metric E8 selector theorem.  The exact support-minimal value is
60, with six support-minimal decompositions, and both independent metric gauges
(vertex E8 and tetracode E8) select the same hyperbolic decomposition
\[
  [(3,68),(4,42),(38,65),(90,144)].
\]
Thus the Holonet architecture may treat the E8 selector as a theorem-backed
companion result rather than a heuristic routing convention; the remaining
refinement is only the full transported tetracode stabilizer orbit.
