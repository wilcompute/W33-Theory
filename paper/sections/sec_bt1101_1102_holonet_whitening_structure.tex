\section{BT1101--BT1102 Holonet Whitened Readout and Gauge Bracket}
\label{sec:bt1101-bt1102-holonet-whitening-structure}

The holonet reservoir readout has now been whitened.  With
\[
KK^*=\frac13 I_{12}+\frac13J_{12},
\]
the packet space splits into the uniform line and eleven transverse packet directions.  The whitening operator is
\[
W=\sqrt{\frac3{13}}P_u+\sqrt3\,P_t,
\]
so
\[
K_{\mathrm{white}}=WK,
\qquad
K_{\mathrm{white}}K_{\mathrm{white}}^*=I_{12}.
\]
Operationally, this turns the reservoir readout into an orthonormal $12$-channel packet while preserving the $54$-dimensional kernel of matter-bookkeeping and trace redundancies.

The packet bracket is also normalized.  The real runtime gauge packet is
\[
\mathfrak u(1)\oplus\mathfrak{su}(2)\oplus\mathfrak{su}(3),
\]
with compact anti-Hermitian generators normalized by the positive trace pairing.  The $\mathfrak u(1)$ channel is central, while
\[
[W_a,W_b]=-\sqrt2\epsilon_{abc}W_c,
\qquad
[C_a,C_b]=-\sqrt2f_{abc}C_c.
\]
Thus the runtime packet now has both an orthonormal readout metric and a fixed bracket table before physical coupling constants are applied.
