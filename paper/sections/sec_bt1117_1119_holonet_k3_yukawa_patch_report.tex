\section{BT1117--BT1119 Holonet K3 Target and Yukawa Map}
\label{sec:bt1117-bt1119-holonet-k3-yukawa-patch-report}

The holonet runtime boundary is now clean.  Finite packet ratios are W33 seed-independent data; dimensionful gravity requires the K3 spectral-action compute.  The compute lane must output $A_0$, $A_2$, and $A_4$ for the chosen K3 metric/refinement, with normalization and curvature conventions stated.

For generation data, the runtime weights remain projector data.  The preferred Yukawa route is a phase-transfer map: the weights determine the generation phase/order, while the Yukawa square-root vector uses a separate Koide-radius map.  Thus the runtime does not confuse reservoir projector strength with physical mass amplitude.

The source patch path is split into small helpers for the W33 and holonet papers, avoiding large unsafe TeX replacement.
