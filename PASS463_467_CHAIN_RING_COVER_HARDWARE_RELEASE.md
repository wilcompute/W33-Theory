# Passes 463–467 — chain-ring, cover-law, and hardware frontier release

This package executes all five v1.7 continuations while keeping unresolved physical and uniform-formal boundaries explicit.

## Pass 463 — exact q=5 collision mechanism

The genuine Pass-456 collision is a square/nonsquare central-character sheet exchange. The two faithful quintics are Galois conjugates over \(\mathbb Q(\sqrt5)\), and their norm is exactly the common degree-ten rational factor. The graphs reverse which quintic is assigned to the square and nonsquare central characters. This closes the adjacency-algebra mechanism without claiming a graph isomorphism or switching trade.

## Pass 464 — finite chain-ring covariance

For \(R=\mathbb Z/p^n\), exact block arithmetic proves \(\sigma_a(B_t)=B_{at}\), Hermiticity, conductor stratification, radical size \(p^{2v_p(t)}\), and totally ramified coefficient orders \(\mathbb Z[\zeta_{p^{n-v_p(t)}}]\). The package includes exact \(\mathbb Z/9\) and \(\mathbb Z/25\) witnesses.

## Pass 465 — q=3 cover law L1–L4

The Lean source extends Pass 462 through L2, L3, fiber independence, and L4. The q=3 intersection array is \(\{8,6,1;1,3,8\}\) with shells \(1,8,16,2\). Uniform parameter identities are proved symbolically. The all-prime-power geometric cardinality theorem remains open.

## Pass 466 — Smith/Bockstein ramification

The universal kernel-growth theorem recovers Smith multiplicities as second differences. For Pass 448, the exponent-six gap is the exact plateau \(d_6=d_7=18\), located at the primitive ninth-cyclotomic ramification index \(\varphi(9)=6\). The top layers are recovered as \(11\) copies of exponent 7 and \(7\) copies of exponent 8.

## Pass 467 — hardware runner

The frozen Pass-451 classifier now has a fail-closed measured-data parser, exact transfer-matrix interface, sealed prediction file, hash-bound reveal, and endpoint scorer. A deterministic synthetic rehearsal verifies software behavior. No measured transfer matrix or holdout is present, so the physical gate remains open and no laboratory result is claimed.

## Reproducibility

The workflow regenerates all five JSON certificates, runs the focused regression suite, and builds the integrated Lean package in a separate pinned job. The hardware templates are under `hardware/pass467/`.
