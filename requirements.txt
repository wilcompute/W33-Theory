# Core dependencies
numpy>=1.20.0
sympy>=1.9

# Testing
pytest>=7.0.0
pytest-json-report>=1.5.0

# Optional dependencies (install separately if needed):
# - SageMath (https://www.sagemath.org/) - required for full algebraic verification
#   Install via: conda install -c conda-forge sage
#   Or use Docker image: sagemath/sagemath

# Topological Data Analysis tooling (recommended for TDA prototypes):
ripser>=0.6.2  # fast persistent homology (ripser.py)
persim>=0.9     # persistence diagram distances / plots (scikit-tda)
giotto-tda>=1.1  # higher-level TDA utilities (optional)

# Note: these packages are optional and only required for the TDA analyses in
# notebooks/quantum_photonics/gbs_benchmark_full.ipynb
