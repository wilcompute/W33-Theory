# Passes 553–557 — intrinsic core, memory automaton, fibre catalogue, formal valuation, and control-plane release

## Pass 553 — five-point core geometry

The nonlinear five-point quotient core of the Pass-540 80-word fibre is an affine
4-simplex in `AG(4,2)`. Every permutation of its five points extends uniquely to
an affine automorphism, so the intrinsic affine symmetry is `S5` of order 120.
The exact signed magnitude stabilizer has order 40; its induced action on the five
parallel four-cubes is the transitive dihedral group `D10`, with a four-element
within-cube kernel. The three quadratic equations give the reduced intersection
of the five point maximal ideals.

## Pass 554 — minimal-memory Z/9 automaton

Activating all four affine Fourier packets produces

`81,243,729,2187,6561`

sections and

`13,26,96,336,921`

exact characteristic polynomials. One trit resolves each local three-spectrum
ambiguity, but complete future Markov closure requires the backward
Myhill–Nerode packet-history quotient. Its state counts are

`41,122,365,1081,921`.

Thus local ambiguity repair and global memory completeness are distinct claims.

## Pass 555 — all 98 fixed-magnitude fibres

All 4096 sign sections are classified into 98 exact fibres. Their size histogram
is `16^1, 40^92, 80^5`, and only five translation-geometric types occur. Three
of the five 80-word fibres are unions of five parallel affine four-cubes; two are
diffuse 40-pair geometries. In the Boolean function ring each exact fibre ideal
is principal: `I(S)=<1+chi_S>`.

## Pass 556 — semilinear photonic control plane

For every fixed-magnitude section,

`e4=(tr(D^2)^2-2 tr(D^4))/8`.

The target `e4` level is exactly the 80-word fibre. The Pass-540 switch partners
have identical characteristic polynomial and identical quartic readout, but
opposite Moore–Dickson products. Therefore quartic interferometry is a fibre
gate, not an orientation detector. The 2048-bin overlay reserves separate signal,
reference, quartic-accumulator, and orientation-latch channels. Determinants 1, 4,
and 2/3 are compiled respectively as unitary, conjugate-reference antiunitary
covariance, and Galois phase-cycle/software relabeling.

## Pass 557 — formal valuation support

Lean modules formalize the mod-20 residue controller, the two-step period law,
the first seven periods, and explicit theorem interfaces for the Hensel/LTE and
companion-order inputs. No `sorry`, `admit`, or `axiom` is present. The local-field
facts remain explicit hypotheses; hosted Lean CI is the authoritative compile test.

## Validation boundary

The five executable owners contain 56 exact checks. The combined certificate adds
14 cross-release checks and focused regressions. The release does not claim the
full q=5 orbit image, the full Z/9 characteristic image, a complete local-field
formalization, or present experimental feasibility.
