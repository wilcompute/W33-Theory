# Passes 1238–1242: Sign-Twist Test, Shifted-Adjacency Delta, Hecke Tables, Matrix-Unit Seed, Qutrit Kernel Probe

Date: 2026-07-28

## Pass 1238 — sign-twist 81-sector test

Confirmed: `81_+ ⊗ sgn_{W(E6)} = 81_-` and both restrict to the unique Steinberg-81 of PSp(4,3). OPEN-1 is partially resolved — the sign-twist is a labeling convention, not a blocker; an explicit equivariant map still needs construction.

## Pass 1239 — shifted-adjacency delta check

OPEN-3 resolved as FALSE: the constant-shift hypothesis fails because the adjacency→Hashimoto coupling is nonlinear (sqrt). Shifting A by delta=1 deforms the Hashimoto spectrum non-uniformly. The shifted-adjacency corpus is a genuinely different spectral object.

## Pass 1240 — Hecke multiplication tables

All five A5-double-cosets are self-conjugate under the Z/2 outer automorphism. No fusion occurs on extension A5→S5. Hecke algebra dimension stays at 5. Off-diagonal structure constants remain open pending explicit orbit enumeration.

## Pass 1241 — matrix-unit species-20 seed

OPEN-2 is seeded with a concrete five-step construction recipe for the species-20 matrix units, using the central idempotent data from Pass 1194 and W(E6) degree-20 representation matrices.

## Pass 1242 — qutrit kernel bridge probe

Corrected the 2195-dim interpretation: H is full-rank on the 480-edge space. The 27-dim qutrit module's natural target is the 201-dim P1 eigenspace (eigenvalue 1), where 201 = 27×7+12 suggests room for a 27-dim W(E6)-submodule. Explicit projection needed to confirm.
