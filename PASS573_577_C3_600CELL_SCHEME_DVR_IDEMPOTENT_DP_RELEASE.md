# Passes 573–577 — hidden C3, 600-cell apex, Singer scheme, cyclotomic kernel, exact idempotents, and posterior DP

## Pass 573 — the dominant Hjelmslev triples are C3 orbits

The structured `F3^13` section family is preserved by exactly six elements of
`SL(2,Z/9)`. Modulo global sign they form the cyclic group generated by

```text
[1 0]
[3 1]
```

of projective order three. Its action preserves every exact characteristic
polynomial in the 797,162-word projective family.

The projective parameter space splits into 265,356 three-cycles and 1,094 fixed
points. Of the 177,400 characteristic-polynomial fibres of projective size
three, exactly 177,377 are single C3 orbits. Only 23 are triples of fixed
points. Quotienting by this C3 leaves 266,450 symmetry orbits and 221,451
spectra, hence 44,999 residual collisions.

### Exact 600-cell comparison

In standard `Z[phi]` coordinates, a 600-cell vertex has exactly twelve nearest
neighbors. They share one affine hyperplane, and their induced graph has 12
vertices, 30 edges, degree five, and 20 triangular faces: an icosahedron. The
chosen vertex is the thirteenth apex outside that hyperplane.

This proves an exact structural analogy with the packet decomposition

```text
12 packet directions + 1 deep-anchor direction.
```

The analogy has a falsifier. Over `F3`, the Hjelmslev order-three action has
Jordan type `3 J3 + 3 J1` on the twelve packet modes, while a natural
120-degree icosahedral vertex rotation has `4 J3`. The deep anchor is an exact
apex-like fixed singlet, but no natural C3-equivariant identification of the
twelve packet coordinates with the twelve icosahedron vertices is claimed.

## Pass 574 — rank-9 Singer-normalizer association scheme

The 336 conjugates of `15:4 = N_GL(4,2)(C15)` form a commutative Schurian
association scheme of rank nine. Its valencies are

```text
1, 60, 60, 60, 60, 60, 15, 15, 5
```

and its multiplicities are

```text
1, 7, 20, 28, 45, 45, 56, 64, 70.
```

The first and second eigenmatrices are recorded exactly over `Q(sqrt(-7))`, and
all 729 intersection numbers are stored in the immutable certificate. The
permutation module is multiplicity-free; the two 45-dimensional constituents
are complex conjugates and correspond to the unique nonsymmetric transpose
pair.

The full color automorphism group is exactly `GL(4,2) ~= A8`. A base object has
a five-neighborhood with only 20 globally signature-compatible permutations.
The pointwise neighborhood kernel has order three. After choosing one extra
vertex from its three-element signature class, relational singleton refinement
fixes all 336 vertices. Hence

```text
|Aut| <= 336 * 20 * 3 = 20160,
```

while the known `GL(4,2)` action already has order 20160.

## Pass 575 — residue kernel equals the uniformizer ideal

The Lean source closes the order-level obligation left open by Pass 570. The
defining quartic gives

```text
5 = lambdaBar * (-lambdaBar^3 + 5 lambdaBar^2 - 10 lambdaBar + 10).
```

For every polynomial representative,

```text
p = p.divX * X + C(p.coeff 0).
```

Residue zero makes the constant coefficient divisible by five, and the quartic
identity makes five divisible by `lambdaBar`. Therefore

```text
ker(residueMap) = Ideal.span {lambdaBar}.
```

The 5-adic completion, complete-field structure, valuation, and total
ramification remain explicit future obligations.

## Pass 576 — exact primitive central idempotents

The signed order-40 Walsh group is identified exactly as

```text
D10 x C4
```

with presentation

```text
r^5 = s^2 = z^4 = 1,  s r s = r^-1,  z central.
```

Its eight one-dimensional and eight two-dimensional characters are constructed
symbolically over `Q(i,sqrt(5))`. For each character,

```text
e_chi = degree(chi)/40 * sum_g chi(g^-1) g
```

is verified by exact group-algebra convolution. The sixteen idempotents are
pairwise orthogonal and sum to the identity. Their ranks in the 4,096-dimensional
Walsh representation are

```text
104 repeated eight times,
408 repeated eight times.
```

The six fixed-magnitude fibre signatures are recovered from these exact
projectors without floating-point eigendecomposition.

## Pass 577 — posterior-state dynamic programming

A finite-grid Bellman solver compares a staged action set with an augmented set
that also contains simultaneous quartic-plus-orientation observations. The
six hypotheses are three triality fibres times two orientations. The grid has
6,188 belief states at denominator 12; the aspirational profile is also checked
on 33,649 states at denominator 18.

At the uniform prior:

| profile | staged value | augmented value | reduction |
|---|---:|---:|---:|
| conservative | 73.0884 | 40.9708 | 43.94% |
| nominal | 32.3635 | 29.1194 | 10.02% |
| aspirational | 4.25746 | 4.25746 | tie |

The aspirational augmented policy still improves 5,268 of the 6,188 denominator-12
belief states, but orientation-only readout is already optimal at the uniform
prior. The tie persists at denominator 18. This is an exact finite-grid policy
improvement result, not yet a continuous-belief Bayes-optimal theorem.

## Validation boundary

The five owners report 58/58 checks, the release lock reports 17/17 checks, and
the focused suite reports 6/6 tests. No natural C3-equivariant identification
with the icosahedron vertex module, complete `9^40` classification, completed
5-adic field, unrestricted E6 embedding, or continuous-posterior optimality is
asserted.
