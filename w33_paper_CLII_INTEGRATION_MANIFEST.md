# CLII Integration Manifest for `w33_paper.tex`

**Date:** 2026-05-01  
**Primary manuscript:** `w33_paper.tex`  
**Insert file:** `w33_paper_CLII_deep_solution_insert.tex`

## What landed

A paper-ready TeX insert was added:

```tex
\input{w33_paper_CLII_deep_solution_insert.tex}
```

Recommended placement: after the Hashimoto/spectral development and before broad phenomenology.

## Core theorem being inserted

The current W(3,3) solution is a finite-to-observable compiler:

\[
q! = 2q \Rightarrow q=3 \Rightarrow W(3,3)=\mathrm{SRG}(40,12,2,4).
\]

The Hashimoto spectrum splits as

\[
\mathbb Q(\sqrt{-10})\oplus\mathbb Q(\sqrt{-7}).
\]

The two nontrivial multiplicities form an E6-sized two-adjoint shell:

\[
24+15=39=3\Phi_3,
\qquad
2(24+15)=78=\dim E_6,
\]

with

\[
24=\dim\mathfrak{su}(5),
\qquad
15=\dim\mathfrak{so}(6)=\dim\mathfrak{su}(4).
\]

Observables are licensed words in two layers:

1. Mixer layer:

\[
C=8/13,
\qquad
T=5/13,
\qquad
D=C-T=3/13.
\]

2. Projection layer:

\[
P(A)=A/\Phi_3.
\]

The unique bridge is

\[
1-D=P(\Phi_4)=10/13.
\]

## First closed physical pipeline

The strong coupling uses the q-lifted carrier

\[
k_{3,\mathrm{bare}}=qC=24/13,
\]

and the negative-sector polar threshold

\[
\tau_{\mathrm{GUT}}=\log\sqrt{\mu/\Phi_6}.
\]

Therefore

\[
\alpha_s(M_{\mathrm{GUT}})
=
\frac{\alpha_{\mathrm{unified}}}{24/13}
\left(1+\frac{\alpha_{\mathrm{unified}}}{2\pi}\log\sqrt{\mu/\Phi_6}\right).
\]

With \(\alpha_{\mathrm{unified}}=1/25\), the selected two-loop RG pipeline gives

\[
\alpha_s(M_Z)=0.11800503473579949,
\]

within \(0.006\sigma\) of PDG input.

## Paper-level rule

The manuscript should promote a physical claim only when it is generated by one of:

1. finite-geometric construction from `W(3,3)`,
2. Hashimoto field decomposition,
3. Ramanujan/E6 mixer operation,
4. `Phi3`-projection operation,
5. a licensed word combining these layers.

Claims not generated by this grammar should remain exploratory or phenomenological until a structural operation licenses them.

## Local PDF render

A standalone render of the insert was compiled locally as:

```text
w33_paper_CLII_deep_solution_insert.pdf
```

The local PDF renders cleanly in 5 pages through Ghostscript.
