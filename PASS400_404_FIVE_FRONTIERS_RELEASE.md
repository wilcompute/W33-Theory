# Passes 400–404 — Five Orthogonal Frontiers

**Status: certified computational release.**

This packet executes five non-sequential moves from the Pass-399 Heisenberg bulk law

\[
(x,y,z)\sim(x',y',z')\iff(x,y)\ne(x',y')\text{ and }z'-z=yx'-xy'.
\]

## Pass 400 — minimal qutrit phase-control breaker

Let `A` be the 27-state native adjacency and `S` cyclically shift the central coordinate on all nine fibres. The Hermitian magnetic triangle

\[
C=\frac{i}{\sqrt3}(S-S^*)
\]

has one-fibre spectrum `{-1,0,+1}`, spectral rank two, and commutes with `A`. At the smallest positive native projective-return time `T=2*pi/3`,

\[
e^{-iTA}=\omega I,\qquad e^{-iTC}=S,\qquad
\boxed{e^{-iT(A+C)}=\omega S}.
\]

All 27 source modes transfer with unit target fidelity and zero leakage. A translation-covariant diagonal control is scalar on each central orbit. A rank-one Hermitian logarithm has at most two spectral phases after removal of a scalar, while the qutrit shift has three. Rank two is therefore minimal in the fibre-local Hermitian circulant class.

The Lie algebra generated by `{iA,iC}` is abelian of real dimension two. A certified 17-vertex zero-forcing set supplies a standard infection construction of `u(27)` with independently addressed local projectors. The spectral multiplicity lower bound is 12, so the exact minimum number of local actuators remains in `[12,17]`.

## Pass 401 — critical-group and Bockstein anatomy

The reduced Laplacian Smith form is exact at `q=3,5`.

\[
\operatorname{SNF}(L_3'):\quad1^{10},3^4,6^4,18,54,216^6,
\]

so

\[
K_3\cong(\mathbb Z/2)^6\oplus(\mathbb Z/8)^6\oplus(\mathbb Z/3)^8\oplus\mathbb Z/9\oplus(\mathbb Z/27)^7.
\]

At `q=5`,

\[
\operatorname{SNF}(L_5'):\quad1^{35},5^{29},20^{20},120^7,600^{10},3000^{23}.
\]

The primary pieces are

\[
K_{5,(5)}\cong(\mathbb Z/5)^{56}\oplus(\mathbb Z/25)^{10}\oplus(\mathbb Z/125)^{23},
\]

\[
K_{5,(2)}\cong(\mathbb Z/4)^{20}\oplus(\mathbb Z/8)^{40},\qquad K_{5,(3)}\cong(\mathbb Z/3)^{40}.
\]

At `q=7`, two explicit Bockstein pages, the tree order, and the cubic Laplacian annihilator give

\[
K_{7,(7)}\cong(\mathbb Z/7)^{176}\oplus(\mathbb Z/49)^{35}\oplus(\mathbb Z/343)^{47},
\]

\[
K_{7,(2)}\cong(\mathbb Z/2)^{42}\oplus(\mathbb Z/16)^{126},\qquad K_{7,(3)}\cong(\mathbb Z/3)^{168}.
\]

For prime `q`, the verified q-primary cumulative counts are

\[
n_1=q^3-1-\binom{q+2}{3},\quad n_2=\binom{q+2}{3}-2,\quad n_3=q^2-2.
\]

They produce the candidate law

\[
K_{q,(q)}\cong(\mathbb Z/q)^{q^3+1-2\binom{q+2}{3}}\oplus(\mathbb Z/q^2)^{\binom{q+2}{3}-q^2}\oplus(\mathbb Z/q^3)^{q^2-2}.
\]

It is certified at `q=3,5,7` and remains explicitly conjectural universally. The candidate odd-q 2-primary law is

\[
K_{q,(2)}\cong(\mathbb Z/2^a)^{q(q-1)}\oplus(\mathbb Z/2^{a+b})^{q(q-1)^2/2},
\]

where `a=v2(q-1)` and `b=v2(q+1)`. It is exact at `q=3,5` and Bockstein/order-certified at `q=7`.

## Pass 402 — exact no-tradeoff expansion/transfer frontier

Let `F` be the disjoint union of complete graphs on the phase fibres and set `H_alpha=A+alpha F`. The exact spectrum is

\[
\begin{array}{rcl}
q^2-1+\alpha(q-1)&:&1,\\
-1+\alpha(q-1)&:&q^2-1,\\
q-1-\alpha&:&q(q^2-1)/2,\\
-q-1-\alpha&:&q(q-1)^2/2.
\end{array}
\]

For

\[
0\le\alpha\le\frac{q+2}{q-2},
\]

the normalized nontrivial radius remains exactly `1/(q-1)`. Beyond this interval the base-sector eigenvalue dominates and mixing worsens.

At `q=3`, every integer `alpha=0,1,2,3,4,5` is in the frontier, and

\[
\boxed{e^{-i(2\pi/3)(A+\alpha F+C)}=\omega^{1+\alpha}S}.
\]

The real routing layer retains contraction `1/2` while the magnetic layer performs exact phase transfer. A phase depending only on the voltage `z'-z` is a pure diagonal gauge `G^*AG` and cannot break the revival obstruction.

## Pass 403 — semilinear voltage classification

The packet constructs the faithful oriented action

\[
H_q\rtimes\Gamma L(2,q)
\]

with order

\[
\boxed{q^3(q^2-1)(q^2-q)f},\qquad q=p^f.
\]

Permutation-group certificates give orders `1296`, `60000`, and `8398080` at `q=3,5,9`. The `q=9` calculation uses `F_3[a]/(a^2+1)` and includes Frobenius.

Spectrum and intersection array alone do not determine a DRACKN. Adding a regular Heisenberg action and an oriented section whose triangle voltage is a nondegenerate alternating form yields uniqueness up to semilinear isomorphism, since all nondegenerate alternating forms in dimension two differ by a scalar absorbed by the determinant action. The full unoriented graph-automorphism group is not asserted without an independent graph-automorphism census.

## Pass 404 — photonic voltage-cover compiler

The nine base modes form `K_9`, whose edges are decomposed into nine near-perfect matchings. Each base pair `u<v` is lifted by

\[
z\mapsto z+\omega(u,v).
\]

The native circuit has 9 time slices, 4 base pairs per slice, 12 conflict-free mode couplers per slice, one idle fibre per slice, and 108 total native coupler activations. Every graph edge occurs exactly once.

The control layer is nine simultaneous oriented magnetic triangles: 27 couplers of magnitude `1/sqrt(3)`, directed phase `+pi/2`, and gate time `2*pi/3`.

The schedule includes detector and loss placeholders. A blinded Choi protocol distinguishes native-only identity from the controlled central shift. No physical experiment is claimed; external counts must use the Pass-397 sealed ingestion path.

## Validation

The release contains five deterministic witnesses, six frozen data products including the 62-kB hardware schedule, 82 passing finite checks, six cross-pass regressions, and a dedicated GitHub Actions gate. Universal-looking critical-group formulas remain labeled conjectural outside the certified finite range.
