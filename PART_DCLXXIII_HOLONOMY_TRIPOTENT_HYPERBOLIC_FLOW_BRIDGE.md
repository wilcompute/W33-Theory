# Part DCLXXIII — Holonomy Tripotent Hyperbolic Flow Bridge

## Why this part exists

`Part DCLXXII` lifted the averaged witness kernel to an exact heat semigroup.

The next deeper question is whether that continuous-time flow still needs the full projector package `P_0, P_+, P_-`, or whether it has already collapsed to the single canonical tripotent `M` from `Part DCLXVII`.

This part proves the stronger statement:

> the entire continuous-time witness-average flow is already a damped hyperbolic flow of the one canonical tripotent `M`.

## Generator in tripotent form

Let `M` be the canonical tripotent from `Part DCLXVII`, with

$$
M^3 = M,
\qquad
M^2 = I - \frac{J}{40}.
$$

Then the semigroup generator from `Part DCLXXII` can be rewritten exactly as

$$
G = \frac{\log(10)}{2} M^2 + \frac{\log(8/5)}{2} M.
$$

So the whole continuous-time lift lies in the tiny algebra generated by one operator satisfying `M^3 = M`.

## Exact hyperbolic flow law

The verifier proves that the full semigroup can be written as

$$
H_t
= P_0
+ e^{-\frac{\log(10)}{2}t}
  \left[
    \cosh\!\left(\frac{\log(8/5)}{2} t\right) M^2
    - \sinh\!\left(\frac{\log(8/5)}{2} t\right) M
  \right].
$$

This is exactly the same semigroup as

$$
H_t = P_0 + 4^{-t} P_+ + \left(\frac25\right)^t P_-.
$$

So the projector description and the tripotent description are the same finite flow written in two languages.

## Time-1 recovery

At `t=1`, the hyperbolic flow recovers the averaged witness kernel from `Part DCLXVIII`:

$$
H_1 = K.
$$

So the one-step averaged witness operator is already the unit-time sample of a tripotent hyperbolic flow.

## Why this is a breakthrough

This sharpens the chain again:

- `DCLXVII` gave one canonical tripotent `M`.
- `DCLXVIII` averaged the 40 witness transvections to one Markov kernel `K`.
- `DCLXXII` lifted `K` to an exact heat semigroup.
- `DCLXXIII` shows that this entire semigroup is already controlled by `M` alone.

So the continuous-time witness dynamics do not need a large family, or even a projector package.

They have collapsed to:

1. one canonical tripotent `M`,
2. one even damping rate `\log(10)/2`,
3. one odd tilt rate `\log(8/5)/2`.

That is the cleanest operator-level compression so far.

## Executable artifact

Verifier:

```text
verify_dclxxiii_holonomy_tripotent_hyperbolic_flow_bridge.py
```

Tests:

```text
tests/test_dclxxiii_holonomy_tripotent_hyperbolic_flow_bridge.py
```

Generated summary:

```text
data/dclxxiii_holonomy_tripotent_hyperbolic_flow_bridge.json
```

---
*W33-Theory | Part DCLXXIII | the exact holonomy heat semigroup is already a damped hyperbolic flow of the single canonical tripotent.*
