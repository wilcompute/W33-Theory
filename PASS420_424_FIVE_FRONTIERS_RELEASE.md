# Passes 420–424 — Five Orthogonal Frontiers

## Pass 420 — Integral incidence gluing

The characteristic-primary critical group is factored through the square biaffine incidence operator
\(S=A+I\).  On augmentation zero, \(S^3=q^2S\).  For \(q=3,5,9\), every positive \(p\)-power Smith factor of \(S\) lifts unchanged to the reduced critical group, followed by the exact tree-quotient layer
\((\mathbb Z/p^{3f})^{q^2-2}\).  The witness reproduces all frozen exact factors and emits exact rank/order/top-layer constraints for \(q=25,27\) without guessing their unresolved middle layers.

## Pass 421 — Qutrit projective phase portrait

The accepted pure branch of the five-qutrit protocol is a homogeneous degree-five self-map of \(\mathbb{CP}^2\).  The certificate resolves 31 simple fixed points, partitioned into Clifford orbits of sizes
\(3,4,6,6,6,6\).  Every fixed point is repelling; the minimum projective Jacobian radius exceeds two.  This closes pure fixed-point distillation while leaving periodic and mixed-state basin classification open.

## Pass 422 — Telemetry-aware coding theorem

Conditioning on the measured divisor turns hidden slips into an enumerative rank code.  The worst cases remain 13 unordered bits and 16 ordered bits, while the uniform-over-histories average lengths are approximately 3.636 and 6.635 bits.  Shortened Hamming frames \([18,13,3]\) and \([21,16,3]\) correct one serialized bit error or any two known erasures and fit in three bytes.

## Pass 423 — Defect-to-hardware inverse compiler

The Pass-418 atlas is compiled into 387 physical hypotheses: detector gains, native couplers, parasitic distance-two paths, local phase trims, and nine shared delay registers.  Orthogonal matching pursuit exactly recovers all deterministic and 24 randomized sparse tests.  Single phase-pair signatures select a local trim; a coherent three-pair fibre signature selects the shared delay register.

## Pass 424 — Custody finite-state model checker

A bounded explicit-state checker explores every unique one- and two-attack state generated by thirteen replay, rollback, deletion, reordering, equivocation, key-collision, and claim-forgery actions.  Every such state violates a hardened v2 invariant, while the checker independently recovers v1 counterexamples.  A TLA+ specification records the invariant surface; the executable claim remains bounded, not an unbounded TLC proof.
