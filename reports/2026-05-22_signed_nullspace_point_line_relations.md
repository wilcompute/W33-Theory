# 2026-05-22 - Signed Point-Line Nullspace Theorem

## Breakthrough

The missing `79` dimensions in the signed projector are now identified.

The signed phase matrix

```text
A : flags -> quadrangles
shape 160 x 1620
rank 81
```

has left nullity

```text
160 - 81 = 79.
```

That 79-dimensional null sector is exactly generated by local signed four-flag relations at points and lines of `W(3,3)`.

## Theorem

For every point `p` of `W(3,3)`, the four flags through `p` satisfy a signed four-term relation among the rows of `A`.

For every isotropic line `L`, the four flags on `L` satisfy a signed four-term relation among the rows of `A`.

There are therefore

```text
40 point relations + 40 line relations = 80 local relations.
```

These relations have one global dependence, so their span has rank

```text
79.
```

The certificate verifies

```text
span(point/line signed four-flag relations) = ker(A^T) = ker(A A^T).
```

## Meaning

The projector decomposition is now explicit:

```text
160-dimensional flag surface
= 81-dimensional protected phase image
+ 79-dimensional local point/line gauge-relation nullspace
```

So the slogan

```text
phase protects homology
```

has a sharper algebraic version:

```text
phase kills exactly the local signed point/line relation space and leaves the 81-dimensional homology image.
```

## Machine certificate

Added:

- `analysis/w33_signed_nullspace_point_line_relations.py`
- `data/w33_signed_nullspace_point_line_relations.json`

The script reconstructs W(3,3), builds the signed phase matrix, finds all point and line four-flag relations, verifies that they annihilate `A`, and checks that their span has rank `79`, equal to the full left nullity.
