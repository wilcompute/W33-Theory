# 2026-05-22 - Visible Pair Orbit-Weyl Torsor Theorem

## Breakthrough

The projective visible `X/Z` incidence set

```text
{(flag, quadrangle): flag lies on the quadrangle's 8-flag boundary}
```

has size

```text
160 * 81 = 1620 * 8 = 12960.
```

This is not merely a biregular count. It is one homogeneous orbit under the projective symplectic automorphism group generated by symplectic transvections.

## Theorem

Let

```text
G = PSp(4,3)
```

act on the 40 projective points of `W(3,3)`, hence on flags and ordinary quadrangles. Then:

```text
|G| = 25920
```

and the visible flag-quadrangle incidence set is a single orbit of size

```text
12960 = 25920 / 2.
```

Therefore the stabilizer of a visible projective pair has order `2`.

At vector level, each projective visible pair has four scalar-phase lifts, because both the `X` ray and the `Z` ray have two nonzero `F_3` representatives. Hence

```text
4 * 12960 = 51840 = |W(E6)|.
```

## Meaning

The previous result said:

```text
nonzero minimal X/Z vector pairings = 51840 = |W(E6)|
```

This theorem explains why structurally:

```text
|W(E6)| = scalar-phase lift of one homogeneous visible PSp(4,3) orbit.
```

So the bridge becomes:

```text
projective geometry:     one PSp(4,3)-orbit, stabilizer C2
phase lift:              four F3 scalar representatives
exceptional shadow:      full Weyl(E6) order
```

## Machine certificate

Added:

- `analysis/w33_visible_pair_orbit_weyl_torsor.py`
- `data/w33_visible_pair_orbit_weyl_torsor.json`

The script reconstructs W(3,3), generates the projective symplectic group from transvections, verifies group order `25920`, computes the orbit of one visible flag-quadrangle pair, and checks that this orbit equals the full visible incidence set.
