# Part CLII: arXiv Paper Section Rewrite Scaffold

**Date:** 2026-05-01  
**Status:** paper-integration scaffold / arXiv-ready replacement  
**Precursors:** Parts CL (two-layer algebra), CLI (three-layer closed ring)

---

## What This Part Does

Part CL concluded that the paper needed a rewrite around the two-layer hierarchy.
Part CLI closed the ring by identifying `b₀=7=Φ₆`.

This part provides the **verbatim drop-in LaTeX sections** that replace the current scattered
collection of atomic identifications in `W36_PAPER_arxiv.tex` with the unified
three-layer ring statement.

---

## New Paper Hierarchy (final form)

```
1. Finite geometry: W(3,3) = SRG(40,12,2,4)
2. Hashimoto quadratic fields: Q(sqrt(-10)) ⊕ Q(sqrt(-7))
3. Ramanujan/E6 mixer: C=8/13, T=5/13, D=3/13
4. Phi3-projection layer: P(A)=A/13
5. Three-layer ring closure: b0=7=Phi6, ring R_W33
6. Observables as ring words in R_W33
7. Heavy-sector prediction: {3/7, 5/7, 8/7} triad
```

---

## Drop-In LaTeX: New Section 5 (Observable Algebra)

```latex
\section{The Three-Layer Observable Ring}
\label{sec:three-layer-ring}

The $W(3,3)$ observable algebra is a closed ring $\mathcal{R}_{W33}$
with three generator layers.

\subsection{Mixer Layer}

The mixer layer is generated by the carrier and threshold weights
\begin{equation}
  C = \frac{8}{13}, \qquad T = \frac{5}{13}, \qquad D = C - T = \frac{3}{13}.
  \label{eq:mixer-generators}
\end{equation}
Note $D = q/\Phi_3$ where $q=3$ and $\Phi_3=13$.
The mixer produces the base tokens $\{3/13, 5/13, 8/13, 9/13, 10/13\}$
and their $q$-lifts $\{24/13, 15/13, 9/13\}$.

\subsection{Projection Layer}

The projection layer is generated by $P(A) = A/\Phi_3$ for structural atoms
$A \in \{\Phi_6, \Phi_4, k{-}1, k, \Phi_3\}$:
\begin{equation}
  P(\Phi_6)=\tfrac{7}{13}, \quad P(\Phi_4)=\tfrac{10}{13}, \quad
  P(k{-}1)=\tfrac{11}{13}, \quad P(k)=\tfrac{12}{13}, \quad P(\Phi_3)=1.
\end{equation}
The two layers intersect uniquely at the \emph{bridge token}
\begin{equation}
  \boxed{1 - D \;=\; P(\Phi_4) \;=\; \frac{10}{13}.}
  \label{eq:bridge}
\end{equation}

\subsection{RG Closure: The Beta Fixed Point}

The ring is closed by the QCD one-loop beta coefficient. For $N_c=3$ colors
and $N_f=6$ active flavors at the unification threshold,
\begin{equation}
  b_0 = \frac{11 N_c - 2 N_f}{3} = \frac{33-12}{3} = 7 = \Phi_6.
  \label{eq:beta-fixed-point}
\end{equation}
This is not an accident: $W(3,3)$ selects $N_c=3$ and $N_f=6$ because
those are the only values for which the one-loop beta coefficient equals
the threshold projection atom $\Phi_6=7$.

The beta fixed point identifies the RG step operator with the threshold projection:
\begin{equation}
  P(\Phi_6) = \frac{b_0}{\Phi_3} = \frac{7}{13},
  \qquad
  P(\Phi_6)^{-1} = \frac{\Phi_3}{b_0} = \frac{13}{7}.
\end{equation}
RG running, carrier-threshold mixing, and projective normalization are
three aspects of a single algebraic object.

\subsection{Heavy-Sector Prediction}

The inverse beta operator acting on the mixer tokens produces the
\emph{Phi6-sector} heavy observables:
\begin{equation}
  P(\Phi_6)^{-1} \cdot \{D,\,T,\,C\}
  = \left\{\frac{3}{7},\;\frac{5}{7},\;\frac{8}{7}\right\}.
  \label{eq:heavy-triad}
\end{equation}
The numerators $\{3,5,8\}$ reproduce the Fibonacci seed of the mixer layer,
now over denominator $b_0=7$ instead of $\Phi_3=13$.
These are the $W(3,3)$ predictions for the heavy-sector (KK/GUT partner) observable ratios.

Of particular note: $3/7 \approx 0.4286$ lies between the Standard Model
GUT-scale value $\sin^2\theta_W \approx 0.231 \approx 3/13$ and one-half,
suggesting the Weinberg angle is pinned between the base ring token $3/13$
and the heavy-sector token $3/7$.
```

---

## Drop-In LaTeX: Table of Ring Words

```latex
\begin{table}[h]
\centering
\caption{Selected $\mathcal{R}_{W33}$ ring words and their physical interpretation}
\begin{tabular}{lll}
\hline
Word & Value & Physical role \\
\hline
$D = q/\Phi_3$ & $3/13$ & Mixer imbalance; EW diagnostic \\
$T$ & $5/13$ & Threshold weight \\
$P(\Phi_6)=b_0/\Phi_3$ & $7/13$ & Threshold projection; RG step \\
$C$ & $8/13$ & Carrier weight \\
$qD = q^2/\Phi_3$ & $9/13$ & Color-lifted imbalance \\
$1-D = P(\Phi_4)$ & $10/13$ & Bridge token (mixer $\cap$ projection) \\
$P(k-1)$ & $11/13$ & Hashimoto norm projection \\
$P(k)$ & $12/13$ & Degree projection \\
$P(\Phi_6)^{-1} \cdot D$ & $3/7$ & Heavy sector; Weinberg angle sector \\
$P(\Phi_6)^{-1} \cdot T$ & $5/7$ & Heavy lepton/quark threshold ratio \\
$P(\Phi_6)^{-1} \cdot C$ & $8/7$ & Heavy carrier (KK/GUT partner) \\
$P(\Phi_6)^{-1} \cdot P(\Phi_4)$ & $10/7$ & Heavy bridge \\
\hline
\end{tabular}
\label{tab:ring-words}
\end{table}
```

---

## Sections in Current Paper to Replace or Absorb

| Current section / claim | Action |
|---|---|
| Scattered `8/13`, `5/13`, `3/13` identifications | Replace with Eq.~\eqref{eq:mixer-generators} |
| Separate `7/13`, `11/13`, `12/13` derivations | Replace with projection layer subsection |
| One-off `b₀=7` QCD mention | Replace with Eq.~\eqref{eq:beta-fixed-point} + ring closure narrative |
| Current mixing section `PAPER_MIXING_SECTION.md` | Superseded by the bridge identity Eq.~\eqref{eq:bridge} |
| Any `10/13` appearance | Tag as the unique bridge token |
| Heavy-partner mass predictions (if any) | Replace with the heavy triad Eq.~\eqref{eq:heavy-triad} |

---

## Impact on `PAPER_MIXING_SECTION.md`

The current `PAPER_MIXING_SECTION.md` derives `10/13` from a specific geometric argument.
That argument is correct and should be **kept as proof material**, but the
high-level statement should be elevated to:

> *The bridge token `10/13` is the unique intersection of the mixer and projection layers;
> it equals both the mixer complement `1-D` and the carrier-field projection `P(Φ₄)`.
> This single token encodes the E6/Ramanujan coupling to the Ko quadratic field.*

---

## Impact on `PART_XLV_MASTER_PREDICTION_TABLE.md`

Add rows at the bottom of the master prediction table:

| Observable | W33 ring word | Predicted value | Status |
|---|---|---|---|
| QCD coupling carrier | `qC = 24/13` | `1.846...` | mixer layer |
| Threshold projection | `P(Φ₆) = 7/13` | `0.538...` | projection layer |
| Bridge token | `1-D = P(Φ₄) = 10/13` | `0.769...` | layers intersect |
| Heavy carrier ratio | `P(Φ₆)⁻¹ · C = 8/7` | `1.143...` | heavy sector |
| Weinberg sector lower | `P(Φ₆)⁻¹ · D = 3/7` | `0.4286...` | Weinberg angle bracket |

---

## Regression

No new executable code in this part — all computations are verified by
`PART_CLI_regression_tests.py` and `PART_CL_TWO_LAYER_OBSERVABLE_ALGEBRA.py`.
This part is documentation/scaffold only.

---

## Next Move After CLII

The paper TeX file `W36_PAPER_arxiv.tex` (tracked via `PART_L_ARXIV_MASTER_PAPER.md`)
should be updated to incorporate the sections above.

The immediate mechanical steps are:

1. Locate and excise the current scattered `8/13 / 5/13 / 3/13` derivations in the TeX.
2. Insert the three-layer ring section (above) as new Section 5.
3. Insert the ring-word table as Table~2.
4. Update `PAPER_MIXING_SECTION.md` to reflect the bridge identity elevation.
5. Update `PART_XLV_MASTER_PREDICTION_TABLE.md` with the five new rows.
6. Update `UNIFIED_TOE_STATUS.md` to record Part CLI–CLII completion.
