# Part CXLIX — Projection Layer for Previously Unclassified W(3,3) Fractions

**Date:** 2026-05-01  
**Status:** projection-layer theorem / grammar-boundary refinement  
**Files:** `PART_CXLIX_PROJECTION_LAYER.py`, `PART_CXLIX_projection_layer_results.json`, `tests/test_projection_layer_cxlix.py`

---

## 1. Why CXLIX matters

CXLVIII deliberately left several attractive W(3,3) fractions unclassified by the Fibonacci/E6 mixer grammar:

\[
\frac{\Phi_6}{\Phi_3}=\frac{7}{13},
\]

\[
\frac{k-1}{\Phi_3}=\frac{11}{13},
\]

\[
\frac{k}{\Phi_3}=\frac{12}{13},
\]

and

\[
\frac{\Phi_3}{\Phi_6}=\frac{13}{7}.
\]

CXLIX classifies them without weakening the grammar boundary.

The key idea is that these are not mixer tokens. They are **projection-layer tokens**.

---

## 2. Projection operation

Define the projection of a structural W(3,3) atom \(A\) by

\[
P(A)=\frac{A}{\Phi_3}.
\]

The inverse projection is

\[
P(A)^{-1}=\frac{\Phi_3}{A}.
\]

This is a new operation layer, distinct from the mixer grammar generated by

\[
C=\frac{8}{13},
\qquad
T=\frac{5}{13},
\qquad
D=C-T=\frac{3}{13}.
\]

---

## 3. Classified projection tokens

The formerly unclassified fractions now receive exact projection tags:

\[
P(\Phi_6)=\frac{\Phi_6}{\Phi_3}=\frac{7}{13},
\]

classified as a **threshold-field projection**.

\[
P(k-1)=\frac{k-1}{\Phi_3}=\frac{11}{13},
\]

classified as a **Hashimoto radial/norm projection**.

\[
P(k)=\frac{k}{\Phi_3}=\frac{12}{13},
\]

classified as an **adjacency-degree projection**.

\[
P(\Phi_6)^{-1}=\frac{\Phi_3}{\Phi_6}=\frac{13}{7},
\]

classified as an **inverse threshold-field projection**.

---

## 4. Overlap with mixer grammar

The projection layer also contains

\[
P(\Phi_4)=\frac{\Phi_4}{\Phi_3}=\frac{10}{13}.
\]

But CXLVII already generated

\[
1-D=\frac{10}{13}.
\]

So \(10/13\) is an overlap token: it is both a field projection and a mixer complement.

This is important.  It suggests that \(\Phi_4\) is where the projection layer and mixer grammar meet.

---

## 5. Boundary rule

Projection tokens are not automatically mixer tokens.

The mixer grammar describes carrier/threshold mixing:

\[
C,T,D,1-D,1+D,qC,qT,qD.
\]

The projection layer describes direct normalization of structural atoms:

\[
P(A)=\frac{A}{\Phi_3}.
\]

This distinction keeps the theory disciplined.

---

## 6. Theorem statement

**The fractions \(7/13,11/13,12/13,13/7\) are generated by a projection layer \(P(A)=A/\Phi_3\) and inverse projection \(P(A)^{-1}\), not by the Fibonacci/E6 mixer grammar.**  This classifies \(\Phi_6/\Phi_3\) as a threshold-field projection, \((k-1)/\Phi_3\) as a Hashimoto radial projection, \(k/\Phi_3\) as a degree projection, and \(\Phi_3/\Phi_6\) as an inverse threshold projection.

---

## 7. Regression status

Local validation of the CXLIX test file:

```text
7 passed in 0.05s
```

The tests verify:

1. projection operation on structural atoms,
2. inverse projection operation,
3. tags for formerly unclassified values,
4. \(\Phi_4/\Phi_3=10/13\) as a mixer/projection overlap,
5. all formerly unclassified values now match the projection layer,
6. projection-layer boundary remains distinct from mixer grammar,
7. audit boundary rule consistency.

---

## 8. Next move

The next structural question is whether observables are generated by a two-layer algebra:

\[
\text{Mixer layer: } C,T,D
\]

and

\[
\text{Projection layer: } P(A)=A/\Phi_3.
\]

A good next audit should define their intersection, union, and forbidden promotions.  The most interesting overlap is

\[
1-D=P(\Phi_4)=\frac{10}{13}.
\]

That may mark the bridge between the \(\mathbb Q(\sqrt{-10})\) carrier field and the projective normalization layer.
