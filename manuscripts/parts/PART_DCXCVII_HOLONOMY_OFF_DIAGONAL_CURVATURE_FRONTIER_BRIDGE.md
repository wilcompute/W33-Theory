# Part DCXCVII — Holonomy Off-Diagonal Curvature Frontier Bridge

## Why this part exists

`Part DCXCVI` proved that no finite ambiguity remains and that the only unresolved content left by the current chain is one curved K3 existence theorem.

The next question is what that curved theorem actually says in exact geometric language.

This part identifies it.

## Exact curved content

The verifier combines the pure-frontier reduction with the existing mixed-plane curvature witness theorems.

It proves that the current host still has zero off-diagonal curvature on the already-fixed support package, while the exact transport-twisted precomplex already carries the nonzero off-diagonal curvature block

$$
\operatorname{rank}=36,
\qquad
\text{supported rows}=4046.
$$

So the one remaining curved theorem is exactly:

> realization of the nonzero off-diagonal curvature witness on the same fixed mixed-plane host.

## Why this matters

This turns the final unresolved theorem from a generic “curved existence” statement into one explicit geometric coupling problem.

## Executable artifact

Verifier:

```text
verify_dcxcvii_holonomy_off_diagonal_curvature_frontier_bridge.py
```

Tests:

```text
tests/test_dcxcvii_holonomy_off_diagonal_curvature_frontier_bridge.py
```

Generated summary:

```text
data/dcxcvii_holonomy_off_diagonal_curvature_frontier_bridge.json
```

---
*W33-Theory | Part DCXCVII | the unique remaining curved theorem is exactly realization of the nonzero off-diagonal curvature block on the already-fixed mixed-plane host.*
