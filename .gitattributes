# Normalize line endings
* text=auto

# Scripts should stay LF
*.sh text eol=lf
*.py text eol=lf
*.ps1 text eol=lf
*.sage text eol=lf
*.md text eol=lf

# Windows scripts can be CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# --- Git LFS candidate entries (conservative suggestions) ---
# Large binaries and generated bundles (please review before migrating)
# Zips and tool archives
tools/gap-4.15.1-core.zip filter=lfs diff=lfs merge=lfs -text
tools/gap/** filter=lfs diff=lfs merge=lfs -text
W33_TEX_AND_PDF_COMPLETE_BUNDLE_v*.zip filter=lfs diff=lfs merge=lfs -text

# Large data outputs
data/repetend_scan/*.json filter=lfs diff=lfs merge=lfs -text

# Large patches / bundle patches
patches/*.patch filter=lfs diff=lfs merge=lfs -text

# Documentation bundles (maniplex / tomotope PDFs)
maniplex_doc_bundle/*.pdf filter=lfs diff=lfs merge=lfs -text

# NOTE: `.secrets.baseline` is large; do NOT add it to LFS automatically. Investigate removal from history or secure storage instead.
# -----------------------------------------------------------
